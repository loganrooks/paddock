---
phase: 01-authored-round-contract
plan: "03"
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: [
  "content/venues/spa-francorchamps.yaml",
  "content/venues/montreal.yaml",
  "content/venues/jeddah-corniche.yaml",
  "content/packs/starter-pack/pack.yaml",
  "content/packs/starter-pack/rounds/spa-eau-rouge-01.yaml",
  "content/packs/starter-pack/rounds/montreal-island-approach-01.yaml",
  "content/packs/starter-pack/rounds/jeddah-night-lights-01.yaml"
]
autonomous: true
requirements: [PACK-02, PACK-03, OPS-01]
user_setup: []
must_haves:
  truths:
    - "Authors have a concrete, checked-in example of how to define venue profiles and authored rounds without any UI."
    - "The starter content demonstrates both `circuit` and `venue` answer targets without drifting into unsupported v1 surfaces."
    - "Fallback-first venues are explicit in content data instead of being hidden behind easier clue media."
  artifacts:
    - path: "content/venues/spa-francorchamps.yaml"
      provides: "Allow-class venue profile example"
      contains: "coverageClass: allow"
    - path: "content/venues/jeddah-corniche.yaml"
      provides: "Fallback-first venue profile example"
      contains: "coverageClass: fallback_first"
    - path: "content/packs/starter-pack/pack.yaml"
      provides: "Starter pack metadata and ordered round references"
      contains: "roundOrder:"
  key_links:
    - from: "content/packs/starter-pack/pack.yaml"
      to: "content/packs/starter-pack/rounds/spa-eau-rouge-01.yaml"
      via: "roundOrder reference"
      pattern: "spa-eau-rouge-01"
    - from: "content/packs/starter-pack/rounds/montreal-island-approach-01.yaml"
      to: "content/venues/montreal.yaml"
      via: "venueRef"
      pattern: "venueRef: montreal"
    - from: "content/packs/starter-pack/rounds/jeddah-night-lights-01.yaml"
      to: "content/venues/jeddah-corniche.yaml"
      via: "fallback-first round uses explicit venue profile"
      pattern: "venueRef: jeddah-corniche"
---

<objective>
Author the first concrete Phase 1 venue profiles and starter-pack round fixtures using the implemented domain schema from Plan 02.

Purpose: Turn the abstract content contract into real checked-in examples that later validation and compile tooling can prove against.
Output: Three venue profiles spanning the coverage classes and a starter pack with ordered mixed-media round YAML files.
</objective>

<execution_context>
@/home/rookslog/workspace/projects/prix-guesser/.codex/get-shit-done/workflows/execute-plan.md
@/home/rookslog/workspace/projects/prix-guesser/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-authored-round-contract/01-CONTEXT.md
@.planning/phases/01-authored-round-contract/01-RESEARCH.md
@packages/domain/src/contracts.ts
@packages/domain/src/schemas.ts
@packages/domain/src/normalize.ts
@discovery/14-gsd-seed.md
@discovery/11-circuit-coverage-audit.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author canonical venue profiles for allow, allow-with-fallback, and fallback-first examples</name>
  <files>content/venues/spa-francorchamps.yaml, content/venues/montreal.yaml, content/venues/jeddah-corniche.yaml</files>
  <read_first>AGENTS.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/01-authored-round-contract/01-CONTEXT.md, .planning/phases/01-authored-round-contract/01-RESEARCH.md, packages/domain/src/schemas.ts, packages/domain/src/normalize.ts, discovery/11-circuit-coverage-audit.md</read_first>
  <action>Author three venue profile YAML files against the implemented Phase 1 venue schema from Plan 01-02, not the earlier type-only sketch. Use `spa-francorchamps` as the `allow` example, `montreal` as the `allow_with_fallback` example, and `jeddah-corniche` as the `fallback_first` example. Each venue file must include `id`, `venueId`, `circuitId`, `displayName`, `coverageClass`, `defaultFallbackStrategy`, `mediaCapabilities`, and `sourceRefs`. In `mediaCapabilities`, use the exact keys `street_view_internal`, `street_view_edge`, `approach_media`, `map_fragment`, `satellite`, `official_stills`, and `authored_text`. Use the exact `defaultFallbackStrategy` values `street_view_primary`, `mixed_media_ladder`, and `non_street_view_only`; do not introduce alternate fallback fields or Google-specific schema keys that were not accepted by `VenueProfileSourceSchema`.</action>
  <verify>
    <automated>test -f content/venues/spa-francorchamps.yaml &amp;&amp; test -f content/venues/montreal.yaml &amp;&amp; test -f content/venues/jeddah-corniche.yaml &amp;&amp; rg -n "coverageClass: (allow|allow_with_fallback|fallback_first)" content/venues/*.yaml &amp;&amp; rg -n "street_view_internal:|street_view_edge:|approach_media:|map_fragment:|satellite:|official_stills:|authored_text:" content/venues/*.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `content/venues/spa-francorchamps.yaml` contains `coverageClass: allow`.
    - `content/venues/montreal.yaml` contains `coverageClass: allow_with_fallback`.
    - `content/venues/jeddah-corniche.yaml` contains `coverageClass: fallback_first`.
    - Every `content/venues/*.yaml` file contains `defaultFallbackStrategy:` and `mediaCapabilities:`.
    - Every `content/venues/*.yaml` file uses the exact `mediaCapabilities` keys frozen in Plan 01.
  </acceptance_criteria>
  <done>The repo contains explicit venue-profile examples for all three coverage classes, and each one records its fallback posture in data rather than in prose.</done>
</task>

<task type="auto">
  <name>Task 2: Author a mixed-media starter pack with ordered round fixtures</name>
  <files>content/packs/starter-pack/pack.yaml, content/packs/starter-pack/rounds/spa-eau-rouge-01.yaml, content/packs/starter-pack/rounds/montreal-island-approach-01.yaml, content/packs/starter-pack/rounds/jeddah-night-lights-01.yaml</files>
  <read_first>AGENTS.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/01-authored-round-contract/01-CONTEXT.md, .planning/phases/01-authored-round-contract/01-RESEARCH.md, packages/domain/src/schemas.ts, packages/domain/src/normalize.ts, discovery/14-gsd-seed.md, discovery/11-circuit-coverage-audit.md</read_first>
  <action>Create `content/packs/starter-pack/pack.yaml` with top-level keys `id`, `slug`, `title`, `description`, `version`, `roundOrder`, and `tags`, but make `roundOrder` use the exact reference shape accepted by the implemented `PackSourceSchema` from Plan 01-02 rather than inventing a parallel YAML shorthand. Create three round YAML files named exactly `spa-eau-rouge-01.yaml`, `montreal-island-approach-01.yaml`, and `jeddah-night-lights-01.yaml`. Every round file must use the exact semantic keys from the implemented shared schema: `id`, `slug`, `answerTarget`, `acceptedAnswers`, `clueSteps`, `reveal`, `scoringProfile`, `sourceRefs`, `venueRef`, and `circuitRef`. Mirror the nested shapes that Plan 01-02 actually accepts for `acceptedAnswers`, `reveal`, each `clueSteps[]` entry, and each `clueSteps[*].media` discriminant payload; do not guess or simplify these nested shapes if the runtime schema chose a stricter form. Keep the explicit Phase 1 keys `answerTarget.kind`, `answerTarget.canonicalId`, and `answerTarget.displayName`; `scoringProfile.kind` with exact values `circuit_standard` or `venue_standard`; and numeric `correctPoints` plus `partialPoints`. Keep v1 answer targets limited to `circuit` and `venue`. Make `spa-eau-rouge-01.yaml` a `circuit` target with a Street View clue in the ladder, `montreal-island-approach-01.yaml` a `venue` target that mixes approach/context media, and `jeddah-night-lights-01.yaml` a `venue` target with explicit `fallbackStrategyOverride: non_street_view_only` so the fallback-first posture is visible. Use the clue-family literals `circuit_internal`, `circuit_edge`, `venue_approach`, and `city_context` where appropriate, and include reveal explanations that describe why the answer was identifiable instead of just restating the answer.</action>
  <verify>
    <automated>test -f content/packs/starter-pack/pack.yaml &amp;&amp; test -f content/packs/starter-pack/rounds/spa-eau-rouge-01.yaml &amp;&amp; test -f content/packs/starter-pack/rounds/montreal-island-approach-01.yaml &amp;&amp; test -f content/packs/starter-pack/rounds/jeddah-night-lights-01.yaml &amp;&amp; rg -n "answerTarget:|acceptedAnswers:|clueSteps:|reveal:|scoringProfile:" content/packs/starter-pack/rounds/*.yaml &amp;&amp; rg -n "canonicalId:|displayName:|correctPoints:|partialPoints:" content/packs/starter-pack/rounds/*.yaml &amp;&amp; rg -nU "media:\\n\\s+kind: street_view" content/packs/starter-pack/rounds/*.yaml &amp;&amp; rg -nU "media:\\n\\s+kind: (image|map_fragment)" content/packs/starter-pack/rounds/*.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `content/packs/starter-pack/pack.yaml` contains `roundOrder:`.
    - `content/packs/starter-pack/rounds/spa-eau-rouge-01.yaml` contains `kind: circuit`.
    - `content/packs/starter-pack/rounds/montreal-island-approach-01.yaml` contains `kind: venue`.
    - `content/packs/starter-pack/rounds/jeddah-night-lights-01.yaml` contains `fallbackStrategyOverride: non_street_view_only`.
    - At least one round file contains `canonicalId:`, `displayName:`, `correctPoints:`, and `partialPoints:`.
    - At least one round file contains a `clueSteps[*].media.kind: street_view` entry, and at least one round file contains `clueSteps[*].media.kind: image` or `clueSteps[*].media.kind: map_fragment`.
    - `content/packs/starter-pack/pack.yaml` mirrors the `roundOrder` reference shape accepted by `PackSourceSchema` instead of inventing a second pack-reference format.
    - The round YAML files mirror the implemented nested shapes for `acceptedAnswers`, `reveal`, and `clueSteps[*].media` from `packages/domain/src/schemas.ts`.
  </acceptance_criteria>
  <done>The repo has a concrete starter pack that demonstrates the semantic round shape, ordered pack structure, mixed media, and explicit fallback modeling required by Phase 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author-authored YAML -> future compiler | These fixture files will become the first real inputs to the validation and compile pipeline. |
| venue profiles -> round fixtures | Incorrect references or implicit fallback assumptions here will silently distort later gameplay. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | `content/venues/*.yaml` | mitigate | Require explicit `coverageClass`, `defaultFallbackStrategy`, and `mediaCapabilities` keys in every venue profile so fallback posture is never inferred from asset choice alone. |
| T-03-02 | T | `content/packs/starter-pack/rounds/*.yaml` | mitigate | Keep v1 rounds limited to `circuit` and `venue` answer targets and the four approved clue-family literals to prevent scope drift into unsupported answer surfaces. |
| T-03-03 | R | Reveal content in round YAML | accept | This phase stores authored reveal rationale as content, but the runtime rendering and player-facing audit trail land later; the immediate control is keeping reveal explanations explicit in checked-in files. |
</threat_model>

<verification>
- [ ] All venue and round YAML files exist at the exact paths listed above
- [ ] Venue profiles cover `allow`, `allow_with_fallback`, and `fallback_first`
- [ ] Starter pack rounds include both `circuit` and `venue` answer targets plus mixed media
</verification>

<success_criteria>

- All tasks completed
- The repository contains canonical content examples for the round and pack contract
- Fallback-first content is explicit, not a silent substitution
- The starter pack remains file-authored and transport-agnostic

</success_criteria>

<output>
After completion, create `.planning/phases/01-authored-round-contract/01-03-SUMMARY.md`
</output>
