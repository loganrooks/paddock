---
phase: 01-authored-round-contract
plan: "03"
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: [
  "content/venues/spa-francorchamps.yaml",
  "content/venues/montreal.yaml",
  "content/venues/jeddah-corniche.yaml",
  "content/packs/starter-pack/pack.yaml",
  "content/packs/starter-pack/rounds/spa-eau-rouge-01.yaml",
  "content/packs/starter-pack/rounds/montreal-island-approach-01.yaml",
  "content/packs/starter-pack/rounds/jeddah-night-lights-01.yaml"
]
autonomous: true
requirements: [PACK-02, PACK-03, OPS-01]
future_preservation:
  preserved_seams:
    - "Exercise real fixture content against canonical venue identity and pack reference shapes."
    - "Keep fallback-first venues explicit rather than silently substituting easier clue media."
  non_decisions:
    - "Do not introduce unsupported active answer surfaces or public-content assumptions."
  posture:
    - "Curated starter content for private expert-fan play."
user_setup: []
must_haves:
  truths:
    - "Authors have concrete, checked-in examples of venue profiles and authored rounds without any UI."
    - "Starter content demonstrates both `circuit` and `venue` targets without drifting into unsupported surfaces."
    - "Fallback-first posture is explicit in content data."
  artifacts:
    - path: "content/venues/jeddah-corniche.yaml"
      provides: "Fallback-first venue profile example"
      contains: "coverageClass: fallback_first"
    - path: "content/packs/starter-pack/pack.yaml"
      provides: "Starter pack metadata and ordered round references"
      contains: "roundOrder:"
---

<objective>
Author the first concrete Phase 1 venue profiles and starter-pack round fixtures using the implemented domain schema from Plan 02.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Author canonical venue profiles for allow, allow-with-fallback, and fallback-first examples</name>
  <files>content/venues/spa-francorchamps.yaml, content/venues/montreal.yaml, content/venues/jeddah-corniche.yaml</files>
  <read_first>AGENTS.md, .planning/phases/01-authored-round-contract/01-CONTEXT.md, .planning/phases/01-authored-round-contract/01-RESEARCH.md, packages/domain/src/contracts.ts, packages/domain/src/schemas.ts, discovery/11-circuit-coverage-audit.md</read_first>
  <action>Author three venue profile YAML files against the implemented venue schema from Plan 02. Use `spa-francorchamps` as the `allow` example, `montreal` as the `allow_with_fallback` example, and `jeddah-corniche` as the `fallback_first` example. Every venue file must include `id`, `displayName`, `circuitId`, `coverageClass`, `defaultFallbackStrategy`, `mediaCapabilities`, and `sourceRefs`. `id` is the canonical venue key targeted by round `venueRef`; do not introduce a parallel `venueId` field or filename-only identity. In `mediaCapabilities`, use the exact keys frozen in the Phase 1 contracts.</action>
  <verify>
    <automated>test -f content/venues/spa-francorchamps.yaml && test -f content/venues/montreal.yaml && test -f content/venues/jeddah-corniche.yaml && ! rg -n "^venueId:" content/venues/*.yaml && rg -n "coverageClass: (allow|allow_with_fallback|fallback_first)" content/venues/*.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `content/venues/spa-francorchamps.yaml` contains `coverageClass: allow`.
    - `content/venues/montreal.yaml` contains `coverageClass: allow_with_fallback`.
    - `content/venues/jeddah-corniche.yaml` contains `coverageClass: fallback_first`.
    - Every `content/venues/*.yaml` file contains `defaultFallbackStrategy:` and `mediaCapabilities:`.
    - No `content/venues/*.yaml` file contains `venueId:`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Author a mixed-media starter pack with ordered round fixtures</name>
  <files>content/packs/starter-pack/pack.yaml, content/packs/starter-pack/rounds/spa-eau-rouge-01.yaml, content/packs/starter-pack/rounds/montreal-island-approach-01.yaml, content/packs/starter-pack/rounds/jeddah-night-lights-01.yaml</files>
  <read_first>AGENTS.md, .planning/phases/01-authored-round-contract/01-CONTEXT.md, .planning/phases/01-authored-round-contract/01-RESEARCH.md, packages/domain/src/contracts.ts, packages/domain/src/schemas.ts, content/venues/spa-francorchamps.yaml, content/venues/montreal.yaml, content/venues/jeddah-corniche.yaml</read_first>
  <action>Create `content/packs/starter-pack/pack.yaml` with top-level keys `id`, `slug`, `title`, `description`, `version`, `roundOrder`, and `tags`, and make each `roundOrder` entry use the exact `{ roundId, path }` shape accepted by the schema. Create three round YAML files named `spa-eau-rouge-01.yaml`, `montreal-island-approach-01.yaml`, and `jeddah-night-lights-01.yaml`. Every round file must use the implemented semantic keys `id`, `slug`, `answerTarget`, `acceptedAnswers`, `clueSteps`, `reveal`, `scoringProfile`, `sourceRefs`, `venueRef`, and `circuitRef`. Keep active v1 answer targets limited to `circuit` and `venue`. Make the Spa round a `circuit` target with Street View in the ladder, the Montreal round a `venue` target that mixes approach/context media, and the Jeddah round a `venue` target with `fallbackStrategyOverride: non_street_view_only`. Include reveal explanations that describe why the answer was identifiable rather than only restating the answer.</action>
  <verify>
    <automated>test -f content/packs/starter-pack/pack.yaml && test -f content/packs/starter-pack/rounds/spa-eau-rouge-01.yaml && test -f content/packs/starter-pack/rounds/montreal-island-approach-01.yaml && test -f content/packs/starter-pack/rounds/jeddah-night-lights-01.yaml && rg -n "roundOrder:|roundId:|path:" content/packs/starter-pack/pack.yaml && rg -n "answerTarget:|acceptedAnswers:|clueSteps:|reveal:|scoringProfile:" content/packs/starter-pack/rounds/*.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `content/packs/starter-pack/pack.yaml` contains `roundOrder:`.
    - `content/packs/starter-pack/rounds/spa-eau-rouge-01.yaml` contains `kind: circuit`.
    - `content/packs/starter-pack/rounds/montreal-island-approach-01.yaml` contains `kind: venue`.
    - `content/packs/starter-pack/rounds/jeddah-night-lights-01.yaml` contains `fallbackStrategyOverride: non_street_view_only`.
    - At least one round file contains `canonicalId:`, `displayName:`, `lineage:`, `correctPoints:`, and `partialPoints:`.
    - At least one round file contains `street_view`, and at least one round file contains `image` or `map_fragment`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author-authored YAML -> future compiler | These fixtures become the first real inputs to the validation pipeline |
| venue profiles -> round fixtures | Incorrect references or hidden fallback assumptions distort later gameplay |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | `content/venues/*.yaml` | mitigate | Require explicit `coverageClass`, `defaultFallbackStrategy`, and `mediaCapabilities` in every venue profile. |
| T-03-02 | T | `content/packs/starter-pack/rounds/*.yaml` | mitigate | Keep v1 rounds limited to the approved answer-target and clue-family literals. |
| T-03-03 | R | reveal content | accept | This phase stores reveal rationale in content; runtime display and audit surfaces come later. |
</threat_model>

<verification>
- [ ] All venue and round YAML files exist at the exact paths listed above
- [ ] Venue profiles cover `allow`, `allow_with_fallback`, and `fallback_first`
- [ ] Starter pack rounds include both `circuit` and `venue` answer targets plus mixed media
</verification>

<success_criteria>
- All tasks completed
- The repository contains canonical content examples for the round and pack contract
- Fallback-first content is explicit, not a silent substitution
- The starter pack remains file-authored and transport-agnostic
</success_criteria>

<output>
After completion, create `.planning/phases/01-authored-round-contract/01-03-SUMMARY.md`
</output>
