---
phase: 01-authored-round-contract
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [
  "packages/domain/src/schemas.ts",
  "packages/domain/src/normalize.ts",
  "packages/domain/src/json-schema.ts",
  "packages/domain/src/index.ts",
  "tests/contract/round-contract.test.ts",
  "tests/contract/mixed-media-round.test.ts",
  "tests/contract/venue-profile-resolution.test.ts"
]
autonomous: true
requirements: [PACK-02, PACK-03, OPS-01]
user_setup: []
must_haves:
  truths:
    - "The domain package can express semantic rounds with explicit answer targets, clue ladders, accepted answers, reveal explanation, and scoring profile."
    - "The same round schema accepts Street View and non-Street-View clue media without changing the authored round shape."
    - "Venue coverage class and fallback strategy resolve explicitly from venue metadata plus round overrides."
  artifacts:
    - path: "packages/domain/src/schemas.ts"
      provides: "Strict runtime schemas for round, pack, venue, and compiled content"
      min_lines: 80
      contains: "z.strictObject"
    - path: "packages/domain/src/normalize.ts"
      provides: "Normalization helpers for aliases and resolved coverage/fallback metadata"
      contains: "resolveRoundCoverageMetadata"
    - path: "tests/contract/round-contract.test.ts"
      provides: "Requirement-level regression coverage for PACK-02"
      contains: "PACK-02"
  key_links:
    - from: "tests/contract/round-contract.test.ts"
      to: "packages/domain/src/schemas.ts"
      via: "contract tests import the round schema"
      pattern: "RoundSourceSchema"
    - from: "tests/contract/mixed-media-round.test.ts"
      to: "packages/domain/src/schemas.ts"
      via: "mixed-media tests exercise one discriminated clue contract"
      pattern: "ClueStepSchema"
    - from: "packages/domain/src/normalize.ts"
      to: "packages/domain/src/contracts.ts"
      via: "coverage/fallback resolution depends on shared contract keys"
      pattern: "CoverageClass|fallback"
---

<objective>
Implement the strict runtime schema layer for the Phase 1 content contract and prove the core round/media/coverage behaviors with contract tests.

Purpose: This is the first place where the authored F1 round model becomes executable instead of remaining a type-only interface sketch.
Output: Zod schemas, normalization helpers, JSON Schema exports, and automated tests for PACK-02, PACK-03, and OPS-01.
</objective>

<execution_context>
@/home/rookslog/workspace/projects/prix-guesser/.codex/get-shit-done/workflows/execute-plan.md
@/home/rookslog/workspace/projects/prix-guesser/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-authored-round-contract/01-CONTEXT.md
@.planning/phases/01-authored-round-contract/01-RESEARCH.md
@.planning/phases/01-authored-round-contract/01-VALIDATION.md
@packages/domain/src/contracts.ts
@discovery/10-critical-inheritance-geoguessr-core.md
@discovery/11-circuit-coverage-audit.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write requirement-level contract tests for round semantics, mixed media, and coverage resolution</name>
  <files>tests/contract/round-contract.test.ts, tests/contract/mixed-media-round.test.ts, tests/contract/venue-profile-resolution.test.ts</files>
  <read_first>AGENTS.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/01-authored-round-contract/01-CONTEXT.md, .planning/phases/01-authored-round-contract/01-RESEARCH.md, .planning/phases/01-authored-round-contract/01-VALIDATION.md, discovery/11-circuit-coverage-audit.md</read_first>
  <behavior>
    - PACK-02: a round without `answerTarget`, `acceptedAnswers`, `reveal`, or `scoringProfile` is rejected.
    - PACK-03: one `clueSteps[]` contract accepts `street_view`, `image`, `map_fragment`, and `text` media while keeping `clueFamily` separate.
    - OPS-01: venue `coverageClass` plus `defaultFallbackStrategy` resolve onto a round, and an explicit round override wins when present.
  </behavior>
  <action>Create the three contract test files first. Use inline fixture objects only in this plan; do not load YAML yet. In `tests/contract/round-contract.test.ts`, name the key tests with the exact requirement token `PACK-02`. In `tests/contract/mixed-media-round.test.ts`, include one test that mixes `street_view` and `image` clue media inside the same round object while preserving `clueFamily` values `circuit_internal` and `venue_approach`. In `tests/contract/venue-profile-resolution.test.ts`, use coverage examples that match the research vocabulary `allow`, `allow_with_fallback`, and `fallback_first`, and assert that a round-level fallback override wins over the venue default. Keep the test descriptions exact and grep-friendly because later plans will rely on these names.</action>
  <verify>
    <automated>test -f tests/contract/round-contract.test.ts &amp;&amp; test -f tests/contract/mixed-media-round.test.ts &amp;&amp; test -f tests/contract/venue-profile-resolution.test.ts &amp;&amp; rg -n "PACK-02|PACK-03|OPS-01" tests/contract/*.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/contract/round-contract.test.ts` contains `PACK-02`.
    - `tests/contract/mixed-media-round.test.ts` contains both `street_view` and `map_fragment`.
    - `tests/contract/mixed-media-round.test.ts` contains `circuit_internal` and `venue_approach`.
    - `tests/contract/venue-profile-resolution.test.ts` contains `allow_with_fallback` and `fallback_first`.
  </acceptance_criteria>
  <done>The requirement-level tests exist with exact strings for PACK-02, PACK-03, and OPS-01, ready to drive the runtime schema implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement strict domain schemas, normalization helpers, and JSON Schema exports until the contract tests pass</name>
  <files>packages/domain/src/schemas.ts, packages/domain/src/normalize.ts, packages/domain/src/json-schema.ts, packages/domain/src/index.ts</files>
  <read_first>AGENTS.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/01-authored-round-contract/01-CONTEXT.md, .planning/phases/01-authored-round-contract/01-RESEARCH.md, .planning/phases/01-authored-round-contract/01-VALIDATION.md, discovery/11-circuit-coverage-audit.md</read_first>
  <behavior>
    - A valid round exposes semantic fields instead of thin coordinate-only data.
    - Only `circuit` and `venue` are accepted by the v1 supported-answer-target guard.
    - Clue media is provider-agnostic, and Street View remains optional data, not a required shape.
  </behavior>
  <action>Implement these files against the tests created in Task 1 and the exact contracts frozen in Plan 01. Create `packages/domain/src/schemas.ts` using `z.strictObject()` throughout. Implement `AnswerTargetSchema` with exact keys `kind`, `canonicalId`, and `displayName`; `ScoringProfileSchema` as a discriminated union over exact `kind` values `circuit_standard` and `venue_standard` with numeric `correctPoints` and `partialPoints`; `ClueMediaSchema`, `ClueStepSchema`, `VenueMediaCapabilitiesSchema`, `VenueProfileSourceSchema`, `RoundSourceSchema`, `PackSourceSchema`, and compiled-output schemas that mirror the interfaces from `packages/domain/src/contracts.ts`. `VenueMediaCapabilitiesSchema` must require the seven exact keys `street_view_internal`, `street_view_edge`, `approach_media`, `map_fragment`, `satellite`, `official_stills`, and `authored_text`. `RoundSourceSchema` must accept optional `fallbackStrategyOverride`, and compiled-output schemas must expose `resolvedCoverageClass` and `resolvedFallbackStrategy`. Create `packages/domain/src/normalize.ts` with `normalizeAlias`, `normalizeAcceptedAnswers`, `isSupportedV1AnswerTarget`, and `resolveRoundCoverageMetadata`. `resolveRoundCoverageMetadata` must merge venue defaults with optional round override fields and return an explicit resolved fallback strategy every time. Create `packages/domain/src/json-schema.ts` using Zod 4 JSON Schema export so later tooling can reuse the same contract surface instead of hand-written schemas. Update `packages/domain/src/index.ts` to export the schemas, helpers, and JSON Schema map.</action>
  <verify>
    <automated>corepack pnpm vitest run tests/contract/round-contract.test.ts tests/contract/mixed-media-round.test.ts tests/contract/venue-profile-resolution.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/domain/src/schemas.ts` contains `z.strictObject`.
    - `packages/domain/src/schemas.ts` contains `z.discriminatedUnion("kind"`.
    - `packages/domain/src/schemas.ts` contains the exact answer-target literals `"circuit"` and `"venue"`.
    - `packages/domain/src/schemas.ts` contains `canonicalId`, `displayName`, `correctPoints`, `partialPoints`, and `street_view_internal`.
    - `packages/domain/src/normalize.ts` contains `resolveRoundCoverageMetadata`.
    - `packages/domain/src/normalize.ts` contains `fallbackStrategyOverride` or `resolvedFallbackStrategy`.
    - `packages/domain/src/json-schema.ts` contains `toJSONSchema`.
  </acceptance_criteria>
  <done>The runtime domain layer enforces the authored round contract and the PACK-02, PACK-03, and OPS-01 tests pass from the repo root.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored round objects -> domain validation | Untrusted authored data crosses into the canonical round/pack/venue contract here. |
| venue profile defaults -> round-level overrides | Incorrect merge logic can silently change coverage or fallback posture. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T | `packages/domain/src/schemas.ts` | mitigate | Use `z.strictObject()` and literal/discriminated unions so extra keys, wrong types, and unsupported answer targets are rejected immediately. |
| T-02-02 | T | `packages/domain/src/normalize.ts` | mitigate | Implement `resolveRoundCoverageMetadata` so every round ends with explicit resolved coverage and fallback values; do not allow implicit Street View defaults. |
| T-02-03 | I | `packages/domain/src/contracts.ts` -> `schemas.ts` | mitigate | Export one JSON Schema map from the same Zod schemas to prevent future tooling from drifting into a second contract definition. |
</threat_model>

<verification>
- [ ] `corepack pnpm vitest run tests/contract/round-contract.test.ts tests/contract/mixed-media-round.test.ts tests/contract/venue-profile-resolution.test.ts` passes
- [ ] `corepack pnpm exec tsc -b` passes after the new exports are added
- [ ] Contract tests mention the exact requirement IDs PACK-02, PACK-03, and OPS-01
</verification>

<success_criteria>

- All tasks completed
- The domain package rejects incomplete semantic rounds and accepts mixed-media clue ladders
- Coverage and fallback metadata resolve explicitly instead of remaining implicit author judgment
- No unsupported v1 answer targets become valid content in this plan

</success_criteria>

<output>
After completion, create `.planning/phases/01-authored-round-contract/01-02-SUMMARY.md`
</output>
