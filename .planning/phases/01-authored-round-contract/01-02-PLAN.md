---
phase: 01-authored-round-contract
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [
  "packages/domain/src/schemas.ts",
  "packages/domain/src/normalize.ts",
  "packages/domain/src/json-schema.ts",
  "packages/domain/src/index.ts",
  "tests/contract/round-contract.test.ts",
  "tests/contract/mixed-media-round.test.ts",
  "tests/contract/venue-profile-resolution.test.ts"
]
autonomous: true
requirements: [PACK-02, PACK-03, OPS-01]
future_preservation:
  preserved_seams:
    - "Keep supported v1 answer targets narrow while preserving explicit lineage for later depth."
    - "Keep clue-family semantics separate from media transport."
  non_decisions:
    - "Do not make `section`, `corner`, or `composite` valid v1 authored content."
  posture:
    - "Strict validation first; no UI-first authoring shortcuts."
user_setup: []
must_haves:
  truths:
    - "Rounds validate as semantic authored objects rather than thin geo records."
    - "Mixed media uses one clue-step contract."
    - "Venue coverage and fallback resolve explicitly."
  artifacts:
    - path: "packages/domain/src/schemas.ts"
      provides: "Strict runtime schemas for round, pack, venue, and compiled content"
      contains: "z.strictObject"
    - path: "tests/contract/round-contract.test.ts"
      provides: "Requirement-level regression coverage for PACK-02"
      contains: "PACK-02"
---

<objective>
Implement the strict runtime schema layer for the Phase 1 content contract and prove the core round, media, and coverage behaviors with contract tests.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write requirement-level contract tests for round semantics, mixed media, and coverage resolution</name>
  <files>tests/contract/round-contract.test.ts, tests/contract/mixed-media-round.test.ts, tests/contract/venue-profile-resolution.test.ts</files>
  <read_first>AGENTS.md, .planning/REQUIREMENTS.md, .planning/phases/01-authored-round-contract/01-CONTEXT.md, .planning/phases/01-authored-round-contract/01-RESEARCH.md, packages/domain/src/contracts.ts, discovery/11-circuit-coverage-audit.md</read_first>
  <action>Create the three contract test files first using inline fixture objects only. In `round-contract.test.ts`, name key tests with `PACK-02` and assert that rounds missing `answerTarget`, required lineage, `acceptedAnswers`, `reveal`, or `scoringProfile` are rejected. In `mixed-media-round.test.ts`, assert one round can mix `street_view` and `map_fragment` media while preserving `circuit_internal` and `venue_approach` clue families. In `venue-profile-resolution.test.ts`, assert venue `coverageClass` plus `defaultFallbackStrategy` resolve onto rounds and that a round-level override wins when present.</action>
  <verify>
    <automated>test -f tests/contract/round-contract.test.ts && test -f tests/contract/mixed-media-round.test.ts && test -f tests/contract/venue-profile-resolution.test.ts && rg -n "PACK-02|PACK-03|OPS-01" tests/contract/*.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/contract/round-contract.test.ts` contains `PACK-02` and `lineage`.
    - `tests/contract/mixed-media-round.test.ts` contains `street_view`, `map_fragment`, `circuit_internal`, and `venue_approach`.
    - `tests/contract/venue-profile-resolution.test.ts` contains `allow_with_fallback` and `fallback_first`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement strict schemas, normalization helpers, and JSON Schema exports until the contract tests pass</name>
  <files>packages/domain/src/schemas.ts, packages/domain/src/normalize.ts, packages/domain/src/json-schema.ts, packages/domain/src/index.ts</files>
  <read_first>AGENTS.md, .planning/REQUIREMENTS.md, .planning/phases/01-authored-round-contract/01-CONTEXT.md, .planning/phases/01-authored-round-contract/01-RESEARCH.md, packages/domain/src/contracts.ts, tests/contract/round-contract.test.ts, tests/contract/mixed-media-round.test.ts, tests/contract/venue-profile-resolution.test.ts</read_first>
  <action>Implement `packages/domain/src/schemas.ts` using `z.strictObject()` throughout. Define strict schemas for answer-target lineage, answer targets, scoring profiles, clue media, clue steps, venue media capabilities, venue profiles, pack round references, rounds, packs, and compiled output. Enforce the v1 supported-answer-target rules from the context: `venue` rounds require `lineage.venueId`, `circuit` rounds require both `lineage.venueId` and `lineage.circuitId`, and deeper keys remain optional reserved depth only. Implement `packages/domain/src/normalize.ts` with alias normalization, supported-target checks, and `resolveRoundCoverageMetadata`. Add `packages/domain/src/json-schema.ts` using Zod JSON Schema export, then export the runtime schemas and helpers from `packages/domain/src/index.ts`.</action>
  <verify>
    <automated>corepack pnpm exec vitest run tests/contract/round-contract.test.ts tests/contract/mixed-media-round.test.ts tests/contract/venue-profile-resolution.test.ts && corepack pnpm exec tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `packages/domain/src/schemas.ts` contains `z.strictObject`.
    - `packages/domain/src/schemas.ts` contains `z.discriminatedUnion("kind"`.
    - `packages/domain/src/schemas.ts` contains `"circuit"` and `"venue"`.
    - `packages/domain/src/schemas.ts` contains `canonicalId`, `displayName`, `lineage`, `correctPoints`, and `partialPoints`.
    - `packages/domain/src/normalize.ts` contains `resolveRoundCoverageMetadata`.
    - `packages/domain/src/json-schema.ts` contains `toJSONSchema`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored round objects -> domain validation | Untrusted authored data crosses into the canonical round/pack/venue contract here |
| venue defaults -> round overrides | Incorrect merge logic can silently change fallback posture |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T | `packages/domain/src/schemas.ts` | mitigate | Use `z.strictObject()` and literal/discriminated unions so unsupported shapes are rejected immediately. |
| T-02-02 | T | `packages/domain/src/normalize.ts` | mitigate | Make coverage and fallback resolution explicit rather than implicit. |
| T-02-03 | I | runtime schema export | mitigate | Export JSON Schema from the same Zod sources to prevent contract drift. |
</threat_model>

<verification>
- [ ] `corepack pnpm exec vitest run tests/contract/round-contract.test.ts tests/contract/mixed-media-round.test.ts tests/contract/venue-profile-resolution.test.ts` passes
- [ ] `corepack pnpm exec tsc -b` passes
</verification>

<success_criteria>
- All tasks completed
- The domain package rejects incomplete semantic rounds and accepts mixed-media clue ladders
- Coverage and fallback metadata resolve explicitly instead of remaining implicit author judgment
- No unsupported v1 answer targets become valid content in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/01-authored-round-contract/01-02-SUMMARY.md`
</output>
