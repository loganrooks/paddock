---
context: phase
phase: 01-authored-round-contract
task: 0
total_tasks: 0
status: paused
last_updated: 2026-04-08T22:00:31.553Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: No recursive GSD orchestration — Do not spawn a generic agent to invoke a GSD skill that itself spawns role agents — Structural mitigation: top-level orchestrator runs the workflow logic and spawns only the concrete role agents directly.
- [ ] CONSTRAINT: No manual substitute plans — Do not hand-author PLAN artifacts or checker outcomes when planner/checker execution is degraded or ambiguous — Structural mitigation: either restore/debug the real planning path or halt and record the blocker before execution.
- [ ] CONSTRAINT: `read_first` must only reference existing files — Create-target files in `read_first` caused a bad Phase 1 plan and broke execution — Structural mitigation: point `read_first` only at files that already exist and that the executor truly must inspect first.
- [ ] CONSTRAINT: Silence is not failure — Short subagent quiet periods led to premature diagnosis and bad recovery decisions — Structural mitigation: use a real gauntlet first: longer wait, artifact/commit check, one status probe, then classify as blocked.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Recursive GSD call graph | A generic spawned agent was told to run `gsd-plan-phase`, which itself was designed to spawn `gsd-phase-researcher`, `gsd-planner`, and `gsd-plan-checker`. This created broken-telephone risk and made the stuck behavior hard to reason about. | blocking | Keep orchestration at the top level. The top-level orchestrator may run workflow logic, but it should spawn only the concrete role agents directly. |
| Manual substitute planning | When the planner/checker path felt uncertain, a manual Phase 1 plan was written and then trusted too far. That directly led to the `read_first` bug and wasted execution time. | blocking | Do not hand-write replacement plan artifacts in place of a real planner/checker pass. If the agent path is degraded, debug it or stop. |
| Invalid `read_first` targets | The earlier bad `01-02-PLAN.md` listed files that did not exist yet in `read_first`, which contradicted the executor contract and stalled execution. | blocking | Before trusting any plan, inspect whether every `read_first` path exists at plan time. Create-targets belong in tasks, not in `read_first`. |
| Premature stall diagnosis | Quiet planner/checker runs were treated as failed too quickly, which fed the bad manual recovery path. | blocking | Give long-running role agents real runway. Check for new artifacts or commits before declaring a stall. |
| Bare `pnpm` assumption | Earlier planning used bare `pnpm` even though this repo/environment expects `corepack pnpm`. | advisory | Prefer `corepack pnpm` unless the environment has already proven bare `pnpm` works. |
| Codex model ambiguity | The repo originally relied on generic GSD profile semantics plus `resolve_model_ids: omit`, which left Codex role-model selection too unclear. | advisory | Use `.planning/config.json` `model_overrides` for explicit Codex role models and apply reasoning effort explicitly at orchestration time. |

**Severity values:** `blocking` — resuming agent must pass understanding check before proceeding. `advisory` — important context, does not gate resumption.

<current_state>
Phase 1 is paused after exploratory context capture and orchestration-policy repair. There are no Phase 1 plans yet. The repo is clean, the core role models are now pinned to `gpt-5.4`, and the next session should restart Phase 1 planning from the top level using direct role-agent spawns only.
</current_state>

<completed_work>

Completed Tasks:
- Task 1: Reset the repo to a clean pre-Phase-1-plan state while preserving the bad attempt on a checkpoint branch.
- Task 2: Re-ran Phase 1 discuss cleanly and produced `01-CONTEXT.md` and `01-DISCUSSION-LOG.md`.
- Task 3: Restored and strengthened `AGENTS.md` guardrails around local GSD usage, orchestration discipline, and plan-authoring constraints.
- Task 4: Patched `.planning/config.json` to pin `gsd-phase-researcher`, `gsd-planner`, `gsd-plan-checker`, and `gsd-executor` to `gpt-5.4`.
- Task 5: Documented the Codex-native model and orchestration policy in `AGENTS.md` and `tooling/portable-gsd/README.md`.
</completed_work>

<remaining_work>

- Restart Phase 1 planning from scratch using top-level orchestration only.
- Spawn `gsd-phase-researcher`, then `gsd-planner`, then `gsd-plan-checker` directly with explicit reasoning effort.
- Only proceed to execution if planning and checking complete cleanly.
</remaining_work>

<decisions_made>

- Use repo-local regular GSD for Codex, not GSD Reflect.
- Treat Codex GSD orchestration as a top-level responsibility; do not nest skills inside generic spawned agents.
- Pin core GSD role agents to `gpt-5.4` via `.planning/config.json` `model_overrides`.
- Apply `gpt-5.4` with `xhigh` reasoning for the top-level orchestrator and `gpt-5.4` with `high` reasoning for the core role agents.
</decisions_made>

<blockers>
- There is no technical repo-state blocker now. The active blocker is methodological: planning must be rerun under the corrected non-recursive orchestration path before any execution resumes.
</blockers>

## Required Reading (in order)
1. [AGENTS.md](/home/rookslog/workspace/projects/prix-guesser/AGENTS.md) — repo-local GSD rules, orchestration constraints, and Codex model policy.
1. [.planning/config.json](/home/rookslog/workspace/projects/prix-guesser/.planning/config.json) — confirms `model_overrides`, `workflow.discuss_mode`, and `auto_advance` state.
1. [.planning/STATE.md](/home/rookslog/workspace/projects/prix-guesser/.planning/STATE.md) — canonical current project position.
1. [01-CONTEXT.md](/home/rookslog/workspace/projects/prix-guesser/.planning/phases/01-authored-round-contract/01-CONTEXT.md) — Phase 1 steering brief.
1. [01-DISCUSSION-LOG.md](/home/rookslog/workspace/projects/prix-guesser/.planning/phases/01-authored-round-contract/01-DISCUSSION-LOG.md) — audit trail of the exploratory discuss session.

## Critical Anti-Patterns (do NOT repeat these)
- Recursive skill-inside-agent orchestration: top-level orchestrator must spawn role agents directly.
- Manual substitute planning after agent uncertainty: debug or halt, do not fabricate plan artifacts.
- Create-target files in `read_first`: validate existence before trusting a plan.
- Treating short silence as failure: require the longer diagnostic gauntlet first.

## Infrastructure State
- Repo-local GSD runtime lives at `.codex/get-shit-done`.
- Portable overlay docs live at `tooling/portable-gsd/README.md`.
- Core GSD role agents are pinned to `gpt-5.4` through `.planning/config.json` `model_overrides`.
- No background servers or watchers are intentionally running for this repo.

<context>
The repo is finally back in a good state, but the next session must not rush. The main technical work has not started yet. The most important thing is to resume with disciplined top-level orchestration: read the context, decide the exact direct agent call graph, and only then launch research/planning/checking. Do not let convenience turn into another recursive or manual workaround.
</context>

<next_action>
Start with: read `AGENTS.md`, `.planning/config.json`, `.planning/STATE.md`, and the Phase 1 context files, then top-level orchestrate Phase 1 planning directly with `gsd-phase-researcher`, `gsd-planner`, and `gsd-plan-checker` using `gpt-5.4` (`xhigh` top level, `high` subagents).
</next_action>
