---
phase: 01-authored-round-contract
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [
  "package.json",
  "pnpm-workspace.yaml",
  "pnpm-lock.yaml",
  "tsconfig.json",
  "vitest.config.ts",
  "packages/domain/package.json",
  "packages/domain/tsconfig.json",
  "packages/domain/src/contracts.ts",
  "packages/domain/src/index.ts",
  "packages/content-tools/package.json",
  "packages/content-tools/tsconfig.json",
  "packages/content-tools/src/index.ts",
  "packages/content-tools/src/cli.ts"
]
autonomous: true
requirements: [PACK-02, PACK-03, PACK-04, OPS-01]
user_setup: []
must_haves:
  truths:
    - "Authors can define packs, venue profiles, and rounds against one shared Phase 1 vocabulary instead of inventing field names per file."
    - "Authors can rely on fixed Phase 1 terms for answer targets, answer-target lineage, clue families, media kinds, scoring profiles, and fallback metadata before any YAML fixtures are written."
    - "Authors and tooling share stable root commands for typecheck, validate, and compile so later plans can extend behavior without renaming the content workflow."
    - "Authors and tooling share one canonical venue identity field and one canonical pack round-reference shape before any fixtures are authored."
  artifacts:
    - path: "package.json"
      provides: "Root workspace scripts and shared Phase 1 tooling dependencies"
      contains: "\"content:validate\""
    - path: "packages/domain/src/contracts.ts"
      provides: "Shared authored-content contract vocabulary and source/compiled interfaces"
      min_lines: 40
      contains: "AnswerTargetLineageSource"
    - path: "packages/content-tools/src/cli.ts"
      provides: "Stable CLI entrypoint path reserved for validate and compile commands"
      contains: "NOT_IMPLEMENTED_PHASE_01"
  key_links:
    - from: "package.json"
      to: "packages/content-tools/src/cli.ts"
      via: "root content scripts"
      pattern: "packages/content-tools/src/cli\\.ts"
    - from: "package.json"
      to: "content/packs/starter-pack/pack.yaml"
      via: "root content scripts target the canonical starter pack path"
      pattern: "content/packs/starter-pack/pack\\.yaml"
    - from: "package.json"
      to: ".planning/artifacts/phase-01/starter-pack.compiled.json"
      via: "root compile script pins one canonical output path"
      pattern: "\\.planning/artifacts/phase-01/starter-pack\\.compiled\\.json"
    - from: "tsconfig.json"
      to: "packages/domain/tsconfig.json"
      via: "project reference"
      pattern: "packages/domain/tsconfig\\.json"
    - from: "tsconfig.json"
      to: "packages/content-tools/tsconfig.json"
      via: "project reference"
      pattern: "packages/content-tools/tsconfig\\.json"
---

<objective>
Establish the repo-local workspace, package boundaries, and shared contract interfaces that every later Phase 1 implementation step will build against.

Purpose: Lock the file-first content-tooling shape early so later plans do not invent competing schemas or package layouts.
Output: Root workspace config, package manifests, shared TypeScript contracts, and a placeholder CLI path that later plans can replace without changing root scripts.
</objective>

<execution_context>
@/home/rookslog/workspace/projects/prix-guesser/.codex/get-shit-done/workflows/execute-plan.md
@/home/rookslog/workspace/projects/prix-guesser/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-authored-round-contract/01-CONTEXT.md
@.planning/phases/01-authored-round-contract/01-RESEARCH.md
@.planning/phases/01-authored-round-contract/01-VALIDATION.md
@discovery/14-gsd-seed.md
@discovery/10-critical-inheritance-geoguessr-core.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap the root workspace and Phase 1 command surface</name>
  <files>package.json, pnpm-workspace.yaml, pnpm-lock.yaml, tsconfig.json, vitest.config.ts</files>
  <read_first>AGENTS.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/01-authored-round-contract/01-CONTEXT.md, .planning/phases/01-authored-round-contract/01-RESEARCH.md, .planning/phases/01-authored-round-contract/01-VALIDATION.md</read_first>
  <action>Create a root `package.json` with `private: true`, `packageManager: "pnpm@10.33.0"`, and scripts named exactly `typecheck`, `test`, `test:contract`, `content:validate`, and `content:compile`. Set those script values explicitly to the final Phase 1 command surface: `typecheck = "tsc -b"`, `test = "vitest run"`, `test:contract = "vitest run --project contract"`, `content:validate = "tsx packages/content-tools/src/cli.ts validate content/packs/starter-pack/pack.yaml"`, and `content:compile = "tsx packages/content-tools/src/cli.ts compile content/packs/starter-pack/pack.yaml --output .planning/artifacts/phase-01/starter-pack.compiled.json"`. Add devDependencies `typescript`, `@types/node`, `vitest`, `tsx`, `zod`, and `yaml`. Create `pnpm-workspace.yaml` for `packages/*`. Create a root `tsconfig.json` with `composite: true`, project references to `packages/domain` and `packages/content-tools`, and no frontend-app assumptions. Create `vitest.config.ts` using Vitest 4 `test.projects` with one `contract` project over `tests/contract/**/*.test.ts`. Run `corepack pnpm install` so `pnpm-lock.yaml` exists and later plans can use local binaries without falling back to bare `pnpm`.</action>
  <verify>
    <automated>corepack pnpm exec vitest --version &gt;/dev/null &amp;&amp; corepack pnpm exec tsc --version &gt;/dev/null &amp;&amp; test -f package.json &amp;&amp; test -f pnpm-workspace.yaml &amp;&amp; test -f tsconfig.json &amp;&amp; test -f vitest.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"packageManager": "pnpm@10.33.0"`.
    - `package.json` contains the exact script keys `"content:validate"` and `"content:compile"`.
    - `package.json` contains `content/packs/starter-pack/pack.yaml`.
    - `package.json` contains `.planning/artifacts/phase-01/starter-pack.compiled.json`.
    - `pnpm-workspace.yaml` contains `packages:` and `- packages/*`.
    - `tsconfig.json` contains `packages/domain/tsconfig.json` and `packages/content-tools/tsconfig.json`.
    - `vitest.config.ts` contains `tests/contract/**/*.test.ts`.
  </acceptance_criteria>
  <done>Root workspace installs cleanly with `corepack pnpm`, and the shared TypeScript/Vitest/content command surface is pinned to the starter-pack path and canonical compile output path needed by later plans.</done>
</task>

<task type="auto">
  <name>Task 2: Create package skeletons and exact shared content interfaces</name>
  <files>packages/domain/package.json, packages/domain/tsconfig.json, packages/domain/src/contracts.ts, packages/domain/src/index.ts, packages/content-tools/package.json, packages/content-tools/tsconfig.json, packages/content-tools/src/index.ts, packages/content-tools/src/cli.ts</files>
  <read_first>AGENTS.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/01-authored-round-contract/01-CONTEXT.md, .planning/phases/01-authored-round-contract/01-RESEARCH.md, .planning/phases/01-authored-round-contract/01-VALIDATION.md, discovery/10-critical-inheritance-geoguessr-core.md</read_first>
  <action>Create `packages/domain` named `@prix/domain` and `packages/content-tools` named `@prix/content-tools`. In `packages/domain/src/contracts.ts`, define the exact shared vocabulary for Phase 1: `CoverageClass = "allow" | "allow_with_fallback" | "fallback_first"`, `FallbackStrategy = "street_view_primary" | "mixed_media_ladder" | "non_street_view_only"`, `ClueFamily = "circuit_internal" | "circuit_edge" | "venue_approach" | "city_context"`, `MediaKind = "street_view" | "image" | "map_fragment" | "text"`, `SupportedV1AnswerTarget = "circuit" | "venue"`, and `ExtensibleAnswerTarget` that reserves `"section"`, `"corner"`, and `"composite"` for later without making them valid v1 content. Freeze the exact Phase 1 inner shapes needed by Plans 02 and 03: `AnswerTargetLineageSource` with exact keys `venueId`, `circuitId`, `sectionId`, and `cornerId`; `AnswerTargetSource` with `kind`, `canonicalId`, `displayName`, and `lineage`; `ScoringProfileSource` as a discriminated union with exact `kind` values `circuit_standard` and `venue_standard` plus numeric `correctPoints` and `partialPoints`; `VenueMediaCapabilitiesSource` with exact boolean keys `street_view_internal`, `street_view_edge`, `approach_media`, `map_fragment`, `satellite`, `official_stills`, and `authored_text`; `VenueProfileSource` with canonical venue identity fields `id`, `displayName`, `circuitId`, `coverageClass`, `defaultFallbackStrategy`, `mediaCapabilities`, and `sourceRefs`; `PackRoundRefSource` with exact keys `roundId` and `path`; `PackSource` with `roundOrder: PackRoundRefSource[]`; `RoundSource` with exact `venueRef` resolving against `VenueProfileSource.id`, `circuitRef`, and optional `fallbackStrategyOverride`; and `CompiledRound` with `answerTargetLineage`, `resolvedCoverageClass`, and `resolvedFallbackStrategy`. Freeze the v1 lineage expectations now: every supported round keeps the full `lineage` object, `venue` rounds must carry `lineage.venueId`, `circuit` rounds must carry both `lineage.venueId` and `lineage.circuitId`, and `sectionId` plus `cornerId` stay optional reserved depth keys only. `VenueProfileSource.id` is the canonical venue key; do not define a parallel venue-profile `venueId` field or any alternate `venueRef` target. This task resolves the remaining `01-RESEARCH.md` open questions by standardizing on global `content/venues/*.yaml` profiles plus round-level fallback overrides, by freezing one canonical pack reference shape early, and by freezing the initial scoring vocabulary to `circuit_standard` and `venue_standard` with numeric fields only. Create `packages/content-tools/src/cli.ts` with `validate` and `compile` command branches that currently throw `NOT_IMPLEMENTED_PHASE_01` so root scripts already point at the final path. Export the shared types from both package `src/index.ts` files. Keep these files type-only; runtime validation arrives in later plans.</action>
  <verify>
    <automated>corepack pnpm exec tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `packages/domain/package.json` contains `"name": "@prix/domain"`.
    - `packages/content-tools/package.json` contains `"name": "@prix/content-tools"` and `@prix/domain`.
    - `packages/domain/src/contracts.ts` contains the exact literals `circuit_internal`, `venue_approach`, and `fallback_first`.
    - `packages/domain/src/contracts.ts` contains `SupportedV1AnswerTarget` and `AnswerTargetLineageSource`.
    - `packages/domain/src/contracts.ts` contains `canonicalId`, `displayName`, `lineage`, `venueId`, `circuitId`, `circuit_standard`, `venue_standard`, `correctPoints`, and `partialPoints`.
    - `packages/domain/src/contracts.ts` contains `VenueProfileSource`, `PackRoundRefSource`, `id`, `roundId`, `path`, `defaultFallbackStrategy`, `fallbackStrategyOverride`, `resolvedFallbackStrategy`, and `street_view_internal`.
    - `packages/content-tools/src/cli.ts` contains `NOT_IMPLEMENTED_PHASE_01`, `validate`, and `compile`.
  </acceptance_criteria>
  <done>The repo has stable package boundaries and one exact Phase 1 contract for answer targets, scoring profiles, venue metadata, media capabilities, and fallback resolution, so later plans can implement and author content without inventing new shapes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored content scripts -> local package entrypoints | Root commands decide which code path later authored files will pass through. |
| shared contracts -> downstream packages | Any drift in the source contract leaks ambiguity into validators, fixtures, and future rules code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | `package.json` scripts | mitigate | Point `content:validate` and `content:compile` only at `packages/content-tools/src/cli.ts` so later validation runs through one explicit entrypoint instead of ad hoc shell commands. |
| T-01-02 | T | `packages/domain/src/contracts.ts` | mitigate | Freeze answer-target, clue-family, media-kind, and coverage-class literals in one exported file so later plans cannot introduce conflicting vocabularies. |
| T-01-03 | D | Root workspace bootstrap | accept | Dependency install is local developer setup only in this phase; later plans add the actual parser and input-validation controls for authored files. |
</threat_model>

<verification>
- [ ] `corepack pnpm exec tsc -b` passes
- [ ] `corepack pnpm exec vitest --version` returns a local version
- [ ] Root scripts and package names match the exact strings in the acceptance criteria
</verification>

<success_criteria>

- All tasks completed
- Root workspace installs with `corepack pnpm`
- Shared contracts expose the exact Phase 1 vocabulary for rounds, packs, venues, clues, and compile results
- Shared contracts freeze `circuit_standard` / `venue_standard` scoring profiles, `street_view_primary` / `mixed_media_ladder` / `non_street_view_only` fallback strategies, answer-target lineage, and the canonical pack round-reference shape before schema or YAML authoring begins
- No app-framework or room-runtime files are introduced in this plan

</success_criteria>

<output>
After completion, create `.planning/phases/01-authored-round-contract/01-01-SUMMARY.md`
</output>
