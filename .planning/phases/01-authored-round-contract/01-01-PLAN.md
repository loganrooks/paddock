---
phase: 01-authored-round-contract
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [
  "package.json",
  "pnpm-workspace.yaml",
  "pnpm-lock.yaml",
  "tsconfig.json",
  "vitest.config.ts",
  "packages/domain/package.json",
  "packages/domain/tsconfig.json",
  "packages/domain/src/contracts.ts",
  "packages/domain/src/index.ts",
  "packages/content-tools/package.json",
  "packages/content-tools/tsconfig.json",
  "packages/content-tools/src/index.ts",
  "packages/content-tools/src/cli.ts"
]
autonomous: true
requirements: [PACK-02, PACK-03, PACK-04, OPS-01]
future_preservation:
  preserved_seams:
    - "Preserve explicit answer-target lineage and canonical pack/venue references."
    - "Keep authored content tooling separate from later rules and room orchestration."
  non_decisions:
    - "Do not decide room runtime or public wrapper features."
    - "Do not widen active v1 gameplay beyond `venue` and `circuit`."
  posture:
    - "Private-only, trusted-circle, file-first authored content workflow."
user_setup: []
must_haves:
  truths:
    - "Authors and tooling share one fixed Phase 1 vocabulary before fixtures are written."
    - "The repo exposes stable root commands for typecheck, test, validate, and compile."
    - "Canonical venue identity and pack round-reference shape are frozen before schema/runtime implementation."
  artifacts:
    - path: "packages/domain/src/contracts.ts"
      provides: "Shared Phase 1 contract vocabulary"
      contains: "AnswerTargetLineageSource"
    - path: "packages/content-tools/src/cli.ts"
      provides: "Stable CLI entrypoint reserved for validate and compile commands"
      contains: "NOT_IMPLEMENTED_PHASE_01"
---

<objective>
Bootstrap the repo-local workspace, package boundaries, and shared contract interfaces that every later Phase 1 plan will build against.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap the root workspace and command surface</name>
  <files>package.json, pnpm-workspace.yaml, pnpm-lock.yaml, tsconfig.json, vitest.config.ts</files>
  <read_first>AGENTS.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/01-authored-round-contract/01-CONTEXT.md, .planning/phases/01-authored-round-contract/01-RESEARCH.md, .planning/phases/01-authored-round-contract/01-VALIDATION.md</read_first>
  <action>Create a root `package.json` with `private: true`, `packageManager: "pnpm@10.33.0"`, and scripts named exactly `typecheck`, `test`, `test:contract`, `content:validate`, and `content:compile`. Set the script values to `tsc -b`, `vitest run`, `vitest run --project contract`, `tsx packages/content-tools/src/cli.ts validate content/packs/starter-pack/pack.yaml`, and `tsx packages/content-tools/src/cli.ts compile content/packs/starter-pack/pack.yaml --output .planning/artifacts/phase-01/starter-pack.compiled.json`. Add devDependencies `typescript`, `@types/node`, `vitest`, `tsx`, `zod`, and `yaml`. Create `pnpm-workspace.yaml` for `packages/*`, a root `tsconfig.json` with project references to `packages/domain` and `packages/content-tools`, and `vitest.config.ts` using a `contract` project over `tests/contract/**/*.test.ts`. Run `corepack pnpm install` so `pnpm-lock.yaml` exists.</action>
  <verify>
    <automated>test -f package.json && test -f pnpm-workspace.yaml && test -f tsconfig.json && test -f vitest.config.ts && corepack pnpm exec tsc --version >/dev/null && corepack pnpm exec vitest --version >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"packageManager": "pnpm@10.33.0"`.
    - `package.json` contains `"content:validate"` and `"content:compile"`.
    - `package.json` contains `content/packs/starter-pack/pack.yaml`.
    - `package.json` contains `.planning/artifacts/phase-01/starter-pack.compiled.json`.
    - `pnpm-workspace.yaml` contains `packages:` and `- packages/*`.
    - `tsconfig.json` contains `packages/domain/tsconfig.json` and `packages/content-tools/tsconfig.json`.
    - `vitest.config.ts` contains `tests/contract/**/*.test.ts`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create package skeletons and exact shared content interfaces</name>
  <files>packages/domain/package.json, packages/domain/tsconfig.json, packages/domain/src/contracts.ts, packages/domain/src/index.ts, packages/content-tools/package.json, packages/content-tools/tsconfig.json, packages/content-tools/src/index.ts, packages/content-tools/src/cli.ts</files>
  <read_first>AGENTS.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/01-authored-round-contract/01-CONTEXT.md, .planning/phases/01-authored-round-contract/01-RESEARCH.md, discovery/10-critical-inheritance-geoguessr-core.md</read_first>
  <action>Create `packages/domain` named `@prix/domain` and `packages/content-tools` named `@prix/content-tools`. In `packages/domain/src/contracts.ts`, define exact Phase 1 literals for `CoverageClass`, `FallbackStrategy`, `ClueFamily`, `MediaKind`, supported v1 answer targets, and reserved extensible answer-target depth. Freeze `AnswerTargetLineageSource`, `AnswerTargetSource`, `ScoringProfileSource`, `VenueMediaCapabilitiesSource`, `VenueProfileSource`, `PackRoundRefSource`, `PackSource`, `RoundSource`, and `CompiledRound` with canonical venue identity rooted at `VenueProfileSource.id` and pack round references rooted at exact `{ roundId, path }`. Keep `packages/content-tools/src/cli.ts` as a stable command path with `validate` and `compile` branches that still throw `NOT_IMPLEMENTED_PHASE_01`. Export the shared types from both package `src/index.ts` files.</action>
  <verify>
    <automated>corepack pnpm exec tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `packages/domain/package.json` contains `"name": "@prix/domain"`.
    - `packages/content-tools/package.json` contains `"name": "@prix/content-tools"`.
    - `packages/domain/src/contracts.ts` contains `AnswerTargetLineageSource`, `PackRoundRefSource`, `VenueProfileSource`, and `CompiledRound`.
    - `packages/domain/src/contracts.ts` contains `venueId`, `circuitId`, `roundId`, `path`, `correctPoints`, `partialPoints`, and `defaultFallbackStrategy`.
    - `packages/content-tools/src/cli.ts` contains `validate`, `compile`, and `NOT_IMPLEMENTED_PHASE_01`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| root scripts -> package entrypoints | Root commands define how all later authored files enter the toolchain |
| shared contracts -> downstream packages | Ambiguity here would leak conflicting vocabulary into schemas, fixtures, and compile tooling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | `package.json` scripts | mitigate | Point validation and compile scripts only at `packages/content-tools/src/cli.ts`. |
| T-01-02 | T | `packages/domain/src/contracts.ts` | mitigate | Freeze answer-target, clue-family, media-kind, coverage-class, and reference vocabulary in one exported file. |
| T-01-03 | D | root bootstrap | accept | Dependency install is local setup only in this plan; authored input handling lands in later plans. |
</threat_model>

<verification>
- [ ] `corepack pnpm exec tsc -b` passes
- [ ] `corepack pnpm exec vitest --version` returns a local version
- [ ] Root scripts and package names match the acceptance criteria
</verification>

<success_criteria>
- All tasks completed
- Root workspace installs with `corepack pnpm`
- Shared contracts expose the exact Phase 1 vocabulary for rounds, packs, venues, clues, and compile results
- No app-framework or room-runtime files are introduced in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/01-authored-round-contract/01-01-SUMMARY.md`
</output>
