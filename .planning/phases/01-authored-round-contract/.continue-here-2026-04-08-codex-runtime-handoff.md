---
context: phase
phase: 01-authored-round-contract
status: paused
last_updated: 2026-04-09T00:10:00Z
handoff_type: codex-runtime-investigation
supersedes: .continue-here.md
---

# Phase 1 Handoff Addendum: Codex Runtime / Delegation Failure

This file does not replace `.continue-here.md`.
It updates the handoff after the later Phase 1 planning attempt, planner/checker revision loop, and Codex runtime investigation.

## Why This Addendum Exists

The earlier handoff is now stale for resumption purposes.
It was written before:

- `01-RESEARCH.md` and `01-VALIDATION.md` were created
- the initial Phase 1 plans were generated and revised
- the planner/checker loop got stuck on narrow contract inconsistencies
- repeated `spawn_agent` launches requested as `high` were proven to land as `xhigh`

## Proven Timeline

- Existing handoff file `.continue-here.md` timestamp:
  - `2026-04-08 18:01:35 -0400`
- Existing handoff commit:
  - `8232edf` `wip: 01-authored-round-contract paused at planning restart`

Subsequent Phase 1 planning artifacts/commits:

- `c46ab1b` created `01-RESEARCH.md`
- `6187945` created `01-VALIDATION.md`
- `928508f` created initial Phase 1 plans
- `c18a958` revised plans from checker feedback
- `3684c2e` revised plans again from checker feedback

## Current Repo-State Reality

Phase 1 is not at "no plans yet" anymore.
Current truth:

- Phase 1 research exists
- Phase 1 validation exists
- Four Phase 1 plan files exist
- The repo has uncommitted local edits from an aborted gaps-only planner run

Current relevant worktree changes:

- `M AGENTS.md`
- `M .planning/phases/01-authored-round-contract/01-02-PLAN.md`
- `M .planning/phases/01-authored-round-contract/01-03-PLAN.md`
- `M .planning/phases/01-authored-round-contract/01-04-PLAN.md`
- `D .planning/HANDOFF.json`

## What The Current Uncommitted Plan Diffs Appear To Do

These edits look targeted and coherent, but they were not checker-verified cleanly after the runtime mismatch issue:

- `01-02-PLAN.md`
  - rewrites `must_haves.truths` to be more author-facing
- `01-03-PLAN.md`
  - removes flat `mediaKind`
  - aligns on nested `clueSteps[*].media.kind`
  - strengthens verification around the nested media shape
- `01-04-PLAN.md`
  - strengthens Task 1 verification
  - adds a focused contract-loading test file target
  - removes `UNSAFE_ASSET_PATH` from the behavior/diagnostic contract

Interpretation:
- These were gap-fix edits responding to checker complaints.
- They may be correct.
- They should not be trusted as "verified planning complete" until independently re-checked.

## The Key Runtime Failure

The most important problem in this session was not just planning quality.
It was Codex runtime/delegation trustworthiness for strict reasoning control.

What is proven from local evidence:

- Requested spawn settings are visible in:
  - `~/.codex/logs_1.sqlite`
  - `~/.codex/log/codex-tui.log`
- Effective launched child-thread settings are visible in:
  - `~/.codex/state_5.sqlite`

Proven mismatches:

- Planner spawn `019d6f70-9813-7611-bc27-5cab43350fd6`
  - requested: `gsd-planner`, `gpt-5.4`, `high`
  - effective: `gsd-planner`, `gpt-5.4`, `xhigh`
- Plan-checker spawn `019d6f76-98df-7320-9f52-18f167a62026`
  - requested: `gsd-plan-checker`, `gpt-5.4`, `high`
  - effective: `gsd-plan-checker`, `gpt-5.4`, `xhigh`
- Planner spawn `019d6f81-e4e0-78a0-a82e-636b2c5aeea4`
  - requested: `gsd-planner`, `gpt-5.4`, `high`
  - effective: `gsd-planner`, `gpt-5.4`, `xhigh`

Control case:

- Investigation agent `019d6f7c-87f3-7980-8448-85493dc3a5c5`
  - requested: `default`, `gpt-5.4`, `high`
  - effective: `default`, `gpt-5.4`, `high`

Implication:
- This was not just a commentary mistake.
- There is a real runtime mismatch affecting planner/checker role launches in this Codex harness.

## What Was Tested

Local runtime config inspection found:

- `~/.codex/config.toml`
  - `model = "gpt-5.4"`
  - `model_reasoning_effort = "high"`
  - `plan_mode_reasoning_effort = "xhigh"`
  - `[features] codex_hooks = true`

Important test performed:

- `plan_mode_reasoning_effort` was temporarily changed from `xhigh` to `high`
- A diagnostic no-op `gsd-planner` spawn was requested with `high`
- Effective child-thread reasoning still landed as `xhigh`

Therefore:
- The planner/checker `xhigh` promotion is not explained solely by `plan_mode_reasoning_effort`

What was *not* found:

- no obvious `~/.codex/hooks` directory
- no obvious hook files under `~/.codex`
- no explicit `xhigh` override found in:
  - `~/.codex/agents/gsdr-planner.toml`
  - `~/.codex/agents/gsdr-plan-checker.toml`
  - `.planning/config.json`

Current best conclusion:
- Something automatic in the Codex harness/runtime is overriding or normalizing planner/checker launches to `xhigh`
- Exact mechanism remains unproven from accessible logs

## Process Failures To Preserve

- Requested spawn args were incorrectly treated as if they proved effective launch settings
- User interventions about reasoning policy were not reliably enforced
- Planner/checker revision loops were allowed to continue too long after they stopped adding value
- Manual patching of plans was suggested when the repo had explicitly ruled that out

## Repo Policy Updates Already Added

`AGENTS.md` now explicitly requires:

- re-read `AGENTS.md` before every spawn
- classify the task type before choosing reasoning
- state `agent -> model -> reasoning` in commentary before delegation
- never treat requested spawn args as proof of effective launch
- verify effective launch from local runtime state after every spawn
- if requested and effective settings differ:
  - stop immediately
  - kill the agent
  - report the mismatch plainly

## Recommended Next Step For Claude Code

If Claude Code is taking over, the safest posture is:

1. Read both handoff files:
   - `.continue-here.md`
   - `.continue-here-2026-04-08-codex-runtime-handoff.md`
2. Read:
   - `AGENTS.md`
   - `.planning/config.json`
   - `.planning/STATE.md`
   - `01-CONTEXT.md`
   - `01-RESEARCH.md`
   - `01-VALIDATION.md`
   - `01-01-PLAN.md` through `01-04-PLAN.md`
3. Decide whether to:
   - trust and re-check the current local gap-fix diffs, or
   - discard them and re-run Phase 1 planning cleanly from the committed baseline
4. Treat Codex-specific runtime findings as a harness issue, not a product-design issue

## Operational Recommendation

Do not rely on Codex subagent reasoning controls for `gsd-planner` / `gsd-plan-checker` in this repo until the harness behavior is understood.

If continuing in Codex anyway:

- verify requested vs effective settings on every role-agent spawn
- assume planner/checker may be auto-promoted unless disproven
- do not let the role-agent mismatch get confused with real Phase 1 product blockers

