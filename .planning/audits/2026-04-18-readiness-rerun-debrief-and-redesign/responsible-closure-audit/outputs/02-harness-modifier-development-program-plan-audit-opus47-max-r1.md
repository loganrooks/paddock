Date: 2026-04-22
Status: frozen external-lane return

# Harness Modifier Development Program Plan Audit

## Overall Judgment

- [g:r:i] Disposition: `accept with revisions`. `166` is the right shape for the next integrated bounded object in the harness-modifier development program. It gathers responsible closure, development-side parallelization, intervention lifecycle discipline, horizons, task-type routing, and actual/virtual intervention surfaces into one carrier without collapsing them, and it keeps deployment-readiness and harness-in-action workflow rewrites out of frame.
- [g:r:i] The revisions are bounded and carry-focused, not a rewrite. The plan's weakest axis is not its structure; it is that it does not yet inherit by name the durable taxonomies already frozen in adjacent lane `01` returns (six-surface split, five-family signal taxonomy, three-class parallelization taxonomy). Without that named inheritance, later consumers will reconstruct those distinctions ad hoc from chat memory or from route note `161` alone, which is exactly what closure lane `01` was trying to prevent.
- [d:r:i] Anti-misread fences the plan must keep (and this audit must keep):
  - [d:r:i] do not let `accept with revisions` collapse into a ship-gate verdict; the plan is a protocol-pressure carrier, not a deployment object
  - [d:r:i] do not let the revisions list become a single implementation queue; they are per-carrier sharpenings with their own ownership
  - [d:r:i] do not demand harness-in-action workflow rewrites from this plan; those stay protected-future carries with their own triggering basis
  - [d:r:i] do not flatten actual and possible intervention surfaces into one inventory; their distinct treatment is part of why `166` is the right next object

## What The Plan Clarifies Well

- [d:r:i] The domain split is load-bearing and correctly named. `166` routes uplift through the six targets (`harness-contract` / `host-artifact` / `harness-agential` / `harness-operational` / `harness-adaptive` / `distribution/deployability`) rather than one overloaded `uplift` umbrella. This preserves the `162` semantic split as the operating vocabulary instead of re-flattening it.
- [d:r:i] The parallelization scope rule is crisp. `Development-side parallelization for the harness-modifier program is the active protocol question now` paired with `Harness-in-action parallelization remains a real later family, but it is not the current implementation target for this plan` closes the main flattening risk that `164` and `165` left open. The `Out of scope for this plan` list in `Parallelization Rule` (harness-in-action workflow rewrites, execute-family/review-family redesigns, broad multi-provider portability) makes the out-of-scope surface a first-class element of the plan rather than an aside.
- [d:r:i] The actual/virtual intervention-surface split is preserved as two distinct sections with different stances. The plan does not treat virtual surfaces as latent implementation debt; it treats them as openable surfaces whose promotion is conditional. That matches the anti-threshold posture from repo doctrine and the protected-futures discipline from `163`.
- [d:r:i] Responsible closure is kept as layered carry, not a finish line. The `Responsible Closure Rule` names the closure question as stronger control / routing / maintainability / adaptive capacity rather than `more proof that the current modifier can run`, and the `Role` section explicitly refuses the ship-gate frame. This is the correct inheritance of lane `01`'s anti-threshold fence.
- [d:r:i] The four-workstream shape (A: dev operations and protocol; B: responsible closure and deployability; C: propagation and verification; D: intervention-surface expansion) partitions ownership cleanly and keeps the current-status line per stream honest (`under-carried`, `first field map completed`, `field map exists but parallelization-adjacent consequences uneven`, `actual partly visible, virtual not yet governed strongly`). The `next type` field on each workstream travels the task-type vocabulary into ownership, which is the right integration move.
- [d:r:i] The `Exact Next Moves` ordering — audit this plan cross-vendor before cutting the next protocol tranche; pair protocol slice with targeted propagation audit on already-landed parallelization-adjacent workflow changes; keep harness-in-action parallelization explicit-but-later — preserves the parallelization internal cross-audit's protocol-pressure recommendation and the closure lane `01`'s pre-deployment sequencing.

## Actual Intervention Surfaces

- [d:r:i] The class-level enumeration in `166` covers the right families: audit/review lane machinery, governance routing, propagation carry, verification and materialization, extraction/rehome work, responsible-closure field mapping, development-overlap governance. Reading it as a map of where intervention is live, the families are partitioned and none is secretly absorbed into another.
- [d:r:i] The weaker axis here is pointer-level. A consumer re-entering the plan has to reconstruct which concrete carriers are meant. The plan would broaden by naming, under each family, at least one current authoritative pointer so the map is usable without a parallel grep pass:
  - [d:r:i] `audit / review lane machinery`: [AUDIT-LANE-PATTERN-LIBRARY.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/AUDIT-LANE-PATTERN-LIBRARY.md), [AUDIT-CANON-ABSORPTION-PROTOCOL.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/AUDIT-CANON-ABSORPTION-PROTOCOL.md), [AUDIT-SUBTREE-AGING-AND-GRADUATION.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/AUDIT-SUBTREE-AGING-AND-GRADUATION.md), [AUDIT-SUBTREE-STATUS-REGISTER.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/AUDIT-SUBTREE-STATUS-REGISTER.md), and the `capture_launch_truth.py --since` launch-truth carrier
  - [d:r:i] `verification and materialization`: [harness_modifier/contract/harness_canary.py](/home/rookslog/workspace/projects/prix-guesser/harness_modifier/contract/harness_canary.py), [harness_modifier/contract/runtime_visibility.py](/home/rookslog/workspace/projects/prix-guesser/harness_modifier/contract/runtime_visibility.py), [harness_modifier/contract/portable_gsd_contract.py](/home/rookslog/workspace/projects/prix-guesser/harness_modifier/contract/portable_gsd_contract.py), `manifest_install_coherence.py` where present, and the `verify-materialized --strict` entry
  - [d:r:i] `propagation carry`: [.codex/get-shit-done/workflows/propagation-review.md](/home/rookslog/workspace/projects/prix-guesser/.codex/get-shit-done/workflows/propagation-review.md), the `propagation-audit/artifacts/02-06` registry v2 layered carriers, and the baseline/delta pair from `95`/`96`
  - [d:r:i] `extraction / rehome work`: [harness_modifier/compatibility/declaration.json](/home/rookslog/workspace/projects/prix-guesser/harness_modifier/compatibility/declaration.json), [harness_modifier/compatibility/declaration.py](/home/rookslog/workspace/projects/prix-guesser/harness_modifier/compatibility/declaration.py), [harness_modifier/overlay/helpers/AUTHORITY-MAP.md](/home/rookslog/workspace/projects/prix-guesser/harness_modifier/overlay/helpers/AUTHORITY-MAP.md), [harness_modifier/overlay/ROSTER.md](/home/rookslog/workspace/projects/prix-guesser/harness_modifier/overlay/ROSTER.md), the first overlay source-authority split from `148`, and the host-planning-shape externalization from `159`
  - [d:r:i] `governance routing`: the register's `Horizon Routing` section as the carrier-class surface, not only the register as a whole; [.planning/AGENTS.md](/home/rookslog/workspace/projects/prix-guesser/.planning/AGENTS.md) `Launch-Truth Discipline` and `Contract-Propagation Hygiene`; [AGENTS.md](/home/rookslog/workspace/projects/prix-guesser/AGENTS.md) `Delegation And Orchestration`; the [CLAUDE.md](/home/rookslog/workspace/projects/prix-guesser/CLAUDE.md) and [.planning/CLAUDE.md](/home/rookslog/workspace/projects/prix-guesser/.planning/CLAUDE.md) wrappers as the cross-vendor translation surface
  - [d:r:i] `development-overlap governance`: [AUDIT-LANE-PATTERN-LIBRARY.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/AUDIT-LANE-PATTERN-LIBRARY.md) `Bounded Parallelization And Overlap` (lines 77-104), with the already-enumerated forbidden-overlap set (governance-role edits, refmap/topology rewrite, installer reinstall, Phase 01 rerun crossing) carried by reference rather than restated
- [d:r:i] One actual surface that is underrepresented: review-workflow carry. The `gsd-review` helper-backed first slice is now landed through `145` plus propagation refresh `53`, and `review-route-audit/` lane `01` is the first completed review-route audit family. `166` currently mentions audit/review lane machinery for harness-improvement-program audits but does not name the review-workflow family as an active intervention surface for the program. It should be added, because later responsible-closure audits and cross-vendor lane launches route through that family.
- [d:r:i] One actual surface that is correctly held at low profile but underpriced: the operator-directive surface in the harness register (`Current Operator Directive`). This is the live steering carrier that tells later lanes what the program cannot shortcut. `166` inherits its spirit (no shortcuts that trade away future control) but does not name it. Naming it turns the directive into one of the plan's own inheritance hooks rather than ambient background.

## Possible Or Virtual Intervention Surfaces

- [d:r:i] The list of nine virtual surfaces in `166` is the right shape for a `virtual`-stance section: development parallelization protocol carrier, intervention lifecycle protocol carrier, single-writer / lane-exclusive authority surface map, fan-out packet template, parallelization impact carrier inside propagation-review, development intervention-surface map, measurement/provenance carrier, observation carrier for host exercises, dogfooding surface.
- [d:r:i] Three of these are not `virtual` in the sense `166` currently uses; they are already classified as `held-later` carries under `parallelization-audit/` lane `01`'s disposition:
  - [d:r:i] `fan-out packet template` — held-later per [parallelization-audit/dispositions/01](../../parallelization-audit/dispositions/01-harness-parallelization-field-map-and-diagnosis-audit-inheritance.md) `Keep Explicitly Later`, with the triggering basis named (`after one more real overlap exercise`)
  - [d:r:i] `single-writer / lane-exclusive authority surface map` — held-later per the same disposition, with the triggering basis named (`verify it against one real multi-lane tranche before treating it as binding`)
  - [d:r:i] `parallelization impact carrier inside propagation-review` — held-later per the same disposition, with the triggering basis named (`when a bounded proposal next touches a parallelization-adjacent surface`)
- [d:r:i] Re-listing these three as `virtual` without naming their prior `held-later` carry risks silently demoting them. The plan broadens by distinguishing two sub-classes inside the section: surfaces that are already `held-later` with a named triggering basis, and surfaces that are net-virtual (no trigger yet earned). The other six entries stay net-virtual.
- [d:r:i] The virtual-surface promotion criteria in `166` are AND-joined as if all five were gates (`repeated pressure appears across more than one family; current carriers cannot hold; ownership can be made explicit; propagation consequences can be named; surface can be audited or verified once opened`). That reads as a bar. It should read as maturity indicators, any of which broadens the case for promotion:
  - [d:r:i] pressure replication signal (repeated across families)
  - [d:r:i] carrier insufficiency signal (current carriers fold the distinction rather than hold it)
  - [d:r:i] ownership nameability (someone could own it explicitly)
  - [d:r:i] propagation traceability (consequences can be enumerated in advance)
  - [d:r:i] audit/verification shape (the surface is inspectable once opened)
  - [d:r:i] rule of thumb: two or more signals earn a bounded proposal; all five earn promotion into an active carrier. Keeping this as signals-not-gates preserves the anti-threshold posture the rest of the plan carries.
- [d:r:i] One virtual surface that is shaped more sharply than `166` admits: `observation carrier for host exercises`. Closure lane `01` already specified what this carrier holds in its first slice (five signal families: `deployment-context` / `expectation-vs-observation` / `semantic-deviation` / `positive-gain` / `measurement-provenance`; observe-only; operator-triggered; single-writer; keyed to one host exercise; v2_split-style provenance). `166` should name this inheritance so the first proposal under that surface is not drafted from scratch.
- [d:r:i] One virtual surface the plan does not carry but the closure lane `01` schedule names: the cross-vendor responsible-closure audit from `161` commitment #6. Closure lane `01` explicitly said this should be held-later-scheduled against payload-home closure plus the first observation-carrier slice, not left ambient. `166` should carry it as a sixth `held-later-with-trigger` entry in the virtual/possible list so it does not quietly evaporate.

## Horizon Routing

- [d:r:i] The three-layer stack (short / medium / long) is present and mostly clean, and the `Scope Rule` at the top declares harness-program horizons distinct from host-product horizons. The separation holds at the doctrinal level but thins at each horizon layer. A later reader skimming only `Short Horizon` could re-import `.planning/LONG-ARC.md` or `.planning/ROADMAP.md` as the default long-horizon frame, which is exactly the misread `163` was written to prevent.
- [d:r:i] One broadening move: label each horizon heading with the program it belongs to (`Short Horizon (harness-program)`) so the inheritance from `163` is visible inside the horizon sections, not only inside the `Scope Rule`. The mapping is:
  - [d:r:i] `Short Horizon` → `Near Horizon` in `163` (current bounded slice plus required neighboring carry)
  - [d:r:i] `Medium Horizon` → `Medium Horizon` in `163` (active family field)
  - [d:r:i] `Long Horizon` → `Far Horizon` in `163` (orienting future, protected futures)
- [d:r:i] The `Long Horizon` protected-futures list in `166` carries five of the six protected futures from `163`: standalone repo, npm/npx distribution, dogfooding, richer telemetry/semantic-signal capture, parallelization-in-action. It does not carry `broader host-context deployability` as an explicit protected future, which `163` named. Adding it closes the protection of the `{.codex, .claude, mixed, regular-GSD host, GSD-Reflect-coexistence host}` field without turning it into an implementation promise.
- [d:r:i] One sequencing point the horizon stack could sharpen: the revision grammar from `163` (`when a review or audit materially changes the path, update near-horizon slice and governance carry; revise medium-horizon family routing if the family shape changed; revise far-horizon orientation only if the result actually changes the protected future or extraction/deployability direction`) is the intervention lifecycle discipline in horizon form. `166` currently points lifecycle pressure at a virtual `intervention lifecycle protocol carrier` but does not name `163`'s revision grammar as already-partly-governing that pressure. Naming it turns `163` into inherited doctrine for the lifecycle family rather than background.

## Task-Type Routing

- [d:r:i] All seven task types (exploration / audit / research / planning / implementation / verification / governance) are routed, and each has at least one concrete carrier or target named. The routing is partitioned by stance (exploration opens; audit tests; research gathers; planning proposes; implementation closes; verification confirms; governance preserves), which keeps the task-type surface from collapsing into one `work queue`.
- [d:r:i] Two sharpenings broaden the task-type routing:
  - [d:r:i] `proposal` is absent as a task type. In the harness-improvement program, `intervention-proposals/` is the analog of `discuss-phase` in host-side GSD: it is where the bounded next carrier is shaped before planning. `166` currently folds this under `planning`. Carrying `proposal` as its own row (with `intervention-proposals/` as the carrier) makes the distinction between shaping a bounded next carrier and planning the tranche that executes it visible.
  - [d:r:i] `measurement / observation` is split awkwardly between `verification` (`propagation refresh validation`) and implicit under `governance`. The five-family signal taxonomy from closure lane `01` does not belong to either of those in the way `166` currently routes them; it belongs to a `measurement` task type that the plan does not yet name. Carrying `measurement` as its own row (with the observation carrier and the `measurement-provenance` family named) keeps later telemetry work from degrading into verification pass/fail tallies.
- [d:r:i] One task-type carry that `166` implies but does not state: governance-side launch-truth and disposition-verb discipline (`accept` / `revise` / `park` / `reject`) is the lifecycle-discipline form that keeps parallel audit lanes individually auditable. `166` names `launch-truth and ledger upkeep` under `governance` but does not carry the disposition-verb set, which is what closure lane `01` singled out as the harness-operational surface. Adding the disposition-verb set to `governance` closes the gap.

## Parallelization And Lifecycle Protocol Pressure

- [d:r:i] Parallelization is correctly routed as a development-program multiplier, not as a harness-in-action rewrite agenda. The plan names the right overlap patterns for this lane (external plan audit + unrelated governance carry; later paired internal audit + parent-thread plan revision; later targeted propagation audit on already-landed workflow slices alongside bounded read-only audit) and the right out-of-scope set (workflow rewrites, execute/review-family redesigns, broad multi-provider).
- [d:r:i] What the plan does not yet inherit by name: the three-class taxonomy from [parallelization-audit/outputs/01](../../parallelization-audit/outputs/01-harness-parallelization-field-map-and-diagnosis-audit-opus47-max-r1.md):
  - [d:r:i] `safe earned` — fan-out on a frozen basis with disjoint outputs, companion-carry on unrelated governance surfaces, detect-only specialist lanes
  - [d:r:i] `promising but not-yet-governed` — review-family wave, pre-execution research split, cross-vendor parity lane, async-parallel cross-AI review
  - [d:r:i] `likely coherence/quality risk` — parallel writes to single-writer governance surfaces, refmap/topology rewrites during a live lane, installer runs during a live lane, consensus-flattening reviewer fan-out, Phase 01 rerun boundary crossed during a returning lane
- [d:r:i] Naming the three-class taxonomy inside `Parallelization Rule` (as inheritance, not re-derivation) turns `166`'s overlap list into instances of classes that later lanes can also instance against, rather than one-off examples. It also lets the forbidden-overlap set (Phase 01 rerun crossing, refmap rewrites during a live lane, installer reinstalls during a live lane, concurrent writes to single-writer governance surfaces) travel without being restated each time.
- [d:r:i] Lifecycle protocol pressure is present but partly virtual. `166` names `propose -> launch -> monitor -> review -> disposition -> propagate -> verify -> revise` under the virtual `intervention lifecycle protocol carrier`. Three of those stages already have governed carriers the plan does not point at:
  - [d:r:i] `launch` — launch-truth capture per [AGENTS.md](/home/rookslog/workspace/projects/prix-guesser/AGENTS.md) `Delegation And Orchestration`, `capture_launch_truth.py --since`, `launch-truth/` subdirectories
  - [d:r:i] `disposition` — disposition-verb requirement per [AGENTS.md](/home/rookslog/workspace/projects/prix-guesser/AGENTS.md) and the `dispositions/NN-*-inheritance.md` shape
  - [d:r:i] `propagate` — propagation-review route per [.planning/AGENTS.md](/home/rookslog/workspace/projects/prix-guesser/.planning/AGENTS.md) `Contract-Propagation Hygiene` and `.codex/get-shit-done/workflows/propagation-review.md`
- [d:r:i] The virtual carrier should name itself as `lifecycle protocol consolidation`, not `lifecycle protocol creation`. The consolidation task is to join the three already-governed stages with the still-under-carried stages (`monitor`, `review`, `revise`) under one readable surface, not to invent the whole pipeline from scratch. That framing keeps the already-earned carry visible and narrows the consolidation proposal's bounded shape.
- [d:r:i] One lifecycle pressure the plan does not yet name: post-return `revise` discipline. `163`'s revision grammar (near → medium → far revision after audit returns) is the `revise` stage in horizon form. `166` should point the virtual `intervention lifecycle protocol carrier` at `163`'s revision rules as partial existing carry so the later consolidation proposal does not silently rewrite horizon doctrine.

## Responsible Closure Sequencing

- [d:r:i] The `Exact Next Moves` ordering is directionally correct:
  1. this plan audited cross-vendor
  2. bounded development-parallelization + intervention-lifecycle protocol slice after that audit
  3. paired targeted propagation audit on already-landed parallelization-adjacent workflow changes
  4. `160` payload-home judgment + responsible-closure deployability bundle returned to from the cleaner protocol baseline
  5. harness-in-action parallelization kept explicit-but-later
- [d:r:i] The weakest sequencing call: `160` payload-home closure is placed in `Medium Horizon` and in `Exact Next Move` step 5, behind protocol + propagation audit. Closure lane `01`'s `Short Horizon` listed `160` payload-home closure as a bounded reread that runs in the same tranche as the governance-spine refresh and the register family-promotion. `160` is a classification exercise on three already-landed neutralization tranches (`154`, `157`, `159`), not a protocol-dependent move. Its inputs are frozen; its output is the `portable-payload / compatibility-shim / still-shared install-boundary` classification. Protocol cleanliness does not change that classification. Moving `160` closure to short horizon (or to early medium before the protocol tranche) broadens what protocol work can inherit, because the protocol tranche's `harness-contract` / `host-artifact` boundary is what `160` is about to sharpen.
- [d:r:i] The second-weakest sequencing call: the `observation carrier` proposal is in medium horizon and the `host-exercise packet` is also in medium. Closure lane `01` scheduled the first bounded `deployment-context observation carrier` proposal and the first bounded second-host dry-run proposal as separate medium-horizon moves, and the dry-run observe-only exercise (mixed `.codex` + held-annotation `.claude` preferred) as a medium-horizon follow-through. `166` is aligned with this but folds the three into two medium-horizon bullets. Splitting them keeps the three-stage dependency (carrier proposal → exercise proposal → exercise run) visible.
- [d:r:i] The cross-vendor responsible-closure audit from `161` commitment #6 is not scheduled in `166`. Closure lane `01`'s `Short Horizon` and `Long Horizon` both carried it as a deferred-carry slot against payload-home closure plus the first observation-carrier slice. `166` should carry the same schedule explicitly rather than letting the audit drift back into ambient chat memory.
- [d:r:i] The `Exact Next Moves` do not name the `Phase 01 rerun boundary` as a forbidden overlap during any lane launched by the plan. The parallelization audit listed crossing that boundary as a `likely coherence/quality risk` overlap class. `166` should carry one explicit line that the Phase 01 rerun boundary remains held across every lane this plan launches.

## What Is Still Blurred Or Missing

- [d:r:i] Taxonomic inheritance is under-carried. Three durable taxonomies from adjacent `01` lanes do not appear by name in `166`:
  - [d:r:i] six-surface split from closure lane `01` (used under `Domain Split` but not credited)
  - [d:r:i] five-family signal taxonomy from closure lane `01` (`deployment-context` / `expectation-vs-observation` / `semantic-deviation` / `positive-gain` / `measurement-provenance`)
  - [d:r:i] three-class parallelization taxonomy from parallelization-audit lane `01` (`safe earned` / `promising but not-yet-governed` / `likely coherence/quality risk`)
- [d:r:i] Protected-futures consolidation is partial. `Long Horizon` carries five protected futures but misses `broader host-context deployability` from `163`'s list; it also does not name `stronger parallelization frameworks` the way `163` phrases it (it names `workflow-local harness parallelization beyond current execution-wave patterns`, which is narrower).
- [d:r:i] A `Held Later` section that consolidates what this plan is explicitly not carrying is absent. Out-of-scope items are scattered under `Parallelization Rule` (workflow rewrites, execute/review-family redesigns, multi-provider), `Long Horizon` (standalone repo, npm/npx, dogfooding, richer telemetry), and closure-adjacent implications. Collecting them into one `Held Later` section keeps the consolidated plan from growing silently during later revisions.
- [d:r:i] The review-workflow family is absent from actual surfaces even though `gsd-review` helper-backed slice `145` plus propagation refresh `53` plus `review-route-audit/` lane `01` are now live. Later responsible-closure audits and cross-vendor lane launches route through this family.
- [d:r:i] The `virtual` vs `possible` distinction is not sharpened. The section title uses them as synonyms. In practice the nine items split into three sub-classes:
  - [d:r:i] `held-later with named trigger` (three items: fan-out packet, single-writer map, Parallelization Impact extension)
  - [d:r:i] `shaped proposal not yet written` (at least the observation carrier; possibly the measurement/provenance carrier and the development intervention-surface map)
  - [d:r:i] `net-virtual` (dogfooding, intervention lifecycle protocol consolidation, development parallelization protocol carrier, host-exercise observation carrier if it is not already shaped-proposal)
- [d:r:i] The anti-threshold posture is carried by the `Responsible Closure Rule` in substance but not named. The repo's CLAUDE wrappers and `.planning/` wrappers both carry the anti-threshold doctrine explicitly. A one-line naming inside `166`'s `Planning Stance` would route the posture through the plan's own text rather than relying on the consumer to carry it from `CLAUDE.md` / `AGENTS.md`.
- [d:r:i] The `Current Operator Directive` from the harness register (no shortcuts that trade away future control, triple-check high-consequence boundaries, follow through until neighboring carriers are in tune) is an active governing carrier for this plan. `166` does not name it. Naming it makes the directive part of the plan's inheritance surface rather than ambient repo memory.
- [d:r:i] The plan does not name `.planning/CLAUDE.md` and `CLAUDE.md` wrappers as part of the governance-routing surface, even though they are the Claude-side translation surface that cross-vendor audit lanes read. Adding them to the governance-routing carrier list keeps cross-vendor lane launches routing through a named surface.
- [d:r:i] Dogfooding posture is named as `long horizon` but the triggering condition is left with one path only (`once repo separation exists`). A second path could run earlier: `once the modifier's development program can be driven by its own audit/review/propagation carriers without ad-hoc host-side scaffolding`. Making the trigger an OR preserves both paths.

## What Should Move Later

- [d:r:i] Workflow-level parallelization rewrites stay held-later with concrete triggering basis (a host-side slice actually wanting the pattern). `execute-phase.md`, `review.md`, `map-codebase.md`, `ingest-docs.md`, `diagnose-issues.md`, `manager.md`, `config.json.parallelization` remain out of scope for this plan, inherited from parallelization-audit lane `01`'s disposition.
- [d:r:i] Standalone harness-modifier repo split, npm / npx packaging, installer binary execution stay held-later per `115`. No pre-authorization from this plan.
- [d:r:i] Full `.claude/` materialization claim stays held-later. `141` fixed `observed_basis_only` as the support window; broadening that is its own bounded proposal, not this plan's work.
- [d:r:i] Full GSD Reflect adaptive stack (auto-collection, reentrancy-locked sensor daemons, synthesizer/reflector agents, lifecycle state machine, per-phase signal caps, cross-project KB aggregation, `session_meta_postlude` hook-install wiring) stays protected-future per `163` plus closure lane `01`. The reference field is absorbed only through the shapes closure lane `01` named (split-provenance v2, signal-type taxonomy, durability ladder, automation-skip-reason vocabulary, automation-level stack, soft-target rigor-gate posture).
- [d:r:i] Workflow-level telemetry subcommand adoption stays held-later. `automation.level ≥ 2` for modifier-owned features stays held-later until at least one real dry-run has produced observations an operator can read.
- [d:r:i] Vanilla `--gemini` / `--opencode` / other-provider support stays explicitly later. `GSD Reflect 1.19.8` removed those paths; the modifier field remains `{.codex, .claude, mixed}`.
- [d:r:i] Promoting `Horizon Routing` into `.planning/AGENTS.md` stays held-later per workspace-state-audit disposition; keep it in the harness register first until more real findings exercise it.
- [d:r:i] Promoting the single-writer governance-surface enumeration into `.planning/AGENTS.md` or `AGENTS.md` stays held-later; keep it in `AUDIT-LANE-PATTERN-LIBRARY.md` first per parallelization-audit lane `01` disposition.
- [d:r:i] Second overlay tranche classification (overwrite-family source-split decision from `148`) stays held-later as an opening note, not implementation.
- [d:r:i] Fan-out packet template creation stays held-later until one more real overlap exercise earns it.
- [d:r:i] Reopening the Phase 01 rerun boundary stays forbidden for every lane this plan launches.

## Exact Revisions To Make

1. [d:r:i] `Domain Split`: name the six-surface split as inherited from [responsible-closure-audit/outputs/01](../outputs/01-harness-modifier-responsible-closure-field-map-audit-opus47-max-r1.md) plus [intervention-proposals/162](../../intervention-proposals/162-harness-uplift-semantics-and-target-mode-split.md) rather than introducing it as if it were new to `166`. One-line inheritance credit, nothing else.
2. [d:r:i] `Actual Intervention Surfaces`: under each of the seven families, add one or two pointer-level carriers so the map is usable without a parallel grep pass. Specific pointers per family are listed in the `Actual Intervention Surfaces` section above.
3. [d:r:i] `Actual Intervention Surfaces`: add one eighth family `review-workflow carry`, with pointers to `145`, propagation `53`, and `review-route-audit/` lane `01` and its disposition.
4. [d:r:i] `Possible Or Virtual Intervention Surfaces`: split the nine items into three sub-classes — `held-later with named trigger` (fan-out packet template, single-writer/lane-exclusive authority surface map, parallelization impact carrier inside propagation-review), `shaped proposal not yet written` (observation carrier for host exercises — cite the five signal families from closure lane `01` as the shape), and `net-virtual` (the remaining items). Cite the parallelization-audit lane `01` disposition as source for the first sub-class so the pre-existing held-later classification is preserved rather than silently reset.
5. [d:r:i] `Possible Or Virtual Intervention Surfaces`: add one sixth held-later-with-trigger entry — the cross-vendor responsible-closure audit from [161](../../intervention-proposals/161-harness-modifier-responsible-closure-deployability-and-adaptive-feedback-route.md) commitment #6, scheduled against payload-home closure plus the first observation-carrier slice, per closure lane `01`.
6. [d:r:i] `Possible Or Virtual Intervention Surfaces`: reframe the five promotion criteria as maturity indicators, not a gate. Add one explicit rule-of-thumb line: two or more indicators earn a bounded proposal; all five earn promotion into an active carrier.
7. [d:r:i] `Short Horizon`: move `160` payload-home judgment closure from `Medium Horizon` to `Short Horizon` as a bounded reread on the three landed neutralization tranches, matching closure lane `01`'s short-horizon schedule. Keep protocol tranche + targeted propagation audit in the same short horizon but name the ordering (payload-home closure may precede or run companion to protocol tranche; propagation audit pairs with the protocol slice, not with payload-home closure).
8. [d:r:i] `Short Horizon`: add one explicit line that the Phase 01 rerun boundary remains held across every lane launched by this plan.
9. [d:r:i] `Medium Horizon`: split the observation-carrier proposal, the host-exercise packet proposal, and the first observe-only dry-run into three distinct bullets so the dependency chain (carrier proposal → exercise proposal → exercise run) is visible. Name the five signal families from closure lane `01` as the observation-carrier's first-slice partition.
10. [d:r:i] `Medium Horizon`: add one bullet for a cross-vendor parity-classifier pass (`.codex` observed basis vs `.claude` held annotation) on the frozen declaration baseline, per closure lane `01`'s medium-horizon schedule.
11. [d:r:i] `Long Horizon`: add `broader host-context deployability` to the protected-futures list to match `163`'s list. Replace `workflow-local harness parallelization beyond current execution-wave patterns` with `stronger parallelization frameworks (harness-in-action and harness-improvement-program)` to match `163`.
12. [d:r:i] `Horizon Routing` labels: rename `Short Horizon`, `Medium Horizon`, `Long Horizon` to `Short Horizon (harness-program)`, `Medium Horizon (harness-program)`, `Long Horizon (harness-program)` and carry a one-line link to [163](../../intervention-proposals/163-harness-program-horizons-and-future-carry-doctrine.md) inside the `Scope Rule` so the horizon mapping is inherited doctrine rather than re-derived.
13. [d:r:i] `Task-Type Routing`: add one row `proposal` with `intervention-proposals/` as the carrier, distinct from `planning`. Add one row `measurement` with the observation carrier and the `measurement-provenance` family named, distinct from `verification` and `governance`.
14. [d:r:i] `Task-Type Routing` `governance`: add `disposition-verb discipline (accept / revise / park / reject)` to the row so the harness-operational surface from closure lane `01` is carried explicitly.
15. [d:r:i] `Parallelization Rule`: inherit by name the three-class taxonomy (`safe earned` / `promising but not-yet-governed` / `likely coherence/quality risk`) from [parallelization-audit/outputs/01](../../parallelization-audit/outputs/01-harness-parallelization-field-map-and-diagnosis-audit-opus47-max-r1.md), and point at the forbidden-overlap set already enumerated there rather than restating.
16. [d:r:i] Virtual `intervention lifecycle protocol carrier` entry: rename to `intervention lifecycle protocol consolidation` and name the already-governed stages (`launch` via launch-truth discipline; `disposition` via disposition-verb requirement; `propagate` via propagation-review route) as partial existing carry. Point at `163`'s revision grammar as partial existing carry for the `revise` stage.
17. [d:r:i] `Planning Stance`: add one line naming the anti-threshold posture explicitly, pointing at the repo [CLAUDE.md](/home/rookslog/workspace/projects/prix-guesser/CLAUDE.md) and [.planning/CLAUDE.md](/home/rookslog/workspace/projects/prix-guesser/.planning/CLAUDE.md) wrappers as the doctrinal carriers.
18. [d:r:i] Add one new section `Inherited Doctrine` (or expand `Role`) that names the `Current Operator Directive` from [HARNESS-IMPROVEMENT-REGISTER.md](/home/rookslog/workspace/projects/prix-guesser/.planning/HARNESS-IMPROVEMENT-REGISTER.md) and the [.planning/CLAUDE.md](/home/rookslog/workspace/projects/prix-guesser/.planning/CLAUDE.md) / [CLAUDE.md](/home/rookslog/workspace/projects/prix-guesser/CLAUDE.md) wrappers as part of the plan's inheritance surface.
19. [d:r:i] Add one new section `Held Later` consolidating what this plan is explicitly not carrying: workflow-level parallelization rewrites; standalone repo split / npm / npx; full `.claude/` materialization beyond held-annotation posture; full GSD Reflect adaptive stack; `automation.level ≥ 2` for modifier-owned features; vanilla `--gemini` / `--opencode` / other-provider paths; promoting `Horizon Routing` into `.planning/AGENTS.md`; promoting single-writer surface enumeration into `.planning/AGENTS.md` or `AGENTS.md`; second overlay tranche classification; fan-out packet template creation; reopening Phase 01.
20. [d:r:i] `Exact Next Moves` step 5: replace the phrase `Return next to 160 payload-home judgment... from the cleaner protocol baseline` with `Close 160 payload-home judgment on a bounded reread in short horizon; return to the responsible-closure deployability bundle from the cleaner protocol baseline as medium-horizon follow-through`, matching closure lane `01`'s sequencing.
21. [d:r:i] Dogfooding entry under `Long Horizon`: make the triggering condition an OR — `either when repo separation lands, or when the modifier's development program can be driven by its own audit/review/propagation carriers without host-side scaffolding`.

## Disposition

- [d:r:i] `accept with revisions` — the plan is the right next integrated bounded object for the harness-modifier development program. The revisions above are exact and bounded; none requires a rewrite, and none opens a harness-in-action workflow move. Land the revisions, then proceed to the bounded protocol tranche + paired targeted propagation audit that `166` already sequences.
