Date: 2026-04-22
Status: completed local inheritance

# Harness Modifier Project Uplift Host-Planning-Shape Neutralization Proposal Reread Inheritance

## Local Disposition

- [d:r:i] `accept with sharper carrier split`
- [d:r:i] Treat lane `09` as the authoritative shape judgment for the third neutralization tranche.

## Carry Forward

- [d:r:i] `158` names the right third tranche surface:
  - state writer reach
  - phase scanner reach
  - state-section label schema
  - phase-layout assumptions
- [d:r:i] Relocation still remains later.
- [d:r:i] `OVERLAY_MANIFEST_REL_PATH` still remains a separate later install-contract slice.
- [d:r:i] The proposal needs one sharper internal split:
  - `state_section.json` for writer-side state-section shape
  - `phase_layout.json` for analysis-path phase discovery and sort grammar
  - `state_writer.py` with distinct render and update entries
- [d:r:i] `state_heading` should stay owned by `output_policy.json` rather than being re-declared in the new carrier.
- [d:r:i] The label tuple should stay typed data while selector dispatch stays in writer code.
- [d:r:i] `seed_migration_inventory.py`, `harness_canary.py`, and `tooling/codex/UPLIFT-HELD-LATER.md` stay untouched in this tranche.

## Land Next

- [d:r:i] Revise `158` into the sharper two-carrier plus writer-module shape lane `09` named.
- [d:r:i] Then implement the third neutralization tranche directly on top of that sharpened proposal.
- [d:r:i] Keep the propagation refresh and `HARNESS-IMPROVEMENT-REGISTER.md` carry explicit on the face of the implementation.

## Keep Explicitly Later

- [d:r:i] `project_uplift.py` relocation.
- [d:r:i] install-contract pointer neutralization for `OVERLAY_MANIFEST_REL_PATH`.
- [d:r:i] `seed_migration_inventory.py` relocation.
- [d:r:i] `audit_refmap.py` movement.
- [d:r:i] second overlay tranche.
- [d:r:i] overwrite-family source split.
- [d:r:i] standalone repo boundary.
- [d:r:i] npm or `npx` packaging.
- [d:r:i] broader `.codex` / `.claude` parity redesign.

## Exact Next Move

1. [d:r:i] Revise `158-harness-modifier-project-uplift-host-planning-shape-neutralization-proposal.md` to carry the sharper two-carrier split plus distinct render/update writer entries.
2. [d:r:i] Implement the tranche directly after that revision.
3. [d:r:i] Only after the tranche lands and its propagation refresh is recorded, reopen the install-contract pointer slice and later payload-home judgment.
