Date: 2026-04-22
Status: completed local inheritance

# Harness Modifier First Overlay Filesystem Rehome Reread Inheritance

## Local Disposition

- [d:r:i] `accept with bounded residue-classification next move`
- [d:r:i] Keep the first specialist source split as a landed gain, not a tentative experiment.
- [d:r:i] Do not widen from this reread directly into a second filesystem tranche, standalone repo split, package distribution, or second-host exercise.

## Carry Forward

- [d:r:i] Keep the new authority split explicit:
  - authoritative source for the first specialist trio now lives under `harness_modifier/overlay/`
  - stable install targets still live through `tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json`
  - materialization and verification authority remains in `harness_modifier/contract/portable_gsd_contract.py`
- [d:r:i] Keep what no longer needs re-litigation explicit:
  - the specialist trio was the right first set
  - source authority and install target can separate inside one manifest
  - workflow-shell text can use a host-neutral source grammar plus package-owned helper shims
- [d:r:i] Keep the residual blur explicit:
  - skill-adapter `execution_context` absolute host-path policy
  - helper-shim stub versus helper-payload authority
  - default-source-root policy inside the portable contract
  - overwrite-mode source-split readiness

## Land Next

- [d:r:i] Open one bounded proposal only:
  - a residue-classification pass over the first source split
- [d:r:i] That proposal should classify:
  - whether the three moved skill adapters should stay install-target-bound or gain source-side token abstraction
  - whether the helper shims count as modifier-owned-source by themselves or only as a temporary bridge
  - whether the default source root should remain anchored at `tooling/portable-gsd/overlay/` for now
  - whether overwrite-mode source indirection is ready for a later bounded exercise

## Keep Explicitly Later

- [d:r:i] No second overlay filesystem tranche in this immediate follow-through.
- [d:r:i] No overwrite-family move yet.
- [d:r:i] No helper-payload promotion yet.
- [d:r:i] No manifest relocation yet.
- [d:r:i] No standalone repo split, package route, or second-host exercise.

## Exact Next Move

1. [d:r:i] Write the bounded residue-classification proposal as the next extraction intervention object.
2. [d:r:i] Update governed state so lane `03` reads as completed and the next extraction object reads as that classification pass.
3. [d:r:i] Treat any later second tranche as a proposal after that classification pass, not as ambient momentum from `148`.
