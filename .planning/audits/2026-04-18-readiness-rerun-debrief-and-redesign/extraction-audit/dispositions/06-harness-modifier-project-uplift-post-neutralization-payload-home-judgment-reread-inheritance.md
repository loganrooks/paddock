Date: 2026-04-22
Status: completed local inheritance

# Harness Modifier Project Uplift Post-Neutralization Payload-Home Judgment Reread Inheritance

## Local Disposition

- [d:r:i] `accept with second neutralization tranche`
- [d:r:i] Treat attempt `2` as the authoritative lane return.
- [d:r:i] Keep attempt `1` explicit as stall evidence rather than laundering the lane into a fake single-clean-return story.

## Carry Forward

- [d:r:i] The neutralization slice in `154` really did thin `project_uplift.py` at the observation, output-policy, and seed-contract seams.
- [d:r:i] The payload-home question is still not ready to reopen as relocation because three materially host-coupled surfaces remain embedded in helper code:
  - the host-doctrine carrier catalog
  - the rerun-boundary plus skill-command vocabulary
  - the direct `.planning/STATE.md` / `.planning/phases/` writer reach
- [d:r:i] The sharper next move is therefore another bounded neutralization slice on the helper itself, not relocation and not a pivot to another adjacent extraction family.
- [d:r:i] Keep `.codex` as observed basis and `.claude` as held annotation.

## Land Next

- [d:r:i] Open one bounded proposal for a second `project_uplift.py` neutralization tranche with:
  - typed host-doctrine carrier catalog under `harness_modifier/`
  - typed uplift vocabulary carrier under `harness_modifier/`
  - runtime-agent registry shape externalized into the catalog or a narrow sibling
  - `project_uplift.py` consuming those carriers while keeping fingerprint logic helper-local
  - focused parity tests so carrier externalization does not smuggle behavioral drift

## Keep Explicitly Later

- [d:r:i] `project_uplift.py` relocation.
- [d:r:i] `seed_migration_inventory.py` relocation.
- [d:r:i] `audit_refmap.py` movement.
- [d:r:i] second overlay tranche.
- [d:r:i] overwrite-family source split.
- [d:r:i] standalone repo boundary.
- [d:r:i] npm or `npx` packaging.
- [d:r:i] broader `.codex` / `.claude` parity redesign.
- [d:r:i] `.planning/STATE.md` writer and `.planning/phases/` scan neutralization; hold that as a later host-planning-shape slice after the catalog tranche lands.

## Exact Next Move

1. [d:r:i] Draft `156-harness-modifier-project-uplift-host-doctrine-catalog-neutralization-proposal.md`.
2. [d:r:i] Challenge that proposal through the normal bounded reread path before implementation.
3. [d:r:i] Only after that tranche lands, reopen the payload-home question again on the further-thinned helper.
