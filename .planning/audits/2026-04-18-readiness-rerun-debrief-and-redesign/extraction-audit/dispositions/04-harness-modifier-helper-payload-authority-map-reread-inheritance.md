Date: 2026-04-22
Status: completed local inheritance

# Harness Modifier Helper Payload Authority Map Reread Inheritance

## Local Disposition

- [d:r:i] `accept with authority-map next move`
- [d:r:i] Keep the per-helper split explicit; do not collapse the return into one umbrella “helper payload authority” answer.
- [d:r:i] Do not widen from this reread into payload relocation, second overlay tranche, overwrite-family source split, standalone repo design, or package/distribution route.

## Carry Forward

- [d:r:i] Keep the authority surfaces explicit:
  - source authority under `harness_modifier/overlay/`
  - install-target authority in `tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json`
  - materialization authority in `harness_modifier/contract/portable_gsd_contract.py`
  - compatibility declaration authority in `harness_modifier/compatibility/declaration.json`
- [d:r:i] Keep the per-helper classification explicit:
  - `project_uplift.py` is the modifier-facing payload candidate, but only after a neutralization slice
  - `seed_migration_inventory.py` is downstream of `project_uplift.py` and cannot lead relocation
  - `audit_refmap.py` belongs to a sharper repo-local audit-tooling family and is out of the later payload-movement candidate set
- [d:r:i] Keep the shim layer as a mixed answer:
  - temporary bridge for `project_uplift.py`
  - derivative bridge for `seed_migration_inventory.py`
  - stable long-lived source/install boundary for `audit_refmap.py`

## Land Next

- [d:r:i] Land one bounded helper authority-map artifact recording the per-helper classification.
- [d:r:i] Refresh `harness_modifier/overlay/ROSTER.md` only where that classification changes current extraction posture.
- [d:r:i] Refresh `.planning/HARNESS-IMPROVEMENT-REGISTER.md` so the split survives outside this extraction subtree.
- [d:r:i] After that authority map lands, open one separate bounded proposal for `project_uplift.py` neutralization. Do not roll neutralization into this reread.

## Keep Explicitly Later

- [d:r:i] Actual helper-payload relocation.
- [d:r:i] Default-source-root migration.
- [d:r:i] Overwrite-family source-indirection readiness or live exercise.
- [d:r:i] Second overlay filesystem tranche.
- [d:r:i] Standalone repo boundary design.
- [d:r:i] npm/`npx` distribution route.
- [d:r:i] Second-host exercise.

## Exact Next Move

1. [d:r:i] Land the bounded helper authority-map artifact and the matching minimal roster/register/governance carry.
2. [d:r:i] Record the lane as completed and inherited in the extraction family surfaces.
3. [d:r:i] Open `project_uplift.py` neutralization as the next bounded extraction proposal, not as a relocation slice.
