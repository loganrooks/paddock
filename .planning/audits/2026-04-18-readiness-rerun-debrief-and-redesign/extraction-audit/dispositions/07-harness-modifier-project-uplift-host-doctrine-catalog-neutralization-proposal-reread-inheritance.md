Date: 2026-04-22
Status: completed local inheritance

# Harness Modifier Project Uplift Host-Doctrine Catalog Neutralization Proposal Reread Inheritance

## Local Disposition

- [d:r:i] `accept with sharpened catalog-vocabulary split`
- [d:r:i] Treat lane `07` as the working-shape judgment for the second neutralization tranche.

## Carry Forward

- [d:r:i] Keep the proposal centered on a typed host-doctrine carrier catalog plus a typed host-facing vocabulary carrier.
- [d:r:i] Keep the runtime-agent registry shape as a named sibling object inside the catalog rather than hiding it under `file_carriers`.
- [d:r:i] Keep `OVERLAY_MANIFEST_REL_PATH` helper-local and explicitly later; it is install-contract wiring, not host-doctrine catalog content.
- [d:r:i] Let the vocabulary carrier own the full operator-facing sentence templates, not only the raw skill-command tokens and rerun-boundary patterns.
- [d:r:i] Keep internal rendering labels and the planning-writer reach helper-local and later, respectively.
- [d:r:i] Preserve `.codex` as observed basis and `.claude` as held annotation.

## Land Next

- [d:r:i] Revise `156` into the sharpened working spec the lane returned.
- [d:r:i] Then land the second neutralization implementation slice with:
  - `carrier_catalog.json` plus loader
  - `vocabulary.json` plus loader
  - `project_uplift.py` consumption of those carriers
  - focused parity tests tied directly to `tooling/codex/tests/test_project_uplift.py`

## Keep Explicitly Later

- [d:r:i] `project_uplift.py` relocation.
- [d:r:i] `seed_migration_inventory.py` relocation.
- [d:r:i] `.planning/STATE.md` writer and `.planning/phases/` scanner neutralization.
- [d:r:i] install-contract pointer neutralization.
- [d:r:i] second overlay tranche.
- [d:r:i] overwrite-family source split.
- [d:r:i] standalone repo boundary.
- [d:r:i] npm or `npx` packaging.
- [d:r:i] broader `.codex` / `.claude` parity redesign.

## Exact Next Move

1. [d:r:i] Sharpen `156` with the lane-07 catalog/vocabulary boundary.
2. [d:r:i] Implement the second neutralization tranche directly on top of that sharpened proposal.
3. [d:r:i] Refresh authority, roster, register, and propagation surfaces in the same batch.
