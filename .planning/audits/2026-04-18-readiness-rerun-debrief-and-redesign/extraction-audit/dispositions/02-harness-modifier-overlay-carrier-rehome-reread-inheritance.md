Date: 2026-04-22
Status: completed local inheritance

# Harness Modifier Overlay Carrier Rehome Reread Inheritance

## Local Disposition

- [d:r:i] `accept with narrower next move`
- [d:r:i] Keep Opus in the lead on the overlay-tranche split, especially:
  - generic versus shared-boundary versus host-local classification
  - mechanism-versus-body treatment for compact prompts
  - materialization override levers that must survive later rehome
- [d:r:i] Do not reopen bulk overlay rehome, standalone repo split, or npm/`npx` packaging from this lane.

## Carry Forward

- [d:r:i] Carry the sharper extraction pressure:
  - overlay/workflow/skill/reference carriers are the real next object
  - but they still need one more classification freeze before filesystem moves
- [d:r:i] Keep the shared-boundary warning explicit:
  - `uplift-project.md` and `propagation-review.md` are mechanism-generic but still embed host audit-workspace paths in their text
  - compact-prompt mechanism is generic while current prompt bodies remain host-owned
- [d:r:i] Keep materialization ownership three-layered:
  - upstream runtime
  - modifier overlay / contract
  - host override control
- [d:r:i] Keep the override levers explicit:
  - `PRIX_COMPACT_PROMPT_FILE`
  - `.codex.local/compact-prompt.txt`
  - `DEFAULT_COMPACT_PROMPT_FILE`
  - `OVERLAY-MANIFEST.json` add/overwrite grammar

## Land Now

- [d:r:i] Open one bounded proposal for the next exact move only:
  - carrier roster under `harness_modifier/overlay/`
  - embedded-host-path scan across overlay workflows and skills
  - explicit eligibility set for the first filesystem rehome slice
- [d:r:i] Update governed state so extraction now reads as:
  - lane `02` completed
  - next move narrowed from “overlay carrier rehome” to “roster + embedded-host-path scan”

## Keep Explicitly Later

- [d:r:i] No filesystem rehome in this exact next move.
- [d:r:i] No standalone repo split.
- [d:r:i] No npm/`npx` packaging.
- [d:r:i] No bulk overlay move.
- [d:r:i] No compact-prompt body extraction until mechanism/body split is explicit.

## Exact Next Move

1. [d:r:i] Write the bounded roster-and-scan proposal as the next extraction intervention object.
2. [d:r:i] Freeze the specialist workflow/skill pairs eligible for the first filesystem rehome only after that roster and scan are written.
3. [d:r:i] Treat the first filesystem rehome as the proposal after that, not as part of this immediate follow-through.
