Date: 2026-04-22
Status: completed local inheritance

# Harness Modifier Project Uplift Post-Second-Neutralization Payload-Home Judgment Reread Inheritance

## Local Disposition

- [d:r:i] `accept with host-planning-shape neutralization next move`
- [d:r:i] Treat lane `08` as the authoritative reopened payload-home judgment after both landed neutralization tranches.

## Carry Forward

- [d:r:i] `154` and `157` really did discharge two of the three host-coupling surfaces lane `06` had named:
  - policy-data carriers
  - host-doctrine plus operator-facing vocabulary carriers
- [d:r:i] The materially remaining host-coupling surface is now narrower and more explicit:
  - `.planning/STATE.md` writer reach
  - `.planning/phases/*/*-CONTEXT.md` scanner reach
  - phase-directory shape assumptions
  - the line-label schema inside `state_section_text`
- [d:r:i] Payload relocation still remains later because moving `project_uplift.py` now would re-host that planning-shape writer/scanner blur rather than dissolve it.
- [d:r:i] `OVERLAY_MANIFEST_REL_PATH` remains a real coupling tail, but it stays its own later install-contract slice rather than widening into the planning-shape tranche.
- [d:r:i] `seed_migration_inventory.py` stays downstream.
- [d:r:i] `harness_canary.py` stays a neighboring consumer.
- [d:r:i] `audit_refmap.py` stays out of the later payload-movement candidate set.
- [d:r:i] Keep `.codex` as observed basis and `.claude` as held annotation.

## Land Next

- [d:r:i] Open one bounded proposal for a third `project_uplift.py` neutralization tranche:
  - typed host-planning-shape carrier under `harness_modifier/uplift/`
  - narrow writer module that owns the `.planning/STATE.md` section rewrite
  - typed phase-layout anchor for `.planning/phases/*/*-CONTEXT.md`, `*-PLAN.md`, and `*-SUMMARY.md` discovery
  - keep `project_uplift.py` calling the writer rather than editing host planning files directly
  - keep focused parity tests so the split does not change rendered state-section bytes or emitted uplift memory
- [d:r:i] The proposal should keep `OVERLAY_MANIFEST_REL_PATH` explicitly out of the tranche and route it to a separate later install-contract slice.

## Keep Explicitly Later

- [d:r:i] `project_uplift.py` relocation.
- [d:r:i] `seed_migration_inventory.py` relocation.
- [d:r:i] `audit_refmap.py` movement.
- [d:r:i] second overlay tranche.
- [d:r:i] overwrite-family source split.
- [d:r:i] standalone repo boundary.
- [d:r:i] npm or `npx` packaging.
- [d:r:i] broader `.codex` / `.claude` parity redesign.
- [d:r:i] candidate-runtime widening inside the observation carrier.
- [d:r:i] install-contract pointer neutralization; hold it as the slice after the host-planning-shape tranche lands.

## Exact Next Move

1. [d:r:i] Draft `158-harness-modifier-project-uplift-host-planning-shape-neutralization-proposal.md`.
2. [d:r:i] Challenge that proposal through the normal bounded reread path before implementation.
3. [d:r:i] Only after that third tranche lands and its propagation refresh is recorded, reopen the payload-home question again on the further-thinned helper.
