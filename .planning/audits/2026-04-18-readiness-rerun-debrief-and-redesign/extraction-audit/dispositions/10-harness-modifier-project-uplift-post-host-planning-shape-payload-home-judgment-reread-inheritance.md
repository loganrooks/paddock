Date: 2026-04-23
Status: completed local inheritance

# Harness Modifier Project Uplift Post-Host-Planning-Shape Payload-Home Judgment Reread Inheritance

## Local Disposition

- [d:r:i] `accept with narrower remaining-coupling split`
- [d:r:i] Treat lane `10` as the authoritative closeout for the reopened `160` payload-home judgment.

## Carry Forward

- [d:r:i] The third neutralization tranche has now discharged the last lane-`06` host-planning-shape blocker:
  - `state_section.json`
  - `phase_layout.json`
  - `state_writer.py`
  now own the host-planning-shape surface that previously kept the helper too thick for a sharper payload-home reread.
- [d:r:i] Relocation still remains later.
- [d:r:i] The remaining coupling is now materially narrower and should stay split into two different later families:
  - install-contract pointer coupling around `OVERLAY_MANIFEST_REL_PATH`
  - held-later artifact-home coupling around `tooling/codex/UPLIFT-HELD-LATER.md`
- [d:r:i] Do not re-blend those two residues into one generic payload-home blur. The install-contract pointer is the next adjacent extraction object; the artifact-home question remains the sibling held-later object after that.
- [d:r:i] Keep the smaller residues explicit but not lane-governing:
  - generic planning-canon existence probes in `analyze_repo`
  - downstream module-path coupling in `seed_migration_inventory.py` and `harness_canary.py`
  - helper bootstrap depth for repo-root imports
  - narrow `".codex"` comparison literals inside classification logic

## Governance Consequence

- [d:r:i] `160` is now closed on a bounded reread basis.
- [d:r:i] The extraction-family exact next move is now [../../intervention-proposals/167-harness-modifier-project-uplift-install-contract-pointer-neutralization-proposal.md](../../intervention-proposals/167-harness-modifier-project-uplift-install-contract-pointer-neutralization-proposal.md).
- [d:r:i] The held-later sibling is now [../../intervention-proposals/168-harness-modifier-project-uplift-held-later-artifact-home-sibling-note.md](../../intervention-proposals/168-harness-modifier-project-uplift-held-later-artifact-home-sibling-note.md).
- [d:r:i] Keep the program-wide next operational move from `166` explicit beside that extraction-family move rather than replacing it:
  - development-parallelization plus intervention-lifecycle protocol consolidation remains the next program-wide protocol tranche
  - extraction-family next move remains `167`

## Keep Explicitly Later

- [d:r:i] `project_uplift.py` relocation itself
- [d:r:i] held-later artifact-home move before `167` lands
- [d:r:i] second overlay filesystem tranche
- [d:r:i] overwrite-family source-indirection widening
- [d:r:i] standalone repo split
- [d:r:i] npm or `npx` packaging
- [d:r:i] broader `.codex` / `.claude` parity redesign beyond the present observed-basis plus held-annotation posture

## Exact Next Moves

1. [d:r:i] Open and route the bounded install-contract pointer neutralization proposal as `167`.
2. [d:r:i] Preserve the held-later artifact-home question as its own sibling note rather than bundling it into `167`.
3. [d:r:i] After `167` lands, reopen the artifact-home sibling tranche.
4. [d:r:i] Only after both later tranches land should the payload-home question reopen again.
