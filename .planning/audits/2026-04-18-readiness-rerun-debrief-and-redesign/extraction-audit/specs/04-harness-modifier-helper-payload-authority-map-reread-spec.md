Date: 2026-04-22
Status: frozen lane spec

# Harness Modifier Helper Payload Authority Map Reread Spec

## Task

- [g:r:i] Audit the newly opened helper-payload authority object and produce the sharper bounded route for what should happen next in the extraction family.

## Required Judgment Shape

- [g:r:i] Keep shim/source/install/materialization authority explicit:
  - helper shims under `harness_modifier/overlay/helpers/`
  - substantive payloads under `tooling/codex/`
  - install/materialization authority in the portable contract and overlay manifest
- [g:r:i] Classify `project_uplift.py`, `seed_migration_inventory.py`, and `audit_refmap.py` separately.
- [g:r:i] Distinguish modifier-facing payload pressure from sharper shared-boundary coupling.
- [g:r:i] Judge the shim layer itself, not only the payloads behind it.
- [g:r:i] Keep later standalone extraction pressure visible without letting it consume this lane.

## Avoid

- [g:r:i] Do not answer with one umbrella “move later” or “keep here” line.
- [g:r:i] Do not reopen second overlay tranche, overwrite-family source split, standalone repo split, or package distribution from this reread.
- [g:r:i] Do not treat `seed_migration_inventory.py` as equivalent in weight or coupling to `project_uplift.py`.
- [g:r:i] Do not dissolve repo-local path grammar inside `audit_refmap.py` into a generic portability note.

## Output Shape

Return markdown with exactly these sections:

```markdown
# Helper Payload Authority Map Reread

## What The Current Split Already Clarified
- ...

## Per-Helper Authority Classification
- `project_uplift.py`: ...
- `seed_migration_inventory.py`: ...
- `audit_refmap.py`: ...

## Shim Boundary Judgment
- ...

## What Still Stays Shared-Boundary
- ...

## What No Longer Needs To Be Re-Litigated
- ...

## Recommended Next Slice
- ...

## Held Later
- ...

## Exact Next Move
- ...
```
