# Wave-2 R5.18 Materialization And Package-Truth Fix Slice

Status: accepted bounded local proposal  
Date: 2026-04-19

## Frame

- [d:r:i] This artifact translates [08-r5-18-executed-bundle-review-pair-inheritance.md](08-r5-18-executed-bundle-review-pair-inheritance.md) into the smallest coherent fix slice that can actually move the accepted Wave-2 program forward.
- [d:r:i] It is not a broad `Proposal F` turn, not a broad workspace reorganization pass, and not a new doctrine-selection lane.
- [d:r:i] The goal is narrower: make the executed `R5.18` frontier reproducible enough and package truth honest enough that later rerun-floor recomputation is anchored in actual carried surfaces rather than one worktree's residue.

## Slice Objective

- [g:r:i] Produce a reviewable, reinstall-testable `R5.18` frontier that no longer relies on soft `partially git-tracked` language for its most load-bearing review/planning and completion-routing carry.

## Included Scope

1. `Overlay parity repair`
   - [g:r:i] Bring tracked overlay truth up to the current live `R5.18` carry at least for:
     - `tooling/portable-gsd/overlay/get-shit-done/references/agent-contracts.md`
     - `tooling/portable-gsd/overlay/get-shit-done/workflows/plan-phase.md`
   - [g:r:i] Verify whether any remaining live-only `R5.18` surfaces still need tracked counterparts or explicit non-ownership language.

2. `Debt-routing carry check`
   - [g:r:i] Re-test whether transition/completion routing still under-reads `completion_mode` / debt metadata after the later `phase.cjs` improvements.
   - [g:r:i] If the gap is real, fix it narrowly at the transition/consumer layer rather than reopening wider doctrine.

3. `R5.8 and package-truth rewrite`
   - [g:r:i] Re-disposition `R5.8` honestly against the materialization evidence.
   - [g:r:i] Rewrite the affected readiness-package truth surfaces so they stop sheltering live mismatch under softer `partially git-tracked` phrasing.

4. `Reinstall-backed verification`
   - [g:r:i] Reinstall or equivalent materialization check must prove that the tracked tree reproduces the repaired frontier.
   - [g:r:i] Capture that proof in a reviewable artifact rather than leaving it as ephemeral terminal memory.

## Excluded Scope

- [d:r:i] Full worker-roster / `.toml` harmonization beyond the already accepted bounded runtime-authority discriminator
- [d:r:i] Broad `.codex` tracking redesign
- [d:r:i] Launch-hook automation
- [d:r:i] General workspace cleanup beyond files directly needed for this fix slice
- [d:r:i] Brake-exit rule or preserve-only activation-trigger doctrine authoring
- [d:r:i] Full rerun-floor recomputation

## Ordered Execution

### Step 1: Truthful Gap Inventory

- [g:r:i] Confirm exact live-vs-overlay drift on the known load-bearing files first.
- [g:r:i] Distinguish:
  - `already repaired later`
  - `still diverged`
  - `live-only with no tracked counterpart`
  - `no longer needed / out of scope`

### Step 2: Overlay / Consumer Repair

- [g:r:i] Patch the tracked overlay surfaces needed for reinstall-backed parity.
- [g:r:i] If `transition.md` still under-reads debt metadata, patch that layer in the same slice rather than leaving a half-carried completion contract.

### Step 3: Package-Truth Repair

- [g:r:i] Update readiness package truth where it still describes materialization risk too softly or keeps `R5.8` in an unreactivated conditional posture.
- [g:r:i] Keep the wording exact: no broad alarmism, but no euphemistic shelter either.

### Step 4: Reinstall / Materialization Proof

- [g:r:i] Re-materialize from tracked sources and verify the repaired frontier survives.
- [g:r:i] Record the proof in one narrow artifact that later slices can cite directly.

## Stop Conditions

- [g:r:i] Stop and checkpoint if the slice reaches reproducible parity for the load-bearing `R5.18` surfaces and names any remainder explicitly.
- [g:r:i] Stop and escalate only if the slice proves that the remaining drift is not bounded materialization debt but a wider harness-branch conflict.

## What This Slice Should Produce

- [d:r:i] a clean implementation patch for the bounded materialization/truth surfaces
- [d:r:i] a narrow verification artifact proving reinstall-backed carry
- [d:r:i] an updated readiness-package truth posture for `R5.8` / tracked-materialization language
- [d:r:i] a clean checkpoint commit

## Immediate Implementation Target

- [d:r:i] The next implementation pass should execute this slice directly rather than opening another opinion lane first.
