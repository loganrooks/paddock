# Wave-2 Lane 06 — Rerun Design (Opus 4.7 Max, R1)

Lane: `rerun-design`
Spec: [../specs/06-rerun-design-spec.md](../specs/06-rerun-design-spec.md)
Packet: [../packets/06-rerun-design-packet.md](../packets/06-rerun-design-packet.md)
Model: `claude-opus-4-7[1m]`
Date: 2026-04-19
Status: draft r1

## Reading Log

- Stage 0 spine: read in full. `AUDIT-CHARTER.md`, `WORKSPACE-AUTHORITY-AND-ORGANIZATION.md`, `INDEX.md`, `CURRENT-STATE.md`, `QUESTION-SET.md`, `EVIDENCE-ARCHITECTURE.md`, `PLAN-PROPOSALS.md`, `STATUS.md`, `MAIN-WAVE-LAUNCH-CONTRACT-AND-PACKET.md`.
- Stage 1: read in full. The Wave-1 comparative disposition plus all four Wave-1 outputs (`mission-reconstruction`, `outcome-and-underreach-audit`, `mapping-adequacy-and-comparative-mapping`, `operator-orchestration-pressure`), the lane-04 comparative disposition plus the Surface B prelicensing / judgeability pass and graded-underreach trial, the lane-04 local-proposal-and-stress-test disposition, the lane-05 comparative disposition, the Wave-2 lane-05 comparative disposition, `.planning/readiness/phase-01-rerun/PLAN.md` and `STATUS.md`, the 04-17 bridge audit `SYNTHESIS.md` and `lane-03-reseed-judgment.md`, `AI-GUARDRAILS.md`, `WORKFLOW.md`, `.planning/AGENTS.md`, `wave-2/README.md`, and both raw Wave-2 lane-05 outputs (`05-suppressed-opportunity-and-non-intervention-opus47-max-r1.md`, `05-suppressed-opportunity-and-non-intervention-gpt54-xhigh-r1.md`).
- Stage 2 challenge packet: opened before finalizing. The lane trended toward a `B-extended plus bounded harness slice plus learning-rerun posture for Checkpoint 7` hybrid, which is adjacent to both packet-spec trigger conditions (`mostly keep the current shape` and `broad machinery rewrite now`). Stage 2 challenge material therefore read in full: the `lane-05` Opus 4.7 1M and GPT-5.4 xhigh main-wave contract cross-reviews, the two raw Wave-2 lane-05 outputs (already read in Stage 1 but reopened for challenge pressure on UU/PSP boundaries). Live runtime probes were also run on `2026-04-19` to avoid reasoning from memory on the `.toml` stale-doctrine, overlay-vs-runtime, and doctrine-vocabulary-propagation facts this lane relies on: `grep -l "CLAUDE.md" .codex/agents/*.toml | wc -l` → `13`; `ls .codex/agents/*.toml | wc -l` → `31`; `head -3 .codex/config.toml` → `model_reasoning_effort = "xhigh"`; `head -3 tooling/portable-gsd/overlay/config.toml` → `model_reasoning_effort = "high"`; `grep -rn "post-verificationist\|post-falsificationist\|gap-exposure\|completeness-challenge\|anti-regret" .codex/` → zero matches. Those probes confirm the load-bearing Wave-1 and Wave-2 runtime-authority facts this lane inherits.
- Stage 2 effect on judgment: the challenge material materially narrowed two candidate program shapes from `recommended` to `rejected with explicit reopen trigger`. Specifically, `Option G — Proposal D maximal multi-wave` was demoted on AI-GUARDRAILS solo-developer posture plus mission-lane T9 precondition-inflation tension, and `Option D — full Proposal F` was demoted on lane-05 operator cross-review's `small branches / minimal irreversible actions` discipline plus the Wave-2 lane-05 UU-5 regret analysis. The challenge stage also tightened the recommended shape's execution-capacity claim, by forcing explicit rollback cost and operator discipline statements per slice rather than letting the hybrid ride on one aggregate budget figure.

## 1. Overall Rerun-Design Judgment

- [d:r:i] The second-attempt program shape now actually justified by the accepted Wave-1 stack plus the Wave-2 lane-05 return is not `Proposal B-extended continuation`, not `Proposal F dominant`, and not `Proposal E now`. It is a narrower hybrid: `Proposal B-extended` plus a bounded harness-first slice inserted before Checkpoint 6 readiness verification, plus an explicit posture change to Checkpoint 7 treating the fresh Phase 01 rerun as a learning pass with pre-commit friction capture, plus three supplementary doctrinal edits the rerun cannot run honestly without (brake-exit rule, activation-trigger doctrine for the `05-gap-closure` preserve-only seams, execution-capacity reopen rule). This shape is narrow enough to fit the charter's operator model and broad enough to address the package's signature failure pattern.
- [d:r:i] The strongest reason the incumbent `Proposal B-extended continuation` shape is insufficient is not its sequencing logic — that is still sound — but its brake-exit problem. Mission lane §89 and tensions T1/T9 explicitly diagnose the package's recursive inward motion as a structural property: the anti-closure brakes kept generating findings the sequencing rules could not absorb, and there is no trigger in the current PLAN for "the sequence has exhausted productive reach, switch shape or accept residual." Continuing the sequence as-is preserves the attractor. The bounded harness slice does not by itself fix the brake-exit problem, so the recommended program adds an explicit brake-exit rule as a prerequisite, not as a later-facing cleanup. Sources: [wave-1/outputs/01-mission-reconstruction-opus47-max-r1.md:88-103](../../wave-1/outputs/01-mission-reconstruction-opus47-max-r1.md); [wave-1/outputs/02-outcome-and-underreach-audit-opus47-max-r1.md:81-94](../../wave-1/outputs/02-outcome-and-underreach-audit-opus47-max-r1.md); [wave-2/dispositions/05-wave-2-lane05-comparative-disposition.md:12-14](../dispositions/05-wave-2-lane05-comparative-disposition.md).
- [d:r:i] The strongest reason `Proposal F dominant` is not warranted now is not that harness work is wrong — the evidence points at runtime-authority drift more than any other surface — but that a full-F program widens current scope before a launch-truth capture on a single spawn has discriminated symptomatic drift from structural drift, which directly enacts the charter's `[g:r:i]` warning that intervention is not a neutral default. The operator-pressure lane plus the mapping-lane Intervention #2 plus AI-GUARDRAILS:103-113 all converge on the same bounded shape: run F as an instrumented slice inside B-extended, not as a program-shape reversal. Sources: [wave-1/outputs/03-mapping-adequacy-and-comparative-mapping-opus47-max-r1.md:155-162](../../wave-1/outputs/03-mapping-adequacy-and-comparative-mapping-opus47-max-r1.md); [wave-1/outputs/04-operator-orchestration-pressure-opus47-max-r1.md:202-207,257-263](../../wave-1/outputs/04-operator-orchestration-pressure-opus47-max-r1.md); [AI-GUARDRAILS.md:103-113](../../../../../AI-GUARDRAILS.md).
- [d:r:i] The strongest reason `Proposal E now` is not warranted is not that a learning rerun is wrong — it is precisely what the package's brake-exit problem calls for — but that a learning rerun launched against the current runtime-authority drift would confound genuine friction evidence with avoidable prep friction. A rerun spawning `gsd-planner` / `gsd-executor` / `gsd-plan-checker` today would launch workers whose `.toml` `developer_instructions` still reference `./CLAUDE.md` and `.claude/skills/` (verified live on 2026-04-19: `13/31`), and doctrine-vocabulary consumers in `.codex/` still carry zero matches for `post-verificationist`, `post-falsificationist`, `gap-exposure`, `completeness-challenge`, or `anti-regret` (verified live). Bounded harness-first cleanup of that specific drift must precede the learning rerun, not follow it. The Surface B prelicensing / judgeability pass required E to have a concrete first slice before adoption; the hybrid recommendation supplies it. Sources: [wave-1/outputs/03-mapping-adequacy-and-comparative-mapping-opus47-max-r1.md:15,119-125](../../wave-1/outputs/03-mapping-adequacy-and-comparative-mapping-opus47-max-r1.md); [wave-1/outputs/02-outcome-and-underreach-audit-opus47-max-r1.md:45,60](../../wave-1/outputs/02-outcome-and-underreach-audit-opus47-max-r1.md); [lane-04-surface-b-prelicensing-judgeability-pass.md:26-44](../../lane-04-surface-b-prelicensing-judgeability-pass.md); live probe on 2026-04-19.
- [d:r:i] The strongest reason `Proposal C-first` or `Proposal D maximal multi-wave` are not warranted is that no Wave-1 or Wave-2 return has fired the `multi-lane reconciliation failure` trigger (mapping-lane ST-5), no audit artifact has been lost across a compaction boundary (operator-pressure ST-5), and mission lane T9 names precondition inflation as a documented failure pattern of the package that a maximal multi-wave would predictably reproduce. Their switch triggers remain live but unfired. Sources: [wave-1/outputs/03-mapping-adequacy-and-comparative-mapping-opus47-max-r1.md:140](../../wave-1/outputs/03-mapping-adequacy-and-comparative-mapping-opus47-max-r1.md); [wave-1/outputs/01-mission-reconstruction-opus47-max-r1.md:102-103](../../wave-1/outputs/01-mission-reconstruction-opus47-max-r1.md).
- [d:r:i] Workspace organization, version-control discipline, packet-freeze boundaries, and runtime-truth surfaces are program-carrying prerequisites for this shape, not background hygiene. The accepted lane-05 disposition already demonstrates that checkpoint and packet-freeze discipline are blocking for main-wave launch; the same logic extends to the recommended rerun program: without overlay-mirror discipline on harness slice edits, without committed slice boundaries, without a recomputed minimum rerun-readiness set (PSP-2), and without the brake-exit rule codified somewhere the rerun will read, the recommended shape collapses back into `Option A continuation`. Sources: [lane-05-comparative-disposition.md:18-20,31-39](../../lane-05-comparative-disposition.md); [wave-2/outputs/05-suppressed-opportunity-and-non-intervention-opus47-max-r1.md:196-213](../outputs/05-suppressed-opportunity-and-non-intervention-opus47-max-r1.md).

## 2. Option Set And Baseline Assumptions

### Baseline assumptions

- [d:r:i] Execution-capacity baseline is the charter's `[d:r:i]` model: one operator orchestrating parallel Codex and Claude lanes over a multi-week budget, capable of multi-module repo-local refactors and bounded follow-through on repo-local harness surfaces, but not assuming broad upstream `gsd` ownership by default. This lane does not revalidate the model substantively — that is PSP-6 work — but notes that the hybrid recommendation consumes visibly less of that budget than either full-F, full-C-first, or full-D would. Sources: [AUDIT-CHARTER.md:20-21](../../AUDIT-CHARTER.md).
- [d:r:i] Status baseline: Checkpoint 5 is still `in progress` with blocking finding 1 (executed R5.18 patch bundle lacks the fresh cross-vendor reread required for closure). Any rerun-shape recommendation must treat that blocker explicitly; it cannot assume Checkpoint 5 closure. Sources: [.planning/readiness/phase-01-rerun/STATUS.md:150-154](../../../../readiness/phase-01-rerun/STATUS.md).
- [d:r:i] Runtime-authority baseline (verified live on 2026-04-19): `13/31` `.toml` agent files still reference `CLAUDE.md`/`.claude/skills/`; live `.codex/config.toml` reasoning effort is `xhigh` while overlay canon is `high`; `.codex/` grep zero for post-falsificationist vocabulary family; `.codex/` remains `.gitignore`d. These are the four runtime facts that most directly shape the option comparison below.
- [d:r:i] Doctrine baseline: the post-falsificationist review doctrine, the bridge-audit guarded-hybrid verdict (now explicitly demoted to `candidate underreach artifact` by the charter), the lane-04 ceremonial-risk-as-test-criterion reading, and the Wave-2 lane-05 accepted bounded-stronger-moves cluster are all load-bearing inputs. None are up for wholesale rewrite in this lane. Sources: [AUDIT-CHARTER.md:7-16](../../AUDIT-CHARTER.md); [lane-04-comparative-disposition.md:13-14,62-68](../../lane-04-comparative-disposition.md); [wave-2/dispositions/05-wave-2-lane05-comparative-disposition.md:10-15](../dispositions/05-wave-2-lane05-comparative-disposition.md).

### Option Set

Seven serious program alternatives are compared in §3. Each is articulated as a concrete option bundle — first slice, prerequisites, execution-capacity model, upside, regret or reversal cost, and evidence that would count against it — rather than as a label. Two of them (`Option A` and `Option B`) are adjacent variants of the current default and are kept distinct to prevent the lane from silently treating `a bit more of the same` as `new program shape`. Two of them (`Option D` and `Option E`) are the loudest stronger moves and are kept distinct to prevent the lane from collapsing them into one `bold rewrite` straw option.

- `Option A` — B-extended minimal continuation. Continue the existing Checkpoint 5 → 6 → 7 sequence exactly as planned; close executed-R5.18 cross-vendor reread (BE-6) as the only pre-rerun addition; inherit the Wave-2 lane-05 bounded experiments as optional reviewer-register carry rather than as slice work.
- `Option B` — B-extended plus bounded harness-first slice. Same sequence as A, but insert a bounded harness-first micro-wave between current Checkpoint 5 closure and Checkpoint 6 verification: land BE-1 (AUDIT-SPEC-TEMPLATE chain-tail rule), BE-5 (`Proceed anyway` replacement), a narrow `.toml` roster slice for planner/executor/verifier only, and a bounded doctrine-vocabulary propagation slice. Then Checkpoint 6 → 7 as in A.
- `Option C` — Proposal C mapping reconciliation first. Before any rerun-design lane locks, open a bounded mapping-reconciliation sublane that produces an intervention-ready map: runtime-authority ledger (BE-4), overlay-vs-runtime drift probe (BE-8), router/helper-CLI first-class inventory, install-time mutation contract, `.codex/gsd-file-manifest.json` completeness work. Then revisit rerun-design.
- `Option D` — Proposal F dominant program shape. Treat harness-code-first as the primary intervention lane and subordinate doctrine/governance/rerun work. Five-slice F program: named-consumer read-sets (partly landed), `.toml` roster unification (all 31 workers, overlay + live), overlay-materialization pre-commit check, install-time drift probe, launch-truth side-effect promotion.
- `Option E` — Proposal E execute-and-learn now. Skip any further prep slice. Launch Phase 01 rerun as a learning pass with pre-commit friction capture; explicit no-canon-mutation posture; tight scope; captured friction evidence feeds later doctrine.
- `Option F` — Recommended hybrid: B-extended plus bounded harness-first slice plus learning-rerun posture for Checkpoint 7 plus three supplementary doctrinal prerequisites. Option B plus an explicit framing change on Checkpoint 7 (treat it as learning pass with pre-commit friction capture) plus three doctrinal prerequisites the rerun cannot run honestly without (brake-exit rule, `05-gap-closure` activation-trigger doctrine, execution-capacity reopen rule).
- `Option G` — Proposal D maximal multi-wave. Parallel lanes for mapping reconciliation, harness slice, brake-exit doctrine authoring, preservation-activation doctrine authoring. Only after all four close, rerun. Synthesis lane integrates.

## 3. Comparative Option Matrix

Register: `comparative option matrix`.

Columns: `first slice` · `prerequisites` · `execution-capacity model` · `upside` · `regret or reversal cost` · `evidence that would count against it`. Each option is compared at the same level of granularity to prevent the incumbent's richer articulation from producing inherited packet advantage (Surface B prelicensing pattern).

### Option A — B-extended minimal continuation

- **First slice**: close blocking finding 1 via a bounded cross-vendor reread of the executed R5.18 patch bundle (BE-6 in Wave-2 lane-05 terms); record disposition; proceed to Checkpoint 6 readiness verification. No Wave-2-bounded experiments promoted to slice work; they remain reviewer-register carry.
- **Prerequisites**: BE-6 reread artifact; frozen packet; committed checkpoint before launch; launch-truth capture on the cross-vendor lane.
- **Execution-capacity model**: one operator, ~1 week to close Checkpoint 5, ~1 week Checkpoint 6, ~3-4 weeks Checkpoint 7. Total: ~5-6 weeks. Fits charter model with margin.
- **Upside**: minimal disruption; preserves earned inheritance; honors bridge-audit guarded-hybrid verdict narrowly; maintains lane-05 disposition's rejection of broad pre-Wave-1 reorganization; respects AI-GUARDRAILS solo-developer posture; preserves critical inheritance disposition for all load-bearing artifacts; lowest per-slice reversal cost; fastest path to rerun under the literal PLAN.
- **Regret or reversal cost**: inherits the `13/31` stale-doctrine count straight into the rerun (mapping-lane U11 + Mismatch Matrix row 1); keeps the doctrine-vocabulary propagation gap (outcome-audit U1) unaddressed at rerun time; preserves the brake-exit attractor (mission-lane T1/T9) that just produced R5.17 → R5.19 cascade; sends `Proposal B-extended` into Checkpoint 7 having never earned a review-space check against E/F (Surface B prelicensing requirement not carried into practice); silently declines the post-Wave-1 anti-regret retests that Wave-2 lane-05 accepted (R3, R6, R11 `undercarried (promote bounded)` classifications become `deferred indefinitely`); the rerun then either absorbs those carries informally (repeating the pattern R5.17d3 named) or hits them mid-rerun and either stalls or widens.
- **Evidence that would count against it**: any Wave-1 launch-truth capture on a planner/executor/verifier spawn that shows inherited `CLAUDE.md` instructions still in `developer_instructions` (mapping-lane switch trigger ST-1 → fires); Wave-2 lane-05 PSP-1 / PSP-2 / PSP-3 / PSP-5 converge as pre-rerun prerequisites the incumbent PLAN does not name; BE-6 reread names a structural gap R5.17d3 operational consequences did not name (Wave-2 lane-05 ST-14 fires). Any one of these makes Option A provably too tame.

### Option B — B-extended plus bounded harness-first slice

- **First slice**: (1) BE-6 cross-vendor reread of executed R5.18 patch bundle; (2) BE-1 AUDIT-SPEC-TEMPLATE chain-tail rule plus matching note in `REVIEW-POLICY.yaml`; (3) BE-5 `Proceed anyway` stall branch replacement with mandatory debt-ledger write; (4) narrow `.toml` roster slice for `gsd-planner`, `gsd-executor`, `gsd-plan-checker`, `gsd-verifier` only (the four doctrine-sensitive workers registered today — four files, overlay + live, with installer regen discipline); (5) bounded doctrine-vocabulary propagation slice into `review.md` and `planner-reviews.md` only (the two named-consumer surfaces R5.17d3 convergent invalid exclusions cited). Five commits, one per slice.
- **Prerequisites**: executed-R5.18 ratified by BE-6; frozen per-slice packet boundaries; overlay mirror discipline enforced; launch-truth capture on any delegated lanes; committed checkpoint between slice and Checkpoint 6.
- **Execution-capacity model**: one operator, ~2-3 weeks for the slice bundle (BE-6 is ~3 days cross-vendor lane; BE-1 + BE-5 + `.toml` slice + vocabulary slice is ~5 file-level edits + overlay mirror + verify), then ~1 week Checkpoint 6, ~3-4 weeks Checkpoint 7. Total: ~7-8 weeks. Within charter model but noticeably wider than A.
- **Upside**: closes the single most live runtime defect narrowly (mapping-lane ST-1 trigger discriminator); closes STATUS blocking finding 1; lands three of the Wave-2 lane-05 bounded experiments that earned warrants at the post-Wave-1 retest (R3 `Proceed anyway`, BE-1 chain-tail rule, BE-6 cross-vendor reread); addresses outcome-audit U1 / U4 / U13 / U16 with single-file edits; preserves bridge-audit inheritance; does not foreclose C/E/F escalations under their trigger conditions; ratifies R5.18 frontier before rerun.
- **Regret or reversal cost**: two slices compete for budget with lane-05's six additional bounded experiments (BE-2 `plan-phase` reads partially already landed, BE-3 verifier/debt probe, BE-4 runtime-authority ledger, BE-7 carrier-case corpus re-trial, BE-8 install-drift probe); omitting them creates the "ratify the ratified" pattern without addressing the still-open ledger (Wave-2 lane-05 S2 `verified_with_debt`, S14 install-time mutation); the narrow `.toml` slice may choose the loudest drift over the most-leveraged one if the overlay/installer coupling is the real defect (mapping-lane Intervention #2 regret); slice-cost re-breaks on next install unless overlay regen is genuinely scripted rather than hand-patched; does not address brake-exit (mission-lane T1/T9) and so the rerun inherits the same attractor; `Proposal B-extended` at Checkpoint 7 is carried without the Surface B prelicensing review-space check having been re-run post-slice (BE-5 in Wave-2 lane-05 terms, a.k.a. BE-4 here).
- **Evidence that would count against it**: a launch-truth capture on a single planner spawn after the slice shows workers still carry `CLAUDE.md` language (the overlay/installer coupling is the defect, slice preempted wrong layer); BE-8 install-drift probe would have surfaced multi-key mutation (but BE-8 was not run); the rerun's fresh discuss-phase still produces the same one-sided non-promotion pattern R5.16a internal did (outcome-audit U13 unaddressed); preserve-only seams (outcome-audit N8, mission-lane T8) collapse in the rerun because activation-trigger doctrine was not authored.

### Option C — Proposal C mapping reconciliation first

- **First slice**: open a bounded mapping-reconciliation sublane (call it `lane-06.5-mapping-reconciliation` or `Checkpoint 5.x: Hybrid Upstream Baseline Reconciliation` per bridge-audit lane-03 reseed-judgment) that produces an intervention-ready map. Concrete deliverables: (a) probed runtime-authority ledger distinguishing `.toml` content from `.md` siblings with regen-anti-drift mechanism; (b) BE-8 install-time mutation drift probe in a throwaway worktree diffing overlay vs live; (c) first-class inventory of router / control-shell plane (manager, next, thread, workstreams, quick, fast, workspace admin) folded into a revised canonical topology; (d) install/materialization chain documentation including `.codex/gsd-file-manifest.json` completeness audit; (e) advisor / general-purpose adapter-mediated pathway resolution; (f) branch/worktree materialization end-to-end evidence under a small parallel-plan execution. Output: updated canonical topology artifact that feeds rerun-design.
- **Prerequisites**: BE-6 cross-vendor reread of executed R5.18 bundle; accepted Wave-2 lane-05 disposition (done); frozen packet for the reconciliation lane; launch-truth capture.
- **Execution-capacity model**: one operator plus one Codex lane plus one Claude cross-vendor lane over ~3 weeks; reuses Wave-1 packet discipline band (`60k-140k` per lane); then ~2-3 weeks for rerun-design revisit; then ~1 week Checkpoint 6; then ~3-4 weeks Checkpoint 7. Total: ~9-11 weeks. Stretches charter model further than B or F.
- **Upside**: closes the mapping lane's `directionally adequate, structurally under-mapped` verdict directly; produces the intervention-ready map the mapping lane's §72 Intervention-Ready Gap names as still unresolved; unlocks ST-5 (multi-lane reconciliation) and discriminates C/F pressures cleanly; the intervention-ready map would make any later F-slice sequencing evidence-driven rather than guess-driven; addresses runtime-authority drift without committing to harness program shape; converts several mapping-lane open questions (stale `.toml` sweep, overlay/installer drift, advisor pathway, worktree materialization) into probed closure.
- **Regret or reversal cost**: pushes rerun ~3-5 weeks further out than A/B/F; risks over-explaining every failure as a mapping failure (`PLAN-PROPOSALS` C weakness explicit); adds one more meta-wave to a package mission-lane T9 already named as having precondition-inflation pattern; the mapping-lane itself rejected this option as Intervention #3 with the specific regret that "C as a pre-Wave-1 gate would delay all four Wave-1 lanes for probably 2-3 weeks of operator bandwidth" — the same regret applies one wave later; the existing Wave-1 structure was able to absorb a bounded drift probe inside the Operator / Orchestration lane (which ran); opening a second reconciliation lane on top of that risks reproducing the pattern R5.17 → R5.19 split named; if mapping defects turn out to be mostly cosmetic, the budget is spent unproductively; during the reconciliation window, the `13/31` stale-doctrine drift persists in live runtime and is inherited into the rerun unchanged.
- **Evidence that would count against it**: no cross-lane reconciliation breakdown has fired (mapping-lane ST-5 held); the mapping lane itself classifies current mapping as "directionally adequate" and judges the intervention-ready map within reach of bounded mapping work inside B-extended (its own Intervention #3 regret); Wave-1 mapping-vs-judgment attribution says the cross-vendor-corrects-internal loop is a governance-process fingerprint, not a mapping defect (so reconciliation does not address it); the A1-A4 raw inventories are already usable as-is for consumers that need them (mapping-lane Intervention #1 regret); any of these makes C-first the wrong bounded next move even if C-inside-B-extended is right.

### Option D — Proposal F dominant program shape

- **First slice**: begin the five-slice F program in sequence. (1) Named-consumer read-set patches across `plan-phase.md:5-13` and `:827-834`, `review.md:117-141,237-283`, `planner-reviews.md:12-56` — BE-2 in Wave-2 lane-05 terms, partly landed already (`plan-phase.md` now references `planner-reviews.md` — verified live). (2) Full `.toml` roster unification — all 31 workers, overlay + live, single coordinated pass with cross-vendor verify after each family (planner/executor/verifier first; researcher/reviewer/debugger next; wrappers last). (3) Overlay-materialization pre-commit check or hook. (4) Install-time drift probe (BE-8) run as canonical rather than bounded experiment. (5) Launch-truth promotion from helper protocol to review-artifact side effect. Doctrine and rerun subordinated throughout.
- **Prerequisites**: explicit stop condition per slice (slice n+1 does not begin until slice n is cross-vendor-reviewed and reverted or ratified); authority-vs-edit distinction preserved (R5.17d3); overlay regen scripted before slice 2 begins; BE-6 cross-vendor reread before slice 1 begins; solo-developer posture checked before each slice; commit per subslice; reverted per subslice if regression.
- **Execution-capacity model**: one operator, ~4-6 weeks for the five-slice program itself; ~1 week Checkpoint 6; ~3-4 weeks Checkpoint 7. Total: ~9-11 weeks. Stretches the charter's multi-week budget near its edge without broad upstream `gsd` ownership; operator-pressure RJ-02 rejected this as a pre-Wave-1 move under the same capacity model.
- **Upside**: acts on the surface the evidence most points at (runtime-authority drift is the strongest live Wave-1 mapping finding, and three operator-pressure rows — OP-03, OP-04, OP-07 — would demote from primary/amplifying to weak); durable if runtime-authoritative behavior is the real blocker; converts the current manual overlay-mirror discipline into machinery; addresses multiple Wave-2 lane-05 UU items and BE items at once rather than per-slice; closes mapping-lane switch triggers ST-1, ST-2, ST-4, ST-6, ST-7 near-simultaneously under their respective discriminating next moves; strongest possible rerun-readiness posture.
- **Regret or reversal cost**: violates AI-GUARDRAILS:103-113 solo-developer posture explicitly ("strong review boundaries, small branches, additive traceability, minimal irreversible actions without explicit signoff"); converts `Proposal B-extended`'s switch-trigger doctrine into rhetorical infrastructure (ST-1 has not fired; adopting F preempts the switch-trigger model the setup itself earned); Wave-2 lane-05 UU-5 regret is directly on point: "full-F before Wave-1-informed prioritization risks choosing the loudest drift instead of the most leveraged one"; lane-05 operator cross-review `small branches` / `minimal irreversible actions` rule directly violated; widens repo-local scope that the operator-pressure RJ-02 also rejected; does not address brake-exit (mission-lane T1/T9) at program level — the recursive inward motion resolves by pivoting to a different attractor (harness refactor), not by resolving; foreclosures bridge-audit continuity (`revise + guarded hybrid reseed`); if BE-6 reread names a structural gap elsewhere, the F program preempts the fix by absorbing attention.
- **Evidence that would count against it**: ST-1 has not fired (no launch-truth capture has yet shown a planner spawn inheriting `CLAUDE.md`); BE-6 reread finds the carrier layer is sufficient and structural extension is not necessary (ST-14 held); mapping-lane Intervention #2 regret (full-F roster-unification symptom-patches without fixing overlay/installer coupling) fires if slice 2 lands before slice 4 drift-probes; any Wave-1 lane showing the router / control-shell plane is the more leveraged missing surface rather than the `.toml` plane (addressing it via F would be category error). Any one of these would reveal Option D as preempting.

### Option E — Proposal E execute-and-learn now

- **First slice**: skip any further prep slice beyond BE-6 closure. Open Phase 01 rerun as a learning pass: fresh `discuss -> context -> plan -> execute` against current canon, current governance docs, current runtime — with explicit no-canon-mutation posture, explicit friction-capture protocol (per-commit observations recorded as review artifacts), and a bounded `Phase 01 rerun as learning slice` success criterion (does the rerun expose the mapping / doctrine / harness defects the package has been reasoning about, and does it discriminate C/F/E/D pressures under real use?).
- **Prerequisites**: BE-6 cross-vendor reread (non-negotiable STATUS closure); frozen Checkpoint 5 with `R8.*` accepted as explicit remainder rather than absorbed; explicit brake-exit rule authored before rerun launch (PSP-3); explicit no-canon-mutation posture including activation-trigger doctrine for the `05-gap-closure` preserve-only seams (PSP-5) — otherwise the rerun's fresh discuss either silently binds or silently preserves eight seams without a trigger doctrine; explicit execution-capacity reopen rule authored (PSP-6).
- **Execution-capacity model**: one operator, ~1 week BE-6 + prerequisite doctrine; ~3-5 weeks learning rerun with pre-commit friction capture; then a short synthesis pass (~1 week). Total: ~5-7 weeks. Fits charter model at similar cost to Option B; different risk profile.
- **Upside**: produces evidence a mapping/debrief lane cannot produce directly — `PLAN-PROPOSALS` E explicitly names this; addresses mission-lane T9 precondition-inflation pattern directly by refusing to add a prep slice; addresses mission-lane T4 (execution-capacity honesty vs enacted time budget) by actually running the capability the charter claims the operator has; tests whether the readiness-rerun unit is itself too abstract (`PLAN-PROPOSALS` E explicit); exits the recursive inward motion by forcing outward discharge (mission-lane T1); forces operator-capacity questions into the open under real use; gives `Proposal E` a concrete first slice (Surface B prelicensing pass explicit requirement).
- **Regret or reversal cost**: spends Phase 01 budget on a learning pass rather than on the strongest rerun (`PLAN-PROPOSALS` E weakness explicit); noisy execution-evidence if launch conditions still too weak — a planner spawn inheriting `CLAUDE.md` instructions would confound genuine doctrine/mapping friction with avoidable prep-level friction; risks conflating avoidable prep failures with product truth (`PLAN-PROPOSALS` E weakness); preserve-only seams could get silently collapsed if activation-trigger doctrine isn't ready — and the package's strongest doctrinal payload is those seams (mission-lane 6b-iii matrix flags each of eight seams `preserve with activation-trigger need`); doctrine tensions get dressed in execution garb — they don't actually resolve, they relocate; the bounded `no-canon-mutation` posture during a learning rerun is hard to enforce against discuss-phase's generative tendency; mapping-lane U11 (`.codex/` repo-ignored) means the learning rerun's overlay mirror discipline remains operator-carried — a rerun that introduces overlay drift would do so silently.
- **Evidence that would count against it**: a launch-truth capture on a planner spawn before the rerun launches shows the `13/31` stale-doctrine fact is load-bearing (rerun would just reproduce it); activation-trigger doctrine for the eight preserve-only seams is not produced before rerun (seams would collapse in fresh discuss); mapping lane or BE-6 reveals a structural defect that would distort observed friction; a rerun artifact flattens preserve-only seams (the doctrine failure-mode trigger explicit in mission-lane T8); the `R8.1`-`R8.4` later-lane ownership surfaces (`ship.md`, `autonomous.md`, `transition.md`, non-TDD `checkpoints.md`) fire during the rerun and the learning-pass protocol cannot disposition them without smuggling canon mutation.

### Option F — Recommended hybrid: B-extended plus bounded harness-first slice plus learning-rerun posture plus three doctrinal prerequisites

- **First slice**: (1) BE-6 cross-vendor reread of executed R5.18 patch bundle (closes STATUS blocking finding 1). (2) Three-file harness-slice atomic edits: BE-1 AUDIT-SPEC-TEMPLATE chain-tail rule + `REVIEW-POLICY.yaml` note; BE-5 `Proceed anyway` stall branch replacement; bounded doctrine-vocabulary propagation slice into `review.md` and `planner-reviews.md` (the R5.17d3 convergent invalid exclusions). (3) Narrow `.toml` roster slice: unify `gsd-planner`, `gsd-executor`, `gsd-plan-checker`, `gsd-verifier` only (four doctrine-sensitive workers), with overlay regen scripted and tested; NOT the full 31-worker roster. (4) Three doctrinal prerequisite edits: codify a brake-exit rule as a sibling to the post-falsificationist `progressive vs degenerating response` distinction (PSP-3 owner: readiness doctrine layer, one-section addition to `POST-FALSIFICATIONIST-REVIEW-DOCTRINE.md` as an operationalization refinement — bounded and earned per Wave-2 lane-05 retest R5 `still defensible narrowed`); author the activation-trigger doctrine for the eight `05-gap-closure` preserve-only seams as a sibling to the preservation doctrine in `.planning/readiness/phase-01-rerun/PLAN.md:93-106` (PSP-5); recompute the minimum rerun-readiness set for Checkpoint 6 against Wave-1 and Wave-2 findings (PSP-2 — in `PLAN.md` or a companion `READINESS-VERIFICATION-SET.md`). (5) Reframe Checkpoint 7 posture: explicit learning-rerun protocol with pre-commit friction capture; no-canon-mutation posture; friction-evidence carry into a final synthesis artifact. (6) Checkpoint 6 runs as a minimum-rerun-readiness verification against the recomputed set; Checkpoint 7 runs under the learning-rerun posture.
- **Prerequisites**: BE-6 complete before any harness edit lands; accepted Wave-2 lane-05 disposition (done); overlay regen scripted before `.toml` slice; brake-exit rule authored and committed before Checkpoint 7 launches; activation-trigger doctrine authored before Checkpoint 7 launches OR deliberately deferred with explicit reopen trigger naming which seams are bound; execution-capacity reopen rule (PSP-6) authored before Checkpoint 7 launches; launch-truth capture on the cross-vendor BE-6 lane and on the fresh planner/executor/verifier spawns at rerun launch.
- **Execution-capacity model**: one operator. Budget breakdown: BE-6 (~3 days cross-vendor), harness-slice + doctrinal-prerequisite edits (~2 weeks across ~8 commits, overlay mirror discipline per commit), Checkpoint 6 verification (~1 week), Checkpoint 7 learning rerun (~3-4 weeks with friction capture discipline), final synthesis (~1 week). Total: ~7-9 weeks. Fits charter model; ~1-2 weeks wider than Option A continuation.
- **Upside**: closes Checkpoint 5 blocker (BE-6); closes the single most live runtime defect narrowly without widening to full F (`.toml` slice gated on ST-1 discriminator); lands three of the Wave-2 lane-05 retest promotions (R3, BE-1, BE-6); addresses outcome-audit U1/U4/U13/U16 via single-file edits; introduces the brake-exit rule mission-lane T1/T9 directly identifies as missing; honors the Surface B prelicensing pass requirement that `Proposal E` be made judgeable via a concrete first slice (Checkpoint 7 learning-rerun posture supplies it); addresses mission-lane T8 preserve-only seam permanence-of-deferral by forcing the activation-trigger doctrine to be authored before the rerun; preserves bridge-audit inheritance; does not foreclose C or F escalations on firing triggers (switch-trigger register in §6); minimum rerun-readiness set recomputation (PSP-2) converts implicit Checkpoint 5 exit criteria into explicit Wave-1-informed set; treats Checkpoint 7 as the `Proposal E` first slice at essentially no added cost because the package was always going to rerun.
- **Regret or reversal cost**: adds ~2-3 weeks versus Option A; adds multiple concurrent slice-types (harness + doctrine + rerun posture edits) which risks coordination cost if the operator does not maintain strict commit discipline per edit; the three doctrinal prerequisites (brake-exit rule, activation-trigger doctrine, min-rerun-readiness recomputation) are readiness-doctrine-layer edits that lane-05 and lane-04 both held back, so they need defending against authority-vs-edit distinction on R5.17d3 grounds — the defense here is that each is a sibling doctrine (not a rewrite) and each is named by Wave-1 and Wave-2 evidence as a load-bearing gap; slice may still choose the loudest drift over most-leveraged (same as B); `Proposal B-extended` review-space check (BE-4 Wave-2 lane-05 terms) is carried forward as required-section in the post-slice reread, so it does not go undone, but it adds another artifact; Checkpoint 7 under learning-rerun posture is harder to declare complete than Checkpoint 7 under original posture — the success criterion is narrower; if the learning rerun fails to discriminate C/F/E pressures, the hybrid's main epistemic value is not realized; AI-GUARDRAILS canon-update rule ("do not silently turn exploratory conclusions into canon; if a canon patch is broad, prefer a proposal artifact first") applies to the three doctrinal prerequisite edits — this option must ship them as three narrow proposal artifacts first, then patch, which adds ~2 commits to the slice sequence; one regret this option cannot dissolve: it defers full F indefinitely, so if the runtime-authority defect is actually structural not symptomatic, a later F wave will be needed anyway.
- **Evidence that would count against it**: (a) BE-6 reread names a structural gap R5.17d3 missed — fires Wave-2 lane-05 ST-14, routes to `Option D` (full F) reclassification; (b) post-slice launch-truth capture on a planner spawn shows workers still carry `CLAUDE.md` instructions — the overlay/installer coupling is the defect and the narrow slice preempted the wrong layer; fires mapping-lane ST-1 toward Option D; (c) brake-exit rule cannot be authored without widening into a broader post-falsificationist doctrine rewrite — fires the authority-vs-edit distinction and suggests the rule belongs in a lane-07 doctrine-authoring lane rather than in lane-06 output; (d) the activation-trigger doctrine requires broader `05-gap-closure` product-doctrine work than a readiness-canon sibling can hold — routes to `Option G` maximal multi-wave; (e) the learning rerun's first discuss pass produces the same one-sided non-promotion default R5.16a internal did (outcome-audit U13 reproduces) — the slice was insufficient; (f) any of PSP-1 through PSP-8 surfaces routed to lane-06 turns out to require a broader intervention than a lane-06 artifact can resolve; (g) charter execution-capacity model proves incorrect under ~8 weeks of sustained multi-slice work (PSP-6 validation negative).

### Option G — Proposal D maximal multi-wave

- **First slice**: parallel lanes. Lane `mapping-reconciliation` (Option C's first-slice deliverables). Lane `harness-slice` (Option D's first slice 1+2+3). Lane `doctrine-authoring` (brake-exit rule + activation-trigger doctrine + execution-capacity reopen rule + source-hierarchy refinement as a four-subpart readiness canon edit). Lane `minimum-rerun-readiness-recomputation`. All four launch in parallel; each cross-vendor reviewed; synthesis lane integrates before Checkpoint 6.
- **Prerequisites**: explicit operator commitment to ~6-10 weeks of parallel multi-lane work; capacity revalidation (PSP-6) before launch; committed checkpoint before each sublane; authority-vs-edit distinction preserved for each doctrine edit (R5.17d3); challenge-packet discipline per lane.
- **Execution-capacity model**: multi-week budget stretched to ~6-10 weeks prep + ~1 week Checkpoint 6 + ~3-4 weeks Checkpoint 7. Total: ~10-15 weeks. At or beyond the charter's comfortable multi-week budget; does not assume broad upstream `gsd` ownership.
- **Upside**: highest epistemic coverage; surfaces all structural seams (runtime-authority, brake-exit, preserve-only seam activation, mapping reconciliation) in one coordinated pass; closes mapping-lane §72 intervention-ready gap, mission-lane T8 preserve-only seam attractor, mission-lane T1/T9 brake-exit, mapping-lane Mismatch Matrix rows 1+2+8+9+10+11 simultaneously; strongest rerun-readiness posture absolutely; consistent with the `no regret` principle if the charter execution-capacity model holds.
- **Regret or reversal cost**: highest coordination overhead of any option; `PLAN-PROPOSALS` D weakness explicit: "easiest way to postpone action indefinitely"; mission-lane T9 precondition-inflation pattern explicitly documented — maximal multi-wave is the canonical recurrence of the pattern the package has been failing on; violates AI-GUARDRAILS `small branches, minimal irreversible actions` posture; contradicts lane-05 comparative disposition's explicit rejection of broad pre-Wave-1 reorganization (applied here to broad pre-rerun reorganization); second-order audit labyrinth risk; Wave-2 lane-05 IR-4 (`name specific sequencing order`) regret applies (sequencing locks budget against evolving capacity model); if any of the parallel lanes returns weak, the synthesis lane inherits the weakness; operator-pressure OP-08 explicitly identified wave-level meta-workspace accumulation as a `weak / post-hoc explanation` tier pressure at current evidence line, but Option G would promote that pressure category to confirmed-primary by enacting it.
- **Evidence that would count against it**: any Wave-1 lane result saying `bounded local fixes cleared the surface` — the maximal option then preempts its own evidence base; mission-lane T4/T9 tensions fire by construction under this option (the ~10-15 week budget is itself a test); operator-pressure ST-6 fires reliably; AI-GUARDRAILS canon-update rule ("if a canon patch is broad, prefer a proposal artifact first") multiplies the proposal-artifact count beyond sustainable pace; lane-05 disposition's rejection of broad pre-Wave-1 reorganization applies in letter and spirit.

## 4. Recommended Next Program

### Recommendation

- [d:r:i] Recommended: **`Option F`** — `B-extended plus bounded harness-first slice plus learning-rerun posture for Checkpoint 7 plus three doctrinal prerequisites`. This is not `Option A` incumbent continuation and not `Option D` broad machinery rewrite. It is a narrower hybrid that addresses the package's signature failure pattern (brake-exit problem + runtime-authority drift + preserve-only seam permanence-of-deferral) without widening into full-F, full-C, or full-D scope.

### Why this over the nearest serious alternatives

- [d:r:i] **Over `Option A` incumbent continuation**: `Option A` preserves the brake-exit attractor by construction. It also defers the Wave-2 lane-05 retest promotions that earned warrants (R3 `Proceed anyway`, BE-1 chain-tail rule, BE-6 cross-vendor reread) into reviewer-register carry, which enacts the tame-closure pattern the charter's failure condition explicitly names. Option A would be correct if the Wave-1 returns had not found the `directionally adequate, structurally under-mapped` runtime-authority drift, or had not found the under-carried R5.17d3 doctrine-vocabulary propagation, or had not found the brake-exit attractor at mission layer. They did. `Option A` therefore fails the anti-regret test: it leaves named, warranted, low-blast bounded moves on the table in exchange for ~1-2 weeks of schedule.
- [d:r:i] **Over `Option B` B-extended plus harness slice without doctrinal prerequisites**: `Option B` closes the runtime-authority slice but does not address the brake-exit problem at all. The Wave-1 mission lane identifies this as the structural weakness — the package's theory of change had no explicit exit condition from its anti-closure brakes. Adding bounded harness slices without also adding a brake-exit rule means the next rerun recurs the attractor. `Option B` also does not supply the activation-trigger doctrine the 05-gap-closure preserve-only seams need, which either binds the seams silently during Checkpoint 7 or refuses to close any of them indefinitely. `Option F` = `Option B` + three doctrinal prerequisites that are named by Wave-1 and Wave-2 returns as load-bearing gaps.
- [d:r:i] **Over `Option C` mapping reconciliation first**: `Option C`'s first slice is a bounded mapping-reconciliation sublane that the mapping lane itself rejected as its own Intervention #3 with the specific regret that the existing Wave-1 structure was able to absorb a bounded drift probe inside the Operator / Orchestration lane. It would push rerun ~3-5 weeks further out against the mission-lane T9 precondition-inflation pattern, and the mapping defects it would close are addressable via bounded probes inside `Option F` (BE-4 runtime-authority ledger and BE-8 install-time drift probe are both `Option F`-compatible if the harness slice produces evidence that warrants them). `Option C` is the right move if and only if ST-5 (multi-lane reconciliation failure) fires, and ST-5 has not fired. `Option F` keeps `Option C` live as a switch path via ST-5 in the register below.
- [d:r:i] **Over `Option D` Proposal F dominant shape**: `Option D`'s regret analysis is covered in detail in §3 and §8. In short: the discriminating trigger for full F (ST-1, launch-truth capture showing inherited CLAUDE.md) has not fired; adopting full F preempts the switch-trigger doctrine the setup earned; the `small branches / minimal irreversible actions` AI-GUARDRAILS posture forbids the multi-slice parallel shape; the brake-exit problem is unaddressed at program level. `Option F` absorbs the narrowest version of `Option D` (the planner/executor/verifier `.toml` slice) as a discriminating step rather than as a program-shape reversal.
- [d:r:i] **Over `Option E` Proposal E execute-and-learn now**: `Option E`'s regret analysis is the strongest of the rejected options precisely because a learning rerun is exactly what the brake-exit problem calls for. But `Option E` executed against current runtime-authority drift would confound avoidable prep friction with genuine mission-lane T1/T2/T4 friction. `Option F` makes `Option E` viable by running the bounded harness slice first (so the rerun's workers carry current doctrine) and adding the three doctrinal prerequisites (so the rerun has brake-exit rule and activation-trigger doctrine to run honestly against). `Option F` is essentially `Option E` plus the minimum prep that `Option E` itself requires to not be noisy — because the Surface B prelicensing pass demanded E have a concrete first slice with noise-control.
- [d:r:i] **Over `Option G` Proposal D maximal multi-wave**: `Option G` is the canonical recurrence of the mission-lane T9 precondition-inflation pattern. It violates lane-05 disposition's rejection of broad pre-Wave-1 reorganization extended to pre-rerun reorganization, and its `~10-15 week` budget sits at or beyond the charter's comfortable range. `Option F` absorbs the three narrowest doctrinal prerequisites that `Option G` would do in parallel and defers mapping-reconciliation to a switch-trigger-gated escalation.

### Concrete first-slice sequence for `Option F`

Executed in order, each as one commit, each with explicit rollback path. The sequence below is a recommendation; lane-06 is authoritative for this recommendation but the operator retains final ordering discretion per Wave-2 lane-05 IR-4's reasoning (ordering is an execution decision that may respond to capacity constraints).

1. **`BE-6` — Cross-vendor reread of executed R5.18 patch bundle.** Launch one cross-vendor lane against the patches on disk (not the revised spec). Scope ~80k tokens; bounded packet-freeze per Wave-1 discipline. Closes STATUS blocking finding 1. Success: the review either ratifies closure or names a specific surface requiring targeted patching. Rollback: the review artifact exists as challenge input only.
2. **`BE-1` — AUDIT-SPEC-TEMPLATE chain-tail rule.** Edit `.planning/readiness/phase-01-rerun/AUDIT-SPEC-TEMPLATE.md` to add the rule `specs covering cross-phase completion must include bin/lib/*.cjs + transition.md + ship.md + autonomous.md in the chain-tail read set`. Add matching note in `REVIEW-POLICY.yaml`. Single commit. Rollback: revert commit.
3. **Doctrine-vocabulary propagation slice.** Add post-falsificationist vocabulary (`post-verificationist`, `post-falsificationist`, `gap-exposure`, `completeness-challenge`, `anti-regret`) to `.codex/get-shit-done/workflows/review.md` in the review-shape sections and to `.codex/get-shit-done/references/planner-reviews.md` as reviewer-register references. Keep governance-authority files untouched (R5.17d3 authority-vs-edit distinction preserved). Overlay mirror per commit. Rollback: revert commit.
4. **`BE-5` — `Proceed anyway` stall branch replacement.** Edit `.codex/get-shit-done/workflows/plan-phase.md` stall branch to replace `Proceed anyway` option with mandatory debt-ledger write that blocks downstream `ship.md` unless explicitly resolved. Single-file edit; overlay mirror. Rollback: revert commit.
5. **Narrow `.toml` roster slice.** Unify `.codex/agents/gsd-planner.toml`, `.codex/agents/gsd-executor.toml`, `.codex/agents/gsd-plan-checker.toml`, `.codex/agents/gsd-verifier.toml` only — replace `CLAUDE.md` references with `AGENTS.md`, `.claude/skills/` with `.codex/skills/`, align `developer_instructions` with repo-local truth. Mirror to `tooling/portable-gsd/overlay/agents/` for each. Test overlay regen: run `scripts/setup-portable-gsd.sh` in a throwaway worktree; verify the unified instructions survive install. Only commit if regen survives. Rollback: revert all slice commits.
6. **Brake-exit rule (doctrinal prerequisite 1).** Author a one-section addition to `.planning/readiness/phase-01-rerun/POST-FALSIFICATIONIST-REVIEW-DOCTRINE.md` naming `brake-exit conditions`. The rule sits beside `progressive vs degenerating response` and names explicit triggers: (a) recursive-inward-motion count per checkpoint (e.g. if a checkpoint spawns more than N parallel-family sublanes, a brake-exit review fires); (b) pre-rerun budget consumption crossing a threshold relative to the rerun's own estimated budget; (c) when two consecutive reviews cannot discriminate progressive from degenerating response; (d) when the post-falsificationist doctrine's stop-and-escalate criteria fire at audit-layer level. Rule explicitly states that brake-exit can fire toward `switch shape`, `accept residual`, or `execute-and-learn` — not only toward more audit. Ship as a proposal artifact first (AI-GUARDRAILS canon-update rule), patch second. Rollback: revert both commits.
7. **Activation-trigger doctrine (doctrinal prerequisite 2).** Author a sibling section to `.planning/readiness/phase-01-rerun/PLAN.md:93-106` naming activation triggers for each of the eight 05-gap-closure preserve-only seams (first post-private wrapper order, first audience-right bundle, later money-family ranking, later host-identity ranking, first explicit outward shell, first true event-memory shell, exact cadence taxonomy, whether preserve-only contribution/discovery seams ever activate). Each seam gets a named trigger condition. Seams that cannot be given a trigger stay preserve-only with explicit `no activation trigger` disposition; this is itself a decision that costs less than silent permanence-of-deferral. Ship as proposal artifact first; patch second. Rollback: revert both commits.
8. **Minimum rerun-readiness set recomputation (doctrinal prerequisite 3).** Edit `.planning/readiness/phase-01-rerun/PLAN.md` Checkpoint 6 exit criteria (or add a companion `READINESS-VERIFICATION-SET.md`) to recompute the minimum rerun-readiness set against Wave-1 and Wave-2 evidence. The set should explicitly include: BE-6 closure ratified; narrow `.toml` slice landed; doctrine-vocabulary propagation landed; brake-exit rule authored; activation-trigger doctrine authored; `R8.*` ownership enumerated with reopen trigger per surface; AI-GUARDRAILS execution-capacity reopen rule authored (PSP-6). Anything not in this set is either (a) deferred as `R9.*` with explicit reopen trigger or (b) handled mid-rerun under the learning-rerun posture's friction-capture protocol. Rollback: revert commits.
9. **Checkpoint 6 readiness verification**. Run against the recomputed minimum rerun-readiness set. If passed: proceed to Checkpoint 7. If any element fails: either land the missing element (route to slice bundle) or explicitly defer with reopen trigger (route to `R9.*`). Under no condition proceed to Checkpoint 7 on elements that the recomputed set names as blocking.
10. **Checkpoint 7 under learning-rerun posture**. Fresh `discuss -> context -> plan -> execute` for Phase 01, treating the rerun as a `Proposal E` learning pass. Pre-commit friction capture: every time the discuss/plan/executor produces a surface, record observed friction points against the PLAN's design expectations. Explicit no-canon-mutation posture; any would-be canon change is recorded as a mid-rerun deviation per existing PROTOCOL.md:124-149 disposition ladder, not landed mid-rerun. Friction evidence carries into a final synthesis artifact that feeds either `R9.*` lane opening or explicit rerun-complete disposition.

### What remains intentionally open in this recommendation

- [o:r:i] Whether BE-2 `plan-phase` reads, already partly landed (`plan-phase.md` now references `planner-reviews.md` in required_reading — verified live), needs further `{reviews_path}` expansion into plan-checker read-sets. Held open; Wave-2 lane-05 BE-2 is already partly done and the remaining expansion can happen as a sub-slice of step 3 above or deferred.
- [o:r:i] Whether BE-3 verifier/debt compatibility probe and BE-4 runtime-authority ledger should run inside `Option F` or be deferred to mid-rerun friction capture. `Option F` treats them as optional additions if step 5 `.toml` slice reveals structural-vs-symptomatic ambiguity; the rerun itself is otherwise the discriminator.
- [o:r:i] Whether BE-7 (one bounded carrier-case corpus-to-carrier re-trial) runs as a separate slice or inside the rerun. The Wave-2 lane-05 disposition held it as `bounded experiment warranted, optional`; `Option F` keeps it reviewer-register only unless the learning rerun produces a case that makes it the discriminating next move.
- [o:r:i] Whether BE-8 install-time drift probe runs inside `Option F` or as a discriminating test for a later ST-4 firing. Not part of recommended first slice.
- [o:r:i] Whether the Checkpoint 7 learning-rerun posture should carry a separate PLAN edit or be captured inline in an updated Checkpoint 7 section. Both are defensible; operator decision.

## 5. Prerequisites And Program-Carrying Surfaces

Register: `prerequisites and program-carrying surfaces register`. Classified by whether evidence says the surface materially shapes execution honesty (not background hygiene).

### Prerequisites (time-ordered, all program-carrying)

| # | Prerequisite | Why this is a prerequisite, not background hygiene | Source evidence |
|---|---|---|---|
| P1 | BE-6 cross-vendor reread of executed R5.18 patch bundle closes STATUS blocking finding 1 | Checkpoint 5 is not closed without this; any rerun without R5.18 ratification inherits unaudited patches in the very routing layer the rerun depends on | [.planning/readiness/phase-01-rerun/STATUS.md:150-154](../../../../readiness/phase-01-rerun/STATUS.md); [wave-2/outputs/05-suppressed-opportunity-and-non-intervention-opus47-max-r1.md:116-124](../outputs/05-suppressed-opportunity-and-non-intervention-opus47-max-r1.md) |
| P2 | BE-1 AUDIT-SPEC-TEMPLATE chain-tail rule landed | The package's signature failure mode is under-read specs producing one-sided non-promotion verdicts; without the rule, the rerun's discuss-phase writer can repeat R5.16a internal's failure | [wave-1/outputs/02-outcome-and-underreach-audit-opus47-max-r1.md:60,163-165](../../wave-1/outputs/02-outcome-and-underreach-audit-opus47-max-r1.md); R5.16c direct recommendation |
| P3 | Brake-exit rule authored (doctrinal sibling to progressive-vs-degenerating response) | Mission-lane T1/T9 identify the brake-exit attractor as the package's structural weakness; without the rule, Checkpoint 7's fresh discuss inherits the same attractor and another R5.17 → R5.19 cascade becomes predictable | [wave-1/outputs/01-mission-reconstruction-opus47-max-r1.md:88-103](../../wave-1/outputs/01-mission-reconstruction-opus47-max-r1.md); [wave-2/outputs/05-suppressed-opportunity-and-non-intervention-opus47-max-r1.md:201-203](../outputs/05-suppressed-opportunity-and-non-intervention-opus47-max-r1.md) |
| P4 | Activation-trigger doctrine for the eight 05-gap-closure preserve-only seams authored | The preserve-only seams are the doctrinal payload the entire package was protecting; without activation triggers, Checkpoint 7 either silently binds a seam or preserves all eight indefinitely; both are failure modes named by mission-lane T8 | [wave-1/outputs/01-mission-reconstruction-opus47-max-r1.md:164-180](../../wave-1/outputs/01-mission-reconstruction-opus47-max-r1.md); [.planning/readiness/phase-01-rerun/PLAN.md:93-106](../../../../readiness/phase-01-rerun/PLAN.md) |
| P5 | Minimum rerun-readiness set recomputation (PSP-2) | Current Checkpoint 6 exit criteria were set before Wave-1 returns; without recomputation the rerun launches against stale readiness criteria; `R8.*` ownership list needs post-Wave-1 reaffirmation or revision | [wave-2/outputs/05-suppressed-opportunity-and-non-intervention-opus47-max-r1.md:201](../outputs/05-suppressed-opportunity-and-non-intervention-opus47-max-r1.md); [.planning/readiness/phase-01-rerun/STATUS.md:155-161](../../../../readiness/phase-01-rerun/STATUS.md) |
| P6 | Execution-capacity reopen rule authored (PSP-6) | Charter execution-capacity model is `[d:r:i]` with Wave-1 evidence pressing on it (mission-lane T4 enacted-budget-vs-stated-capacity tension); without the reopen rule, the model silently persists into rerun decisions where it is load-bearing | [AUDIT-CHARTER.md:20-21](../../AUDIT-CHARTER.md); [wave-1/outputs/01-mission-reconstruction-opus47-max-r1.md:96-97](../../wave-1/outputs/01-mission-reconstruction-opus47-max-r1.md); [wave-2/outputs/05-suppressed-opportunity-and-non-intervention-opus47-max-r1.md:209](../outputs/05-suppressed-opportunity-and-non-intervention-opus47-max-r1.md) |
| P7 | Launch-truth capture protocol applied per WORKFLOW.md:69-82 on doctrine-sensitive rerun spawns (including the fresh planner/executor/verifier) | Operator-pressure OP-03 classified this as the one `primary` pressure; without capture, the rerun's planner spawn inheriting (or not) `CLAUDE.md` instructions cannot be established post-facto | [WORKFLOW.md:69-82](../../../../../WORKFLOW.md); [wave-1/outputs/04-operator-orchestration-pressure-opus47-max-r1.md:55-72](../../wave-1/outputs/04-operator-orchestration-pressure-opus47-max-r1.md) |
| P8 | Checkpoint-commit discipline per slice and per rerun stage | The workspace has already demonstrated that checkpoint discipline is blocking for delegated launch (lane-05 disposition); the same logic applies to the rerun's own stages; without per-stage commits, the learning-rerun friction capture cannot be reconstructed after-the-fact | [WORKFLOW.md:49-67](../../../../../WORKFLOW.md); [lane-05-comparative-disposition.md:18-20](../../lane-05-comparative-disposition.md) |
| P9 | Overlay-mirror discipline scripted or pre-commit-checked during the `.toml` slice | Mapping-lane Intervention #2 regret explicitly: the `13/31` stale drift is symptomatic of overlay/installer coupling; a `.toml` pass without overlay regen discipline re-breaks on next install | [wave-1/outputs/03-mapping-adequacy-and-comparative-mapping-opus47-max-r1.md:157-162](../../wave-1/outputs/03-mapping-adequacy-and-comparative-mapping-opus47-max-r1.md); [scripts/setup-portable-gsd.sh:58-91](../../../../../scripts/setup-portable-gsd.sh) |

### Program-carrying surfaces (where edits land)

| # | Surface | Classification | Why this carries program shape |
|---|---|---|---|
| S1 | `.codex/agents/{gsd-planner,gsd-executor,gsd-plan-checker,gsd-verifier}.toml` + overlay siblings | Runtime authority | The four doctrine-sensitive workers' `developer_instructions` are the first thing a rerun spawn reads; `13/31` stale at review time; narrow slice is the discriminating test for mapping-lane ST-1 |
| S2 | `.codex/get-shit-done/workflows/plan-phase.md` + overlay | Workflow carrier | `Proceed anyway` stall branch is one of the three convergent permissive branches R5.16c named; `planner-reviews.md` load path is already partly landed |
| S3 | `.codex/get-shit-done/workflows/review.md` + `references/planner-reviews.md` + overlay | Doctrine-vocabulary carrier | Outcome-audit Anchor 2 names doctrine-vocabulary non-propagation as an `explicit miss`; these two files are the R5.17d3 convergent invalid exclusions cited by name |
| S4 | `.planning/readiness/phase-01-rerun/AUDIT-SPEC-TEMPLATE.md` + `REVIEW-POLICY.yaml` | Audit-spec carrier | Without the chain-tail rule, the same under-read failure mode recurs in the next lane-writing or rerun-friction-capture work |
| S5 | `.planning/readiness/phase-01-rerun/POST-FALSIFICATIONIST-REVIEW-DOCTRINE.md` | Doctrine authority | Brake-exit rule lands here as a sibling section; the authority-vs-edit distinction R5.17d3 preserved is honored because the edit is an operationalization refinement, not a doctrine rewrite |
| S6 | `.planning/readiness/phase-01-rerun/PLAN.md` (§§ preserve-only seams + Checkpoint 6 exit criteria + Checkpoint 7 posture) | Readiness PLAN authority | Three of the doctrinal prerequisites land here: activation-trigger doctrine as a sibling section, Checkpoint 6 recomputation via exit-criteria edit, Checkpoint 7 posture change to learning-rerun |
| S7 | `.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/` (workspace-local) | Audit scaffolding | Already checkpointed per lane-05 disposition; must remain committed per slice for auditability; lane-06 output is itself part of this surface |
| S8 | `scripts/setup-portable-gsd.sh` + `tooling/portable-gsd/overlay/` | Installer / overlay | Overlay regen scripting is the prerequisite for `.toml` slice durability; if this surface is hand-edited mid-slice, the slice re-breaks on next install |
| S9 | `.planning/readiness/phase-01-rerun/STATUS.md` + `GATES/checkpoint-5.md` + `GATES/checkpoint-6.md` (new) | Mutable state authority | Post-slice STATUS edits carry the bounded-wave closure; Checkpoint 6 gate material may need to be authored as a new file against the recomputed readiness set |
| S10 | `LAUNCH-LEDGER.md` (audit workspace) | Launch-history carrier | BE-6 cross-vendor lane and all rerun-phase delegated spawns need ledger entries with frozen-packet SHA and launch-truth capture |

### Surfaces treated as background hygiene, not prerequisites

- [d:r:i] `.gitignore` status of `.codex/` — Wave-2 lane-05 BE-3 held this as a bounded experiment worth spiking later; not a prerequisite for `Option F` because mirror discipline carries the audit-diff need; revisit under ST-8 or after the rerun.
- [d:r:i] Audit-workspace directory reorganization — lane-05 disposition already rejected broad pre-Wave-1 reorganization; the same logic extends to pre-rerun; the `wave-1/` and `wave-2/` topology is sufficient. Revisit if navigation actually breaks during rerun friction capture.
- [d:r:i] `.codex/hooks.json` expansion — operator-pressure RJ-03 rejected; hook maturity is experimental; protocol carry suffices. Revisit under ST-4.
- [d:r:i] Generic branch-strategy / remote-protection redesign — lane-05 Opus cross-review explicitly ruled out as pre-launch prerequisite; same logic applies here.
- [d:r:i] Wholesale `.toml` roster unification beyond the narrow four-worker slice — UU-1 in Wave-2 lane-05, preserved here. Revisit under ST-1 firing.

## 6. Switch-Trigger Register

Register: `switch-trigger register`. These triggers move the workspace away from the recommended `Option F` program toward other options. Each trigger specifies what evidence would fire it, what switch it licenses, and the execution-capacity assumption behind the switch. Triggers are organized by target option.

### Triggers toward `Option A` (minimal continuation)

| # | Trigger | Fires on | Execution-capacity assumption |
|---|---|---|---|
| T-A-1 | Budget constraint forces slice scope cut | Operator capacity drops below ~7 weeks total for `Option F`; the narrow slice must be cut | Charter model shrinks; fall back to incumbent PLAN with BE-6 only |
| T-A-2 | BE-1 chain-tail rule cannot be narrowed to a single-paragraph addition | AUDIT-SPEC-TEMPLATE edit starts requiring broader template work (authority-vs-edit distinction violated) | Revert the edit; reviewer-register only; continue incumbent PLAN |

### Triggers toward `Option B` (harness slice without doctrinal prerequisites)

| # | Trigger | Fires on | Execution-capacity assumption |
|---|---|---|---|
| T-B-1 | Brake-exit rule authoring would require a broader post-falsificationist doctrine rewrite | The sibling-section shape cannot hold the rule without expanding into a rewrite | Fall back to `Option B`; defer brake-exit to a separate lane-07 doctrine-authoring lane; proceed to Checkpoint 6 without it |
| T-B-2 | Activation-trigger doctrine requires broader product-doctrine work than readiness canon can hold | Each of the eight seams demands product-doctrine revision; the preservation-doctrine-sibling shape fails | Fall back to `Option B`; keep preserve-only seams as-is with explicit `no activation-trigger doctrine authored at rerun time` disposition; rerun proceeds with seam preservation preserved and seam activation explicitly deferred |

### Triggers toward `Option C` (mapping reconciliation first)

| # | Trigger | Fires on | Execution-capacity assumption |
|---|---|---|---|
| T-C-1 | Runtime/docs/mapping cannot be reconciled during `Option F` slice without ontology lane | BE-6 reread or `.toml` slice encounters categorical disagreement between readiness mapping, docs corpus, and live runtime that no slice can dissolve | Open `lane-07-mapping-reconciliation` before Checkpoint 6; delay rerun ~3 weeks; treat `Option F` doctrinal prerequisites as paused during the reconciliation lane |
| T-C-2 | Two or more Wave-2 lanes converge on the same categorical re-classification | Later Wave-2 work (lane-07 or beyond) produces evidence the four-lane envelope itself is wrong | Escalate to full Proposal C |

### Triggers toward `Option D` (Proposal F dominant shape)

| # | Trigger | Fires on | Execution-capacity assumption |
|---|---|---|---|
| T-D-1 | Post-slice launch-truth capture on planner/executor/verifier spawn still shows inherited `CLAUDE.md` instructions | `.toml` slice landed but overlay/installer coupling re-breaks on install (per mapping-lane Intervention #2 regret) | Open full F program; slice 2 (full roster) becomes obligatory; slice 3 (overlay pre-commit hook) becomes obligatory; defer rerun until F program completes |
| T-D-2 | BE-8 install-time drift probe shows multi-key mutation beyond `model_reasoning_effort` | Installer mutates multiple steering keys post-install; any slice that ignores this is symptomatic | Open `Option D` slice 4 (drift probe as canonical); add overlay-canon-vs-runtime contract edit; defer rerun |
| T-D-3 | BE-6 cross-vendor reread names structural gap R5.17d3 operational consequences did not name | The executed R5.18 patches are insufficient and need further structural work | Open `Option D` bounded slice targeting the named gap; treat as post-Checkpoint-5 slice rather than pre-rerun; may force rerun delay |

### Triggers toward `Option E` (execute-and-learn now without slice)

| # | Trigger | Fires on | Execution-capacity assumption |
|---|---|---|---|
| T-E-1 | BE-6 reread clean + BE-1 landed + operator accepts open `.toml` roster with explicit reactivation trigger | The STATUS blocker closes; the template rule lands; operator decides the `13/31` count is not distorting enough to block rerun | Skip `Option F` steps 5 (`.toml` slice) through 8; proceed to recomputed Checkpoint 6 and learning rerun; `R8.*` ownership holds on `.toml` roster as explicit remainder |
| T-E-2 | Checkpoint 7 learning-rerun posture decision taken without the three doctrinal prerequisites | The operator judges the three edits as canon-mutation risk and defers them beyond the rerun | Rerun proceeds under `Option E` posture; doctrinal prerequisites become post-rerun review work; regret if rerun flattens preserve-only seams |

### Triggers toward `Option G` (maximal multi-wave)

| # | Trigger | Fires on | Execution-capacity assumption |
|---|---|---|---|
| T-G-1 | Multiple Wave-2 lanes (at least three of T-C-1, T-D-1, T-D-2, T-B-1, T-B-2) fire near-simultaneously | The `Option F` shape is insufficient on multiple axes at once | Open maximal multi-wave; accept ~10-15 week budget; PSP-6 execution-capacity reopen is mandatory before launch |

### Triggers that reinforce `Option F`

| # | Trigger | Fires on | What it confirms |
|---|---|---|---|
| T-F-1 | BE-6 reread ratifies executed R5.18 | Checkpoint 5 closes cleanly | `Option F` first slice is sound; proceed to doctrinal prerequisites |
| T-F-2 | Narrow `.toml` slice holds across one install cycle | Overlay regen survived install; no hand-edit drift | `Option F` slice shape is discriminating; full-F preemption was correctly rejected |
| T-F-3 | Brake-exit rule authored as a sibling section fits inside post-falsificationist doctrine without rewrite | The authority-vs-edit distinction is preserved; the doctrine earns a refinement rather than a rewrite | `Option F` doctrinal prerequisite 1 is discriminating; doctrine-rewrite regret does not apply |
| T-F-4 | Activation-trigger doctrine for the eight seams is authored without requiring product-doctrine revision | Each seam gets a trigger or explicit `no trigger` disposition | `Option F` doctrinal prerequisite 2 is discriminating; the 05-gap-closure preserve-only seams stop being permanence-of-deferral without forcing product-doctrine reopening |
| T-F-5 | Checkpoint 7 learning-rerun posture produces discriminating friction evidence | The rerun distinguishes C/F/E/D pressures under real use; the brake-exit rule fires at least once in Checkpoint 7 | `Option F` was the right program shape; future audits can inherit the hybrid |

## 7. What Must Stay Open

Register: `unresolved register`. Items this lane explicitly does not settle, with each item's routing.

- [o:r:i] Whether the narrow `.toml` slice's four-worker scope (`gsd-planner`, `gsd-executor`, `gsd-plan-checker`, `gsd-verifier`) is the right discriminator, or whether the researcher/reviewer/debugger family should also land. The slice is deliberately narrow to act as a discriminator; a broader sweep remains reachable via T-D-1. Routing: operator decides during slice execution; the switch trigger register covers the escalation path.
- [o:r:i] Whether the brake-exit rule belongs in `POST-FALSIFICATIONIST-REVIEW-DOCTRINE.md` or in `PROTOCOL.md`. Both are authority files the R5.17d3 distinction preserved. Routing: ship as proposal artifact first with both candidate locations named; defer the final placement decision to the proposal-artifact review step.
- [o:r:i] Whether the activation-trigger doctrine for the 05-gap-closure preserve-only seams should be authored at readiness-doctrine layer or at product-doctrine layer. `Option F` places it at readiness-doctrine layer as a sibling to the preservation doctrine already there; Wave-2 lane-05 IR-5 identified this placement as the narrower defensible shape. Routing: if T-B-2 fires, escalate to product-doctrine pass; otherwise proceed at readiness layer.
- [o:r:i] Whether the charter execution-capacity model (`multi-week budget, bounded repo-local refactors`) holds under `Option F`'s ~7-9 week sustained slice-plus-rerun work. This lane relies on the model without revalidating it; PSP-6 explicitly names the reopen obligation. Routing: operator records budget expenditure against the model during each slice; escalate to execution-capacity reopen if Wave-1 evidence plus actual `Option F` expenditure pattern cannot be reconciled.
- [o:r:i] Whether the Checkpoint 7 learning-rerun posture requires a formal PLAN edit or only an inline note at the Checkpoint 7 section. Both shapes fit; the wider AI-GUARDRAILS canon-update rule suggests a proposal artifact first for the posture change. Routing: ship as proposal artifact if the edit is broader than one paragraph; inline note if narrower.
- [o:r:i] Whether BE-7 (one bounded corpus-to-carrier re-trial on a non-doctrine case) runs inside `Option F` or is held for a later bounded Wave-3 slice. This lane does not require it; Wave-2 lane-05 held it as optional. Routing: if Checkpoint 7 learning-rerun produces a carrier-surface case where corpus distinctions might earn partial operational inheritance, BE-7 becomes the post-rerun follow-up; otherwise the narrow non-promotion stands.
- [o:r:i] Whether the Wave-2 lane-05 `verified_with_debt` structural extension (S2 → UU-2-adjacent) should fire from within `Option F` or post-rerun. Routing: if BE-6 reread names consumer-side routing that ignores `completion_mode`, this fires pre-rerun as T-D-3; otherwise post-rerun.
- [o:r:i] Whether the `.codex/` repo-tracking spike (BE-3 in Wave-2 lane-05, BE-5 otherwise) should run as a separate Wave-2 artifact alongside `Option F` or be deferred. `Option F` does not depend on it. Routing: operator discretion; lower priority than the doctrinal prerequisites.
- [o:r:i] Whether Wave-2 should launch a counterpart lane-06 GPT-5.4 xhigh review of this artifact before `Option F` execution begins. Wave-1 disposition explicitly left this open. This lane does not recommend it as a lane-06 obligation (per Wave-2 lane-05 IR-6 reasoning: that choice is the operator's disposition decision, not the lane's). Routing: operator decides during lane-06 disposition planning.
- [o:r:i] Whether the R8.1–R8.4 broader-remainder ownership list (ship.md, autonomous.md, transition.md, non-TDD checkpoints.md, summary.md template, gsd-code-reviewer.toml, non-first-wave researcher/planner/checker pairing) should receive a ratification review artifact before Checkpoint 6 or be folded into the recomputed minimum-rerun-readiness set. `Option F` folds R8.* reaffirmation into prerequisite P5; a separate ratification review artifact remains defensible if the operator prefers explicit second-layer review. Routing: P5 execution surfaces this decision.
- [o:r:i] Whether `Option F`'s execution itself needs a mid-slice checkpoint review between the harness slice (steps 1-5) and the doctrinal prerequisites (steps 6-8). Both shapes fit `WORKFLOW.md:49-67`; a mid-slice review would add ~1 week but would catch overlay/installer coupling before the doctrinal edits are wasted against the wrong runtime. Routing: default to mid-slice review unless the harness slice returns uniformly clean; the review gate criterion is whether step 5 `.toml` slice survived overlay regen without drift.
- [o:r:i] Whether the lane-06 output itself has become sovereign doctrine by virtue of being recommended. Per charter [g:r:i] "any single cross-review output, including prestige-model output, is challenge input rather than governing conclusion unless the workspace explicitly accepts the residual risk in writing or compares it against another independent lane" — this artifact is challenge input, not sovereign doctrine. Routing: operator disposition writes the comparative-inheritance artifact; if counterpart lane runs, compare.

## 8. Interventions Considered And Rejected

Register: `interventions considered and rejected ledger`. The spec requires at least `4` stronger interventions considered and rejected; eight are named here to honor the spec's generative quota and to surface the full space this lane weighed before recommending `Option F`. Each names consideration-reason, upside, regret analysis (with execution-capacity model and mitigation path considered), and reopen signal.

### IR-1 — Full `Option G` Proposal D maximal multi-wave as recommended program

- **Why considered**: Mapping-lane §72 names an intervention-ready gap that a single lane cannot fully close; mission-lane §86-90 identifies multiple structural weaknesses simultaneously (brake-exit, precondition inflation, preserve-only permanence, docs-compensating-for-harness diagnosis); Wave-2 lane-05 PSP-1 through PSP-8 route eight distinct pressures to lane-06; a maximal multi-wave would address them in parallel. Upside: highest epistemic coverage; closes all structural seams in one coordinated pass; strongest rerun-readiness posture.
- **Execution-capacity model considered**: ~10-15 week budget with parallel sublanes; operator commits to sustained orchestration; each sublane cross-vendor reviewed independently.
- **Mitigation path considered**: PSP-6 execution-capacity reopen pre-launch; per-sublane packet-freeze; independent cross-vendor review per sublane; synthesis lane integrates.
- **Regret analysis counting against**: (a) `PLAN-PROPOSALS` D weakness explicit: "easiest way to postpone action indefinitely"; (b) mission-lane T9 precondition-inflation pattern is the documented failure mode this maximal shape most closely reproduces; (c) violates AI-GUARDRAILS `small branches, minimal irreversible actions`; (d) Wave-2 lane-05 IR-4 (`sequencing locks budget against evolving capacity`); (e) operator-pressure OP-08 identifies wave-level meta-workspace accumulation at `weak / post-hoc` tier at current evidence; Option G would promote that to confirmed-primary by enacting it; (f) lane-05 comparative disposition's rejection of broad pre-Wave-1 reorganization extends to pre-rerun maximal reorganization by the same logic; (g) the six-to-eight-week harness-slice-plus-rerun budget in `Option F` is already near the charter's comfortable range; maximal widens ~2x.
- **Reopen signal**: Triggers toward Option G (T-G-1): three or more of the `Option F` switch triggers fire near-simultaneously, AND PSP-6 execution-capacity validation succeeds.

### IR-2 — Full `Option D` Proposal F as dominant program shape

- **Why considered**: Runtime-authority drift is the strongest live Wave-1 finding (`13/31` stale workers verified live on 2026-04-19, plus install-time mutation plus overlay/runtime divergence); operator-pressure demotion of three rows (OP-03, OP-04, OP-07) from primary/amplifying; mission-lane T6 (`docs compensating for harness` diagnosis correct, remedy under-applied) points at harness; five-slice F program would close multiple switch triggers at once. Upside: durable if runtime is the real blocker; converts manual overlay-mirror to machinery.
- **Execution-capacity model considered**: ~9-11 weeks multi-slice refactor under explicit stop conditions per slice; cross-vendor reread after each slice; solo-developer posture checked pre-slice.
- **Mitigation path considered**: Five-slice sequencing gated on stop conditions; revert per subslice; overlay regen scripted before slice 2 (full roster) begins; authority-vs-edit distinction preserved (no governance-file rewrite).
- **Regret analysis counting against**: (a) ST-1 discriminator (launch-truth capture showing inherited CLAUDE.md) has NOT fired; adopting full F preempts the switch-trigger model the setup earned; (b) Wave-2 lane-05 UU-5 regret: "full-F before Wave-1-informed prioritization risks choosing the loudest drift instead of the most leveraged one"; (c) lane-05 operator cross-review `small branches / minimal irreversible actions` rule directly violated; (d) AI-GUARDRAILS:103-113 solo-developer posture violated; (e) does not address brake-exit problem at program level — the recursive inward motion resolves by pivoting to a different attractor (harness refactor), not by resolving; (f) forecloses bridge-audit continuity (`revise + guarded hybrid reseed`); (g) mapping-lane Intervention #2 regret: the `13/31` stale count is symptomatic of overlay/installer coupling that a `.toml` pass does not fix. Five convergent regrets, all independent.
- **Reopen signal**: T-D-1, T-D-2, or T-D-3 fire.

### IR-3 — Pure `Option E` Proposal E execute-and-learn now without harness slice

- **Why considered**: Mission-lane T4 (execution-capacity honesty vs enacted time budget) names the charter model as "accurate but routinely violated by how budget is spent"; mission-lane T9 names precondition inflation as a pattern; `PLAN-PROPOSALS` E explicit: "generates evidence a mapping/debrief lane cannot produce directly"; Surface B prelicensing pass required E to have a concrete first slice; treating Checkpoint 7 rerun AS the first slice supplies it. Upside: exits recursive inward motion by forcing outward discharge (mission-lane T1); fastest path to real product signal.
- **Execution-capacity model considered**: ~5-7 weeks total (BE-6 + prerequisite doctrine ~1 week + 3-5 week rerun + 1 week synthesis); fits charter model at similar cost to `Option F` with different risk profile.
- **Mitigation path considered**: Explicit no-canon-mutation posture; pre-commit friction capture; brake-exit rule authored pre-launch; activation-trigger doctrine authored pre-launch.
- **Regret analysis counting against**: (a) `13/31` stale-doctrine drift verified live would confound real friction with prep-level friction in the rerun; (b) `PLAN-PROPOSALS` E weakness explicit: "noisy evidence if the launch conditions are still too weak"; (c) `PLAN-PROPOSALS` E weakness explicit: "risks conflating avoidable prep failures with product truth"; (d) preserve-only seams could silently collapse without activation-trigger doctrine ready — and doctrine authoring without a harness slice means the rerun reads doctrine-vocabulary that the runtime doesn't carry (mapping-lane U1 still unaddressed); (e) the bounded `no-canon-mutation` posture during a learning rerun is hard to enforce against discuss-phase's generative tendency; (f) `.codex/` repo-ignore means overlay-mirror discipline during rerun remains operator-carried — a rerun that introduces overlay drift would do so silently.
- **Reopen signal**: T-E-1 (BE-6 clean + operator accepts open `.toml`) or T-E-2 (operator defers doctrinal prerequisites).

### IR-4 — Pure `Option C` mapping reconciliation first

- **Why considered**: Mapping-lane §17 bottom line: "mapping is strong enough to read a fresh rerun against, but not yet strong enough to ship an intervention program against"; mapping-lane §72 intervention-ready gap names six specific additions; bridge-audit lane-03 reseed-judgment explicitly proposed `Checkpoint 5.x: Hybrid Upstream Baseline Reconciliation`. Upside: would close the mapping gap cleanly; unlocks ST-5; intervention-ready map would make any later F-slice evidence-driven.
- **Execution-capacity model considered**: ~9-11 weeks including rerun; one Codex lane + one Claude cross-vendor lane for the reconciliation; reuses Wave-1 packet discipline.
- **Mitigation path considered**: Scope the reconciliation lane narrowly to the six §72 gap items; reuse existing A1-A4 raw inventories as baseline; cross-vendor reread on the reconciliation output.
- **Regret analysis counting against**: (a) the mapping lane itself rejected this as its own Intervention #3 with the specific regret that the existing Wave-1 structure was able to absorb a bounded drift probe (BE-8) inside `Option F`; (b) `PLAN-PROPOSALS` C weakness explicit: "risks over-explaining every failure as a mapping failure"; (c) ST-5 (multi-lane reconciliation failure) has NOT fired; (d) mapping-lane §72 gap items (a) through (d) are bounded-mapping-work reachable inside `Option F` (BE-4 runtime-authority ledger, BE-8 drift probe); items (e) and (f) need either live execution pass or deeper harness probe — which `Option F` supplies via Checkpoint 7; (e) adds ~3-5 weeks to rerun schedule; (f) the A1-A4 raw inventories are already usable as-is; a reconciliation lane that produces one canonical topology runs the mapping-lane Intervention #1 regret of premature categorical freezing under operator bandwidth; (g) during the reconciliation window, the `13/31` drift persists in live runtime and would get inherited into the rerun anyway — making the reconciliation a delay tax rather than a fix.
- **Reopen signal**: T-C-1 (during `Option F` slice, runtime/docs/mapping cannot be reconciled) or T-C-2 (two or more Wave-2 lanes converge on re-classification).

### IR-5 — Direct governance-authority file rewrites (POST-FALSIFICATIONIST-REVIEW-DOCTRINE + REVIEW-TEMPLATE + REVIEW-POLICY.yaml + PROTOCOL)

- **Why considered**: Wave-2 lane-05 UU-4 considered this; the content-propagation path has consistently underreached (outcome-audit U9, R5.17d3 authority-vs-edit contestation); direct governance-file rewrite would apply to future lane specs by default. Upside: replaces the underreached content-propagation path with direct doctrine uplift.
- **Execution-capacity model considered**: Bounded doctrine editing; one operator; each governance file independently editable; no delegation; cross-vendor reread per edit.
- **Mitigation path considered**: Single commit per edit with inheritance disposition in commit message; cross-vendor reread on each edit; explicit authority-vs-edit distinction preservation in commit message rationale.
- **Regret analysis counting against**: (a) R5.17d3 preserved the `governing authority vs must-edit-now` distinction as a load-bearing resolution rule; editing the governance files directly collapses the distinction the workspace earned; (b) lane-04 Surface C explicitly held `CLAIM-TYPES.md` / `POST-FALSIFICATIONIST-REVIEW-DOCTRINE.md` rewrite because the doctrine-stressed case set has not shown operational delta — Wave-1 returns did not produce that case set; (c) the smaller UU-4-adjacent edits that are earned (BE-1 AUDIT-SPEC-TEMPLATE chain-tail rule + REVIEW-POLICY.yaml note + the brake-exit rule as a doctrine-sibling section) fit in the `still defensible narrowed` retest category without reopening the authority-vs-edit distinction. `Option F` includes precisely those edits, not the broader rewrite.
- **Reopen signal**: Post-`Option F` Checkpoint 7 rerun produces the same one-sided non-promotion default R5.16a internal did (outcome-audit U13 reproduces) despite the content-propagation path having landed named-consumer edits.

### IR-6 — Post-Checkpoint-5 `autonomous.md` + `ship.md` + `transition.md` permissive-branch bundle (promote three convergent surfaces to blocking debt-ledger writes now)

- **Why considered**: Wave-2 lane-05 UU-2 considered this; R5.17d3 named the three surfaces as convergent wider-scope signals; the convergence is the signal; leaving them as `R8.*` ownership re-exports the Checkpoint 4 converged synthesis finding; `Option F` BE-5 (`Proceed anyway` replacement) addresses the same shape on a single surface and this would widen to the three convergent surfaces. Upside: closes the broader convergence pattern at the same time as BE-5.
- **Execution-capacity model considered**: ~1 week bounded refactor (three files plus overlay); single operator; one-file edits per commit.
- **Mitigation path considered**: Carry debt-ledger vocabulary from R5.18c (phase.cjs completion_mode) into the three workflow files; keep permissive language but require a debt-ledger entry first; test on one rerun dry-run.
- **Regret analysis counting against**: (a) `R8.*` ownership was accepted at R5.17d3 adjudication explicitly, not by implicit omission; promoting the three surfaces now bypasses the adjudication rather than honoring a new evidence trigger; (b) the Wave-1 returns sharpen the case but do not supply a concrete consumer-side failure where `R8.*` deferral produced avoidable harm; (c) `Option F` BE-5 `Proceed anyway` replacement is a single-case discriminator that covers the same shape with lower scope cost; (d) the `R8.*` surfaces are the right lane-06 rerun-readiness question (PSP-2 minimum rerun-readiness set recomputation routes them) rather than a pre-rerun bundle; (e) expanding the bundle at slice time would re-create the precondition inflation pattern T9 warned about.
- **Reopen signal**: BE-6 cross-vendor reread finds one of the three surfaces governing by silence in the executed R5.18 bundle; or the Checkpoint 7 learning rerun shows one of the permissive branches firing over a lone-high-signal concern (operator-pressure ST-1 trigger).

### IR-7 — Full `.codex/agents/*.toml` worker roster unification (all 31 workers, overlay + live) as `Option F` step 5

- **Why considered**: Mapping-lane live probe `13/31` on 2026-04-19 is the strongest concrete evidence against `mapping is sufficient`; closing the full roster in one coordinated pass removes the defect entirely; restores Named-agent authority to high confidence on the mapping-confidence register.
- **Execution-capacity model considered**: Multi-week bounded repo-local refactor; sliced by worker family (planner/executor/verifier first, then researcher/reviewer/debugger, then wrappers); cross-vendor verify per slice; overlay regen scripted.
- **Mitigation path considered**: Four-family sequencing; revert per-file on regression; cross-vendor reread per family.
- **Regret analysis counting against**: (a) Wave-2 lane-05 UU-1 regret: full roster unification widens current scope before Wave-1 mapping-adequacy findings have been consumed by lane-06 — exactly what the charter warns against; (b) the stale `13/31` count is symptomatic of overlay/installer coupling that a `.toml` pass without overlay regen scripting does not fix; the narrow four-worker slice in `Option F` step 5 is the discriminator for this; (c) the full sweep competes for budget with `Option F`'s doctrinal prerequisites (BE-1, brake-exit rule, activation-trigger doctrine), which are each independently warranted; (d) doing full-roster before BE-6 ratifies executed R5.18 leaves the closure path unfinished; (e) none of these regrets is about `too large`; they are about `not now at this breadth`.
- **Reopen signal**: Mapping-lane switch trigger ST-1 fires (launch-truth capture shows inherited CLAUDE.md); OR `Option F` step 5 narrow slice regresses after install; OR Wave-2 lane-05 BE-6 reread names runtime-authoritative drift as the blocking surface.

### IR-8 — Launch-truth automation as Codex hook (promote `capture_launch_truth.py` to hook-triggered spawn capture) inside `Option F`

- **Why considered**: Operator-pressure OP-03 is the one `primary` pressure on the register; promoting `capture_launch_truth.py` to hook would demote it to `amplifying`; closes the one primary pressure; lowers mapping-lane U10 severity.
- **Execution-capacity model considered**: Bounded hook authoring; one operator; the repo-local GSD hook posture is already pilot-ready; `.codex/hooks.json` exists.
- **Mitigation path considered**: Narrow hook to `PreToolUse` on doctrine-sensitive spawn commands only; keep existing `capture_launch_truth.py` helper as ground truth; add `verification-and-honesty-rule` preserving note that hook output is evidence, not proof.
- **Regret analysis counting against**: (a) `AI-GUARDRAILS.md:87-100` explicitly forbids treating requested settings as proof, and operator-pressure RJ-03 identified hook-based capture as a quiet violation risk; (b) Codex hooks are experimental, Bash-only for tool interception, fail-open for many outputs, and concurrent across files; hook cannot replace operator on `--since` boundary without weakening honesty rule; (c) switching before a single captured drift incident on operator-carried protocol uses experiment budget on a move that removes current protocol's friction without first proving the friction was producing drift; (d) `WORKFLOW.md:160-166` explicitly: hooks should be deterministic, removable, and not a substitute for explicit review boundaries; (e) `Option F` BE-6 cross-vendor lane requires launch-truth capture and the protocol suffices at that cadence.
- **Reopen signal**: Any `Option F` lane where effective settings diverged from requested and the divergence was not noticed before review closure (operator-pressure ST-4).

### Count and stance summary

- Rejected interventions named with full regret analysis and reopen signals: eight (IR-1 through IR-8).
- Quota requirement: `4` minimum. Met with margin.
- Stance check: none rejected on `too large` or `not yet` alone; each rejection cites at least three independent regret reasons including at least one Wave-1 or Wave-2 evidence citation plus at least one governance-posture citation (AI-GUARDRAILS or WORKFLOW or AUDIT-CHARTER).
- Of the eight rejected, three (IR-2 full F, IR-3 pure E, IR-4 pure C) are the loudest stronger moves the lane was required to distinguish from the recommended hybrid; three (IR-5 governance-file rewrite, IR-6 post-Checkpoint-5 permissive-branch bundle, IR-7 full `.toml` roster) are the broader versions of `Option F` slice edits; two (IR-1 maximal multi-wave, IR-8 launch-truth hook) are orthogonal axes where a bolder move was considered and rejected.

## Self-Check Against Spec Failure Conditions

- **Fails if it defaults to the incumbent option without anti-regret comparison.** Avoided. `Option A` B-extended minimal continuation is explicitly rejected in §4 with named anti-regret reasoning: it preserves the brake-exit attractor, inherits `13/31` stale-doctrine into rerun, leaves Wave-2 lane-05 retest promotions (R3, R6, R11) on the table, and fails the charter's `failure condition` test by enacting the tame-closure pattern.
- **Fails if it recommends a stronger option without a concrete first slice and execution-capacity model.** Avoided. `Option F` specifies a ten-step first-slice sequence in §4, each step with explicit rollback path; §5 enumerates nine prerequisites each with source evidence; the execution-capacity model is stated as `~7-9 weeks total under the charter's multi-week budget`, with per-slice time breakdown.
- **Fails if it treats workspace/hygiene prerequisites as irrelevant when the evidence says they shape execution honesty.** Avoided. §5 classifies ten surfaces as program-carrying (runtime authority, workflow carriers, doctrine authorities, installer/overlay, launch history) and separately names five as background hygiene with explicit reasoning. BE-6 cross-vendor reread, overlay-mirror discipline, checkpoint commit per slice, launch-truth capture, and `.toml` slice with overlay regen discipline are all explicitly classified as program-carrying prerequisites, not hygiene.
- **Fails if it cannot name at least 4 serious program alternatives and 4 stronger interventions it considered and rejected.** Avoided. §3 compares seven serious program alternatives (`Option A` through `Option G`), each at the same level of granularity (first slice / prerequisites / execution-capacity model / upside / regret or reversal cost / evidence against). §8 names eight rejected stronger interventions (IR-1 through IR-8) each with execution-capacity model, mitigation path considered, regret analysis citing at least three independent reasons, and reopen signal.
- **Challenge stage requirement**: the lane trended toward a hybrid that is adjacent to both `mostly keep the current shape` and `broad machinery rewrite now`. Stage 2 was opened before finalizing. The lane-05 Opus and GPT-5.4 main-wave contract cross-reviews, AI-GUARDRAILS, WORKSPACE-AUTHORITY-AND-ORGANIZATION, and the two raw Wave-2 lane-05 outputs were read as challenge material. The challenge stage materially demoted `Option G` (maximal multi-wave) from candidate-recommended to rejected with explicit trigger, and demoted `Option D` (full F) from candidate-recommended to rejected with explicit trigger; it also tightened `Option F`'s execution-capacity claims by forcing per-slice rollback cost and authority-vs-edit distinction preservation.

## Sources

- `.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/AUDIT-CHARTER.md`
- `.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/WORKSPACE-AUTHORITY-AND-ORGANIZATION.md`
- `.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/INDEX.md`
- `.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/CURRENT-STATE.md`
- `.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/QUESTION-SET.md`
- `.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/EVIDENCE-ARCHITECTURE.md`
- `.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/PLAN-PROPOSALS.md`
- `.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/STATUS.md`
- `.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/MAIN-WAVE-LAUNCH-CONTRACT-AND-PACKET.md`
- `.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/wave-1/dispositions/01-wave-1-opus-r1-comparative-disposition.md`
- `.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/wave-1/outputs/01-mission-reconstruction-opus47-max-r1.md`
- `.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/wave-1/outputs/02-outcome-and-underreach-audit-opus47-max-r1.md`
- `.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/wave-1/outputs/03-mapping-adequacy-and-comparative-mapping-opus47-max-r1.md`
- `.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/wave-1/outputs/04-operator-orchestration-pressure-opus47-max-r1.md`
- `.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/lane-04-comparative-disposition.md`
- `.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/lane-04-surface-b-prelicensing-judgeability-pass.md`
- `.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/lane-04-surface-b-graded-underreach-trial.md`
- `.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/lane-04-local-proposal-and-stress-test-disposition.md`
- `.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/lane-05-comparative-disposition.md`
- `.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/lane-05-opus47-max-main-wave-contract-cross-review.md`
- `.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/lane-05-gpt54-xhigh-main-wave-contract-cross-review.md`
- `.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/wave-2/README.md`
- `.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/wave-2/dispositions/05-wave-2-lane05-comparative-disposition.md`
- `.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/wave-2/outputs/05-suppressed-opportunity-and-non-intervention-opus47-max-r1.md`
- `.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/wave-2/outputs/05-suppressed-opportunity-and-non-intervention-gpt54-xhigh-r1.md`
- `.planning/readiness/phase-01-rerun/PLAN.md`
- `.planning/readiness/phase-01-rerun/STATUS.md`
- `.planning/readiness/phase-01-rerun/POST-FALSIFICATIONIST-REVIEW-DOCTRINE.md`
- `.planning/readiness/phase-01-rerun/AUDIT-SPEC-TEMPLATE.md`
- `.planning/audits/2026-04-17-gsd-upstream-docs-readiness-comparative-audit/SYNTHESIS.md`
- `.planning/audits/2026-04-17-gsd-upstream-docs-readiness-comparative-audit/lane-03-reseed-judgment.md`
- `AI-GUARDRAILS.md`
- `WORKFLOW.md`
- `.planning/AGENTS.md`
- `scripts/setup-portable-gsd.sh`
- `tooling/portable-gsd/overlay/config.toml`
- `.codex/config.toml`
- `.codex/agents/*.toml` (live probe 2026-04-19: `13/31` reference `CLAUDE.md`)
- `.codex/get-shit-done/workflows/plan-phase.md` (live probe 2026-04-19: already carries `planner-reviews.md` in required_reading)
- live probe 2026-04-19: `grep -rn "post-verificationist|post-falsificationist|gap-exposure|completeness-challenge|anti-regret" .codex/` returns zero matches
