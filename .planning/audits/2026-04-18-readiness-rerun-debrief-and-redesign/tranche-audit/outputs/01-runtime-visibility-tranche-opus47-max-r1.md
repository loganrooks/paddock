Date: 2026-04-20
Reviewer: Claude Opus 4.7 (1M context) — r1
Frozen basis: `cf402e3`
Scope: runtime-visibility tranche (proposals 07–10, companion artifacts, tool, doc carry)

# Runtime Visibility Tranche Cross-Vendor Review

## Load-Bearing Gains

1. **Semantic separation of updater-boundary truth from final-runtime truth was preserved instead of collapsed.** [d:r:i] The earlier coherence framing (03-manifest-install-coherence-proposal) had been pressing toward one artifact doing both jobs. The disposition in 08 rejected that collapse on evidence: the live `detect-custom-files` probe returns fifty custom files — agent `.toml`s, repo-local hooks, `gsd-rigorous-research` skill files — which only makes sense if the manifest is an upstream/custom boundary, not a complete runtime inventory. [e:c+i] Sources: [08-manifest-semantic-contract-disposition.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/intervention-proposals/08-manifest-semantic-contract-disposition.md:18), [.codex/get-shit-done/bin/gsd-tools.cjs](/home/rookslog/workspace/projects/prix-guesser/.codex/get-shit-done/bin/gsd-tools.cjs:1190). Had this collapsed, the existing updater protection for custom carry would have been the casualty.

2. **The five-class scheme replaces a binary `matches / does not match` frame with discriminating vocabulary.** [d:r:i] The pilot names `intentional materialized carry`, `repo-local config carry`, `selective overlay boundary`, `unknown live drift`, and `obsolete live residue`. The smoke-test numbers (23 / 1 / 161 / 0 under the four implemented classes) demonstrate the leverage: most live-only differences are overlay-scope issues, not mystery drift. [e:c+i] Sources: [07-live-vs-overlay-drift-register-pilot.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/intervention-proposals/07-live-vs-overlay-drift-register-pilot.md:21), [10-final-runtime-visibility-first-pass-disposition.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/intervention-proposals/10-final-runtime-visibility-first-pass-disposition.md:25). The real carry is not the count; it is the classification — earlier intervention work could have read all 161 live-only surfaces as problems.

3. **The paired-pattern choice (on-demand command + optional `--output`) threads between two degenerate shapes.** [d:r:i] An artifact-only surface goes stale by default and needs out-of-band refresh discipline to stay honest. A verifier-only surface produces no durable record for audit inheritance. The paired shape defaults to freshness and offers persistence only where an audit lane earns it. [e:c+i] Sources: [09-final-runtime-visibility-proposal.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/intervention-proposals/09-final-runtime-visibility-proposal.md:33), [10-final-runtime-visibility-first-pass-disposition.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/intervention-proposals/10-final-runtime-visibility-first-pass-disposition.md:8).

4. **Materialization-aware comparison is encoded in code, not only in proof notes.** [d:r:i] `normalize_overlay_text` promotes `__PROJECT_ROOT__` and `__COMPACT_PROMPT_FILE__` to first-class template substitutions; `classify` has a family-specific rule for `agent_toml` that strips `model_reasoning_effort = ` lines before equality comparison. That lifts two previously narrative explanations into a reproducible classifier. [e:c+i] Sources: [tooling/codex/runtime_visibility.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/runtime_visibility.py:74), [tooling/codex/runtime_visibility.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/runtime_visibility.py:117).

5. **Doc-layer carry was propagated minimally and at the right seams.** [d:r:i] `.planning/AGENTS.md:64–66` names the tool with the correct framing ("prefer it over vague `overlay/live drift` language"), and `tooling/codex/README.md:67–69` gives the tool a one-paragraph situating description alongside the other probe utilities. Instruction bloat did not follow the implementation. [e:c+i] Sources: [.planning/AGENTS.md](/home/rookslog/workspace/projects/prix-guesser/.planning/AGENTS.md:64), [tooling/codex/README.md](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/README.md:67).

6. **Ceremony-risk language is named explicitly in every tranche artifact.** [d:r:i] 08 warns that the disposition fails if it is paraphrased into "leave the manifest alone." 09 warns the proposal fails if it becomes another static report nothing uses, or a second opaque manifest. This is not a mitigation, but it names the failure mode in a place later reviewers will hit. [e:c+i] Sources: [08-manifest-semantic-contract-disposition.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/intervention-proposals/08-manifest-semantic-contract-disposition.md:66), [09-final-runtime-visibility-proposal.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/intervention-proposals/09-final-runtime-visibility-proposal.md:85).

## Gaps And Weak Spots

1. **`obsolete live residue` is named in the typology but absent from the classifier.** [d:r:i] The pilot defines it as the single discriminating class cleanup work needs. 10 acknowledges the omission as a constraint. Nothing in the tranche specifies the signal pattern — which is constructible from existing repo state (`live_exists and not overlay_exists and not in manifest and not in backup-meta and not an install-script mutation target`). Its absence collapses genuine residue into the 161-entry `selective overlay boundary` bucket and forces cleanup back to manual review. [e:c+i] Sources: [07-live-vs-overlay-drift-register-pilot.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/intervention-proposals/07-live-vs-overlay-drift-register-pilot.md:30), [10-final-runtime-visibility-first-pass-disposition.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/intervention-proposals/10-final-runtime-visibility-first-pass-disposition.md:34).

2. **The `selective overlay boundary` class is unsplit at first pass.** [d:r:i] 161 of 185 entries live there. That is faithful to the pilot's logic, but the class currently conflates at least four sub-populations: upstream-shipped files never intended for overlay, installer-produced `bin/lib/*` artifacts, repo-added workflow/reference surfaces, and files that were once tracked and then silently dropped. Without sub-classification, the tool's dominant signal at this moment is "overlay coverage is narrow" — a fact already known to the audit. [e:c+i] Source: [10-final-runtime-visibility-first-pass-disposition.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/intervention-proposals/10-final-runtime-visibility-first-pass-disposition.md:28).

3. **`repo-local config carry` is only detectable for `config` and `agent_toml` families.** [d:r:i] `classify` has no analogous rule for `workflow`, `reference`, or `bin_lib`. A legitimate repo-local patch to a workflow file — exactly the pattern checkpoint-5 already surfaced as real (`planner review-consumption and debt-aware completion semantics drifted into live workflow/reference surfaces beyond tracked overlay`) — will be labeled `unknown live drift` rather than `repo-local config carry`. The tool under-represents a class it claims to support. [e:c+i] Sources: [tooling/codex/runtime_visibility.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/runtime_visibility.py:111), [HARNESS-INTERVENTION-ONBOARDING.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/HARNESS-INTERVENTION-ONBOARDING.md:45).

4. **No persistence discipline.** [d:r:i] `--output` exists; nothing names where snapshots land, whether they are ignored, which audit lanes pin a snapshot, or how to diff against a prior one. The proposal's success signal depends on repeated use; a one-shot utility without a convention will either produce no durable record or will proliferate inconsistently. [e:c+i] Sources: [09-final-runtime-visibility-proposal.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/intervention-proposals/09-final-runtime-visibility-proposal.md:78), [tooling/codex/runtime_visibility.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/runtime_visibility.py:228).

5. **No test artifact for the classifier.** [d:r:i] The 185 number is a one-time local observation. There is no fixture tree exercising each classification branch, no regression harness catching an upstream rename that would silently break `normalize_overlay_text` or the stripped-reasoning rule. For a tool expected to carry intervention planning across upgrade cycles, this is a carry liability that the ceremony-risk language in 09 explicitly warned against. [e:r:i] Sources: [tooling/codex/runtime_visibility.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/runtime_visibility.py:1) (no adjacent tests).

6. **The authority register names `.planning/config.json` as a live runtime input; the visibility surface does not see it.** [d:r:i] This is a deliberate bounded choice ("do not widen the first pass into a total `.codex/` inventory"), but it leaves an explicit authority-register entry without a visibility analog. A reader of the register who then runs the tool will get no feedback on a surface the register just named as first-rank runtime input. [e:c+i] Sources: [RUNTIME-MATERIALIZATION-AND-AUTHORITY.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/RUNTIME-MATERIALIZATION-AND-AUTHORITY.md:46), [tooling/codex/runtime_visibility.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/runtime_visibility.py:28).

7. **The tool is not tied to any forcing function.** [d:r:i] The ceremony-risk clause in 09 was a warning against orphan utilities. `.planning/AGENTS.md:64–66` describes when to prefer it, but no workflow step, handoff template, post-spawn disposition, or intervention-planning checkpoint requires its output. A tool without a hook point serves whoever remembers it exists. [e:c+i] Sources: [09-final-runtime-visibility-proposal.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/intervention-proposals/09-final-runtime-visibility-proposal.md:86), [AGENTS.md](/home/rookslog/workspace/projects/prix-guesser/AGENTS.md:125).

8. **Coordination gap between `runtime_visibility.py` and `capture_launch_truth.py`.** [d:r:i] Both are repo-local visibility tools for otherwise-invisible runtime state. `AGENTS.md:131–135` makes launch-truth capture load-bearing for doctrine-sensitive spawned work. Nothing in this tranche names the relationship: when does a doctrine-sensitive review want both, when does one subsume the other, when is one misused as a substitute? This is a coordination deferral, not an accident. [e:c+i] Sources: [AGENTS.md](/home/rookslog/workspace/projects/prix-guesser/AGENTS.md:131), [tooling/codex/README.md](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/README.md:63).

9. **Normalized SHA is checkout-local.** [d:r:i] `normalize_overlay_text` substitutes `__PROJECT_ROOT__` with the resolved absolute repo root, so the same overlay computed on another checkout produces a different `normalized_overlay_sha256`. For the current intervention-planning use this is fine; the report does not currently say so. A later consumer comparing two snapshots across machines will silently mis-match. [e:c+i] Sources: [tooling/codex/runtime_visibility.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/runtime_visibility.py:74), [tooling/codex/runtime_visibility.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/runtime_visibility.py:194).

10. **The globs assume flat family directories.** [d:r:i] `collect_rel_paths(root, rel_glob)` with e.g. `get-shit-done/workflows/*` only matches direct children. Any future workflow/reference subdirectory (compact-prompt subsets, nested templates) is silently invisible. The current runtime does not exercise depth, but the scope is implicitly flat — a failure mode that will not raise an error, only shrink coverage. [e:c+i] Sources: [tooling/codex/runtime_visibility.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/runtime_visibility.py:28), [tooling/codex/runtime_visibility.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/runtime_visibility.py:134).

11. **The `rel_path` parameter in `classify` is unused.** [d:r:i] Minor, but it signals contemplated path-aware rules that did not land. Path-aware classification (e.g., `bin/lib/*.js` recognized as installer-produced, `workflows/compact-prompts/*` recognized as parametric template) would lift a portion of the 161 out of `selective overlay boundary` into more discriminating classes. [e:c+i] Source: [tooling/codex/runtime_visibility.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/runtime_visibility.py:83).

## Runtime-Visibility Tool Judgment

**Shape is correct for the purpose.** [d:r:i] The paired pattern serves both intervention-planning one-shot use and durable audit persistence without forcing either. SHA256 of raw, normalized, and live texts preserves downstream cross-checking without bloating the report. The four-class classifier correctly prioritizes the pilot's core insight — live-only surfaces are usually overlay-scope issues, not mystery drift. No stronger narrow shape is visible from the packet.

**Three narrow design weaknesses have stronger alternatives available.**

- **Missing `obsolete live residue` detection.** [d:r:i] Stronger alternative: a fifth classification branch keyed on `live_exists and not overlay_exists and not in_manifest(rel_path) and not in_backup_meta(rel_path) and not in_install_mutation_targets(rel_path)`. All four input sources are already in the repo: `.codex/gsd-file-manifest.json` (files dict), `.codex/gsd-local-patches/backup-meta.json` (carried-subset), `scripts/setup-portable-gsd.sh` (mutation targets around lines 46–86), and the existing live tree. Implementing this splits the 161-entry `selective overlay boundary` bucket into two — genuinely selective vs. actual residue — which is the precise discrimination later cleanup work needs. Why stronger: it turns the largest, most ambiguous bucket into two discriminable populations without reopening any semantic contract.

- **No `repo-local config carry` rule for `workflow`, `reference`, or `bin_lib`.** [d:r:i] Stronger alternative: extend `classify` with a narrow path-targeted rule set (specific filenames known to carry repo-local edits, paired with a normalized line-set diff that recognizes localized patches rather than file equality). Narrower than semantic diffing, stronger than the current "anything non-equal is unknown" collapse. Why stronger: it prevents `unknown live drift` from absorbing false positives the moment a workflow accumulates a deliberate repo-local patch.

- **No test harness for the classifier.** [d:r:i] Stronger alternative: a `tooling/codex/tests/runtime_visibility/` fixture tree exercising each classification branch plus the two normalization rules. Small bounded work. Why stronger: it is the difference between a tool that reports one number on the day it lands and a tool whose behavior future upgrades can trust. Given the ceremony-risk framing in 09, a silently drifting classifier is itself a carry liability.

**Smaller issues.** [d:r:i]

- `--pretty` help text is not quite what the code does (line 225 makes output pretty when `--pretty` OR no `--output`, not "default true when writing to stdout"). Documentation-level.
- Normalized SHA path-locality needs a metadata line in the report payload.
- Glob shape should either be documented as "flat family directories only" or widened to `**/*` where depth matters.
- `rel_path` should either be removed from the `classify` signature or put to work.

## AGENTS.md -> CLAUDE.md Translation Judgment

**Starting framing.** [d:r:i] This repo's runtime is Codex; `AGENTS.md` is Codex's agent-facing instruction surface. A Claude CLI run does not read `AGENTS.md` automatically — Claude Code reads project `CLAUDE.md` and user/global instructions. The `tooling/codex/` directory already establishes precedent for cross-vendor Claude lanes (see the headless probe notes at [tooling/codex/README.md](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/README.md:6)). So the question is not whether Claude lanes exist in this repo — they do — but what shape a CLAUDE.md equivalent should take so that it carries repo-local authority without duplicating the policy layer.

**Three options, clear tradeoffs.**

- **Direct mirroring — reject.** [d:r:i] Verbatim copy loses earned distinctions. `AGENTS.md:110–113` encodes Codex-native model policy (`gpt-5.4`, `xhigh`, `high`). That policy does not apply under Claude — there is no Codex `gpt-5.4` lane to select in a Claude CLI run. Verbatim mirroring also propagates `~/.codex/state_5.sqlite` launch-truth rules (`AGENTS.md:131–135`) into a file read by a model with no such artifact. The mirror would mislead by omission or by mismatch.

- **Selective translation — partial support.** [d:r:i] Repo-local `CLAUDE.md` carrying only the vendor-neutral policy layer: product posture (§2), runtime canon rules for `.planning/` (§3 minus `.codex/` references), quality bar (§4 — the strongest cross-vendor payload), claim-type grammar pointers to `.planning/CLAIM-TYPES.md`, the headless-probe reciprocal (repo-local path over `/tmp`, explicit permissions), and the runtime-materialization awareness that `runtime_visibility.py` is the canonical visibility surface for intervention-planning work here. Deliberate omissions: Codex model policy, Codex sqlite launch-truth rules, `.codex/get-shit-done` as the Claude runtime, the Codex-specific `spawn_agent` authorization doctrine.

- **Translation plus local wrapper/launcher discipline — strongest available shape.** [d:r:i] Same selective translation, plus a small wrapper: the `CLAUDE.md` carries only the translation delta (what differs under Claude) and points at `AGENTS.md` as the master doctrine for non-vendor-specific rules. The stable policy layer stays in one file; Claude-specific adjustments live in the wrapper. This is stronger than flat selective translation because it prevents the quality-bar text and claim-type grammar from forking into two files that paraphrase apart under later edits. The `tooling/codex/README.md` probe discipline is the working precedent: narrow, Claude-specific, coexists with `AGENTS.md` rather than duplicating it.

**Risks of naive mirroring, ordered by severity.**

1. **Doctrine fork.** [d:r:i] Two files with ~90% overlap drift. The quality bar is where this bites hardest — careful language like "load-bearing gain" vs. "adequate" paraphrases badly under later edits, and nothing keeps two copies aligned except discipline.
2. **Vendor-specific fields leaking cross-vendor.** [d:r:i] `gpt-5.4` and Codex sqlite paths in a CLAUDE.md are doctrinal noise at minimum; they can mislead a Claude run that tries to follow the rules literally.
3. **Launch-truth divergence.** [d:r:i] Codex has `state_5.sqlite`; Claude has no equivalent. A mirror naming Codex launch-truth discipline as universal will either be silently ignored (weakening the rule) or will trigger fabricated equivalents.
4. **Agent-spawn authorization mistranslation.** [d:r:i] Claude Code's subagent model is Task-tool + `subagent_type`, not Codex `spawn_agent`. Copying the authorization clause without translation may over-authorize or fail to authorize Claude's Task subagents cleanly.

**Should this land now?** [d:r:i] The runtime-visibility tranche has no Claude-specific payload and no forcing function. A speculative CLAUDE.md companion is itself ceremony. The stronger move is to wait until a specific Claude lane needs to carry repo-local authority — a scheduled cross-vendor review, a headless Claude audit, a Claude probe required to follow repo-local policy — and then translate bounded by that lane's actual need, in the wrapper shape. The precedent for writing "what not to do under Claude" already exists in the probe README and is the right pattern to extend when the time comes.

## Recommended Next Moves

### Do Now

1. **Add `obsolete live residue` as the fifth classification branch.** [d:r:i] Consult `.codex/gsd-file-manifest.json` and `.codex/gsd-local-patches/backup-meta.json` for live-only files; subtract known install-script mutation targets. This splits the 161-entry `selective overlay boundary` bucket along the exact seam later cleanup work needs. Data is already in the repo; this is design completion, not new research.

2. **Add a minimal test harness under `tooling/codex/tests/runtime_visibility/`.** [d:r:i] Fixtures for each classification branch plus the two normalization rules. Small, bounded, and the main lever against silent classifier regression across upstream upgrades.

3. **Add one metadata line to the report noting `normalized_overlay_sha256` is checkout-local.** [d:r:i] Pre-empts cross-machine snapshot-diff mis-matches that would otherwise only surface later as a puzzle.

### Do Next

4. **Extend `repo-local config carry` detection to `workflow`, `reference`, and `bin_lib` families.** [d:r:i] Narrow path-targeted rule set plus a normalized line-set diff for localized patches. Prevents `unknown live drift` from absorbing legitimate repo-local carry the moment a workflow accumulates a deliberate patch (checkpoint-5 already showed this pattern exists).

5. **Decide the snapshot-persistence policy.** [d:r:i] Where `--output` artifacts land, whether they are tracked or ignored, which audit lanes pin a snapshot, and whether diff-against-prior-snapshot should be a tool mode. One short note in `.planning/AGENTS.md` or `tooling/codex/README.md` is enough; the current ambiguity is the weak spot.

6. **Tie `runtime_visibility.py` to at least one forcing function.** [d:r:i] Candidates: the post-spawn disposition step in `AGENTS.md:125–132`, or the intervention-planning handoff template in the audit workspace. A tool without a hook point serves whoever remembers it; a tool tied to a required step survives ceremony risk.

7. **Name the `runtime_visibility.py` / `capture_launch_truth.py` relationship in one short note.** [d:r:i] When doctrine-sensitive work wants both, when one suffices, when one is misused as a substitute. Coordination, not new surface area.

### Hold

8. **Widening tool scope to `.planning/config.json` and other runtime inputs.** [d:r:i] The authority register names it, but bringing it in now dilutes the tranche's core insight about `.codex/` materialization. Land after `obsolete residue` is cleanly separated.

9. **Full `.codex/` exhaustiveness.** [d:r:i] The proposal explicitly bounded away from this; the disposition honored it; the current output does not pressure a reversal.

10. **`AGENTS.md -> CLAUDE.md` translation layer.** [d:r:i] No Claude lane is currently carrying repo-local authority in a way that forces this. When one does, translate bounded by that lane's need, in the wrapper shape described above — not as a flat mirror companion to this tranche.

11. **Reopening `gsd-file-manifest.json` semantics.** [d:r:i] 08 settled this; the tool respected it; any later reopening should come with a fresh intervention proposal, not ad hoc drift.
