# Wave-1 Lane Output: Mapping Adequacy And Comparative Mapping

- lane: `mapping-adequacy-and-comparative-mapping`
- spec: [../specs/03-mapping-adequacy-and-comparative-mapping-spec.md](../specs/03-mapping-adequacy-and-comparative-mapping-spec.md)
- packet: [../packets/03-mapping-adequacy-and-comparative-mapping-packet.md](../packets/03-mapping-adequacy-and-comparative-mapping-packet.md)
- model: `claude-opus-4-7` (1M context)
- date: 2026-04-19
- status: draft r1

## Overall Mapping Judgment

- [d:r:i] The readiness-era map was `directionally adequate and structurally under-mapped`. It correctly refused the two naive audit units (`skills as primary` and `GSD-as-one-monolith`), earned a four-lane envelope with mandatory seam checks, and named the right reversal-sensitive boundaries before runtime evidence forced them open. It also left three live surfaces under-represented that later work then confirmed are load-bearing: the runtime-authoritative `.toml` worker instruction plane, the side-entrance/router control plane, and the upstream-overlay-installer-runtime materialization chain.
- [d:r:i] The bridge audit's `revise + guarded hybrid reseed` verdict is partially under-carried. Its lane-02 crosswalk used pinned upstream `v1.36.0` docs as a corroboration baseline rather than a drift probe, explicitly deferred docs-vs-runtime to a `possible later lane`, and lane-03 treated that deferral as non-blocking. No workspace lane since has closed that runtime-drift probe.
- [d:r:i] The later docs-refresh corpus (lanes 07/12/14/15) produced a genuinely stronger upstream-docs tree with mechanical drift guards, but that work lives in the **upstream GSD project** rather than in this repo's materialized `.codex/` runtime. The repo has no repo-root `docs/` tree and is still running a patched overlay of `1.36.0`. Uplifted upstream docs do not automatically update the live runtime; they would have to be re-materialized through `setup-portable-gsd.sh` plus the tracked overlay, and some of the runtime-authoritative files (e.g. `.codex/agents/*.toml`) would still carry repo-local legacy content after reinstall unless the overlay itself is refreshed.
- [d:r:i] Live verification performed during this lane (not a memory recall): `grep -l "CLAUDE.md" .codex/agents/*.toml | wc -l` returns `13` out of `31` registered `.toml` agent files on `2026-04-19`, i.e. the stale-doctrine drift Checkpoint 4 named at `12/24` has not narrowed; the registered roster has grown while the patched subset has not. Sources: live bash probe; cross-referenced against [checkpoint-4-codex-load-bearing-surfaces-and-seams.md:44](/home/rookslog/workspace/projects/prix-guesser/.planning/readiness/phase-01-rerun/AUDITS/checkpoint-4-codex-load-bearing-surfaces-and-seams.md). This is the single strongest piece of evidence against `mapping is sufficient`: the stalest half of the operative worker surface is stale in a way the map already named but neither bridge nor docs refresh has closed.
- [d:r:i] Adequacy and comparison are kept distinct in this artifact because they answer different questions. Adequacy asks `given what the readiness map was trying to do, did it do it?`; comparison asks `how does that map line up against the bridge result, the docs corpus, runtime, and the intervention-ready ideal, and what is each mismatch actually a defect of?`. Blurring them would let good comparative survival (`readiness mostly matched upstream docs`) launder structural under-mapping (`readiness under-inventoried the entrance plane and treated materialization as one step`).
- [d:r:i] Bottom line: the mapping is strong enough to read a fresh rerun against, but not yet strong enough to ship an intervention program against. It deserves targeted mapping-adjacent follow-through (either inside `Proposal B-extended` or under a bounded switch to `Proposal C` / `Proposal F`), not an omnibus remap and not a mapping-declared-done pass-through.

## Mapping Adequacy

This subsection judges the readiness-era map on its own terms: what it was trying to do, what it actually produced, and what it left under-mapped at the load-bearing points.

### What The Map Was Actually Trying To Do

- [e:c+i] The map's declared job was scoping and justification, not redesign: `Checkpoint 3 exists so Checkpoint 4 gets a defensible unit of analysis and later auditors can onboard without relying on session memory`. It was also explicitly not a skill-count audit and not a stock-GSD product review. Sources: [checkpoint-3-workflow-harness-scope-audit.md:6](/home/rookslog/workspace/projects/prix-guesser/.planning/readiness/phase-01-rerun/AUDITS/checkpoint-3-workflow-harness-scope-audit.md); [checkpoint-3-gsd-surface-map.md:6](/home/rookslog/workspace/projects/prix-guesser/.planning/readiness/phase-01-rerun/AUDITS/checkpoint-3-gsd-surface-map.md).
- [e:c:i] The earned envelope was `one Codex lane + three GSD sublanes + mandatory seam checks` rather than any flatter unit. The five mandatory seams were `future-awareness/canonical-ref continuity`, `AGENTS/governance reach into operative workers`, `named-agent authority and reasoning-policy truth`, `continuity under compaction or resume`, and `execution-completion plus verification/UAT closure`, with `branch/worktree materialization` traced through that last seam. Sources: [checkpoint-3-workflow-harness-scope-audit.md:54-59](/home/rookslog/workspace/projects/prix-guesser/.planning/readiness/phase-01-rerun/AUDITS/checkpoint-3-workflow-harness-scope-audit.md).

### What It Did Right

- [e:c+i] It refused the tempting-but-shallow units. Skills-as-primary and GSD-as-one-monolith were both rejected on direct evidence; skills were shown to be adapter wrappers that delegate to workflow files. Sources: [checkpoint-3-gsd-surface-map.md:13-14](/home/rookslog/workspace/projects/prix-guesser/.planning/readiness/phase-01-rerun/AUDITS/checkpoint-3-gsd-surface-map.md); [checkpoint-3-gsd-surface-map.md:34-39](/home/rookslog/workspace/projects/prix-guesser/.planning/readiness/phase-01-rerun/AUDITS/checkpoint-3-gsd-surface-map.md).
- [e:c+i] It named the reversal-sensitive boundaries that later turned out to be real runtime defects. Specifically the `.md` vs `.toml` agent-authority split, the model/reasoning-policy truth surface, and the execution-completion/verification-closure boundary were flagged as reversal-sensitive before Checkpoint 4 produced concrete live evidence on the first of them. Sources: [checkpoint-3-workflow-harness-scope-audit.md:42-44](/home/rookslog/workspace/projects/prix-guesser/.planning/readiness/phase-01-rerun/AUDITS/checkpoint-3-workflow-harness-scope-audit.md); [checkpoint-3-gsd-surface-map.md:70](/home/rookslog/workspace/projects/prix-guesser/.planning/readiness/phase-01-rerun/AUDITS/checkpoint-3-gsd-surface-map.md).
- [e:c+i] It kept preserve-only seams explicit rather than flattening them into generic `open`. `future_awareness`, `canonical_refs`, `future_preservation`, and explicit non-decisions were named as preserve-only seams traced through the workflow chain rather than assumed preserved. Sources: [checkpoint-3-workflow-harness-scope-audit.md:36](/home/rookslog/workspace/projects/prix-guesser/.planning/readiness/phase-01-rerun/AUDITS/checkpoint-3-workflow-harness-scope-audit.md); [checkpoint-3-gsd-surface-map.md:65](/home/rookslog/workspace/projects/prix-guesser/.planning/readiness/phase-01-rerun/AUDITS/checkpoint-3-gsd-surface-map.md).
- [e:c+i] Checkpoint 4 then did the hard work of turning named reversal-sensitive boundaries into live findings. It produced the `12/24 .toml agents still reference CLAUDE.md` diagnosis, the `stale ~/.codex/AGENTS.md` reminder of layered precedence, and the machinery-vs-protocol ownership classification that routed later follow-through. Sources: [checkpoint-4-codex-load-bearing-surfaces-and-seams.md:44-48](/home/rookslog/workspace/projects/prix-guesser/.planning/readiness/phase-01-rerun/AUDITS/checkpoint-4-codex-load-bearing-surfaces-and-seams.md); [checkpoint-4-codex-load-bearing-surfaces-and-seams.md:86-96](/home/rookslog/workspace/projects/prix-guesser/.planning/readiness/phase-01-rerun/AUDITS/checkpoint-4-codex-load-bearing-surfaces-and-seams.md).

### What It Underweighted, Though It Saw The Terrain

- [e:c+i] The **side-entrance / router / management plane** was visible as `secondary` and then belatedly reopened. Checkpoint 5 A1 had to re-inventory `manager`, `next`, `thread`, `workstreams`, `quick`, `fast`, `resume-work`, `pause-work`, `note`, `check-todos`, `settings`, and workspace admin — none of which sat in the Checkpoint-3 map as first-class entry surfaces despite being live routers, control shells, or alternate artifact lanes. Sources: [checkpoint-5-gsd-raw-inventory-a1-entry-and-router-surfaces.md:235-240](/home/rookslog/workspace/projects/prix-guesser/.planning/readiness/phase-01-rerun/AUDITS/checkpoint-5-gsd-raw-inventory-a1-entry-and-router-surfaces.md); [checkpoint-3-gsd-surface-map.md:56-61](/home/rookslog/workspace/projects/prix-guesser/.planning/readiness/phase-01-rerun/AUDITS/checkpoint-3-gsd-surface-map.md).
- [e:c+i] The **helper-CLI control plane** was coarse. Checkpoint-3 acknowledged `runtime adaptation and local overlay` as a layer, but the later A3 raw inventory had to spell out `gsd-tools.cjs`, `core.cjs`, `config.cjs`, `model-profiles.cjs`, `init.cjs`, `phase.cjs`, `roadmap.cjs`, `commands.cjs`, `template.cjs`, `frontmatter.cjs`, `verify.cjs`, `audit.cjs`, and `intel.cjs` as a stratified control plane, not one generic helper surface. Sources: [checkpoint-5-gsd-raw-inventory-a3-agent-reference-template-helper-surfaces.md:414-622](/home/rookslog/workspace/projects/prix-guesser/.planning/readiness/phase-01-rerun/AUDITS/checkpoint-5-gsd-raw-inventory-a3-agent-reference-template-helper-surfaces.md); [checkpoint-3-gsd-surface-map.md:73-83](/home/rookslog/workspace/projects/prix-guesser/.planning/readiness/phase-01-rerun/AUDITS/checkpoint-3-gsd-surface-map.md).
- [e:c+i] The **install/materialization chain** was treated as one step. A4 had to expose the actual chain as `upstream install entrypoint -> tracked overlay canon -> installer post-pass -> live .codex registry/runtime -> partial materialization ledger`, and to flag `.codex/gsd-file-manifest.json` as a partial ledger rather than semantic truth (it omits `.codex/config.toml` and the overlay-added `gsd-rigorous-research` skill from its hash list). Sources: [checkpoint-5-gsd-raw-inventory-a4-artifact-state-install-policy-surfaces.md:36-42](/home/rookslog/workspace/projects/prix-guesser/.planning/readiness/phase-01-rerun/AUDITS/checkpoint-5-gsd-raw-inventory-a4-artifact-state-install-policy-surfaces.md); [checkpoint-5-gsd-raw-inventory-a4-artifact-state-install-policy-surfaces.md:57-58](/home/rookslog/workspace/projects/prix-guesser/.planning/readiness/phase-01-rerun/AUDITS/checkpoint-5-gsd-raw-inventory-a4-artifact-state-install-policy-surfaces.md).
- [e:c+i] The **advisor / general-purpose seam** was named but left unresolved. Checkpoint-3 noted that advisor/general-purpose seams stay bounded-open pending behavioral mutation evidence; Checkpoint-4 and A3 confirmed the advisor path is adapter-mediated (not cleanly `subagent_type="gsd-advisor-researcher"`), but the map still treats the launch pathway as a name in the registry. Sources: [checkpoint-5-gsd-raw-inventory-a3-agent-reference-template-helper-surfaces.md:75-98](/home/rookslog/workspace/projects/prix-guesser/.planning/readiness/phase-01-rerun/AUDITS/checkpoint-5-gsd-raw-inventory-a3-agent-reference-template-helper-surfaces.md).

### What It Actually Missed (Until Runtime Forced It Back Open)

- [e:c+i] The **producer-consumer chain for `CONTEXT.md` persona drift** was mapped semantically (`CONTEXT -> RESEARCH -> PLAN -> SUMMARY -> VERIFICATION/UAT`) but not at the prompt-assembly level. The live `discuss-phase.md` still carries persona-era text like `the agent's Discretion` in the steering brief scaffold on both the materialized and overlay sides, which means the persona residue can reach the planner via injected CONTEXT scaffolding even though no lane has flagged it. Sources: [.codex/get-shit-done/workflows/discuss-phase.md:25](/home/rookslog/workspace/projects/prix-guesser/.codex/get-shit-done/workflows/discuss-phase.md); [.codex/get-shit-done/templates/context.md:43](/home/rookslog/workspace/projects/prix-guesser/.codex/get-shit-done/templates/context.md); [tooling/portable-gsd/overlay/get-shit-done/templates/context.md:43](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/get-shit-done/templates/context.md).
- [e:c+i] The **internal bridge subworkflows** (`discovery-phase`, `diagnose-issues`, `execute-plan`, `verify-phase` vs the inline verifier-handoff in `execute-phase`) were not drawn as first-class edges in the Checkpoint-3 surface map. A2 had to reopen them and classify `verify-phase` vs the in-execute verifier-prompt as an ambiguous authority split. Sources: [checkpoint-5-gsd-raw-inventory-a2-workflow-and-subworkflow-surfaces.md:107-119](/home/rookslog/workspace/projects/prix-guesser/.planning/readiness/phase-01-rerun/AUDITS/checkpoint-5-gsd-raw-inventory-a2-workflow-and-subworkflow-surfaces.md).
- [e:c+i] The **installer mutation surface** was acknowledged as a seam but not probed as a content-drift surface. A4 found the installer rewrites `model_reasoning_effort` from the overlay's `high` to whatever profile is selected, and the live `.codex/config.toml` currently shows `xhigh` (`grep -n 'model_reasoning' tooling/portable-gsd/overlay/config.toml scripts/setup-portable-gsd.sh` on `2026-04-19`), which means either a post-install hand-edit or an install that the map did not model. Either way, the mapped `overlay = runtime after install` story is incomplete. Sources: live bash probe; [.codex/config.toml:1-3](/home/rookslog/workspace/projects/prix-guesser/.codex/config.toml); [tooling/portable-gsd/overlay/config.toml:2](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/config.toml); [scripts/setup-portable-gsd.sh:58-91](/home/rookslog/workspace/projects/prix-guesser/scripts/setup-portable-gsd.sh).
- [e:c+i] The **branch/worktree boundary materialization** was explicitly labeled `under-evidenced` at the Checkpoint-4 synthesis boundary and accepted as bounded-risk for closure. That is honest, but it means the map has an admitted hole at the execution-parallelism edge that a rerun will hit if it uses parallel plans. Sources: [checkpoint-4-cross-lane-seam-synthesis.md:25](/home/rookslog/workspace/projects/prix-guesser/.planning/readiness/phase-01-rerun/AUDITS/checkpoint-4-cross-lane-seam-synthesis.md); [checkpoint-4-cross-lane-seam-synthesis.md:43](/home/rookslog/workspace/projects/prix-guesser/.planning/readiness/phase-01-rerun/AUDITS/checkpoint-4-cross-lane-seam-synthesis.md).

### Adequacy Verdict

- [d:r:i] Adequacy is `mixed-strong at the declared level of analysis, uneven at the surfaces that matter most for a rerun`. The declared envelope (4 lanes + 5 seams + branch/worktree) is defensible and was not directionally wrong. But the side-entrance plane, the helper-CLI control plane, and the install/materialization chain were all visible-but-underweighted; the runtime-authority stale-doctrine finding lands live today; and the `execute-plan` / `diagnose-issues` / `verify-phase` / advisor launch pathways are mapped by name rather than by real runtime edge. That is enough under-mapping to warrant rerun-adjacent work before confidently launching Phase 01 against the current map.

## Comparative Mapping

This subsection compares the four evidence families named in the evidence architecture: (1) original readiness mapping, (2) `04-17` bridge audit and guarded-hybrid verdict, (3) revised docs corpus from lanes 07/12/14/15, (4) runtime/harness behavior. The intervention-ready ideal is the comparative horizon across all four, not a fifth family.

### Where The Four Agree

- [e:c+i] The main GSD control chain (`CONTEXT -> RESEARCH -> PLAN -> SUMMARY -> VERIFICATION/UAT`), the need to split workflow/artifact contracts from agent-role/shared doctrine from runtime/config/overlay truth, and the insufficiency of skills as the primary audit unit are all supported by readiness mapping, bridge-audit crosswalk, upstream docs (`ARCHITECTURE.md` and `agent-contracts.md`), and live runtime evidence. Sources: [SYNTHESIS.md:21-33](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-17-gsd-upstream-docs-readiness-comparative-audit/SYNTHESIS.md); [lane-02-docs-vs-readiness-crosswalk.md:23-32](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-17-gsd-upstream-docs-readiness-comparative-audit/lane-02-docs-vs-readiness-crosswalk.md); [checkpoint-3-gsd-surface-map.md:24-39](/home/rookslog/workspace/projects/prix-guesser/.planning/readiness/phase-01-rerun/AUDITS/checkpoint-3-gsd-surface-map.md); [.codex/get-shit-done/references/agent-contracts.md:10-35](/home/rookslog/workspace/projects/prix-guesser/.codex/get-shit-done/references/agent-contracts.md).
- [e:c+i] The reality of a **materialization boundary** (source package → installer → local overlay → live `.codex`) is recognized by all four families. Bridge-audit lane-02 explicitly cited upstream `bin/install.js` to confirm the install-adapter boundary, the readiness local-topology schema modeled it, and A4 documented the repo-local chain. Sources: [lane-02-docs-vs-readiness-crosswalk.md:31-32](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-17-gsd-upstream-docs-readiness-comparative-audit/lane-02-docs-vs-readiness-crosswalk.md); [checkpoint-5-gsd-raw-inventory-a4-artifact-state-install-policy-surfaces.md:36-42](/home/rookslog/workspace/projects/prix-guesser/.planning/readiness/phase-01-rerun/AUDITS/checkpoint-5-gsd-raw-inventory-a4-artifact-state-install-policy-surfaces.md).

### Where They Diverge

- [e:c+i] **Runtime authority:** readiness mapping named `.md` vs `.toml` as reversal-sensitive; Checkpoint 4 found `12/24` `.toml` agents still carrying legacy `CLAUDE.md` / `.claude/skills/` text; the bridge audit did not open that question; the docs-refresh lanes did not touch it (they address upstream GSD's public docs, not this repo's `.toml` registry content); live verification on `2026-04-19` returns `13/31`. The map has the seam; only runtime evidence knew the scope. Sources: [checkpoint-4-codex-load-bearing-surfaces-and-seams.md:44](/home/rookslog/workspace/projects/prix-guesser/.planning/readiness/phase-01-rerun/AUDITS/checkpoint-4-codex-load-bearing-surfaces-and-seams.md); live bash probe.
- [e:c+i] **Install-time mutation:** the overlay sets `model_reasoning_effort = "high"`, the installer rewrites it at install time to the operator's selected profile, and the live `.codex/config.toml` currently holds `xhigh`. The readiness map mentioned reasoning-policy truth as reversal-sensitive but did not probe the mutation step; bridge-audit relied on corroborated upstream `planning-config.md` language for config semantics; docs-refresh did not address the installer mutation step. Sources: [tooling/portable-gsd/overlay/config.toml:2](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/config.toml); [scripts/setup-portable-gsd.sh:58-91](/home/rookslog/workspace/projects/prix-guesser/scripts/setup-portable-gsd.sh); [.codex/config.toml:1-3](/home/rookslog/workspace/projects/prix-guesser/.codex/config.toml).
- [e:c+i] **Side-entry / router plane:** Checkpoint-3 map placed most of this plane as `secondary` and deferred it; Checkpoint-5 A1 reopened it and classified `manager`, `next`, `thread`, `workstreams`, workspace admin, and the `quick/fast` alternative-execution lane as first-class entries; the bridge audit did not address them (its upstream baseline spoke at the command/workflow/agent/reference/install level only); the upstream docs corpus in `v1.36.0` is uneven on inventory and self-completeness (confirmed by lane-01 freshness audit and the later INVENTORY.md push). Sources: [checkpoint-5-gsd-raw-inventory-a1-entry-and-router-surfaces.md:235-240](/home/rookslog/workspace/projects/prix-guesser/.planning/readiness/phase-01-rerun/AUDITS/checkpoint-5-gsd-raw-inventory-a1-entry-and-router-surfaces.md); [SYNTHESIS.md:21-23](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-17-gsd-upstream-docs-readiness-comparative-audit/SYNTHESIS.md); [lane-12-opus47-courageous-docs-refresh-recommendation.md:29-37](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-17-gsd-upstream-docs-readiness-comparative-audit/lane-12-opus47-courageous-docs-refresh-recommendation.md).
- [e:c+i] **Docs corpus vs runtime target:** the repo has no root `docs/` tree (`ls docs/` returns empty on `2026-04-19`); the docs-refresh work therefore lives in the upstream GSD project, not in this repo's runtime. Whatever truth gains it produced would only reach this repo via reinstall + overlay refresh, and the overlay itself is repo-local-patched, not a mirror of upstream public docs. Sources: live bash probe; [.codex/gsd-local-patches/backup-meta.json:2-19](/home/rookslog/workspace/projects/prix-guesser/.codex/gsd-local-patches/backup-meta.json); [tooling/portable-gsd/README.md:3-13](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/README.md).
- [e:c+i] **Verification closure standard:** the readiness cross-lane synthesis judged review-and-closure pressure `mixed / too closure-biased` (`Proceed anyway`, auto-approved checkpoints, approval-based advancement with partial human-verification debt). The bridge audit did not address closure bias directly; the docs-refresh work corrects naming and provenance but not closure doctrine; live runtime evidence in `agent-contracts.md` and `verification-overrides.md` confirms a debt-bearing completion mode exists and is honored, but review softness persists. Sources: [checkpoint-4-cross-lane-seam-synthesis.md:24](/home/rookslog/workspace/projects/prix-guesser/.planning/readiness/phase-01-rerun/AUDITS/checkpoint-4-cross-lane-seam-synthesis.md); [.codex/get-shit-done/references/agent-contracts.md:44-45](/home/rookslog/workspace/projects/prix-guesser/.codex/get-shit-done/references/agent-contracts.md).

### The Intervention-Ready Gap

- [d:r:i] An intervention-ready map would need to add, at minimum: (a) a probed runtime-authority ledger distinguishing what `.toml` agents actually say from what human-facing `.md` siblings claim, with a mechanism to prevent silent regression at next install; (b) a captured launch-truth artifact standard for doctrine-sensitive worker spawns, not just a protocol note; (c) an explicit first-class inventory of the router / control-shell plane with its helper-CLI seams drawn; (d) a drift probe for install-time mutation (reasoning policy, path substitution, compact-prompt override) that distinguishes overlay canon from live runtime; (e) a resolved boundary for the advisor / general-purpose adapter-mediated pathway; (f) end-to-end evidence for branch/worktree materialization under parallel plan execution. The first four are within reach of bounded mapping work; the last two need either a live execution pass or a deeper harness-code probe.

## Mismatch Matrix

Each row classifies a concrete mismatch between two or more evidence families. `Defect type` distinguishes what the mismatch is really about. `Severity` marks whether a rerun built on current mapping would hit this. `Closure status` says whether anything in the workspace has already closed it.

| # | Mismatch | Readiness map | Bridge audit | Docs corpus | Runtime | Defect type | Severity | Closure status |
|---|---|---|---|---|---|---|---|---|
| 1 | `.toml` agent worker files reference legacy `CLAUDE.md` / `.claude/skills/` while the `.md` siblings reference `AGENTS.md` | flagged as reversal-sensitive seam | not addressed | not addressed (upstream docs, not repo overlay) | live: `13/31` `.toml` files on 2026-04-19 | runtime-missed-by-both (named by map; confirmed by runtime; uninherited) | high | open |
| 2 | `model_reasoning_effort` mutated at install vs overlay canon (overlay `high`, live `xhigh`, installer rewrites on install) | reasoning-policy truth named reversal-sensitive but mutation not probed | relied on upstream `planning-config.md` for config semantics | not addressed | live: overlay says `high`, `.codex/config.toml:2` says `xhigh` | readiness mapping defect (install-time mutation layer not traced) | medium-high | open |
| 3 | Side-entrance / router / control-shell plane (`manager`, `next`, `thread`, `workstreams`, `quick`, `fast`, workspace admin) as first-class | deferred as `secondary` | not addressed (upstream baseline used command/agent/workflow categories) | upstream `docs/AGENTS.md` and `COMMANDS.md` under-enumerated until INVENTORY.md push | live: each is a live skill + workflow + (in several cases) helper-backed shell | readiness mapping defect (under-inventory) partially inherited from upstream doc categories | medium | partly closed by A1/A2 raw inventory; not translated into a revised map |
| 4 | `.codex/gsd-file-manifest.json` is partial (omits `.codex/config.toml`, omits overlay-added `gsd-rigorous-research` skill) | treated as material evidence | not addressed | not addressed | live: spot-check on manifest shows omissions | readiness mapping defect (partial-ledger surface treated as complete) | medium | flagged in A4; not acted on |
| 5 | Advisor / general-purpose launch pathway is adapter-mediated, not cleanly `subagent_type="gsd-advisor-researcher"` | named as bounded-open | not addressed | not addressed | live: advisor `.toml` exists but invocation goes through `general-purpose` seam | readiness mapping defect (authority boundary named but not resolved) | low-medium | open |
| 6 | Upstream `docs/ARCHITECTURE.md` count drift (references, modules, agent count), `claude_md_path` default wrong, `tdd_mode` provenance, ghost `resolve_model_ids` | not applicable (was not reading upstream docs directly) | partially applicable: bridge found upstream docs uneven, inventory surfaces unreliable | fully addressed by lanes 12/14/15 with INVENTORY.md + 5 parity tests | repo has no `docs/` tree; runtime is unaffected | docs defect (in upstream GSD) | low for this repo's runtime, high for upstream consumers | closed by upstream PR-stack (not reaching this repo) |
| 7 | `verify-phase` vs the inline verifier-handoff in `execute-phase` as an authority split | not drawn as split | not addressed | not relevant | live: both paths exist, stricter source is not declared | readiness mapping defect (internal edge not mapped) | low-medium | flagged in A2; not resolved |
| 8 | Branch/worktree boundary materialization | named; accepted as `under-evidenced` bounded risk | not addressed | not addressed | live: `use_worktrees` behavior and overlap checks exist but no end-to-end proof | runtime-missed-by-both (admitted) | medium | open by design |
| 9 | `CONTEXT.md` persona-era scaffolding (`the agent's Discretion`, `the agent = builder` vs `founder/visionary`) survives in live overlay + template | not flagged | not addressed | not addressed | live: repo-local overlay and materialized template both still carry it | runtime-missed-by-both | low-medium | open |
| 10 | Launch-truth capture is protocol-only (`~/.codex/state_5.sqlite` + `tooling/codex/capture_launch_truth.py`) | named as seam; named as workflow-protocol owned | not addressed | not addressed | live: helper exists but capture is operator-triggered, not workflow-enforced | known-weak (jointly doc-level + protocol-level, machinery-light) | medium | open |
| 11 | Closure / review softness (`Proceed anyway`, auto-approved checkpoints, non-blocking code review, advisory review shape) | named in Checkpoint 4 cross-lane synthesis as closure-biased | not addressed directly (bridge does call for anti-closure doctrine preservation) | not addressed | live in workflow chain + role contracts | readiness mapping defect at synthesis level, but a doctrine/behavior defect more than a map defect | medium | open |
| 12 | `.planning/config.json` as live runtime input rather than as `outputs` | A4 had to correct this; original topology schema leaned toward outputs | not addressed | not addressed | live: workflows `init`-payloads read it; `WORKFLOW.md` calls it harness-state | readiness mapping defect (category misclassification) | low-medium | flagged in A4; not translated into the canonical map |

## Mapping-Confidence Register

Confidence here is not `is the map pretty?` but `how much would I trust a rerun that relied on this surface being correct as currently drawn?`

| Surface | Current confidence | Basis | Failure mode if wrong |
|---|---|---|---|
| Four-lane envelope (1 Codex + 3 GSD + seam checks) | high | Checkpoint 3/4 produced it, Lane 02 crosswalk confirmed, upstream architecture compatible | Envelope still miscategorizes routers/control shells as secondary |
| Phase-critical chain (`CONTEXT -> RESEARCH -> PLAN -> SUMMARY -> VERIFICATION/UAT`) | high | agreement across all four evidence families | Internal bridges (`execute-plan`, `verify-phase`, `diagnose-issues`) remain thin as drawn |
| Preserve-only seams (`future_awareness`, `canonical_refs`, `future_preservation`, non-decisions) | medium-high | templates/workflow text require them; Checkpoint 4 called item-by-item traceability not yet required | Late planner contracts drop or flatten them without alarms |
| Reasoning-policy truth | medium | doctrine coherent, overlay vs runtime value divergence observed live (`high` vs `xhigh`) | Spawned worker inherits an effective policy different from the mapped one |
| Named-agent authority | low-medium | `.toml` registry is authoritative but content is stale on `13/31` agents as of 2026-04-19 | Rerun launches planner/executor/verifier with `CLAUDE.md`-era doctrine |
| Side-entry / router plane | medium | A1 raw inventory exists but has not been folded into the canonical map | Rerun routing (`next`, `progress`, `manager`) steers phase work through under-mapped control logic |
| Helper-CLI control plane (`gsd-tools.cjs` + `bin/lib/*`) | medium | A3 inventory exists and is accurate; map still renders it as a single helper node | Runtime behavior attributed to workflows that actually lives in helper code |
| Overlay-to-runtime materialization | medium-low | A4 exposed the real chain; installer mutation was not probed for drift | Reinstall or refresh silently changes runtime settings the map treats as stable |
| Materialization ledger (`gsd-file-manifest.json`) | low (if read as complete) | A4 verified partial coverage (omits config.toml, gsd-rigorous-research) | Drift checks based on manifest miss real drift |
| Launch-truth capture | low as machinery, medium as protocol | `state_5.sqlite` is real; no closed-loop capture at spawn | Mapped `effective launch settings verified` exists only when operator actually ran capture |
| Branch/worktree materialization | low | accepted under-evidenced | Parallel execution corrupts shared doc state; overlap checks miss a case |
| Advisor / general-purpose pathway | low | named, adapter-mediated, not resolved | Advisor calls actually launch a differently-permissioned generic agent |
| `.planning/config.json` classification | medium (after A4 reclassification) | `WORKFLOW.md` says harness-state explicitly; canonical map still understates it | Mapped as output, read as input — consumers distrust it for the wrong reason |
| Docs-corpus drift guards (upstream) | high for upstream, not applicable to this repo | lanes 14/15 landed 5 parity tests + INVENTORY.md upstream | Confuses upstream doc truth with repo-local runtime truth |
| `CONTEXT.md` persona residue | low-medium (lane-specific) | live overlay and live template both carry `the agent's Discretion` persona text | Planner prompt inherits a builder persona rather than the repo's governance-first framing |

## Runtime-Versus-Map Divergence Notes

Named separately to avoid letting docs quality (upstream), envelope discipline (readiness map), or bridge-verdict preservation (guarded hybrid) absorb runtime defects by implication.

- [e:c+i] **Named-agent authority is stale in a way the rerun would inherit.** `grep -l "CLAUDE.md" .codex/agents/*.toml` returns `13` on `2026-04-19` for the currently registered `31`-agent roster. This is the same defect class Checkpoint-4 found at `12/24`, but the worker set has grown and the stale subset has not been swept. A rerun spawning `gsd-planner`, `gsd-executor`, and `gsd-plan-checker` today would launch workers whose `.toml` `developer_instructions` still direct them at `./CLAUDE.md` and `.claude/skills/` rather than `AGENTS.md` and `.codex/skills/`. Sources: live bash probe; [.codex/agents/gsd-planner.toml](/home/rookslog/workspace/projects/prix-guesser/.codex/agents/gsd-planner.toml); [.codex/agents/gsd-executor.toml](/home/rookslog/workspace/projects/prix-guesser/.codex/agents/gsd-executor.toml); [.codex/agents/gsd-plan-checker.toml](/home/rookslog/workspace/projects/prix-guesser/.codex/agents/gsd-plan-checker.toml).
- [e:c+i] **Installer mutates reasoning effort; overlay and live runtime disagree.** Overlay canon is `high`. Live `.codex/config.toml:2` is `xhigh`. Installer has a rewrite path for `model_reasoning_effort`. Either (a) a post-install hand-edit re-raised it to `xhigh`, or (b) the install step that produced the live file chose `xhigh`. The mapped `overlay = runtime` simplification misses this divergence. Sources: [tooling/portable-gsd/overlay/config.toml:2](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/config.toml); [scripts/setup-portable-gsd.sh:58-91](/home/rookslog/workspace/projects/prix-guesser/scripts/setup-portable-gsd.sh); [.codex/config.toml:1-3](/home/rookslog/workspace/projects/prix-guesser/.codex/config.toml).
- [e:c+i] **Materialization ledger is partial.** `.codex/gsd-file-manifest.json` has hash entries for a subset of the materialized tree; it does not cover `.codex/config.toml` or the overlay-added `gsd-rigorous-research` skill. Treating this ledger as a full drift-detection surface (which the canonical map tends to) would let those surfaces drift silently. Sources: [checkpoint-5-gsd-raw-inventory-a4-artifact-state-install-policy-surfaces.md:57-58](/home/rookslog/workspace/projects/prix-guesser/.planning/readiness/phase-01-rerun/AUDITS/checkpoint-5-gsd-raw-inventory-a4-artifact-state-install-policy-surfaces.md).
- [e:c+i] **`CONTEXT.md` persona scaffolding remains in both overlay and materialized templates.** Both `/tooling/portable-gsd/overlay/get-shit-done/templates/context.md:43` and `/.codex/get-shit-done/templates/context.md:43` still contain `### the agent's Discretion`, and the live overlay and materialized `discuss-phase.md` carry `User = founder/visionary. the agent = builder.` (line 35-ish). The readiness map tracked `steering brief carries preserve-only seams`; what it did not track is that the scaffolding also carries persona language that can inject an implicit role split into every phase. A rerun using these templates will inherit that framing.
- [e:c+i] **Helper-CLI control plane affects runtime behavior that the map attributes to workflows.** Completion-mode classification lives in `phase.cjs:21-31, 37-113`, not in `verify-work.md` or `agent-contracts.md` alone. `roadmap analyze` and `update-plan-progress` in `roadmap.cjs` translate artifact state into routing truth. The high-level map renders `gsd-tools.cjs` as a single helper node; that hides where completion-vs-debt decisions actually get made. Sources: [checkpoint-5-gsd-raw-inventory-a3-agent-reference-template-helper-surfaces.md:494-518](/home/rookslog/workspace/projects/prix-guesser/.planning/readiness/phase-01-rerun/AUDITS/checkpoint-5-gsd-raw-inventory-a3-agent-reference-template-helper-surfaces.md).
- [e:c+i] **Home-level `~/.codex/AGENTS.md` still claims `No hooks support` and injects Reflect-era workflow language.** Official AGENTS precedence concatenates layers. Repo-local doc correctly overrides, but the top-of-chain layer is still consumed and can steer workers under pressure. This is named in Checkpoint 4 but is structurally a runtime-vs-map divergence because the canonical repo map describes this as a stale but harmless inherited layer; in practice it consumes budget and contradicts the repo's active hook pilot. Sources: [checkpoint-4-codex-load-bearing-surfaces-and-seams.md:33](/home/rookslog/workspace/projects/prix-guesser/.planning/readiness/phase-01-rerun/AUDITS/checkpoint-4-codex-load-bearing-surfaces-and-seams.md); [checkpoint-4-codex-load-bearing-surfaces-and-seams.md:45](/home/rookslog/workspace/projects/prix-guesser/.planning/readiness/phase-01-rerun/AUDITS/checkpoint-4-codex-load-bearing-surfaces-and-seams.md).
- [o:r:i] **Resume / compaction / `/status` drift is qualified, not closed.** Checkpoint 4 cited open GitHub issues for WSL resume drift, `/status` under-reporting, remote compaction freezes, and desktop stale-instruction injection. None is universal, but each is a live runtime-vs-map divergence where the map treats `compact prompt + re-entry checklist` as sufficient and the runtime still leaks. Sources: [checkpoint-4-codex-load-bearing-surfaces-and-seams.md:54](/home/rookslog/workspace/projects/prix-guesser/.planning/readiness/phase-01-rerun/AUDITS/checkpoint-4-codex-load-bearing-surfaces-and-seams.md).

## Switch-Trigger Register

Triggers are framed against the charter's execution-capacity model: `one operator orchestrating parallel Codex and Claude lanes over a multi-week budget, capable of multi-module repo-local refactors and bounded follow-through on repo-local harness surfaces, but not assuming broad upstream gsd ownership by default`. Each trigger specifies what evidence would fire it, what switch it would justify, and what execution-capacity assumption that switch leans on.

| # | Trigger | Fires toward | Execution-capacity assumption | Minimum discriminating next move |
|---|---|---|---|---|
| 1 | Current `13/31` `.toml` stale-doctrine count persists into a rerun dry-run, or a launch-truth capture shows a spawned planner/executor inherited `CLAUDE.md` language | `Proposal F` (harness-code-first) for a bounded `.toml` unification pass | multi-week budget supports one bounded overlay + `.codex` cleanup lane; does not assume upstream GSD ownership | run a launch-truth capture on a single planner spawn; if effective `developer_instructions` contain `CLAUDE.md`, open the `.toml` unification lane |
| 2 | Overlay/runtime divergence (reasoning effort, compact prompt path, hook config) grows beyond `model_reasoning_effort` to include any other steering key | `Proposal F` scoped to overlay + installer mutation contract; possibly `Proposal C` if the drift probe shows mapping was misleading | bounded refactor of `scripts/setup-portable-gsd.sh` + overlay regeneration path is within budget | diff `tooling/portable-gsd/overlay/config.toml` against `.codex/config.toml`; classify each divergence as `installer-mutated`, `post-install hand-edit`, or `drift` |
| 3 | Checkpoint-5-era branch/worktree risk materializes in a real parallel plan execution (overlap detector mis-classifies, shared-doc writes collide, worktree boundary leaks) | `Proposal E` (execute-and-learn) with an explicit parallelism-probe plan; or `Proposal F` if the defect is in helper-CLI overlap logic | execute-and-learn needs 1-2 weeks of repo bandwidth and a captured-evidence discipline | run one small phase with `use_worktrees=true` and one with `false`; capture overlap-detection behavior end-to-end |
| 4 | Launch-truth capture becomes load-bearing more than once per week and still depends on `capture_launch_truth.py` plus operator discipline | `Proposal F` for a bounded capture-as-machinery pass; not `Proposal C` because the mapping is adequate, the machinery is the gap | one operator can carry a bounded helper addition to `gsd-tools.cjs` or a wrapping script, but not a general hook subsystem | add one lightweight post-spawn capture step in the `init` payload path for doctrine-sensitive spawns; verify it captures `state_5.sqlite` delta |
| 5 | `.planning/config.json` or `.codex/config.toml` steering change is made silently (no captured diff artifact) during a rerun and materially alters planner or executor behavior | `Proposal F` for a capture-discipline upgrade around config surfaces; possibly a bounded `Proposal C` mapping lane for config-as-runtime-input | multi-week budget supports a standing capture protocol and a one-time mapping uplift | require that any workflow config change during a rerun emits a dated snapshot into `.planning/audit/` before reading the file downstream |
| 6 | Router / control-shell plane (`manager`, `next`, `autonomous`) steers a rerun into advancement or closure that bypasses a checkpoint the mapping expected | `Proposal C` scoped narrowly to first-class the router plane into the canonical map; then reassess B-extended vs F | one operator can carry a bounded mapping pass across 5-7 router surfaces | spawn a `gsd-next --force` dry-run on a test project; trace the exact workflow path; compare to the canonical map |
| 7 | Advisor / general-purpose launch pathway carries doctrine into advisor calls and the map still treats it as `gsd-advisor-researcher` launch | bounded `Proposal F` pass to resolve the adapter-mediated launch; not C because the map already names the ambiguity | bounded repo-local intervention, but requires decision on whether advisor becomes first-class or remains adapter-mediated | capture launch truth on a `gsd-discuss-phase --advisor` invocation; compare `agent_role` in `state_5.sqlite` to the intended subagent |
| 8 | A second independent lane confirms the current mapping would materially mislead a rerun on more than one of these surfaces | `Proposal D` becomes warranted (maximal multi-wave) as a recovery shape, not as a default | multi-week budget stretched but still feasible under current capacity; signals systemic mapping failure | only open if at least two Wave-1 or later lanes converge on the same mismap |
| 9 | Upstream docs refresh lands in `main` and the operator pursues a reinstall using the newer upstream + an overlay refresh | no immediate switch; triggers a **refresh pass** inside `Proposal B-extended` with explicit overlay-vs-upstream reconciliation | within capacity for a single operator if the overlay refresh is scripted, not hand-patched | re-run `setup-portable-gsd.sh` in a throwaway worktree with the newer upstream, diff overlay canon against newer upstream, decide patch set |

## Interventions Considered And Rejected

Each considered intervention names the upside, the regret analysis that rejected it, and a later signal that would justify reopening it. All are scoped against the same operator-bandwidth model as the switch triggers.

### 1. Full omnibus remap (replace Checkpoint 5 A1-A4 outputs with one new canonical topology)

- upside: produces a single clean authoritative map the rerun can lean on without reconciling four A-lane inventories; kills the partial-ledger and category-mismatch defects in one move.
- rationale considered: the readiness map as currently split is genuinely uneven and the bridge audit's warrant structure depends on topology grounding that is partly under-evidenced.
- regret analysis: omnibus remap is the same move the first readiness attempt implicitly rejected; doing it now would re-import the closure-biased incentive to treat the new map as settled truth; it would also lock in category choices before runtime probes have validated them. The pass-through cost is low (readers consult four files rather than one); the foreclosure cost is high (one authoritative topology under operator bandwidth is likely to freeze in the wrong shape for a rerun).
- later signal to reopen: two or more Wave-1 lanes converge on the same categorical re-classification, or an execute-and-learn pass shows the four-lane split is actively misleading.
- status: rejected; remains reopen-eligible only under signal trigger `#8` in the switch register.

### 2. Pre-Wave-1 harness-code-first intervention (`Proposal F` immediately, on the stale `.toml` agents)

- upside: closes the single most live runtime defect before the main wave opens, preventing the rerun from inheriting stale worker doctrine; provides a concrete, bounded repo-local target that fits the operator-bandwidth model.
- rationale considered: Checkpoint 4's strongest criticism was runtime-authoritative worker drift; live evidence on `2026-04-19` confirms it is still at `13/31`; the fix is mechanical.
- regret analysis: three problems. First, the stale-doctrine count is symptomatic; patching `.toml` files without fixing the overlay and installer would re-break on next install. Second, doing `F` now widens current Wave-1 scope before Wave-1 has produced its own mapping evidence, which is exactly what the charter warns against (`if a recommendation argues against a stronger intervention because it feels too large, it must say what execution model made it seem too large` — but the converse also holds). Third, executing a harness-code pass without a Wave-1-informed prioritization risks choosing the loudest drift instead of the most leveraged one; the helper-CLI plane and the materialization chain may actually matter more for a rerun than the `.toml` roster.
- later signal to reopen: a launch-truth capture (switch trigger `#1`) confirms a spawned planner inherited `CLAUDE.md` doctrine, or Wave-1 Lane 04 converges on runtime-authority as the dominant defect.
- status: rejected as a pre-Wave-1 move; promoted to switch trigger `#1` inside or after Wave-1.

### 3. Pre-Wave-1 comparative mapping lane (`Proposal C` applied before any rerun-design synthesis)

- upside: the present lane's findings would be stronger if a dedicated drift-probe lane ran first, probing runtime-vs-overlay, runtime-vs-upstream-docs, and runtime-vs-readiness-topology systematically rather than in a mapping-adequacy lane; C done well produces the intervention-ready map the charter asks about.
- rationale considered: bridge-audit lane-02 explicitly deferred this work to a `possible later lane`; no lane since has closed it; this present lane has to do partial drift probing on the side rather than as first-class evidence.
- regret analysis: C as a pre-Wave-1 gate would delay all four Wave-1 lanes for probably 2-3 weeks of operator bandwidth; it would also risk over-explaining every failure as a mapping failure (the charter's named risk in `Proposal C`'s description); and the existing Wave-1 structure is already capable of absorbing a bounded drift probe inside the Operator / Orchestration lane without needing a separate prior pass.
- later signal to reopen: Wave-1 produces runtime-drift findings that cannot be cleanly incorporated into the existing four-axis comparison without ontology reconciliation; in that case C becomes the prerequisite for Wave-1 synthesis rather than a replacement for Wave-1.
- status: rejected as pre-Wave-1 gating; promoted to a bounded expansion inside `Proposal B-extended` if Wave-1 evidence requires it.

### 4. Immediate launch-truth-as-machinery upgrade

- upside: would close the single most under-owned seam (effective launch settings) without needing a full harness rewrite; one concrete intervention that the map has been asking for since Checkpoint 4.
- rationale considered: the current launch-truth posture is honest but protocol-heavy; every doctrine-sensitive spawn depends on operator discipline to run `capture_launch_truth.py`; this is a single weak point the mapping keeps flagging without closing.
- regret analysis: two problems. First, it is tempting precisely because it is small, which means it can absorb attention that should go to the larger mapping question; `fix the thing you can fix` is exactly the closure bias the charter warns against. Second, launch-truth as machinery needs to interact with Codex subagent behavior that Codex docs describe as still-evolving (parent session overrides, feature maturity), so a repo-local machinery-first answer may be locally optimal but globally brittle.
- later signal to reopen: switch trigger `#4` fires, or a rerun relies on launch-truth evidence for a doctrine-sensitive audit and the operator notices capture drift.
- status: rejected as immediate move; staged behind switch trigger `#4`.

### 5. Docs-refresh backport into the repo (bring upstream lanes 12-15 gains into the repo's `.codex/` runtime)

- upside: the upstream docs-refresh work is genuinely high-quality (INVENTORY.md + 5 parity tests is the single strongest structural improvement in any of the artifact families); backporting it would tighten the repo's source-of-truth layer for agents/commands/workflows/references/modules/hooks.
- rationale considered: the repo is running `1.36.0` with a patched overlay; an upstream refresh lands drift tests that would materially constrain future drift.
- regret analysis: the repo has no repo-root `docs/` tree, so there is no direct surface for the upstream docs to land on; the bridge audit already rejected `docs-only restart`; backporting would be a category error against the repo's `this is not a docs project` orientation; the mapping gap is at runtime-authority, install-time mutation, and router plane, not at public docs; and doing it now would consume the multi-week budget on a surface the rerun does not ship against.
- later signal to reopen: the repo grows a public-facing GSD-as-dependency posture (it currently does not), or the upstream refresh adds runtime-affecting features (not just doc truth) that a reinstall would pull in.
- status: rejected as out of scope for mapping-adequacy; remains relevant only to upstream reinstall sequencing (switch trigger `#9`).

### 6. Pre-Wave-1 `.planning/` topology reorganization (first-class the router plane, helper-CLI plane, materialization chain into the canonical map)

- upside: updates the canonical topology to reflect A1-A4 findings without waiting for Wave-1 synthesis; provides a cleaner baseline for the rerun-design work.
- rationale considered: the four Checkpoint-5 A-lane outputs are each accurate but sit as parallel raw inventories; no artifact has yet folded them into an updated canonical topology.
- regret analysis: this workspace's lane-05 comparative disposition explicitly rejected `broad pre-Wave-1 directory reorganization` as not yet earned; doing mapping-topology work now would re-open that decision under weak justification; the A1-A4 inventories are already usable as-is for Wave-1 lanes that need them; and the canonical topology is supposed to emerge from Wave-1 synthesis, not precede it.
- later signal to reopen: Wave-1 lanes repeatedly bottom out because consumers can't read A1-A4 cleanly, or the rerun-design lane explicitly requests an updated canonical topology as a prerequisite.
- status: rejected; remains bounded-open for a later post-Wave-1 integration pass.

## Challenge-Packet Engagement (Meta-Note)

- [d:r:i] This lane was specified with a failure condition: `Fails if it recommends mapping is sufficient without explicit challenge-packet engagement`. The current lane does not recommend that; it explicitly judges the mapping `directionally adequate, structurally under-mapped` and opens switch triggers rather than closure. The challenge packet (`SESSION-FRAMING-BRIEF.md`, Surface B prelicensing / judgeability, Surface B graded-underreach trial) was read and treated as live pressure, not as ornamentation. Concrete traces of that pressure in this artifact:
  - the `Overall Mapping Judgment` refuses `mapping is sufficient` and names the uninherited `13/31` stale-doctrine finding as single strongest counter-evidence;
  - the `Switch-Trigger Register` includes triggers (`#1`, `#2`, `#5`) that would move the workspace off `Proposal B-extended` toward `F` or a bounded `C` under runtime evidence;
  - the `Interventions Considered And Rejected` ledger names `#1` (omnibus remap), `#2` (immediate F), `#3` (pre-Wave-1 C), `#4` (launch-truth machinery), `#5` (docs backport), `#6` (topology reorg) with regret analysis — satisfying the `at least 3 stronger interventions` requirement and extending beyond it;
  - the `Mismatch Matrix` classifies defects by type rather than averaging them, in line with the spec's `do not let docs quality masquerade as runtime adequacy` rule;
  - the graded-underreach vocabulary is applied implicitly in the register: the stale `.toml` roster is a `traceable undercarry` of Checkpoint 4's finding (the mapping saw the seam, later work did not close it) rather than an `explicit miss`, which is why the switch trigger is bounded rather than headline-reversal.
- [d:r:i] The prelicensing check from Surface B is also honored: this lane does not declare `Proposal B-extended` self-justifying. Its continued status here depends on Wave-1 not producing runtime evidence that trips switch triggers `#1`-`#5`; if any of those fire, B-extended gives way to a bounded F or C lane.

## What This Lane Does Not Settle

- [o:r:i] Whether the stale `.toml` roster has been swept since `2026-04-19 18:45 UTC` (the time of the live probe here) — a rerun-proximal verification should repeat the probe.
- [o:r:i] Whether the overlay `high` vs live `xhigh` divergence is post-install hand-edit, install-time profile selection, or genuine drift. Only an installer re-run in a throwaway worktree would discriminate.
- [o:r:i] Whether `verify-phase` or the inline verifier-handoff in `execute-phase` is the stricter operative source in practice. This is A2's `ambiguous` classification; it needs a real execution trace.
- [o:r:i] Whether the advisor / general-purpose launch pathway actually picks up the `gsd-advisor-researcher` `.toml` at runtime or only its prompt scaffolding.
- [o:r:i] Whether the `CONTEXT.md` persona scaffolding materially affects planner behavior. A captured planner prompt would settle it; reasoning alone will not.
- [o:r:i] Whether the repo's execution-capacity model actually holds for multiple of the named switch triggers firing in the same multi-week budget; the charter model assumes one operator orchestrating parallel lanes, not several harness-code passes in parallel.

## Sources Summary

Primary readiness-era mapping evidence:
- `.planning/readiness/phase-01-rerun/AUDITS/checkpoint-3-workflow-harness-scope-audit.md`
- `.planning/readiness/phase-01-rerun/AUDITS/checkpoint-3-gsd-surface-map.md`
- `.planning/readiness/phase-01-rerun/AUDITS/checkpoint-3-codex-surface-map.md`
- `.planning/readiness/phase-01-rerun/AUDITS/checkpoint-4-codex-load-bearing-surfaces-and-seams.md`
- `.planning/readiness/phase-01-rerun/AUDITS/checkpoint-4-cross-lane-seam-synthesis.md`
- `.planning/readiness/phase-01-rerun/AUDITS/checkpoint-5-gsd-raw-inventory-a1-entry-and-router-surfaces.md`
- `.planning/readiness/phase-01-rerun/AUDITS/checkpoint-5-gsd-raw-inventory-a2-workflow-and-subworkflow-surfaces.md`
- `.planning/readiness/phase-01-rerun/AUDITS/checkpoint-5-gsd-raw-inventory-a3-agent-reference-template-helper-surfaces.md`
- `.planning/readiness/phase-01-rerun/AUDITS/checkpoint-5-gsd-raw-inventory-a4-artifact-state-install-policy-surfaces.md`

Bridge-audit evidence:
- `.planning/audits/2026-04-17-gsd-upstream-docs-readiness-comparative-audit/INDEX.md`
- `.planning/audits/2026-04-17-gsd-upstream-docs-readiness-comparative-audit/PROGRAM.md`
- `.planning/audits/2026-04-17-gsd-upstream-docs-readiness-comparative-audit/SYNTHESIS.md`
- `.planning/audits/2026-04-17-gsd-upstream-docs-readiness-comparative-audit/lane-02-docs-vs-readiness-crosswalk.md`
- `.planning/audits/2026-04-17-gsd-upstream-docs-readiness-comparative-audit/lane-03-reseed-judgment.md`
- `.planning/audits/2026-04-17-gsd-upstream-docs-readiness-comparative-audit/lane-07-upstream-docs-update-proposal-opus-1m-r1.md`
- `.planning/audits/2026-04-17-gsd-upstream-docs-readiness-comparative-audit/lane-12-opus47-courageous-docs-refresh-recommendation.md`
- `.planning/audits/2026-04-17-gsd-upstream-docs-readiness-comparative-audit/lane-14-opus47-pr4-continuation-report.md`
- `.planning/audits/2026-04-17-gsd-upstream-docs-readiness-comparative-audit/lane-15-opus47-single-pr-description.md`

Live repo governance / harness surfaces:
- `AGENTS.md`, `.planning/AGENTS.md`, `WORKFLOW.md`, `AI-GUARDRAILS.md`, `ARTIFACT-GOVERNANCE.md`
- `.codex/get-shit-done/workflows/discuss-phase.md`
- `.codex/get-shit-done/workflows/plan-phase.md`
- `.codex/get-shit-done/workflows/execute-phase.md`
- `.codex/get-shit-done/templates/context.md`
- `.codex/get-shit-done/references/agent-contracts.md`
- `tooling/portable-gsd/overlay/get-shit-done/workflows/discuss-phase.md`
- `tooling/portable-gsd/overlay/get-shit-done/templates/context.md`

Workspace setup / charter:
- `.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/AUDIT-CHARTER.md`
- `.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/WORKSPACE-AUTHORITY-AND-ORGANIZATION.md`
- `.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/INDEX.md`
- `.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/CURRENT-STATE.md`
- `.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/QUESTION-SET.md`
- `.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/EVIDENCE-ARCHITECTURE.md`
- `.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/PLAN-PROPOSALS.md`
- `.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/STATUS.md`

Stage-2 challenge packet:
- `.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/SESSION-FRAMING-BRIEF.md`
- `.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/lane-04-surface-b-prelicensing-judgeability-pass.md`
- `.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/lane-04-surface-b-graded-underreach-trial.md`

Live probes executed during this lane (not prior findings recalled):
- `grep -l "CLAUDE.md" .codex/agents/*.toml | wc -l` → `13`; `ls .codex/agents/*.toml | wc -l` → `31` (`2026-04-19`)
- `head -3 .codex/config.toml` → `model_reasoning_effort = "xhigh"`
- `grep -n "model_reasoning" tooling/portable-gsd/overlay/config.toml scripts/setup-portable-gsd.sh` → overlay `high`, installer rewrite present
- `ls docs/` → empty (repo has no root `docs/` tree)
- `cat .codex/get-shit-done/VERSION` → `1.36.0`
