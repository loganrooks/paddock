Date: 2026-04-22
Status: landed change-triggered refresh

# Codex Claude Parity Classification Carrier Change-Triggered Refresh

## Purpose

- [g:r:i] Record the next bounded `change_triggered_slice_refresh` after `49`.
- [g:r:i] The trigger is not wider parity install.
- [g:r:i] The trigger is that the repo-local materialization contract now carries a typed runtime-specific reference report for the `.codex` / `.claude` parity branch.

## Trigger

- [e:c+i] [../intervention-proposals/135-codex-claude-parity-classification-carrier-implementation.md](../intervention-proposals/135-codex-claude-parity-classification-carrier-implementation.md) lands the first classified carrier cleared by lane `19`.
- [d:r:i] The propagation pressure is specific:
  - `portable_gsd_contract.py` now preserves a typed parity-reference report inside `verify-materialized`
  - the current known three-hit baseline is no longer only in lane prose
  - unreviewed hits now surface distinctly for contextual reread instead of blending into one warning bucket
  - the route still refuses fake confidence by keeping contextual reread above helper output

## Refresh Result

- [d:r:i] The typed `v2` declared-contract layer now widens the portable materialization contract from overlay validation plus live coherence into bounded parity-reference disclosure too.
- [d:r:i] The semantic layer now keeps three extra relations explicit instead of ambient:
  - `portable_gsd_contract.py` -> typed runtime-specific reference report
  - known baseline `.claude` hits -> explicit contextual classification
  - unreviewed runtime-specific hits -> contextual-reread obligation rather than silent quieting
- [d:r:i] The evidence layer now carries:
  - the landed implementation note `135`
  - the widened portable-contract tests
  - this `50` refresh row

## Current Consequence

- [d:r:i] The parity branch now carries:
  - lane `19` external widening
  - lane `19` inheritance
  - the live contract carrier inside `verify-materialized`
  - the matching propagation refresh
- [d:r:i] Later parity work should keep distinguishing:
  - classified baseline visibility
  - unreviewed-hit reread obligation
  - later install/materialization widening
  - later `.claude` freshness or maintained-carrier questions
