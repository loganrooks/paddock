Date: 2026-04-21
Status: landed seed-consumer carry change-triggered refresh

# Seed Consumer Carry Change-Triggered Refresh

## Purpose

- [g:r:i] This note records the next bounded `change_triggered_slice_refresh` after `28`.
- [g:r:i] The trigger here is a seed producer/consumer route move: `plant-seed` now preserves strengthening carry in an explicit section, and `new-milestone` now consumes that section together with `Why This Matters` instead of only scanning trigger timing and planted-during context.

## Trigger

- [e:c+i] The seed family now carries a bounded landed slice through [73-seed-consumer-carry-first-slice-proposal.md](../intervention-proposals/73-seed-consumer-carry-first-slice-proposal.md) and [74-seed-consumer-carry-first-slice-implementation.md](../intervention-proposals/74-seed-consumer-carry-first-slice-implementation.md).
- [e:r:i] That slice changed the propagation field in four concrete ways:
  - the tracked overlay now owns `skills/gsd-plant-seed/SKILL.md`
  - `plant-seed` now preserves strengthening meaning in an explicit section
  - `new-milestone` now extracts `Why This Matters` and `Strengthening Carry` from matching seeds
  - the milestone-open consumer route now treats selected seeds as richer requirement-shaping context rather than only as trigger-matched ideas

## Refresh Result

- [d:r:i] The typed `v2` declared-contract layer now names the seed producer/consumer follow-through contract explicitly instead of leaving this bridge ambient behind one workflow and one consumer note.
- [d:r:i] The semantic layer now keeps `plant-seed`, `gsd-plant-seed`, and the stronger `plant-seed -> new-milestone` consumer relation explicit.
- [d:r:i] The evidence layer now records the landed `74` implementation note and focused seed contract test as anchors for this slice.
- [d:r:i] The coverage layer now records another real non-uplift `change_triggered_slice_refresh`, so typed `v2` keeps proving itself against long-horizon and self-overcoming carry movement rather than only against earlier onboarding, repair, update, compatibility, and lifecycle routes.

## Current Consequence

- [d:r:i] The typed `v2` registry now keeps seed producer/consumer carry visible as a bounded family movement instead of leaving seed meaning as another flatter pocket behind milestone-open scan logic.
- [d:r:i] Later refreshes should keep following actual movement into seed doctrine-vintage, broader seed consumers, and any later wider entry-wrapper retrofit rather than reopening the whole long-horizon field in one jump.

