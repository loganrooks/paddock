Date: 2026-04-22
Status: completed bounded reread output

# Seed Migration Pointer Bridge Harden Reread — Opus 4.7 Max R1

## What The Hardened Bridge Now Makes More Explicit

- [e:r:i] The helper-side progress-note now carries a wider operator-facing triad than the earlier first bridge landing (`89/90` plus lane `37`) carried:
  - `seed_migration_candidate_count` — aggregated scalar (already present after `89/90`).
  - `seed_migration_candidate_breakdown` — a new compact per-category string (`legacy X / noncurrent Y / shape-gap Z`) produced by [`seed_migration_candidate_breakdown_text`](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/project_uplift.py:751) and emitted by [`progress_note_seed_fields`](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/project_uplift.py:785).
  - `seed_migration_inspect_pointer` and `seed_migration_write_pointer` — the inspect/write split surfaced separately from the first-landing's single `seed_migration_pointer`. The inspect pointer preserves the reread-deeper reading, while the write pointer preserves the durable-memory side effect as a separately disclosed move.
- [e:r:i] The helper exports the pointer literals as module constants: [`SEED_MIGRATION_SKILL_COMMAND`](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/project_uplift.py:64) and [`SEED_MIGRATION_WRITE_COMMAND`](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/project_uplift.py:65). The earlier landing carried the command text as a plain string beside helper logic; the harden slice now carries it as a named symbol the tests can bind to.
- [e:r:i] Durable uplift memory now preserves the aggregated counts inside `seed_corpus_posture`. The live manifest at `.planning/UPLIFT-MANIFEST.json` already carries both `migration_candidate_count` and the structured `migration_candidate_breakdown` dict; drift detection in [`seed_corpus_drift_reasons`](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/project_uplift.py:762) now tracks both fields, so movement on either surfaces as a write-recommendation reason rather than only at progress-note composition time.
- [e:r:i] The two consumer workflows `progress.md` and `resume-project.md` now carry the same bridge block shape. Each consumer gates the disclosure block under `UPLIFT_NOTE.show_seed_migration_pointer` and renders four lines (candidates, breakdown, inspect, write). The contract test [test_seed_operator_consumer_follow_through_contract.py:26](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/tests/test_seed_operator_consumer_follow_through_contract.py) now uses a regex that pins the gate directive line plus the four-line block shape in both workflows; before the harden slice, the consumer test only asserted the single `seed_migration_pointer` line's presence.
- [e:r:i] Specialist wrapper binding is now typed. [test_seed_migration_inventory_follow_through_contract.py:39](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/tests/test_seed_migration_inventory_follow_through_contract.py) asserts the helper-exported `SEED_MIGRATION_SKILL_COMMAND` and `SEED_MIGRATION_WRITE_COMMAND` constants appear in the wrapper SKILL.md. A rename on either end would now fail the test, closing a gap the earlier landing left ambient as prose-match.
- [e:r:i] The held-later trail is now a three-pointer path: [`UPLIFT-HELD-LATER.md:10`](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/UPLIFT-HELD-LATER.md) carries `seed-migration-inventory.md | 92-seed-migration-pointer-bridge-harden-follow-through-implementation.md | 38-seed-migration-pointer-bridge-harden-change-triggered-refresh.md`, so inheritance readers following the breadcrumb now land on the hardened evidence floor rather than the first bridge landing. The live `UPLIFT-MANIFEST.json` `held_later_families` block carries the same three-pointer list typed as JSON.
- [e:r:i] The typed propagation layer now carries explicit harden-side edge kinds. [artifacts/04-propagation-registry-v2-semantic-map.json](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/propagation-audit/artifacts/04-propagation-registry-v2-semantic-map.json) now labels the `project_uplift -> progress` / `project_uplift -> resume-project` edges as `read_only_json_bridge_with_seed_posture_visibility_candidate_breakdown_and_split_specialist_packet_disclosure` and labels the `progress/resume -> seed_migration_inventory_workflow` edges as `surfaces_bounded_specialist_packet_disclosure_with_candidate_breakdown_and_inspect_write_split_when_seed_migration_candidates_are_present`. [artifacts/05-propagation-registry-v2-evidence-index.json](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/propagation-audit/artifacts/05-propagation-registry-v2-evidence-index.json) now carries `seed_migration_pointer_bridge_impl_90`, `seed_migration_bridge_harden_impl_92`, `seed_migration_manifest_shape_fixture`, and `seed_migration_pointer_harden_refresh_38` as distinct evidence rows anchored to the bridge nodes.
- [e:r:i] The evidence floor itself widens. The earlier landing left the non-trivial branch purely synthetic-and-implicit inside helper tests. The harden slice now commits [propagation-audit/artifacts/07-seed-migration-manifest-shape-fixture.json](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/propagation-audit/artifacts/07-seed-migration-manifest-shape-fixture.json) — a frozen mixed-corpus manifest shape covering legacy-unversioned, noncurrent-v1, and current-contract-shape-gap entries — and [test_seed_migration_pointer_bridge_e2e.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/tests/test_seed_migration_pointer_bridge_e2e.py) drives a full chain: synthesize three vintage seeds, run the helper, parse the consumer workflow blocks, substitute the progress-note fields, and compare the specialist inventory output against the fixture. The non-trivial branch now has an exercised chain rather than only spot-tested components.

## Where The Live Hardened Bridge Still Compresses Distinct Jobs

- [d:r:i] The breakdown string `legacy X / noncurrent Y / shape-gap Z` collapses all noncurrent versions into one bucket. Durable memory keeps `noncurrent_version_counts` typed as a dict (for example `{"1": 1}` in the fixture), but the operator surface presents noncurrent as one scalar. A live corpus carrying multiple vintages simultaneously (for example `v1=3, v2=1`) would render as `noncurrent 4` without exposing the asymmetry. This is a deliberate staging choice — the bridge has one noncurrent vintage in view — but the compression lives in the progress-note output without being disclosed as a staging choice anywhere a later reader can see it.
- [d:r:i] The consumer-side gate is a template directive, not a runtime mechanism. The workflow text carries `Only show these lines when UPLIFT_NOTE.show_seed_migration_pointer is true:` as a markdown comment-style instruction, and the block lives inside a fenced `markdown` code block beneath it. Both contract tests assert presence of the gate text plus the block shape; the e2e test substitutes into that block with `show_seed_migration_pointer = true` and checks the rendered lines. No test exercises the suppressed branch (gate directive respected with `false`) against a rendered output. Suppression depends on the operator or downstream agent actually honoring the directive. The harden slice sharpens the contract but leaves the gate's `false` branch covered only by helper-side unit tests (for example `test_write_outputs_and_progress_note_detect_doctrine_change` confirming `show_seed_migration_pointer` is `False` when no corpus exists), not by a consumer-side render assertion.
- [d:r:i] The inspect/write split is now visible as two commands, but the meaning difference (read-deeper-inventory versus write-`.planning/SEED-MIGRATION-REPORT.md`-plus-manifest) lives inside the `--write` flag's consequence, disclosed only inside the specialist workflow and wrapper, not at the bridge surface. The operator reading the progress-note sees two labels (`Seed migration inventory`, `Seed migration write packet`) and two commands; the one-line semantic contrast between `inspect` and `write` is carried by the label text alone. This is legible to an operator who already reads the specialist workflow, and thin for one who does not.
- [d:r:i] The held-later trail now points at three evidence anchors but still labels the family `partially landed`. The label is load-bearing for later inheritance (rewrite remains held), yet the trail itself does not disclose which specific moves remain unlanded. A reader following only the held-later line would know where to read, not what is still owed. The longer trail widens the breadcrumb and leaves the next-move map implicit inside the referenced artifacts.
- [d:r:i] The specialist helper's `build_seed_entry` in [`seed_migration_inventory.py:52`](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/seed_migration_inventory.py) falls back to a stem-split heuristic for `seed_id` when the frontmatter `id` is absent (`path.stem.split("-", 2)[0] + "-" + path.stem.split("-", 2)[1]`). The committed fixture carries explicit frontmatter `id` values on all three entries, so the fallback path is unexercised in the chain test. A real legacy seed without frontmatter `id` would route through that fallback, and a filename not matching `SEED-NNN-slug.md` shape would produce a truncated id. The hardened bridge inherits this helper unchanged; the compression is inside the specialist packet rather than at the bridge, but it rides through.
- [d:r:i] The typed semantic-map edge kinds now carry long composite strings (`surfaces_bounded_specialist_packet_disclosure_with_candidate_breakdown_and_inspect_write_split_when_seed_migration_candidates_are_present`). The typed registry is sharper, and the edge-kind shape is still an ever-growing single label rather than a factored attribute set. A later widening that adds any further disclosure property would further extend the label rather than decomposing into typed facets.

## How The Current No-Seed-Corpus Repo Posture Should Shape Inheritance

- [e:r:i] The live repo still has no `.planning/seeds/` directory. The live `UPLIFT-MANIFEST.json` reports `seed_corpus_posture.posture = "no_seed_corpus"`, `seed_file_count = 0`, `migration_candidate_count = 0`, and `seed_dir_present = false`. The operator-facing bridge block has never fired on this repo under real first-read.
- [d:r:i] What this posture exercises live:
  - The quiet-branch progress-note emission: every `progress-note` invocation returns `show_seed_migration_pointer = false`, and consumers respect the gate on their own. The helper's quiet-branch correctness is actively exercised.
  - The durable uplift-memory shape: `UPLIFT-MANIFEST.json` already carries the typed zero-state `migration_candidate_breakdown` dict (`{"legacy_unversioned": 0, "noncurrent_version": 0, "current_contract_shape_gap": 0}`), so the memory schema exists land-anchored.
  - The held-later breadcrumb's three-pointer path: rendered into `UPLIFT-HELD-LATER.md` and into the manifest's `held_later_families` block right now.
  - The typed propagation registry refresh (artifacts `04`-`07`): land-anchored independent of corpus firing.
- [d:r:i] What this posture leaves unexercised on the live repo:
  - A real first-read rendering of the four-line disclosure block in progress/resume against a non-zero candidate count. The e2e test proves this path via tempdir synthesis; the live repo has never shown the block.
  - Cross-session persistence of the aggregated count across multiple uplift passes when the corpus is present and moving.
  - A real `$gsd-seed-migration-inventory --write` run producing `.planning/SEED-MIGRATION-REPORT.md` plus `.planning/SEED-MIGRATION-MANIFEST.json` as durable artifacts coexisting with `UPLIFT-MANIFEST.json`. Those two durable outputs have never landed live on this repo.
  - The operator-facing handoff: the bridge imagines an operator reading progress, seeing the pointer, running the specialist wrapper, and returning to durable memory. None of that loop has run end-to-end on this repo.
- [d:r:i] What would sharpen later confidence: the first real corpus appearance (even one legacy seed) would activate the quiet-to-non-trivial transition live; a `--write` run would prove the two durable outputs coexist cleanly with uplift memory; and cross-session drift from `no_seed_corpus` to populated-corpus would prove the drift-detection path routes the progress-note through to a `--write` recommendation as the test `test_progress_note_recommends_write_after_seed_corpus_movement` asserts synthetically.
- [d:r:i] What stays land-anchored and not dependent on corpus firing: the helper constants, the durable-memory schema, the consumer-workflow block shape, the specialist wrapper binding, the held-later breadcrumb's fuller path, the typed propagation refresh, and the committed fixture. The harden slice moves these from proposal to durable artifact even while the non-trivial branch sits waiting for a live corpus.

## Neighboring Carriers That Still Deserve Sharpening

- [d:r:i] The consumer-gate render shape deserves bounded sharpening. The gate currently lives only as a template directive plus tests asserting directive presence. A small shared render helper that takes `progress-note` JSON and emits the gated block (with real suppression when the show flag is false) would move the gate from contract-by-convention to code-carried behavior and let consumer widening inherit one mechanism rather than five workflow copies. This is not entry-wrapper widening; it is a bounded render carrier under the existing bridge.
- [d:r:i] The specialist helper's `seed_id` fallback heuristic inside `build_seed_entry` deserves a narrow sharpening before a real legacy corpus lands. One additional fixture row covering a frontmatter-id-missing legacy file (and an accompanying parametrization in the chain test) would drive the fallback path without opening rewrite. The harden slice added a fixture; this is one fixture row more, not a family move.
- [d:r:i] The operator-facing breakdown line deserves a narrow sharpening against multi-vintage noncurrent posture. Either (a) render per-version noncurrent counts when more than one vintage is present, or (b) annotate the current single-bucket form as a documented staging choice at the progress-note layer rather than leaving the compression implicit. Both routes are inside the current bridge footprint.
- [d:r:i] The specialist `seed-migration-inventory.md` workflow has no back-pointer to the bridge or to `UPLIFT-HELD-LATER.md`. An operator arriving via the progress/resume pointer currently lands in the specialist workflow without a one-line route-back trail, and an operator reading the workflow directly does not see how it surfaces through routine re-entry. A single pointer line in each direction would tighten operator legibility without widening the specialist surface.
- [d:r:i] The typed semantic-map edge kinds now carry composite string labels. A narrow later refresh could factor those kinds into typed attribute sets (`gating: conditional`, `fields: [count, breakdown, inspect, write]`, `disclosure: bounded`) rather than extending the single-string labels further. This is registry-shape work inside the propagation family, not a new family move.

## Adjacent Route To Inherit Next

- [d:r:i] Further harden passes on the seed-migration bridge itself would now carry diminishing live yield against the current repo. The bridge has helper constants, consumer-workflow block shape, specialist wrapper binding, durable-memory preservation, held-later breadcrumb, typed propagation refresh, fixture, and e2e chain test. The remaining pressures are either (a) inside neighboring carriers (render gate, breakdown granularity, helper id-fallback, registry shape), or (b) outside the seed family.
- [d:r:i] Inside the seed family, the two still-open widening surfaces are both explicitly held in the disposition:
  - Generic entry-wrapper sweep beyond `progress`/`resume-project` — held until later consumer edges are typed explicitly rather than assumed by analogy; the current refresh carries two consumer edges.
  - Broader audit-open widening beyond the landed milestone-close row — held.
- [d:r:i] The cleanest next inheritance sits in a different family altogether: [intervention-proposals/93-uplift-agent-assist-and-propagation-baseline-split-note.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/intervention-proposals/93-uplift-agent-assist-and-propagation-baseline-split-note.md) already names an adjacent family — bounded uplift agent-assist plus a cleaner upstream-baseline-versus-repo-local-delta propagation split. That open-family note drains different pressure: it moves the refresh frontier away from repeated seed-family harden-without-corpus toward a separate uplift-family question (agent-assist) and a separate propagation-registry shape question (baseline/delta split). Inheriting that family next keeps seed-family harden paused where real corpus appearance would actually sharpen it, while the propagation-baseline split sharpens the registry layer that already carries this bridge.
- [d:r:i] An explicit second-choice adjacent route, if the next inheritance should stay inside the seed/propagation cluster, is a narrow render-gate carrier inside the bridge footprint (see Neighboring Carriers) rather than a new family. That is inheritance-shaped work, not family-shaped work, and is appropriate as a preamble to any later consumer widening.
- [d:r:i] Rewrite or normalization automation stays outside the next move. The disposition holds it until the current bridge has real corpus exercise behind it, and the no-seed-corpus posture does not meet that bar.

## How This Hardened Bridge Should Be Inherited

### Carry Forward

- [d:c+i] The helper-side triad in [`progress_note_seed_fields`](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/project_uplift.py:785): aggregated candidate count, compact migration breakdown string, split inspect/write pointers. These are the operator-facing specialist-packet contract and should inherit as is.
- [d:c+i] The consumer-workflow block shape — four gated lines under `UPLIFT_NOTE.show_seed_migration_pointer` — as the canonical render contract for both `progress.md` and `resume-project.md`, pinned by the regex in the seed operator-consumer follow-through contract test.
- [d:c+i] Durable uplift-memory preservation of `migration_candidate_count` and `migration_candidate_breakdown` inside `seed_corpus_posture`, tracked by `seed_corpus_drift_reasons`, so drift surfaces through the `--write` recommendation path rather than reconstructed at progress-note time.
- [d:c+i] The helper-exported `SEED_MIGRATION_SKILL_COMMAND` and `SEED_MIGRATION_WRITE_COMMAND` constants as the typed binding between the helper and the specialist wrapper contract, asserted live by `test_project_uplift_pointer_commands_stay_bound_to_specialist_skill`.
- [d:c+i] The committed `propagation-audit/artifacts/07-seed-migration-manifest-shape-fixture.json` plus the `test_seed_migration_pointer_bridge_e2e.py` chain test as the bridge's exercised evidence floor for the non-trivial branch.
- [d:c+i] The widened held-later breadcrumb path (`seed-migration-inventory.md | 92 | 38`) as the inheritance anchor, carried in `UPLIFT-HELD-LATER.md` and mirrored in the manifest's typed `held_later_families` block.
- [d:c+i] The typed v2 refreshes named in `artifacts/04`-`07` plus lane `38`, including the harden-side edge-kind labels on the bridge nodes and the four new evidence rows (`seed_migration_pointer_bridge_impl_90`, `seed_migration_bridge_harden_impl_92`, `seed_migration_manifest_shape_fixture`, `seed_migration_pointer_harden_refresh_38`).

### Revise Before Widening

- [d:c+i] The breakdown string compression. Before the bridge inherits onto additional consumer surfaces or meets a real multi-vintage corpus, either render per-noncurrent-version counts when more than one vintage is present, or annotate the single-bucket form as a disclosed staging choice on the progress-note layer.
- [d:c+i] The consumer-side gate render. Before further consumer widening, add either a narrow render helper that consumes `progress-note` JSON and produces the gated block with real suppression on `false`, or a dedicated test that confirms the block is absent in a rendered output when the helper says the gate is off. The current contract is directive-plus-test-presence rather than directive-plus-test-executed-suppression.
- [d:c+i] The specialist helper's `seed_id` fallback. Before a real legacy corpus arrives, one additional fixture row covering a frontmatter-id-missing legacy seed (and a narrow parametrization) would exercise the stem-split heuristic under realistic legacy shape. This stays inside the specialist packet, not the bridge.
- [d:c+i] The specialist workflow's back-pointer to the bridge and the `UPLIFT-HELD-LATER.md` anchor. A one-line pointer in each direction would sharpen operator legibility for the loop the bridge imagines without widening the specialist surface.

### Hold For Later

- [d:c+i] Rewrite or normalization automation for actual seed files. Held until the current bridge has real corpus exercise behind it; the no-seed-corpus posture does not meet that bar.
- [d:c+i] Generic entry-wrapper widening beyond `progress`/`resume-project`. Held until later consumer edges are typed explicitly rather than assumed by analogy from two edges.
- [d:c+i] Broader audit-open consumer widening beyond the already-landed milestone-close seed row. Held.
- [d:c+i] Cross-session disclosure trace (observability of when the bridge surfaced and what the operator did) — later observability family, not this bridge.
- [d:c+i] Typed-registry edge-kind factoring (from long composite strings into typed attribute sets). Held as a later propagation-family registry-shape refresh rather than an immediate next move.
- [d:c+i] Later bounded uplift agent-assist and the cleaner upstream-baseline-versus-repo-local-delta propagation split (carried in `intervention-proposals/93`). Named as the adjacent-family inheritance candidate but kept explicit as a separate family rather than absorbed into this bridge.

## Live Open Questions

- [o:r:i] When a real seed corpus first lands on this repo, does the bridge's quiet-to-non-trivial transition surface cleanly in live first-read, or does the template-directive gate leak the block under one of the consumer paths?
- [o:r:i] After a real `$gsd-seed-migration-inventory --write` run, do `.planning/SEED-MIGRATION-REPORT.md` and `.planning/SEED-MIGRATION-MANIFEST.json` coexist with `UPLIFT-MANIFEST.json` in a way that sharpens or compresses the operator's two-durable-output picture?
- [o:r:i] Does the next inheritance move widen into the propagation-baseline-split family (`93` note) before further seed-family harden, or does bounded render-gate carrier work come first as a preamble inside the current bridge footprint?
- [o:r:i] Should the typed semantic-map edge kinds be refactored into typed attribute sets before or after the next consumer edge inherits — and does the answer change if that edge is an entry-wrapper widening versus a render helper?
