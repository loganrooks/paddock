Date: 2026-04-21
Status: bounded external reread output (Opus 4.7 max, r1)

# Propagation Registry System — Opus 4.7 Max R1

## How The Registry System Should Be Layered

- [d:r:i] The registry family currently reads as one hybrid surface (`14`) with one curated JSON slice (`13`, `artifacts/01-propagation-field-registry-v1.json`). That single surface does real work, and the widening it earns is not another flat artifact but a typed stack where each layer carries a distinct epistemic claim and refuses the claims that belong to another layer.
- [d:r:i] The stronger layered shape is five layers plus three gates:
  - **L0 Inventory roster** — maintained-doc upstream carry ([/home/rookslog/workspace/projects/get-shit-done-upstream/docs/INVENTORY.md](/home/rookslog/workspace/projects/get-shit-done-upstream/docs/INVENTORY.md:1)) and repo-local companion docs. Claim shape: *this shipped surface exists at this path under this family-slot at this version pin*. This layer does not reason about propagation.
  - **L1 Declared contracts** — authored declarations with machine-validated typing. [OVERLAY-MANIFEST.json](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json:1) plus [portable_gsd_contract.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/portable_gsd_contract.py:40) is the prototype: authored ownership/typing on one side, deterministic validation/verification on the other. Claim shape: *this path has this declared role under this contract, and the contract is internally coherent today*.
  - **L2 AI-authored semantic map** — families, sub-family modes, cross-family edges, change-shape propagation obligations, named invariants, held-pressure roster. This is the layer that `08–13` already populate in prose and that `v1` currently compresses into JSON. Claim shape: *these carriers belong together as one family under this propagation mode; these edges cross families; these obligations fire when this change shape appears; these pressures are explicitly held rather than absorbed*.
  - **L3 Generated evidence** — runtime snapshots, fingerprints, manifest outputs, coherence reports, launch-truth captures, threshold-scan outputs. [project_uplift.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/project_uplift.py:1), [runtime_visibility.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/runtime_visibility.py:1), [portable_gsd_contract.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/portable_gsd_contract.py:137), `manifest_install_coherence.py`, `capture_launch_truth.py`, `scan_threshold_language.py`. Claim shape: *under this basis commit, the state or fingerprint of this carrier or cross-surface alignment was observed this way*.
  - **L4 Operator / audit tooling** — the later tooling the registry enables: registry-snapshot diff, impact-adjacent reread suggestion, coverage parity against L0, lane-scoped slicing for audits. None of this exists yet; naming the layer gives the family a place to refuse to conflate it with L2 authorship.
- [d:r:i] The three gates sitting across the stack:
  - **G1 Coverage gate** — every L0 roster entry either lands in at least one L2 family/sub-family slot or is explicitly named as out-of-scope with a recorded reason. Silent omission is the failure shape.
  - **G2 Contract-coherence gate** — every L1 declared contract has a passing L3 validation output recorded at the basis commit. Unvalidated L1 is a shape that hides drift rather than carries it.
  - **G3 Refresh-inheritance gate** — every registry version records basis commit, source-note set, inherited dispositions, and the named held-pressures the refresh deliberately did not resolve.
- [e:c+r:i] The v1 artifact already performs L2 work (families, carriers, edges) and references its basis commit and source notes, which is the `G3` shape in partial form. Source: [artifacts/01-propagation-field-registry-v1.json](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/propagation-audit/artifacts/01-propagation-field-registry-v1.json:1). The widening is not to replace that surface but to type it so L0 entries, L1 entries, and L3 pointers no longer share one cell.
- [d:r:i] Read in layer terms, the registry family currently compresses L0, L2, and L3 into a single curated JSON plus a prose body. That compression lets one artifact ship, and it also blurs which claim is inventory-backed, which claim is AI-judged, which claim is machine-validated, and which claim is evidence-pointing. The layered shape preserves all four at once.

## What Maintained Docs Should Seed

- [e:c+i] Upstream `docs/INVENTORY.md` declares itself the authoritative roster of shipped surfaces, enumerates every family (agents, commands, workflows, references, CLI modules, hooks) at a version pin, and flags that new surfaces must land there first before broader docs carry them. Sources: [/home/rookslog/workspace/projects/get-shit-done-upstream/docs/INVENTORY.md](/home/rookslog/workspace/projects/get-shit-done-upstream/docs/INVENTORY.md:3), [/home/rookslog/workspace/projects/get-shit-done-upstream/docs/INVENTORY.md](/home/rookslog/workspace/projects/get-shit-done-upstream/docs/INVENTORY.md:9).
- [e:c+i] The drift-guard tests in the upstream tree (`tests/inventory-counts.test.cjs`, `tests/commands-doc-parity.test.cjs`, `tests/agents-doc-parity.test.cjs`, `tests/cli-modules-doc-parity.test.cjs`, `tests/hooks-doc-parity.test.cjs`, `tests/architecture-counts.test.cjs`, `tests/command-count-sync.test.cjs`) anchor roster claims against the filesystem. Source: [/home/rookslog/workspace/projects/get-shit-done-upstream/docs/INVENTORY.md](/home/rookslog/workspace/projects/get-shit-done-upstream/docs/INVENTORY.md:9).
- [d:r:i] The current `14` framing treats INVENTORY as a seed input to a refresh cycle. That framing carries one kind of contribution (one-shot discovery for the next refresh) and compresses two stronger ones:
  - **Coverage contract.** INVENTORY declares a complete roster. A propagation registry that silently omits a roster entry is claiming the entry does not matter for propagation — which is a semantic claim the registry owes in explicit form. Seeding does not require justification of silence; coverage does. The registry's stronger relation to INVENTORY is *any roster entry that is not in the semantic map must appear as `held-with-reason` or `out-of-scope-with-reason`*.
  - **Family-slot candidate set.** INVENTORY already clusters shipped surfaces into six family slots (agents, commands, workflows, references, CLI modules, hooks). The propagation registry's L2 families are not the same slots — they are propagation-shape families, not surface-kind families — but the mapping between the two slot systems is itself a semantic claim worth recording. For example, `commands/gsd/*.md` and `get-shit-done/workflows/*.md` both land inside L2's workflow-output family; `agents/gsd-*.md` and `.codex/agents/*.toml` split across L2's doctrine and registry families. Naming that cross-mapping is a stronger ownership than using INVENTORY only as a discovery prompt.
- [d:r:i] Local maintained docs should then seed complementary claims, each with its own ownership shape:
  - [tooling/codex/README.md](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/README.md:54) owns the **helper cohort roster** — the eight named helpers with their producer/consumer description. The registry's helper family should cite this document as its roster source and record a coverage claim (every helper named here appears in L2 or is explicitly held).
  - [tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json:1) owns the **tracked-overlay roster** and typing — 51 entries with `add`/`overwrite` declarations. The registry's install_overlay family should cite this artifact and should not re-author its typing.
  - Root [AGENTS.md](/home/rookslog/workspace/projects/prix-guesser/AGENTS.md:49) and [.planning/AGENTS.md](/home/rookslog/workspace/projects/prix-guesser/.planning/AGENTS.md:137) own the **propagation-doctrine language** — five carrier kinds, three-surface invariant, contract-carry hygiene. The registry's doctrine family should cite these surfaces and treat changes to them as L2 revision triggers rather than L3 re-snapshot triggers.
  - Audit governance docs (`CURRENT-STATE.md`, `STATUS.md`, `ARTIFACT-INVENTORY.md`, `GOVERNANCE-READING-AND-UPDATE-PROTOCOL.md`) own the **audit-lane scoping** — which lanes are open, which are inherited, which held-later roster entries became partially-landed. The registry's governance family should cite these.
- [d:r:i] The registry should therefore not rely on INVENTORY alone for seeding. It should declare, per L2 family row, which maintained doc owns the roster for that family. That makes the registry a joined view over several authored rosters rather than a parallel copy, and it lets coverage tooling check each family against its specific roster source instead of against one global seed.
- [d:r:i] Where no maintained doc yet owns a roster (e.g., the launch-truth artifact family, the compact-prompt carrier family, the held-later registry family), the registry itself is currently the de facto owner. That is not a failure of the registry — it is a signal that an authored roster surface is missing for those families, and a later slice could lift them into local maintained docs that the registry then cites back.

## What Should Remain AI-Authored Semantic Mapping

- [d:r:i] Semantic mapping work is the set of decisions that a determinate extractor cannot make because the answer depends on interpretation of propagation mode, not on file existence. Specifically:
  - **Family membership.** Whether `.codex/config.toml` belongs to the registry family, the install_overlay family, or both through a cross-family edge is a reading of how policy propagates across surfaces, not a string match. `12` already treats this edge explicitly as `Install Outputs -> Helper Inputs`. Source: [propagation-audit/12-cross-family-edge-supplement.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/propagation-audit/12-cross-family-edge-supplement.md:13).
  - **Sub-family mode split.** `09` split the workflow family into four sub-rows (workflow->output, template->shape, reference->behavior, lib->runtime) because those carriers impose different propagation duties even when they appear in the same backup-meta bundle. That split is a reading call. Source: [propagation-audit/09-sharpened-propagation-field-split.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/propagation-audit/09-sharpened-propagation-field-split.md:57).
  - **Edge directionality and kind.** `12` names directed edges (`portable_contract -> project_uplift`), bidirectional edges (governance pressure in both directions), and edge kinds (`materialized_runtime_sampled_by_helper`, `repo_doctrine_pressure`, `workspace_queue_records_lane_force`). Kind naming is interpretive; a graph extractor could find that two nodes mention each other, but not what kind of propagation link they carry.
  - **Named invariants.** The model-policy three-surface invariant in `10` is a rule that emerged from reading three surfaces together (root doctrine, `QUALITY_REASONING`, materialized agent `.toml` files). The invariant itself is not in any of the three. Source: [propagation-audit/10-model-policy-three-surface-invariant.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/propagation-audit/10-model-policy-three-surface-invariant.md:20).
  - **Held-pressure naming.** Which families to hold rather than absorb (`required-reading installation practice`, `cross-runtime uplift composition`, `upstream-template drift machinery`, `aged-bespoke deep merge`, `audit-subtree aging carry`, `forensics / archived-milestone integration`, `workstream parent/child posture reconciliation`) is a judgment about which pressures the current slice would over-compress. Source: [.planning/UPLIFT-MANIFEST.json](/home/rookslog/workspace/projects/prix-guesser/.planning/UPLIFT-MANIFEST.json:26).
  - **Change-shape propagation obligations.** `08`'s per-family reread lists (`If A Governing Doctrine Surface Changes`, `If A Runtime Install / Overlay Surface Changes`, etc.) are rules about what a later reader should check. Source: [propagation-audit/08-broader-network-producer-consumer-and-carrier-map.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/propagation-audit/08-broader-network-producer-consumer-and-carrier-map.md:56). These are semantic because they express what the propagation family believes about adjacency, not what exists on disk.
- [d:r:i] Carrier annotation — the `role` and `path` fields inside an L2 carrier entry — is a partially different duty. Path is machine-findable (from L0 or L1); role is AI-authored but more routine than family membership or edge naming. Treating carrier annotation and family-graph semantics as one blended AI task over-simplifies both. The stronger AI-authored split is:
  - **Semantic mapping** (family membership, sub-family modes, edge directionality/kind, invariants, held-pressures, obligations) — refuses full automation, refuses full hand-memory, prefers explicit inheritance across refreshes.
  - **Carrier annotation** (role label, propagation mode, cited source doc) — tolerates scaffolding from determinate tools (e.g., "all agent `.toml` files auto-populate as carriers in the registry family with role template 'materialized runtime registry contract'") but still requires reading to confirm the mode.
- [d:r:i] The semantic layer should additionally own two surfaces that `14` does not yet name explicitly:
  - **Out-of-scope justification.** For each L0 roster entry the registry does not map, the semantic layer owes a reason. `UPLIFT-HELD-LATER.md` is the prior art for this shape and should be generalized into the registry itself rather than only living next to it. Source: [tooling/codex/README.md](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/README.md:100).
  - **Cross-refresh inheritance decisions.** Which disposition from the previous registry version carries forward, which is revised, which is explicitly held — the same `Carry Forward / Revise / Hold Explicit` shape used in the lane-inheritance files.

## What Determinate Tooling Can Legitimately Contribute

- [d:r:i] Determinate tooling in this family should make narrow claims and refuse broader ones. A compact way to state the contract per tool kind:
  - **Roster-existence checks.** Given L0 (e.g., upstream INVENTORY) and the L2 semantic map, emit the set of shipped surfaces not yet mentioned in L2, and the set of L2 carriers pointing at paths that no longer exist. Narrow claim: *this name or path is / is not accounted for*. Refusal: *cannot decide whether a missing entry is intentional*.
  - **Schema / contract validation.** Given an L1 declared contract (`OVERLAY-MANIFEST.json`, `UPLIFT-MANIFEST.json` schema, future L2 schema), report invalid modes, missing backings, type mismatches. `portable_gsd_contract.py` already does this for the overlay contract. Source: [tooling/codex/portable_gsd_contract.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/portable_gsd_contract.py:137). Narrow claim: *the declared contract is internally consistent today*. Refusal: *cannot decide whether the declared contract is semantically right*.
  - **Fingerprinting and drift detection.** Normalized content hashes per carrier (`content_sha256`, `frontmatter_hash`, `normalized_toml_hash`, `marker_block_hash`, `inventory_item_hash` already exist in `project_uplift.py`). Source: [tooling/codex/project_uplift.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/project_uplift.py:48). Narrow claim: *this carrier's fingerprint changed between basis A and basis B*. Refusal: *cannot decide whether the change should trigger family rework or a smaller local revise*.
  - **Cross-surface coherence.** Given several declaration/output surfaces (updater-boundary truth, backup-carried-subset truth, frozen runtime snapshot), report whether they still agree. `manifest_install_coherence.py` already performs this across `gsd-file-manifest.json`, `backup-meta.json`, and a runtime snapshot. Source: [tooling/codex/README.md](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/README.md:74). Narrow claim: *the three surfaces are / are not in contradiction under this scope*. Refusal: *cannot decide whether a contradiction is a real drift or a deliberate transitional state*.
  - **Materialization verification.** After install/apply, check that declared overlay carriers materialize into live targets and that install-mutation targets carry the expected local edits. `portable_gsd_contract.py`'s `verify-materialized` path is this shape. Narrow claim: *the materialization step produced / did not produce the declared outputs*. Refusal: *cannot decide whether the declared outputs are the right outputs*.
  - **Registry-snapshot diff.** Given two registry snapshots at basis A and basis B, emit added/removed families, added/removed carriers, added/removed edges. This tool does not exist yet. Narrow claim: *the L2 graph changed this way between refreshes*. Refusal: *cannot decide whether any deletion was a loss of coverage or an intentional absorption*.
  - **Launch-truth capture.** `capture_launch_truth.py` records requested-vs-effective launch state per lane. Source: [tooling/codex/README.md](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/README.md:65). Narrow claim: *a specific spawn received these settings*. Refusal: *cannot decide whether that matches registry-declared policy* — that comparison is the registry-to-launch-truth edge (`12`) and is semantic.
  - **Threshold-language scanning.** `scan_threshold_language.py` flags heuristic hits against forbidden framing. Source: [tooling/codex/README.md](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/README.md:80). Narrow claim: *this string pattern appears in this file at this line*. Refusal: *cannot certify a surface is framing-clean, only that no heuristic hit surfaced*.
- [d:r:i] In every case the narrow claim / refusal pair is the epistemic boundary the tool must carry, because the registry family is where semantic compression is done and those tools are not the semantic compressors.
- [d:r:i] A concrete consequence: determinate tools can populate many of the L0 and L3 cells in the layered registry without being asked to make L2 claims. Example — an auto-generated `carrier_candidates.json` listing every path under the tracked overlay, the helper cohort, the agent `.toml` registry, and the state-continuity writers would reduce the AI authorship burden at L2 to *edge and mode* rather than *path discovery plus edge and mode*.

## How The Current Runtime/Tooling-Evidence Idea Should Be Split Or Clarified

- [d:r:i] The `14` framing names `runtime/tooling evidence` as one of three classes (alongside AI-authored mapping and hybrid contract). That one-phrase grouping compresses at least seven sources that do different epistemic jobs, each with a different relation to the registry:
  - **Material-truth snapshots.** `runtime_visibility.py` reports live-vs-overlay classifications across five high-leverage surface families with stable subclassification fields. Source: [tooling/codex/runtime_visibility.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/runtime_visibility.py:44). What it can back: *a carrier exists in this classification bucket (intentional materialized carry / repo-local config carry / selective overlay boundary / obsolete live residue / unknown live drift)*. What it cannot back: family membership or edge kind.
  - **Declared-contract validation outputs.** `portable_gsd_contract.py validate-manifest` and `verify-materialized` emit structured reports against the overlay contract. Source: [tooling/codex/portable_gsd_contract.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/portable_gsd_contract.py:137). What it can back: *the overlay contract is internally coherent and materialization ran*. What it cannot back: whether the declared overlay typing is semantically right for a given carrier.
  - **Materialization traces.** The installer script `scripts/setup-portable-gsd.sh` plus post-apply verify provides a real trace from upstream pristine to live repo-local state. What it can back: *the install pipeline produced these live files*. What it cannot back: whether those files still fit their declared family/mode.
  - **Helper sample-and-fingerprint outputs.** `project_uplift.py` writes `.planning/UPLIFT-REPORT.md`, `.planning/UPLIFT-MANIFEST.json`, and the `## Project Uplift` STATE section with typed carriers, fingerprints, secondary signals, phase-boundary signals, and held-later roster. Source: [tooling/codex/project_uplift.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/project_uplift.py:91). What it can back: *a curated representative carrier cohort was sampled at this basis with these fingerprints and this held-later roster*. What it cannot back: *that cohort is the full L2 carrier set* — the cohort is deliberately selective.
  - **Cross-surface coherence outputs.** `manifest_install_coherence.py` compares updater-boundary truth (`gsd-file-manifest.json`), backup-carried-subset truth (`backup-meta.json`), and a frozen runtime-visibility snapshot. Source: [tooling/codex/README.md](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/README.md:74). What it can back: *three install-side surfaces are / are not in contradiction under this scope*. What it cannot back: cross-family edges (e.g., registry to launch-truth, helper to state-continuity) that live outside its scope.
  - **Launch-truth capture.** `capture_launch_truth.py` plus per-lane `launch-truth/` dirs plus `LAUNCH-LEDGER.md`. Source: [tooling/codex/README.md](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/README.md:65). What it can back: *a spawn received these exact settings*. What it cannot back: whether the declared registry policy should have produced them.
  - **Heuristic scan outputs.** `scan_threshold_language.py`, `audit_refmap.py verify`. Source: [tooling/codex/README.md](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/README.md:80). What they can back: *the specified heuristic did / did not hit*. What they cannot back: that the artifact is framing-clean or reference-clean as a whole.
- [d:r:i] `14`'s grouping treats these seven sources as one layer supplying `live runtime evidence`. That framing reads the outputs as one block of truth, which flattens two distinctions that do real propagation work:
  - **Scope of the evidence.** Some outputs are whole-surface (runtime-visibility across five families), some are slice-scoped (manifest coherence over selected lanes), some are single-surface (individual agent `.toml` fingerprints). Collapsing them hides when a registry L3 cell is backed by a partial rather than whole claim.
  - **Ownership shape.** Some outputs back L1 declared contracts (overlay validation); some back L2 sampled carriers (uplift manifest); some back L3 cross-surface alignment (coherence); some back lane-scoped launch observations. A registry that points `runtime/tooling evidence` at all of them without typing conflates contract coherence with carrier sampling with launch observation.
- [d:r:i] The stronger split is therefore into four sub-layers:
  - **L3a Contract-validation evidence** — reports from `portable_gsd_contract.py` and future contract validators. Backs L1 claims about declared-contract coherence.
  - **L3b Carrier-sample evidence** — reports from `project_uplift.py` with fingerprints and representative-cohort scope. Backs L2 claims about per-carrier fingerprint state and held-pressure roster.
  - **L3c Cross-surface coherence evidence** — reports from `manifest_install_coherence.py` and any later multi-surface tools. Backs L2 invariant claims (like the model-policy three-surface invariant) but explicitly scoped.
  - **L3d Capture/observation evidence** — launch-truth captures, threshold scans, reference-graph verify outputs. Back audit-lane claims (G3 refresh-inheritance) more than registry-wide claims.
- [d:r:i] With that split, `runtime/tooling evidence` stops being a single block and becomes a typed support column for the registry: each L2 entry can declare *which sub-layer of evidence currently backs this row*, and silence in one column (e.g., no carrier-sample evidence for the helper family's smaller helpers) becomes explicit rather than ambient.
- [d:r:i] One further clarification the current framing blurs: **machine-validated declared contracts** are not the same class as generated evidence. `14` names `OVERLAY-MANIFEST.json` inside the Hybrid Contract subsection but keeps `UPLIFT-MANIFEST.json`, runtime-visibility, and overlay validation inside the Code-Generated Evidence subsection. The stronger typing is *declared contracts are L1 regardless of who validates them*, and *validation outputs are L3a regardless of which contract they target*. That keeps `OVERLAY-MANIFEST.json` in its own layer instead of sharing a row with `UPLIFT-MANIFEST.json`, which is a different shape (sampled fingerprints with held-pressure roster, not a typed contract).

## What The Current Local Direction Still Flattens

- [d:r:i] **AI authorship is treated as one role.** `14` names `AI-authored` for the `families`, `carriers`, and `edges` of the registry. That phrasing compresses two roles that the layered view separates:
  - **Semantic mapping** (family membership, sub-mode split, edge direction/kind, invariants, held-pressure naming, change-shape obligations) — interpretive work where determinate tools can refuse to participate.
  - **Carrier annotation** (role labels, family slots, source-doc citation) — routine work that determinate tools can scaffold without usurping semantic decisions.
  A registry policy that calls both `AI-authored` without distinction understates how much of L2 can be scaffolded and how little of L2 can be delegated.
- [d:r:i] **Maintained-doc seeding is compressed to one relation.** `14` treats INVENTORY as a seed input. The stronger relations are coverage-contract ownership and family-slot mapping (see `What Maintained Docs Should Seed` above). A refresh that uses INVENTORY only as a seed loses the coverage signal that the roster entry was shipped and the registry remains silent about its fate.
- [d:r:i] **Runtime/tooling evidence is one bucket.** The seven sources above do seven epistemic jobs. Grouping them flattens when a registry row is backed by a whole-surface snapshot versus a lane-scoped observation, and when it is backed by a declared contract validation versus a sampled carrier fingerprint.
- [d:r:i] **Refresh is framed as one pipeline.** `14`'s seeding order (inventory -> broad docs -> runtime evidence -> prose audit family) reads as one top-to-bottom chain. The layered view distinguishes three refresh kinds that the current direction does not separate:
  - **Scheduled whole-registry refresh** — periodic regeneration at a new basis commit, consuming all four seed sources.
  - **Change-triggered slice refresh** — a specific L2 family or L1 contract moved, so the registry re-derives only that slice.
  - **Lane-scoped refresh** — a challenge lane needs a registry view at its own basis without committing to a new global version.
  Conflating these hides that scheduled refresh owes coverage re-verification, slice refresh owes edge revalidation, and lane-scoped refresh owes scope restriction so it does not silently overwrite global state.
- [d:r:i] **No explicit refusal surface.** `14`'s current direction does not name what the registry refuses to carry. Upstream-template drift detection, cross-runtime composition, aged-bespoke merge, whole-harness auto-extraction, and operator impact-suggestion are all load-bearing refusals today. Naming them as refusals (rather than gaps) clarifies the registry's boundary and preserves the held-later pressure instead of letting it ambient-drift into later refreshes.
- [d:r:i] **No stated coverage relation to `docs/INVENTORY.md`.** Because INVENTORY is authored as the authoritative shipped-surface roster, the propagation registry owes it a *mapped / held / out-of-scope* disposition for every entry. `14` says "seed from inventory" but does not yet say "a registry that omits a shipped surface silently is under-carrying the roster." That unstated relation is part of what lets later refreshes grow stale without the staleness being visible.
- [d:r:i] **The `v1` artifact carries load-bearing structure but is flat.** `artifacts/01-propagation-field-registry-v1.json` packs L0 path claims, L1 contract references, L2 semantic claims, and L3 implicit evidence in one object per carrier. That flat shape makes L2 revision entangled with L0 path updates (a carrier renaming or moving triggers a semantic-map rewrite) and hides which column of the layered stack actually moved. Source: [artifacts/01-propagation-field-registry-v1.json](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/propagation-audit/artifacts/01-propagation-field-registry-v1.json:1).
- [d:r:i] **No support-mode column per carrier.** `14` and `13` do not yet say, per carrier row, which lower layer currently backs the claim. Adding a support-mode column (`l0_roster_cited`, `l1_contract_validated`, `l3a_coherence_passing`, `l3b_fingerprinted_at_basis`, `l3c_coherence_scope`, `l3d_lane_captured`, `prose_audit_only`) gives later readers the same epistemic visibility that `[e:c:i]` gives for prose claims.
- [d:r:i] **Cross-refresh inheritance is unframed.** Lane inheritance files already carry `Carry Forward / Revise / Hold Explicit` across lanes. The registry has no analogue for refresh-to-refresh inheritance. `14`'s seeding order talks about what to read next, not about how to inherit last-refresh's held-pressures, deliberate absorptions, and known limits.

## Stronger Design For The Propagation Registry Family

- [d:r:i] A stronger system design would carry the five layers, the three gates, and two small governing rules, and would represent the registry as a small bundle rather than one flat JSON. Concretely:
  - **`registry/v2/inventory-roster.json` (L0).** A coverage-oriented roster that cites `docs/INVENTORY.md` at its version pin, plus the repo-local rosters from `tooling/codex/README.md`, `OVERLAY-MANIFEST.json`, root/planning `AGENTS.md`, and workspace governance docs. Each entry has `source_doc`, `surface_family_slot`, `path`, `version_pin`. No semantic mapping lives here.
  - **`registry/v2/declared-contracts.json` (L1).** A typed index of authored contracts and their validators: `OVERLAY-MANIFEST.json` validated by `portable_gsd_contract.py`, the `UPLIFT-MANIFEST.json` schema validated by a future helper, any future declared contracts. Each entry has `contract_path`, `validator_path`, `last_valid_at_basis_commit`.
  - **`registry/v2/semantic-map.json` (L2).** Families, sub-family modes, carriers, edges, invariants, change-shape obligations, held-pressures, out-of-scope justifications, inheritance-from-v1. Each carrier row carries a `support_mode` column naming which lower-layer evidence backs it. This is the AI-authored layer.
  - **`registry/v2/evidence-index.json` (L3).** Pointers to the outputs: latest uplift manifest, latest runtime-visibility snapshot, latest overlay-contract validation, latest coherence report, per-lane launch-truth paths, latest scan outputs. Does not duplicate the outputs; indexes them at their basis commits.
  - **`registry/v2/coverage-and-refresh.json` (G1+G3).** One file that records, at the current basis, which L0 roster entries are `mapped`, `held-with-reason`, or `out-of-scope-with-reason`; the set of inherited dispositions from v1; and the named held-pressures this refresh does not resolve. This is the refresh-inheritance surface.
- [d:r:i] Two governing rules hold the bundle together:
  - **Layer-specific refresh discipline.** L0 is refreshed when maintained docs (INVENTORY, tooling README, overlay manifest) move. L1 is refreshed when declared-contract validators emit changes. L3 is refreshed when basis commits change or when a specific evidence tool is re-run. L2 is refreshed as the result of reading at least one of the above plus a prose reread, and requires explicit inheritance from the prior L2. G1 and G2 fire on every refresh; G3 fires at L2 refresh.
  - **Coverage-before-completeness.** A registry is considered usable when L0 coverage is explicit (every roster entry has a G1 disposition) and L1 contracts have passing L3a evidence. Silent omissions in either surface are a structural concern, even when the semantic map looks complete.
- [d:r:i] This shape preserves what `14` already earned — hybrid framing, curated slice discipline, AI-authored semantic work — and extends it by giving each part of the hybrid its own artifact, its own refresh trigger, and its own refusal surface. It does not yet introduce whole-harness auto-extraction (still held-later), upstream-template drift machinery (still held-later under `11`), cross-runtime composition (still held-later), or registry-to-impact suggestion tooling (still L4 and not opened).
- [d:r:i] The prose family (`08–14`) remains the interpretive layer and is strengthened by the layering rather than replaced. `08` stays the top-level field view; `09` stays the sub-family split; `10`–`12` stay the named invariants/edges; `13` stays the first-slice discipline note; `14` stays the generation and seeding policy and gains a `registry classes` section that names L0–L4 and the gates, replacing the current three-class compression.
- [d:r:i] Two additional surfaces the stronger design should name explicitly:
  - **A per-family roster-source column in L2.** Each family row declares which L0 roster entry owns discovery for that family (`governing_doctrine -> root+planning AGENTS.md`, `helper -> tooling/codex/README.md`, `install_overlay -> OVERLAY-MANIFEST.json`, `registry -> docs/INVENTORY.md plus .codex/agents/*.toml glob`, `launch_truth -> local audit governance`). That exposes which families are under-rostered today (e.g., launch-truth has no maintained-doc owner) and turns that visibility into a candidate backlog rather than an ambient gap.
  - **A refusal ledger.** A small section in the L2 or the coverage file listing what the registry deliberately does not attempt: whole-harness auto-extraction, declared-contract derivation without an author, launch-truth-to-registry comparison, upstream-pristine diff detection, cross-runtime topology mapping beyond `runtime_dirs_present`. Making refusals explicit keeps later refreshes from silently over-reaching.
- [d:r:i] Read this way, the registry family becomes a joined, typed view over already-maintained rosters and already-machine-validated contracts, with an explicit AI-authored compression layer, an explicit evidence-pointer layer, and explicit gates — rather than one flat JSON standing in for all five jobs at once.

## Bounded Next Design Move

- [d:r:i] The bounded move with the highest yield from the current baseline is **to split the v1 registry into the four typed files named above without introducing any new tooling, refresh cadence, or extraction logic**. Concretely:
  - Produce `registry/v2/inventory-roster.json` by reading `docs/INVENTORY.md`, `tooling/codex/README.md`, and `OVERLAY-MANIFEST.json` directly and emitting one roster entry per shipped surface with citation back to its source doc. No coverage claim yet — just the rostered paths.
  - Produce `registry/v2/declared-contracts.json` as a two-entry file (the overlay contract, the uplift manifest) pointing at their declarations and validators.
  - Produce `registry/v2/semantic-map.json` by migrating the current `v1` `families`/`carriers`/`edges` into the new shape, adding a `support_mode` column per carrier that cites which lower-layer evidence today backs it, and adding a `roster_source` column per family.
  - Produce `registry/v2/evidence-index.json` as a small pointer file naming the current `.planning/UPLIFT-MANIFEST.json`, latest runtime-visibility snapshot, latest overlay validation, latest coherence report, and latest launch-truth captures under the active audit.
  - Leave the `coverage-and-refresh.json` slot named but **held-later** for a subsequent move. Do not open G1 coverage enforcement in this bounded move; only stage its file slot so the later move does not need a structural precondition.
- [d:r:i] What this bounded move explicitly refuses:
  - It does not introduce whole-harness auto-extraction.
  - It does not introduce registry-diff tooling (L4 stays held).
  - It does not introduce upstream-pristine drift detection (stays held under `11`).
  - It does not attempt cross-runtime composition (stays held under the uplift held-later roster).
  - It does not claim full L0 coverage yet; G1 enforcement is staged but held.
- [d:r:i] Why this move is the right size now:
  - It preserves every semantic decision the `v1` slice already earned. No L2 work is thrown away.
  - It makes the compression visible. A later operator can see exactly which column of the registry moved on any refresh.
  - It sets up the cheapest next tool (registry diff between v2 snapshots) without requiring that tool to exist.
  - It gives the coverage gate a staged home before the gate is enforced, so the gate can be opened one family at a time rather than all at once.
  - It stays inside the prose family's current bounds — it is a typing pass over already-written content, not a widening into new propagation territory.
- [d:r:i] Alternative smaller moves, if the typed split is too large for the current slice:
  - **Smallest move A — support-mode column only.** Add a `support_mode` column to the existing `v1` carriers array, citing which lower-layer evidence backs each row. This lands the epistemic visibility benefit without restructuring the file.
  - **Smallest move B — roster-source column only.** Add a `roster_source` field per family in the existing `v1` families array. This lands the coverage-contract relation to INVENTORY and the helper README without opening L0 as its own artifact.
  - **Smallest move C — refusal ledger only.** Add a `refusals` section to `14` or to the `v1` artifact, naming what the registry does not attempt. This carries the boundary without requiring any schema change.
- [d:r:i] These smaller moves compose — they are the first three columns/sections the full typed split would land anyway — so picking them in order is itself a bounded rollout of the stronger design.

## How This Should Be Inherited Locally

### Carry Forward

- [d:r:i] **Hybrid framing.** `14`'s refusal of both pure automatic extraction and pure hand-memory is load-bearing and should carry forward. The widening is about typing the hybrid, not removing it. Source: [propagation-audit/14-propagation-registry-generation-and-seeding-policy.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/propagation-audit/14-propagation-registry-generation-and-seeding-policy.md:7).
- [d:r:i] **First-slice discipline.** `13`'s "curated snapshot, not automatic whole-harness extraction" framing and its named limits should carry forward as the posture the typed split preserves. Source: [propagation-audit/13-machine-readable-propagation-registry-first-slice.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/propagation-audit/13-machine-readable-propagation-registry-first-slice.md:29).
- [d:r:i] **Named invariants and edges.** `10`'s three-surface invariant and `12`'s six named cross-family edges carry forward as canonical L2 content in the typed split. Sources: [propagation-audit/10-model-policy-three-surface-invariant.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/propagation-audit/10-model-policy-three-surface-invariant.md:20), [propagation-audit/12-cross-family-edge-supplement.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/propagation-audit/12-cross-family-edge-supplement.md:12).
- [d:r:i] **Install-frontier split.** `09` and `11` already separate pristine and local-overlay frontiers. That distinction is the first L1/L2 example that the typed split can lean on. Sources: [propagation-audit/09-sharpened-propagation-field-split.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/propagation-audit/09-sharpened-propagation-field-split.md:52), [propagation-audit/11-upstream-pristine-frontier-propagation-obligation.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/propagation-audit/11-upstream-pristine-frontier-propagation-obligation.md:12).
- [d:r:i] **Sampled-carrier cohort with fingerprints and held-later roster.** `project_uplift.py`'s output shape — typed carriers, fingerprints, secondary signals, phase-boundary signal, held-later entries — is the L3b prior art that the typed split cites back. Source: [tooling/codex/project_uplift.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/project_uplift.py:87).
- [d:r:i] **Disposition discipline.** The existing `Carry Forward / Revise / Hold Explicit` lane-inheritance shape carries forward as the refresh-inheritance shape for L2 revisions.

### Revise

- [d:r:i] **Split `AI-authored` into semantic mapping and carrier annotation.** `14`'s single `AI-authored mapping layer` should revise to two named roles: semantic mapping (family/edge/mode/invariant decisions) and carrier annotation (role labels, source citations). Scaffolding from determinate tools is welcome in the second, refused in the first.
- [d:r:i] **Split `runtime/tooling evidence` into L3a/b/c/d.** `14`'s single evidence bucket should revise into contract-validation evidence, carrier-sample evidence, cross-surface coherence evidence, and capture/observation evidence. Each sub-layer cites a specific registry column rather than pointing at the whole.
- [d:r:i] **Promote `OVERLAY-MANIFEST.json`-style contracts to L1.** `14`'s `Hybrid Contract Layer` subsection should revise so machine-validated declared contracts are named as their own layer, not blended with evidence. `UPLIFT-MANIFEST.json` moves out of that subsection and into L3b (sampled carrier evidence).
- [d:r:i] **Treat `docs/INVENTORY.md` as a coverage contract, not only a seed.** `14`'s seeding order should revise so INVENTORY is named as the roster owner for shipped-surface families, and so the registry carries an explicit disposition per roster entry (`mapped` / `held-with-reason` / `out-of-scope-with-reason`).
- [d:r:i] **Type the `v1` JSON.** The flat `families`/`carriers`/`edges` shape should revise into the four typed files (`inventory-roster`, `declared-contracts`, `semantic-map`, `evidence-index`) plus a staged `coverage-and-refresh` slot. The semantic map gains a `support_mode` column per carrier and a `roster_source` column per family.
- [d:r:i] **Name refusals explicitly.** `14` should revise to carry a Refusals section — whole-harness auto-extraction, declared-contract derivation-without-author, launch-truth-to-registry comparison, upstream-pristine drift detection, cross-runtime topology mapping — so later refreshes inherit the boundary rather than rediscovering it.
- [d:r:i] **Separate refresh kinds.** `14`'s seeding order should revise to distinguish scheduled whole-registry refresh, change-triggered slice refresh, and lane-scoped refresh, each with its own G1/G2/G3 firing pattern.

### Hold Explicit

- [d:r:i] **Whole-harness auto-extraction.** Remains out of scope. The registry refuses to become a dependency extractor and should name that refusal on the surface.
- [d:r:i] **Upstream-template drift detection.** Held under `11`; typed split does not open it. Source: [propagation-audit/11-upstream-pristine-frontier-propagation-obligation.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/propagation-audit/11-upstream-pristine-frontier-propagation-obligation.md:30).
- [d:r:i] **Cross-runtime composition.** Held under the uplift held-later roster. `runtime_dirs_present` already carries a minimal observation; the registry should refuse a fuller topology claim until that held-later family is opened. Source: [.planning/UPLIFT-MANIFEST.json](/home/rookslog/workspace/projects/prix-guesser/.planning/UPLIFT-MANIFEST.json:26).
- [d:r:i] **Aged-bespoke deep merge / audit-subtree aging carry.** Both held under the uplift roster. The registry does not yet model them and should not silently absorb them at v2.
- [d:r:i] **L4 operator tooling.** Registry-diff, impact-adjacent reread suggestion, roster-parity enforcement, coverage-gate enforcement — all staged and deliberately not opened in the bounded next move. Opening them is earned after the typed split carries at least one refresh cycle.
- [d:r:i] **Launch-truth to registry comparison.** The edge is named (`12` — `Registry -> Launch-Truth`). Comparison of declared registry policy against per-spawn launch truth should stay held until both L1 contracts and L3d capture outputs are first-class citizens of the typed registry, so the comparison does not collapse back into a single opaque check. Source: [propagation-audit/12-cross-family-edge-supplement.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/propagation-audit/12-cross-family-edge-supplement.md:28).
- [d:r:i] **Scheduled refresh cadence.** Refresh discipline is named, but a scheduled cadence (e.g., per-milestone, per-audit-lane) stays held until the typed split has landed and the coverage gate is at least staged. Picking a cadence before the layered shape exists would repeat the compression that this review asks to split.
- [d:r:i] **Registry authorship across vendors.** The cross-vendor question (when do Claude lanes, Codex lanes, and future vendor lanes each author registry revisions, and how do they inherit) stays held. The typed split names semantic mapping as an authorship role, but the cross-vendor authorship policy is a separate family that this review does not open.
