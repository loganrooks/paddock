Date: 2026-04-21
Status: bounded external reread output (Opus 4.7 max, r1)

# Broader Network Propagation Field Mapping — Opus 4.7 Max R1

## How The Current Broader Propagation Field Now Divides

- [e:c+i] `08` splits the propagation field into seven named carrier families (governing doctrine; runtime install / materialization; runtime registry / launch authority; workflow / template / reference contracts; skill / wrapper routing; helper -> durable output chains; governance / audit carriers) and for each names main producers, main consumers, narrative mirrors, materialization / registry carriers, and a typical propagation obligation. Source: [propagation-audit/08-broader-network-producer-consumer-and-carrier-map.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/propagation-audit/08-broader-network-producer-consumer-and-carrier-map.md:17).
- [e:c+i] `08` also supplies a change-type routing layer — per family, it names the adjacent surfaces that should be reread when that family moves. Source: [propagation-audit/08-broader-network-producer-consumer-and-carrier-map.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/propagation-audit/08-broader-network-producer-consumer-and-carrier-map.md:54).
- [e:c+i] Beneath that layer, `02` and `05` supply one worked example (the uplift chain) in two complementary shapes: a per-surface role table, then a directed-flow chain map with edge-strength labels. Sources: [propagation-audit/02-project-uplift-producer-consumer-and-impact-map.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/propagation-audit/02-project-uplift-producer-consumer-and-impact-map.md:24), [propagation-audit/05-project-uplift-chain-map.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/propagation-audit/05-project-uplift-chain-map.md:48).
- [e:c+i] `07` adds an installer / materialization contract layer. `OVERLAY-MANIFEST.json` types each tracked overlay path as `add` or `overwrite`, `portable_gsd_contract.py` carries the shared validation/application/verification logic, and `setup-portable-gsd.sh` both pre-validates and post-verifies materialization. Sources: [tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json:1), [tooling/codex/portable_gsd_contract.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/portable_gsd_contract.py:137), [scripts/setup-portable-gsd.sh](/home/rookslog/workspace/projects/prix-guesser/scripts/setup-portable-gsd.sh:22).
- [d:r:i] Read together, the current field offers three disclosure surfaces at three scales:
  - one worked chain in role-table and directed-flow forms (families 6, with edges into 2, 4, 5, 7)
  - one installer-boundary gate (family 2)
  - one whole-field layer stack of seven families with change-type routing (`08`)
- [d:r:i] The layer stack is what the present reread has to judge; the chain and boundary artifacts are what that stack is building from.
- [d:r:i] Inside the seven-family split, family 2 (install / materialization) and family 7 (governance / audit carriers) already show visible sub-structure in the current tooling but not yet in the `08` table: the installer carries two distinct frontiers (pristine vs local-overlay), and the governance layer carries three distinct scopes (repo-root doctrine, workspace governance, audit-lane artifacts). The other five families read as single rows but each already hides further distinctions that the rest of this review names.
- [d:r:i] The change-type routing layer in `08` is currently per-family, not cross-family. It answers `when family X changes, what else inside this field should move?` but not `when a change crosses family X and family Y, which edge owns the propagation?` That is the next disclosure question the widened baseline has earned.

## What The Current Field Already Makes Easier To See And Carry

- [d:r:i] The 08 layer split gives an operator a routable first question when a contract changes: `which of these seven families does this touch first?` That is a sharper first question than the pre-08 option (`is the uplift chain clean?`), because it routes to family-specific propagation obligations rather than forcing every change through the uplift-chain lens.
- [d:r:i] Family 2 (install / materialization) now carries a real machine-readable contract. `OVERLAY-MANIFEST.json` enumerates 52 tracked overlay entries with explicit `add` or `overwrite` typing, and the installer runs strict validation before the copy and strict verification after the copy plus the reasoning-default apply step. An operator reading the manifest plus the helper can answer `what does this file mean inside the overlay contract?` without reading historical chat. Sources: [tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json:1), [tooling/codex/portable_gsd_contract.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/portable_gsd_contract.py:40).
- [e:c+i] Family 2's centralization is visible downstream too: `runtime_visibility.py` now imports `install_mutation_targets`, `compact_prompt_file`, and `render_overlay_text` from `portable_gsd_contract.py` rather than re-deriving install-mutation truth from the shell script. Sources: [tooling/codex/runtime_visibility.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/runtime_visibility.py:13), [tooling/codex/runtime_visibility.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/runtime_visibility.py:107).
- [d:r:i] Family 6's worked example (the uplift chain) now carries:
  - one authoritative producer (`project_uplift.py`) with no parallel consumer parsing paths
  - typed held-later carry (`held` vs `partially landed`) and schema_version 3 manifest outputs
  - tracked overlay ownership of `gsd-progress` / `gsd-resume-work` / `gsd-uplift-project` skill routes
  - a dedicated chain-flow artifact (`05`) with edge-strength labels
  - governance pointers from root `AGENTS.md` to the propagation-audit subtree
  Sources: [tooling/codex/project_uplift.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/project_uplift.py:606), [tooling/codex/UPLIFT-HELD-LATER.md](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/UPLIFT-HELD-LATER.md:1), [AGENTS.md](/home/rookslog/workspace/projects/prix-guesser/AGENTS.md:49).
- [d:r:i] Family 7 already discloses governance-doc role separation explicitly through `GOVERNANCE-READING-AND-UPDATE-PROTOCOL.md` — `INDEX.md` is the controlled read order, `ARTIFACT-INVENTORY.md` is the denser discovery surface, `CURRENT-STATE.md` is the short governing synthesis, `CURRENT-STATE-TRACE.md` is the cumulative trace, and `STATUS.md` is the mutable queue. That gives family 7 a stronger internal split than the other six families in `08`. Source: [GOVERNANCE-READING-AND-UPDATE-PROTOCOL.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/GOVERNANCE-READING-AND-UPDATE-PROTOCOL.md:16).
- [d:r:i] The governance carrier now carries a contract-change recording section that names the minimum governed record for a contract-changing slice: active family baseline, adjacent carrier surfaces updated, intentionally-held neighbors, and the next audit/verification surface when the burden exceeds one slice. That gives family 7 a structured intake surface, not just a read-surface. Source: [GOVERNANCE-READING-AND-UPDATE-PROTOCOL.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/GOVERNANCE-READING-AND-UPDATE-PROTOCOL.md:132).
- [d:r:i] Change-type obligations in `08` already give usable reread lists for five change shapes: governing doctrine movement, install/overlay movement, registry/agent-contract movement, workflow/template/reference movement, helper/output-chain movement. An operator at any of those change shapes has a short list of adjacent surfaces to reread without reconstructing it from chat. Source: [propagation-audit/08-broader-network-producer-consumer-and-carrier-map.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/propagation-audit/08-broader-network-producer-consumer-and-carrier-map.md:56).
- [d:r:i] Root `AGENTS.md` Contract Propagation now names the four partial-visibility tools, warns against treating them as whole-network proof, and routes the reader at the worked example in `propagation-audit/`. That makes the layer-stack framing operational doctrine, not just audit artifact. Source: [AGENTS.md](/home/rookslog/workspace/projects/prix-guesser/AGENTS.md:49).
- [d:r:i] `.planning/AGENTS.md` Contract-Propagation Hygiene names the five propagation surface categories (producers, consumers, narrative mirrors, runtime/registry carriers, durable outputs) at the planning-local layer, so the obligation carries inside planning work as well as root doctrine. Source: [.planning/AGENTS.md](/home/rookslog/workspace/projects/prix-guesser/.planning/AGENTS.md:137).
- [d:r:i] The audit subtree `propagation-audit/` now carries the family as governed audit lineage: opening note (`01`) -> local impact map (`02`) -> second-consumer proposal and landing (`03`, `04`) -> chain-flow map (`05`) -> strengthening batch (`06`) -> installer contract gate (`07`) -> broader-network widening (`08`). That trace gives later operators a readable audit history rather than chat memory.
- [d:r:i] Progressive disclosure is already real at family 7: `INDEX.md` fast re-entry is short, `PLAIN-LANGUAGE-STATE.md` / `PLAIN-LANGUAGE-GLOSSARY.md` carry low-shorthand re-entry, and `CURRENT-STATE.md` stays compact because `CURRENT-STATE-TRACE.md` absorbs cumulative buildup. The reader who only wants the family baseline does not have to read the whole workspace.

## Where Carrier Placement Or Boundary-Setting Still Thins

- [d:r:i] Family 2 (install / materialization) currently blends two distinct frontiers into one row. The `portable_gsd_contract.py` helper already distinguishes them internally (`install_mutation_targets` for upstream-pristine-plus-repo-local-post-edit, `OVERLAY-MANIFEST.json` entries for tracked overlay carry), and the `backup-meta.json` sits on the pristine-frontier side while `OVERLAY-MANIFEST.json` sits on the local-overlay-frontier side. Treating both as one `runtime install / materialization` row hides a load-bearing distinction about which side owns which truth. The stronger split is `upstream pristine install frontier` (npx get-shit-done-cc -> `gsd-file-manifest.json` -> `backup-meta.json` -> live) vs `local overlay frontier` (tracked overlay -> `OVERLAY-MANIFEST.json` -> `portable_gsd_contract.py` -> live). The carrier that stronger split should own is `install-frontier distinction — pristine vs local-overlay boundary`.
- [d:r:i] Family 6 (helper -> durable output chains) currently names only `project_uplift.py` as a concrete helper, with `future helper families, state/report emitters` as the placeholder for everything else. The repo already has a named helper cohort with distinct producer/consumer shapes: `audit_refmap.py` (reference-graph producer with `.planning/` outputs), `runtime_visibility.py` plus `capture_runtime_visibility_snapshot.py` (runtime classification producer with snapshot outputs), `manifest_install_coherence.py` (three-surface coherence producer with coherence-report outputs), `portable_gsd_contract.py` (overlay-contract producer with validation/verification outputs), `scan_threshold_language.py` (threshold-finding producer with scan-report outputs), `capture_launch_truth.py` (launch-truth producer with `LAUNCH-LEDGER.md` and per-lane launch-truth outputs). Sources: [tooling/codex/README.md](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/README.md:54). Collapsing all of these into one row hides that each has a different consumer family and a different propagation shape. The carrier a stronger split should own is `helper-cohort disclosure — stronger per-helper producer/consumer/output shape`.
- [d:r:i] Family 5 (skill / wrapper routing) collapses two skill propagation modes. `gsd-progress` / `gsd-resume-work` / `gsd-uplift-project` / `gsd-discuss-phase` / `gsd-do` / `gsd-plan-phase` / `gsd-review` are **invocation-binding wrappers** — they route `$gsd-*` invocations to workflows and change mostly when the workflow or entry contract changes. `gsd-rigorous-research` is a **reference-owner skill** — it carries embedded reference files (`references/method.md`, `references/output-template.md`, `references/repo-canon.md`) that are also sampled by `project_uplift.py` as doctrine-sensitive marker carriers. Sources: [tooling/codex/project_uplift.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/project_uplift.py:118), [tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json:49). The stronger split is `invocation-binding wrapper` vs `reference-owner skill`. The carrier it should own is `skill-family mode distinction — invocation-binding vs reference-owner`.
- [d:r:i] Family 4 (workflow / template / reference contracts) blends four sub-kinds with different propagation modes:
  - workflows that write durable `.planning/` outputs (e.g., `plan-phase.md` -> `PLAN.md`, `discuss-phase.md` -> `CONTEXT.md`, `research-phase.md` -> `RESEARCH.md`)
  - templates that shape those outputs (`templates/phase-prompt.md`, `templates/context.md`, `templates/research.md`)
  - references that shape agent behavior (`references/agent-contracts.md`, `references/planner-reviews.md`, `references/planning-config.md`, `references/verification-overrides.md`)
  - `bin/lib/*` that carries runtime implementation (`bin/lib/config.cjs`, `bin/lib/phase.cjs`, `bin/lib/roadmap.cjs`)
  These four sub-kinds appear in `backup-meta.json` as overwrite-backed pristine modifications, suggesting they are already treated as one unit at the installer layer — but they are not one unit at the propagation layer. Workflow -> output is a different propagation mode than template -> output-shape, reference -> agent-behavior, and lib -> runtime-behavior. Source: [.codex/gsd-local-patches/backup-meta.json](/home/rookslog/workspace/projects/prix-guesser/.codex/gsd-local-patches/backup-meta.json:5). The stronger split is four explicit sub-rows. The carrier it should own is `intra-workflow-family carrier-mode disclosure`.
- [d:r:i] Family 3 (runtime registry / launch authority) reads as a single row but already carries a three-surface contract internally. Model policy is carried in `AGENTS.md` (`gpt-5.4` xhigh for orchestration, high for execution), in `portable_gsd_contract.py` `QUALITY_REASONING` dict (17 agent -> effort mappings), and in every `.codex/agents/*.toml` as a `model_reasoning_effort = "..."` line that `apply-reasoning-defaults` rewrites. Sources: [AGENTS.md](/home/rookslog/workspace/projects/prix-guesser/AGENTS.md:141), [tooling/codex/portable_gsd_contract.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/portable_gsd_contract.py:19), [tooling/codex/portable_gsd_contract.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/portable_gsd_contract.py:214). When model-policy moves, all three surfaces must stay in tune. The carrier the stronger split should own is `model-policy three-surface invariant — doctrine + QUALITY_REASONING + agent TOML`.
- [d:r:i] Family 7 (governance / audit carriers) blends three distinct scopes:
  - **repo-root governing doctrine** (root/planning `AGENTS.md`, `CLAUDE.md` wrappers, `WORKFLOW.md`, `AI-GUARDRAILS.md`, `ARTIFACT-GOVERNANCE.md`) — repo-wide
  - **workspace governance** for this audit root (`INDEX.md`, `CURRENT-STATE.md`, `STATUS.md`, `ARTIFACT-INVENTORY.md`, `WORKSPACE-AUTHORITY-AND-ORGANIZATION.md`, `PLAIN-LANGUAGE-*`) — workspace-scoped
  - **audit lane artifacts** (packet/spec/prompt/output/disposition sets) — lane-scoped
  These have different propagation duties. Repo-root doctrine changes ripple into every workspace; workspace governance ripples inside one audit root; lane artifacts stop at the lane. Collapsing them into one cell hides the scope distinction. The stronger split is `repo-root doctrine` vs `workspace governance` vs `audit-lane artifact`. The carrier it should own is `governance-scope disclosure — three distinct scopes inside one family`.
- [d:r:i] The field does not yet name **launch-truth** as its own carrier. `capture_launch_truth.py` produces requested-vs-effective launch data and `LAUNCH-LEDGER.md` records durable launch history; per-lane launch-truth files live under each challenge subtree (`propagation-audit/launch-truth/`, `tranche-audit/launch-truth/`, etc.). This is a distinct propagation shape from runtime registry (family 3), because registry answers `what does the repo declare?` while launch-truth answers `what did a specific spawned run actually receive?`. Source: [tooling/codex/README.md](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/README.md:65). The carrier an added row should own is `launch-truth / orchestration-capture — requested-vs-effective launch history`.
- [d:r:i] The field does not yet name **compact-prompt carry** as its own small carrier. `tooling/compact-prompts/project.md` and `tooling/compact-prompts/readiness.md` sit in the tracked overlay as `add` entries, `.codex.local/compact-prompt.txt` is a repo-local override selector, and the installer accepts `--compact-prompt-file` both as env var and CLI flag. Source: [scripts/setup-portable-gsd.sh](/home/rookslog/workspace/projects/prix-guesser/scripts/setup-portable-gsd.sh:11), [tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json:54). The carrier a small added row should own is `compact-prompt carry — repo-local prompt-override cohort`.
- [d:r:i] The field does not yet name **state-continuity** as distinct from `helper -> durable output`. `.planning/STATE.md` is shown in `08` as `helper / durable output`, but that compresses its multi-owner role: phase execution writes phase sections, session continuity writes its own marker, and `project_uplift.py` writes the `## Project Uplift` section. Source: [tooling/codex/project_uplift.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/project_uplift.py:816). When the compact continuity contract changes, more than the helper chain must move. The carrier an added sub-row should own is `state-continuity surface — multi-owner durable memory distinct from per-helper output`.
- [d:r:i] The field does not yet name **cross-runtime composition** as an explicit held-pressure family. `runtime_dirs_present` in `project_uplift.py` globs seven runtime directories (`.codex`, `.claude`, `.gemini`, `.config/opencode`, `.opencode`, `.config/kilo`, `.kilo`) and already types the repo as `cross-runtime uplift` when any non-`.codex` appears. Source: [tooling/codex/project_uplift.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/project_uplift.py:30). The held-later registry names `cross-runtime uplift composition — held`, but `08` has no family row that surfaces the current cross-runtime topology or the propagation obligation if a second runtime becomes active. The carrier a held-pressure row should own is `cross-runtime topology — explicit held-pressure with current active-runtime disclosure`.
- [d:r:i] The field does not yet name **held-later registries** as a first-class carrier shape. `UPLIFT-HELD-LATER.md` appears inside family 6 as `held-family registry`, but this is a generalizable pattern — any chain that touches held-or-absorbed neighbors has the same need. The carrier an explicit cross-family row should own is `held-family progression — generalizable typed non-absorption carry`.
- [d:r:i] The field does not yet name the **upstream-pristine-install frontier** as a carrier surface. `npx get-shit-done-cc --codex --local` is the only named installer command, but pristine files with content that changes upstream (e.g., a new `gsd-file-manifest.json` entry under `workflows/`) are a distinct propagation frontier. When upstream ships a new file, `OVERLAY-MANIFEST.json` may need a new entry, `portable_gsd_contract.py`'s validate-manifest will surface any `backup_overlay_not_overwrite` condition, and repo doctrine may need an added capability. The carrier an added sub-row should own is `upstream-pristine frontier — pressure propagation when upstream ships a new or changed file`.
- [d:r:i] Change-type routing in `08` is per-family but not cross-family. Many real contract changes cross families: a helper that writes to `.planning/STATE.md` touches family 6 (helper) and family 7 (governance); a new overlay `add` under `workflows/` touches family 2 (install) and family 4 (workflow); a new agent `.toml` touches family 2 (install-mutation target) and family 3 (registry). Cross-family edges are implicit in the family-specific obligations but not explicitly disclosed as their own propagation object. The carrier an added supplement should own is `cross-family-edge disclosure — named edges where two families must move together`.

## What Relations Still Deserve Stronger Disclosure

- [d:r:i] **Install outputs are helper inputs.** `setup-portable-gsd.sh` + `apply-reasoning-defaults` writes `.codex/config.toml` and `.codex/agents/*.toml`; `project_uplift.py` then samples those same files as `runtime_registry` carriers with `normalized_toml_hash` fingerprinting. Sources: [tooling/codex/portable_gsd_contract.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/portable_gsd_contract.py:214), [tooling/codex/project_uplift.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/project_uplift.py:312). Family 2's outputs are family 6's inputs through a real directed edge. `08` treats these as independent rows; the edge deserves explicit naming so a reader sees that reinstall can move the helper's fingerprint even when no doctrine content moved.
- [d:r:i] **Overlay carriers are helper-sampled carriers.** `workflows/uplift-project.md` is an `add` entry in `OVERLAY-MANIFEST.json`; `workflows/plan-phase.md`, `templates/context.md`, `references/agent-contracts.md` are `overwrite` entries that also appear as strengthening-carrier markers and additive-install carriers inside the uplift helper's sampled cohort. Sources: [tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json:42), [tooling/codex/project_uplift.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/project_uplift.py:96). A change in the tracked overlay flows through family 2 (installer), into family 4 (live workflow/template/reference), and then into family 6 (helper sampling). `08` discloses each family but not the three-hop chain.
- [d:r:i] **Governance is both downstream consumer and upstream pressure of the operational families.** When a propagation-family artifact lands, `CURRENT-STATE.md`, `STATUS.md`, and `ARTIFACT-INVENTORY.md` must move; that is family 7 as downstream consumer of family 6's audit work. When root `AGENTS.md` names a new propagation obligation, every future slice across families 2-6 inherits the new obligation; that is family 7 as upstream pressure. Source: [AGENTS.md](/home/rookslog/workspace/projects/prix-guesser/AGENTS.md:49). `08` reads governance as a family beside the others; the bidirectional-edge shape deserves naming.
- [d:r:i] **Read-only consumer skills participate in two distinct relations.** `gsd-progress` / `gsd-resume-work` are overlay-tracked skills (Route D landed them); they are also consumers of the uplift helper's JSON bridge contract. So their SKILL.md files live in family 5 (skill routing), but their behavior binding lives in family 6 (helper -> durable output chain), and their materialization lives in family 2 (overlay install). The three-family participation is not disclosed in the `08` table.
- [d:r:i] **Reference-owner skills participate in the doctrine-sensitive cohort.** `gsd-rigorous-research/references/output-template.md` is sampled by `project_uplift.py` as the `strengthening_research` marker carrier (marker: `Strengthening Opportunities`). Source: [tooling/codex/project_uplift.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/project_uplift.py:118). That places the same file in family 5 (skill) and in family 1 (governing doctrine) at the same time, through a marker-block-hash fingerprint edge. `08` treats family 1 and family 5 as separate rows and does not disclose this overlap.
- [d:r:i] **The held-later registry has an implicit edge to workspace governance.** When a held entry moves to `partially landed`, both `UPLIFT-HELD-LATER.md` and the audit-workspace `CURRENT-STATE.md` must move (as `06` did for `routed-entry hooks beyond progress` -> `partially landed: propagation-audit/04`). The edge is real and bidirectional, but `08` places the held-later registry inside family 6 and governance in family 7 without naming the edge.
- [d:r:i] **Launch-truth has a registry-to-capture edge.** When `.codex/config.toml` or `.codex/agents/*.toml` declares a model/effort, every spawned agent produces a requested-vs-effective record that should match that declaration. Source: [.planning/AGENTS.md](/home/rookslog/workspace/projects/prix-guesser/.planning/AGENTS.md:156). Registry claim meets launch-truth observation on a real edge that today lives only in AGENTS prose.
- [d:r:i] **`manifest_install_coherence.py` currently consumes runtime-visibility snapshots but is not wired into the uplift propagation chain.** Its three-surface coherence frame (updater-boundary truth, carried-subset truth, frozen-runtime truth) is the strongest existing cross-family correspondence tool the repo carries. Source: [tooling/codex/manifest_install_coherence.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/manifest_install_coherence.py:106). That a whole-chain coherence frame already exists but is not yet connected to the propagation family as a routine gate is an under-disclosed relation.
- [d:r:i] **Checkpoint-3 lineage and `08` are cited as the stronger evidence basis but not routed as a read-surface from `08` itself.** `08` cites three passages from `checkpoint-3-gsd-surface-map.md` as the source for the layer-stack framing; later operators reading `08` in isolation do not get a direct pointer back to the Checkpoint-3 basis. Source: [propagation-audit/08-broader-network-producer-consumer-and-carrier-map.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/propagation-audit/08-broader-network-producer-consumer-and-carrier-map.md:15). The layer-stack idea has a lineage; that lineage deserves a routable edge rather than only citation.

## Bounded Strengthening Routes From The Widened Baseline

- [d:r:i] Route **H — Helper-cohort disclosure**. Revise `08` (or add a companion `08a`) to split the `helper -> durable output chains` row into one sub-row per named helper: `project_uplift.py` (uplift chain), `audit_refmap.py` (reference-graph), `runtime_visibility.py` + `capture_runtime_visibility_snapshot.py` (runtime classification), `manifest_install_coherence.py` (three-surface coherence), `portable_gsd_contract.py` (overlay contract), `scan_threshold_language.py` (threshold scan), `capture_launch_truth.py` (launch-truth). For each, name producer, direct consumers, narrative mirrors, durable outputs, and typical propagation obligation. Low blast radius — this is a docs-level split of an already-named family into sharper sub-rows. The carrier it should own is `per-helper producer/consumer/output disclosure`. This is the highest-yield next move because it directly sharpens the family where the current worked example lives without opening new territory.
- [d:r:i] Route **I — Install-frontier split**. Revise family 2 (runtime install / materialization) into two linked sub-rows: `upstream-pristine frontier` (npx install -> `gsd-file-manifest.json` -> `backup-meta.json` -> live) and `local-overlay frontier` (tracked overlay -> `OVERLAY-MANIFEST.json` -> `portable_gsd_contract.py` -> live). Name the contract between them: which files are pristine-only, which are overlay-overwrite (requires backup-meta presence), which are overlay-add (must stay outside backup-meta). `portable_gsd_contract.py`'s `build_manifest_validation_report` already enforces this split — it just hasn't been lifted into the `08` disclosure layer. The carrier it should own is `install-frontier distinction — two linked frontiers inside one install family`.
- [d:r:i] Route **J — Workflow-family carrier-mode split**. Revise family 4 into four sub-rows: `workflow -> durable planning output`, `template -> output shape`, `reference -> agent behavior`, `bin/lib -> runtime implementation`. Each sub-row has its own propagation obligation shape. The carrier it should own is `intra-workflow-family carrier-mode disclosure`.
- [d:r:i] Route **K — Skill-family mode split**. Revise family 5 into `invocation-binding wrapper` (routes `$gsd-*` invocations to workflows) and `reference-owner skill` (carries embedded references that also participate in doctrine-sensitive sampling). For each, name when the mode's propagation obligation fires. The carrier it should own is `skill-family mode distinction — invocation-binding vs reference-owner`.
- [d:r:i] Route **L — Launch-truth family row**. Add an eighth family row: `orchestration-capture / launch-truth`. Members: `capture_launch_truth.py`, `LAUNCH-LEDGER.md`, per-lane `launch-truth/` subtrees, `~/.codex/state_5.sqlite`. Propagation obligation: when registry (family 3) or doctrine (family 1) changes model policy, the next spawn's launch-truth record should be verified against the new declared truth. The carrier it should own is `launch-truth / orchestration-capture — requested-vs-effective as its own family`.
- [d:r:i] Route **M — Cross-runtime held-pressure disclosure**. Add a held-pressure row under family 2 or as its own micro-family that explicitly discloses the current cross-runtime topology: which `runtime_dirs` are present, which are sampled, which are treated as active. Keep the full composition family held-later, but make the current topology visible rather than leaving it only inside `project_uplift.py` globbing logic. The carrier it should own is `cross-runtime topology — explicit held-pressure with current active-runtime disclosure`.
- [d:r:i] Route **N — Compact-prompt carrier row**. Add a small sub-row naming `tooling/compact-prompts/*`, `.codex.local/compact-prompt.txt`, and the `--compact-prompt-file` plumbing as a distinct carrier. Keep it small — this is not a family, it is a named carrier inside install/materialization (family 2). The carrier it should own is `compact-prompt carry — repo-local prompt-override cohort`.
- [d:r:i] Route **O — Model-policy three-surface contract disclosure**. Add a propagation-audit artifact (or extend `08`) that names the three-surface contract explicitly: AGENTS.md model-policy doctrine + `QUALITY_REASONING` dict + agents/*.toml lines. Named invariant: when model-policy changes, all three must move. This could live as a small `09-model-policy-three-surface-contract.md` or as an extra section under `08`. The carrier it should own is `model-policy three-surface invariant`.
- [d:r:i] Route **P — Upstream-pristine-install frontier disclosure**. Land a small artifact (possibly merged with Route I) that names the propagation obligation when pristine upstream content shifts: reread OVERLAY-MANIFEST for collisions, rerun validate-manifest, check backup-meta consistency, surface any doctrine/workflow capability added by upstream. The carrier it should own is `upstream-pristine frontier propagation`.
- [d:r:i] Route **Q — State-continuity sub-family disclosure**. Carve `.planning/STATE.md` out of family 6's `durable outputs` row into a sub-row that names its multi-owner status (phase execution, session continuity, uplift helper). Keep it compact; this is a disclosure move, not an architectural change. The carrier it should own is `state-continuity surface — multi-owner durable memory`.
- [d:r:i] Route **R — Governance-scope split**. Split family 7 into `repo-root doctrine`, `workspace governance`, `audit-lane artifact` sub-rows. Each carries a different scope and a different propagation obligation. The carrier it should own is `governance-scope disclosure — three distinct scopes inside one family`.
- [d:r:i] Route **S — Cross-family-edge supplement**. Add a short `09-cross-family-edges.md` (or a section extending `08`) that names the real edges between families: install outputs -> helper inputs; overlay -> workflow -> helper three-hop; governance bidirectional edge with all six operational families; reference-owner skill -> doctrine-sensitive cohort; held-later -> workspace governance; registry -> launch-truth; helper chain -> coherence tool. Each edge should name the load-bearing invariant. The carrier it should own is `cross-family-edge disclosure`.
- [d:r:i] Route **T — Wire `manifest_install_coherence.py` into the propagation chain as an optional periodic gate**. The coherence tool already reasons about updater-boundary truth, carried-subset truth, and selected-runtime truth as three surfaces; that frame is the strongest whole-chain correspondence frame the repo carries. Add doctrine that after a multi-family change the operator should capture a clean-boundary runtime-visibility snapshot and run `manifest_install_coherence.py --strict` against it. Keep it bounded — this is a gate rule, not a new tool. The carrier it should own is `multi-family slice coherence gate`.
- [d:r:i] Route **U — Propagation-tool pointer from `tooling/codex/README.md` to `propagation-audit/`**. The README currently names each helper tool but does not point readers at the propagation-audit family where those tools' dependency relations are mapped. A short `See also: .planning/audits/.../propagation-audit/` line in the README would close the inventory-to-audit-lineage loop. The carrier it should own is `tooling-inventory-to-audit-family routing`.

- [d:r:i] Sequencing preference: **H, I, J, K, R** are all disclosure splits of already-named families — they carry the highest yield per unit of write because they sharpen what the map already holds without opening new territory. **L, Q** are low-cost family-row additions. **N** is small enough to land with any other route. **O, P, S** deserve their own small artifacts because each lands a named invariant, and those invariants should be citable surfaces later. **T, U** are quality-of-discovery routes that can land whenever the family docs next move. **M** is a held-pressure disclosure that should land only if a cross-runtime move becomes real pressure; otherwise the pressure row risks front-running a family the held-later registry still wants held.
- [d:r:i] Blast radius stays bounded for all routes above — H through S are map-level or small-artifact moves; T and U are doctrine/pointer edits. None widen the propagation family into additive install, cross-runtime reconciliation, forensics, or whole-harness rewrites.

## Wider Families To Keep Explicit Later

- [d:r:i] **Additive install routing** for absent additive carriers (currently `absent` typing is supported in the uplift helper but not turned into automated install). Widening this into an install flow has its own blast radius and should stay later-family. Source: [tooling/codex/UPLIFT-HELD-LATER.md](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/UPLIFT-HELD-LATER.md:10).
- [d:r:i] **Cross-runtime reconciliation** (not just disclosure as in Route M, but active composition across `.claude`, `.gemini`, `.kilo`, `.opencode`). Composition semantics for shared uplift memory, overlay ownership, and install mutation are a separate family.
- [d:r:i] **Upstream-template drift machinery**. How to absorb a pristine-content shift into repo-local overlays (with detection, diff routing, and conflict review) is a later family beyond the current upstream-pristine frontier disclosure in Route P.
- [d:r:i] **Aged-bespoke deep merge**. When a long-lived repo-local carrier stabilizes and upstream has caught up, how to route the bespoke carrier back upstream (or retire it locally) is a later family.
- [d:r:i] **Audit-subtree aging carry**. The propagation-audit subtree itself will age; when its lessons are inherited into doctrine and its trace becomes history, how to route its own retirement without dropping load-bearing edges is a later family.
- [d:r:i] **Forensics / archived-milestone integration**. How uplift posture should relate to completed milestones and archived phase trees is a later family held in `UPLIFT-HELD-LATER.md`.
- [d:r:i] **Workstream parent/child posture reconciliation**. Multi-workspace uplift composition is held-later and should remain so.
- [d:r:i] **Third routed consumer beyond `progress` + `resume-project`**. The two-consumer baseline is the current floor; a third consumer should land only after a stronger propagation baseline confirms the current two-consumer chain is still clean.
- [d:r:i] **Whole-harness external network challenge**. The current reread is over the widened-carrier-map; a stronger whole-harness challenge that stress-tests the widened baseline against adversarial reread should stay its own later lane after the bounded strengthening routes above land.
- [d:r:i] **Harness-intervention onboarding docs refresh** (`HARNESS-INTERVENTION-ONBOARDING.md`, `RUNTIME-MATERIALIZATION-AND-AUTHORITY.md`, `GOAL-TO-SURFACE-INTERVENTION-INDEX.md`, `SURFACE-STATUS-AND-DELTA.md`). These were written against the pre-widening baseline; rerouting them to inherit from `08`, the split sub-rows (H/I/J/K/R), and the new edge supplement (S) is later work.
- [d:r:i] **Repo-local GSD compatibility surface**. Explicit statement of which upstream GSD versions this harness posture is meant to work with (file, tooling-generated manifest, or small section under runtime/install authority chain). This stays the open question named in `STATUS.md` and should remain held until the install-frontier split (Route I) lands — otherwise the compatibility surface risks carrying the wrong contract shape. Source: [STATUS.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/STATUS.md:249).
- [d:r:i] **Wider helper-cohort audit**. Beyond the per-helper disclosure split in Route H, a full audit of each helper's dependency relations (as the uplift helper has received through `02`/`05`) is its own later lane — lands only if a specific helper needs stronger propagation discipline rather than only stronger disclosure.
- [d:r:i] **Read-only routed consumer cohort expansion**. Beyond `progress` and `resume-project`, any workflow that could become a read-only uplift consumer (`settings`, `check-todos`, `do`, `discuss-phase`) remains held-later.
- [d:r:i] **`STATE.md` multi-owner contract hardening**. The disclosure route (Q) names the multi-owner status; hardening the contract so that no owner can silently overwrite another's section belongs in a later family.

## How This Broadened Propagation Family Should Be Inherited

### Carry Forward

- [d:r:i] The seven-family split in `08` as the active widened baseline (governing doctrine; install/materialization; registry/launch authority; workflow/template/reference; skill routing; helper -> durable output chains; governance / audit carriers), together with the per-family change-type routing layer.
- [d:r:i] The worked chain (`02`, `05`) in two complementary shapes — per-surface role table plus directed-flow chain map with edge-strength labels.
- [d:r:i] The installer / materialization contract layer (`07`): `OVERLAY-MANIFEST.json` with typed `add`/`overwrite`, `portable_gsd_contract.py` as the central validation/application/verification helper, and `setup-portable-gsd.sh` with pre-copy strict validation and post-materialization strict verification.
- [d:r:i] Shared truth inheritance: `runtime_visibility.py` inherits install-mutation and template-materialization truth from `portable_gsd_contract.py`; no downstream helper re-derives install logic from shell.
- [d:r:i] Two live routed read-only consumers (`progress`, `resume-project`) on the same `UPLIFT_NOTE.show` gate and shared `build_progress_note` JSON contract, with regressible JSON-to-prose contract tests.
- [d:r:i] Typed held-later registry (`held` vs `partially landed`) with schema_version 3 outputs and helper-side parsing.
- [d:r:i] Governance routing from root `AGENTS.md` Contract Propagation and `.planning/AGENTS.md` Contract-Propagation Hygiene into `propagation-audit/` as the worked-example reference.
- [d:r:i] Workspace governance-disclosure discipline at family 7: `INDEX` / `ARTIFACT-INVENTORY` / `CURRENT-STATE` / `CURRENT-STATE-TRACE` / `STATUS` / `GOVERNANCE-READING-AND-UPDATE-PROTOCOL` as role-distinct surfaces with a contract-change recording section.
- [d:r:i] Checkpoint-3 lineage as the evidential basis for the layer-stack framing over a flat-skill-list framing.
- [d:r:i] The propagation-audit subtree itself as governed audit lineage with numbered progression from opening note through widened-field map.

### Strengthen Next

- [d:r:i] Route **H**: split `08`'s helper row into per-helper sub-rows (project_uplift, audit_refmap, runtime_visibility + snapshot, manifest_install_coherence, portable_gsd_contract, scan_threshold_language, capture_launch_truth) with distinct producer/consumer/output shapes.
- [d:r:i] Route **I**: split family 2 into `upstream-pristine frontier` and `local-overlay frontier` sub-rows, naming the contract between them that `portable_gsd_contract.py` already enforces.
- [d:r:i] Route **J**: split family 4 into `workflow -> output`, `template -> output shape`, `reference -> agent behavior`, and `bin/lib -> runtime implementation` sub-rows.
- [d:r:i] Route **K**: split family 5 into `invocation-binding wrapper` and `reference-owner skill` sub-rows.
- [d:r:i] Route **R**: split family 7 into `repo-root doctrine`, `workspace governance`, and `audit-lane artifact` sub-rows.
- [d:r:i] Route **L**: add launch-truth / orchestration-capture as an eighth family row.
- [d:r:i] Route **Q**: add state-continuity as a distinct durable-output sub-row.
- [d:r:i] Route **N**: add compact-prompt as a small named carrier under install / materialization.
- [d:r:i] Route **O**: land a small model-policy three-surface contract artifact.
- [d:r:i] Route **P**: land the upstream-pristine frontier propagation obligation.
- [d:r:i] Route **S**: add a cross-family-edge supplement naming install-output-to-helper-input, overlay-to-workflow-to-helper three-hop, governance bidirectional edge, reference-owner-skill-to-doctrine overlap, held-later-to-governance edge, registry-to-launch-truth edge, and helper-chain-to-coherence-tool edge.
- [d:r:i] Route **T**: doctrine that multi-family slices should capture a clean-boundary runtime-visibility snapshot and run `manifest_install_coherence.py --strict` as an optional gate.
- [d:r:i] Route **U**: a `See also: propagation-audit/` pointer from `tooling/codex/README.md`.

### Hold Explicit

- [d:r:i] Route **M** (cross-runtime topology disclosure) unless active cross-runtime pressure surfaces — the held-later registry still wants the composition family held, and a pressure row should not front-run that decision.
- [d:r:i] Additive install routing for absent additive carriers.
- [d:r:i] Cross-runtime reconciliation across `.claude` / `.gemini` / `.kilo` / `.opencode` runtimes.
- [d:r:i] Upstream-template drift machinery (beyond the Route P disclosure).
- [d:r:i] Aged-bespoke deep merge.
- [d:r:i] Audit-subtree aging carry.
- [d:r:i] Forensics / archived-milestone integration.
- [d:r:i] Workstream parent/child posture reconciliation.
- [d:r:i] Third routed uplift consumer beyond `progress` + `resume-project`.
- [d:r:i] Whole-harness external network challenge lane beyond the current widened-baseline reread.
- [d:r:i] Harness-intervention onboarding docs refresh against the widened baseline.
- [d:r:i] Repo-local GSD compatibility surface (holds until Route I lands).
- [d:r:i] Wider per-helper dependency audit beyond the Route H disclosure split.
- [d:r:i] Read-only routed consumer cohort expansion beyond `progress` and `resume-project`.
- [d:r:i] `STATE.md` multi-owner contract hardening beyond the Route Q disclosure split.
