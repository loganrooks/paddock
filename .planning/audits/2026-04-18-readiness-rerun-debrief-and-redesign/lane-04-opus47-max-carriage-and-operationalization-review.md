# Lane 04 — Opus 4.7 Max Carriage And Operationalization Review

Reviewer: `Opus 4.7 Max` via Claude Code CLI (`opus[1m]`, `max`).
Spec: `.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/OPUS-CARRIAGE-AND-OPERATIONALIZATION-SPEC.md`.
Output mode: single markdown artifact, this file.

## Frame Note

- [g:r:i] This lane is a carriage-and-operationalization review, not a rewrite lane and not a doctrine-promotion referendum. It asks which corpus distinctions let this workspace carry audit design, claim discipline, workflow scaffolding, and future-preserving judgment better — and which of those gains justify bounded local proposals, bounded next stress tests, reviewer-register use, or no inheritance at all.
- [g:r:i] Lane-03 is treated as what it is: a bounded test that found `rephrasing_only` on three readiness-era cases framed around changed recommendation / warrant / salience / rephrasing. This lane's question is broader. Carriage quality, operationalizability, inheritance discipline, reproducibility by later reviewers, and reduced dependence on heroic operator prose are all live even when recommendation does not flip.
- [g:r:i] The corpus is treated as challenge input, not as sovereign doctrine. The existing claim-marker system, the readiness-era doctrine, and the runtime workflow are all treated as load-bearing, not as final ontology. Both sides of that comparison are testable, and the lane succeeds only if it makes the difference between real carriage gain and elegant relabeling legible to a skeptical reader.
- [g:r:i] The lane stays within the named surface set. It does not widen into upstream `gsd` proposals, ecosystem doctrine, or repo-wide template rewrites. Where a candidate move would cascade outside the surface set, this review names that fact and downgrades the disposition.
- [g:r:i] `CLAIM-TYPES.md` is explicitly treated as a concrete load-bearing inheritance surface that could warrant resituation, limitation, supplementation, or no change. This review does not privilege rewrite as the default move and does not punish non-change when the baseline genuinely carries the job.

## Contamination Control Record

- [s:r:i] Staged read order followed. Stage 0 (governing packet: `INDEX.md`, `WORKSPACE-AUTHORITY-AND-ORGANIZATION.md`, `AUDIT-CHARTER.md`, `CURRENT-STATE.md`, `QUESTION-SET.md`, `EVIDENCE-ARCHITECTURE.md`, `PLAN-PROPOSALS.md`, `STATUS.md`) was read first. Stage 1 (baseline carrier packet: `CLAIM-TYPES.md`, `POST-FALSIFICATIONIST-REVIEW-DOCTRINE.md`, `AGENTS.md`, `.planning/AGENTS.md`, `WORKFLOW.md`, `ARTIFACT-GOVERNANCE.md`, `discuss-phase.md`, `context.md`) was read next. The `Baseline Surface Survey` below was drafted internally before any Stage 2 file was opened. Stage 2 (challenge-and-corpus packet: lane-01 disposition, lane-03 spec + output, `SESSION-FRAMING-BRIEF.md`, `COMMENTARY-CORPUS-READSET.md`, derivative translation, and the ten chunk files named in the spec) was opened only after the baseline survey was committed in working memory.
- [s:r:i] The baseline survey was not silently rewritten after corpus exposure. No factual correction was required; no `Baseline Correction Note` is appended. Where corpus-pressured reinterpretation occurred, it is labeled as corpus-added leverage in the per-surface sections rather than backfilled into the baseline-strengths subsection.
- [s:r:i] `lane-02-opus47-max-resituation-review.md` was not read. The staged packet did not leave a genuine unresolved ambiguity that required the optional reserve.
- [s:r:i] Residual leakage risk that procedure cannot eliminate:
  - The spec itself names a candidate operationalization list (declared/effective authority, answerable indirect governance and minimum sufficient legibility, productive tension and decay, conservative openness, fertile/empty incompleteness, graded underreach, research obligation and actionable inquiry debt, interrupt infrastructure and reconfigured deferral). Those framings were in this reviewer's working context before Stage 2, which biases toward finding those specific distinctions in the corpus. Mitigation: each per-surface subsection names corpus distinctions that actually did diagnostic work, and the `Earned Nothing` and `Reviewer-Register Only` buckets explicitly downgrade candidates that mapped onto the surface only as elegant labels.
  - Lane-03's `rephrasing_only` verdicts on three doctrine-friendly cases bias toward under-reading corpus contribution. Mitigation: the lane explicitly reopens the carriage-quality question (not the verdict-change question) and asks whether operational gain is present even when recommendation does not flip.
  - The `SESSION-FRAMING-BRIEF.md` posture (anti-tame, anti-human-timeline, intervention-friendly) biases toward finding operational gains. Mitigation: the burden-of-proof rules are applied symmetrically, and candidate operationalizations that cannot name a closure pressure they would introduce are downgraded.

## Carriage Criteria

This review applies the spec's nine criteria explicitly. A candidate gain is stronger when it improves several at once. The criteria are used as test lenses, not as a scoring rubric; they rule out some moves (e.g., a move that adds only sharper labels without changing diagnosis / routing / reproducibility / implementability is downgraded to `reviewer_register_only`) and raise others (e.g., a move that improves reproducibility, burden visibility, and implementability at once is eligible for `ready_for_local_proposal`).

1. **Diagnostic precision** — does the move sharpen what failure mode is being named?
2. **Burden / authority visibility** — does it expose who owes what justification?
3. **Intervention routing** — does it help pick among remedy families?
4. **Reproducibility by later reviewers** — can a later reader redo the judgment without the current operator's prose?
5. **Reduced dependence on unusually strong operator prose or heroics** — does it survive a less-heroic orchestrator?
6. **Indirect-carrier legibility** — does it make small scaffolds answerable without inflating them into doctrine?
7. **Openness that actually redistributes defaults** — does it de-prelicense rather than only defer?
8. **Technical or procedural implementability** — is it buildable as bounded infrastructure, not only as prose?
9. **Improved attainable-outcome quality rather than merely adequate pass/fail sufficiency** — does it raise the ceiling, not just clear a floor?

## Baseline Surface Survey

This survey was drafted before Stage 2 was opened. It uses only the Stage 0 governing packet and Stage 1 baseline carrier packet. It cites no corpus chunks.

### Surface A baseline

What the current surface already carries well:

- `WORKSPACE-AUTHORITY-AND-ORGANIZATION.md` already defines a five-class authority model (governing setup spine; briefing and procedural scaffolding; challenge inputs; inquiry corpus; historical and superseded trail) with explicit `what this class can do / cannot do on its own` statements for each class.
- `INDEX.md` already names a source-of-truth hierarchy inline with file-by-file roles.
- `CURRENT-STATE.md` separates unchanged ground, prior findings (04-17 bridge), narrowly-settled lane-03 results, and still-unsettled questions without flattening.
- `STATUS.md` tracks current position, accepted starting decisions, next pending actions, and open questions as live mutable state, and already carries the `Lane-03 is narrowly authoritative` disposition.
- Read discipline by task (`current setup judgment` / `interpreting a challenge lane` / `corpus inheritance` / `launching a new external lane`) routes reader attention concretely and reduces the risk that later work treats flat-pile proximity as authority.
- Lane-03's narrow scope is already legibly preserved within the authority note itself (`lane-03 answered a narrow question because its spec asked ...`).

Where carriage seems thin, brittle, ceremonial, or overly prose-dependent:

- Authority classing is established in prose but not annotated per-artifact. A later reader who opens a single file in the directory has no compact cue about which class it belongs to; the class is carried centrally by `WORKSPACE-AUTHORITY-AND-ORGANIZATION.md` and must be looked up.
- `Current Working Judgments` and `What Must Stay Open` are one operator's framing without an independent reproducibility check — two reviewers reading the same underlying files might not recover the same list without re-synthesizing the authority note from scratch.
- The class system does not explicitly say how a challenge input's force is reduced after disposition, or whether rejection of part of a challenge input (as in lane-01 disposition) feeds back into future challenge authority. Currently that is handled in prose in the disposition file itself.
- `STATUS.md` is live mutable state. A later reviewer reading it at time `t+1` cannot recover what its state was at time `t` without git blame.

What current non-change or defer positions seem strongest:

- The refusal to treat lane-03 as a global no-change verdict is already written in and does not need rescuing.
- The refusal to directly mutate `CLAIM-TYPES.md` or readiness doctrine from this workspace is well-defended by the `audit trail + program design, not canon` rule.

What remains unclear without further pressure:

- Whether the authority note itself is being respected in practice or only asserted.
- Whether later readers who cite a single file can recover its class without operator guidance.

### Surface B baseline

What the current surface already carries well:

- `AUDIT-CHARTER.md` explicitly rejects both `passes review` as satisfying and `non-intervention as neutral default`, and (critically) also `intervention as neutral default`. The burdens are symmetric.
- `QUESTION-SET.md` includes a `Generative Anti-Tame Quota` requiring ≥3 rejected-stronger-interventions per serious lane, and symmetric `Burden-Of-Proof Rules` that apply to both `leave untouched` and `modify/widen` recommendations.
- `EVIDENCE-ARCHITECTURE.md` sets a four-axis comparison model (original readiness mapping + 04-17 bridge + improved docs + runtime/harness behavior) with explicit packeting discipline (60k-140k band for substantive lanes, 20k-40k for sharp adversarial packets).
- `PLAN-PROPOSALS.md` evaluates six program shapes (A-F), recommends `Proposal B-extended` as working default, and names explicit `Switch Triggers` toward C/D/E/F — so the program shape is not silently preselected.
- Required output registers are listed in `QUESTION-SET.md` (theory-of-change, inheritance-disposition matrix, mapping-confidence, suppressed-opportunity, non-intervention ledger, interventions-considered-and-rejected ledger, future-preservation judgment, rerun-prerequisites, recommended next-program options).
- The charter includes an explicit execution-capacity model (`one operator orchestrating parallel Codex and Claude lanes over a multi-week budget ... bounded multi-module repo-local refactors`), so stronger moves are ruled out against a named model, not an unspecified ordinary-human baseline.

Where carriage seems thin, brittle, or overly prose-dependent:

- The anti-tame quota is a prose rule rather than a structurally enforced output requirement; a lane that satisfies it with three weak entries would technically comply. There is no rubric for what makes an entry count.
- Required output registers are listed but not templated. Each lane reinvents format for `inheritance-disposition matrix`, `mapping-confidence register`, etc.; reproducibility across lanes depends on operator discipline.
- The symmetric burdens do not fix a sequence. A lane can meet both burdens in any order — for example, by inflating the `what stronger move was considered` register after the recommendation is already written. The burdens work only if read prospectively.
- The four-axis comparison model is named but not scaffolded. A lane that collapsed back to binary (`docs vs readiness`) would have to be caught by spec review; nothing in the output register structure forces the four-axis comparison to appear in the output.
- `EVIDENCE-ARCHITECTURE.md` says lanes making `no change` or `mapping sufficient` claims should receive the strongest contrary evidence too, but does not specify what that contrary evidence looks like per lane type.

What current non-change or defer positions seem strongest:

- The decision to keep `Proposal B-extended` as working default while preserving E/F as live alternatives with named switch triggers is well-defended; widening the proposal set further without evidence of another lever would be ceremony.
- The decision to not template the required output registers is defensible: templates can flatten comparison and force false convergence.

What remains unclear without further pressure:

- Whether the existing anti-tame quota actually pushes lanes harder or is satisfied with weak entries.
- Whether the four-axis comparison is used or collapsed back to binary in practice.
- Whether `underreach` (the word used throughout) is doing too much work as a single register when the evidence clearly supports different strengths of the claim.

### Surface C baseline

What the current surface already carries well:

- `CLAIM-TYPES.md` carries seven claim types (`e/d/a/o/p/s/g`), three support modes (`c/r/b`), and three basis codes (`i/d/t`) with mixed forms (`c+r`, `i+d`, `i+d+t`) and interpretation rules. The system has already survived readiness-era usage across many audit lanes without obvious structural failure.
- `POST-FALSIFICATIONIST-REVIEW-DOCTRINE.md` distinguishes verificationist / falsificationist / post-verificationist / post-falsificationist review postures with working definitions, epistemic gap subtypes (evidential / inferential / comparative / anomaly-accounting / propagation / operationalization / standards / scope), `progressive vs. degenerating response`, and the load-bearing `Anti-Regret Scope Judgment` clause (two-sided promotion + non-promotion).
- `AGENTS.md` and `.planning/AGENTS.md` carry delegation and model policy, quality bar, and the `Future-Flexibility Statusing` bucket set (`direct doctrine`, `bounded-open branches`, `preserve-only seams`, `reversal-sensitive boundaries`, `inquiry debt`) — already more textured than a flat `open / deferred` scheme.
- `ARTIFACT-GOVERNANCE.md` carries the artifact class taxonomy (canon / phase work / audit trail / exploration / generated corpus) and staleness protocol.
- Lane-03 concretely tested the corpus against three readiness-era cases with the existing marker set and found the markers sufficient to discriminate operative moves (gate-vs-headline, anti-regret two-sided reasoning, function/force adjudication) without requiring a sibling analytic layer.

Where carriage seems thin, brittle, or overly prose-dependent:

- `CLAIM-TYPES.md` has no temporal dimension. An `[o:r:i]` claim can sit on a surface indefinitely without revisiting its warrant; the reader has no compact cue that the claim's support may have decayed since it was written.
- The marker system does not explicitly surface indirect carriers: claims carried by artifact structure, sequencing, repeated vocabulary, or inherited burdens rather than by explicit assertion. It assumes load-bearing claims are marked, which is an operator-discipline assumption.
- The distinction between `g` (governing) and `s` (stipulated) does real work, but when a stipulation functions as governance for the current session (as many `[s:r:i]` claims in the audit workspace do), no compact mechanism forces the promotion to be legible.
- Support mode `b` (bare) is semantically `needs strengthening` but the system has no scaffold for moving a bare claim toward cited or reasoned; bareness is diagnosed but not routed.
- `POST-FALSIFICATIONIST-REVIEW-DOCTRINE.md` is readiness-era and still Checkpoint-5-anchored; its general authority beyond the readiness package is implicit. Adjacent doctrine surfaces (`AUDIT-COMPARISON-POLICY`, cited but not read here) are repeatedly invoked without a compact reread cadence.
- The `AGENTS.md` and `.planning/AGENTS.md` files repeat claim-type reminders; if those repetitions drift, the canonical surface is `CLAIM-TYPES.md`, but nothing structurally enforces the drift-detection.

What current non-change or defer positions seem strongest:

- The refusal to rewrite `CLAIM-TYPES.md` is strong: lane-03 tested the function/force and declared/effective distinctions against three cases and found rephrasing only; the claim-marker system did the case-discrimination work.
- The refusal to treat `POST-FALSIFICATIONIST-REVIEW-DOCTRINE.md` as general-purpose repo doctrine without explicit resituation is strong; the doctrine is load-bearing on the readiness surface and is still reachable from other surfaces through citation.

What remains unclear without further pressure:

- Whether the claim markers are being used by downstream agents (researcher, planner, executor) or only in audit surfaces.
- Whether the doctrine's posture decays over time as the ecology shifts.
- Whether indirect-carrier load-bearing claims (the kind the corpus will later foreground) are adequately caught by current prose.

### Surface D baseline

What the current surface already carries well:

- `discuss-phase.md` has explicit `scope_guardrail` (no scope creep, redirects to deferred ideas), `gray_area_identification` (phase-specific, not generic labels), `answer_validation` (retry on empty, text mode for remote sessions), and `synthesis_priority` (scout before asking, derive before reopening, prioritize by leverage, respect uncertainty).
- Exploratory mode addition preserves genuine uncertainty and requires `working assumptions, not fake certainty`. The `--auto` exploratory branch only auto-selects when strongly grounded.
- `context.md` template has seven distinct steering sections: `<decisions>`, `<working_model>`, `<derived_constraints>`, `<open_questions>`, `<epistemic_guardrails>`, `<future_awareness>`, `<deferred>`. `<future_awareness>` is structurally distinct from `<deferred>` (shape constraint on current work vs. out-of-scope ideas).
- `<future_awareness>` has four buckets: `Protected Seams`, `Explicit Non-Decisions`, `Current Posture`, `Future Shape Notes` — already more textured than a single `open` list.
- Advisor research mode, `--analyze` mode, and `--batch` mode provide multiple pacing options for different operator/session conditions.
- Incremental checkpoint discipline (`DISCUSS-CHECKPOINT.json` after each area) allows interrupted sessions to resume without data loss.
- Canonical refs must be MANDATORY per the template, which is the closest the workflow comes to requiring downstream-agent legibility on the steering brief itself.

Where carriage seems thin, brittle, or overly prose-dependent:

- The `<open_questions>` section lists questions but has no required disposition per entry. A phase's open questions pass forward without state (are they research-bound, plan-bound, deferred-with-trigger, or retired?). Downstream agents read them as pending but not as dispositioned.
- The `<future_awareness>` section lists 4 buckets with the `None` convention, but the sections can harden into `None` placeholders that carry no information. A later phase with genuine protected seams and one with none produce identically-shaped sections.
- The workflow requires downstream agents to `MUST read` canonical_refs, but does not enforce compliance; whether the researcher/planner actually read them is prose-dependent.
- There is no explicit interrupt infrastructure. If mid-phase evidence challenges an earlier decision (either in this phase or in prior CONTEXT.md files), the workflow has no disposition vocabulary for reopening.
- Exploratory-mode auto-select depends on `workflow.discuss_mode=exploratory` being configured; the default and routing posture are not obvious from carrier prose alone without reading the initialization code in `gsd-tools.cjs`.
- The workflow exercises governance force on downstream planning and execution — it shapes what questions get asked, what decisions get locked, what future-awareness constraints get carried forward — but the carrier itself does not state its provenance, override conditions, or downstream effect surfaces in the way it asks its outputs to state theirs.
- The `<deferred>` section captures ideas that came up but are out of scope; it does not specify how those deferred ideas become interruptible in later phases.

What current non-change or defer positions seem strongest:

- The decision to keep `<future_awareness>` distinct from `<deferred>` is strong; collapsing them would lose the shape-constraint vs. out-of-scope distinction.
- The decision to not template every section's prose content is defensible: prose flexibility is what lets different phase domains get phase-appropriate CONTEXT.md shapes.

What remains unclear without further pressure:

- Whether the `<open_questions>` section gets acted on downstream or only acknowledged.
- Whether the workflow's own governance force is legible to a later reviewer asking `why does this scaffold carry this much weight?`
- Whether the `None` placeholder convention erases distinction or preserves it.

## Surface A: Workspace Authority And Organization

### Baseline strengths (restated from survey, uncontaminated)

- Five-class authority model with explicit `can do / cannot do` prose per class.
- Lane-03 narrow-disposition already written into the authority note.
- Read-discipline-by-task routing.
- Live-mutable `STATUS.md` that records accepted starting decisions, pending actions, and open questions.

### Corpus-added or challenge-added leverage

Two distinctions change how I read this surface:

1. **Declared / effective authority split** (chunk 04, echoed in chunk 13 as `function / force`). The five-class model names declared authority classes. It does not name effective authorities. In this workspace, the effective authorities include: whichever file the current orchestrator cites most often when routing a lane; `SESSION-FRAMING-BRIEF.md` whenever a lane has to be situated; `STATUS.md` whenever a reviewer wants to know `what is next`; the lane-03 output whenever someone wants to know `does the corpus change anything`. The corpus's distinction would let the authority note name which class each artifact is declared into AND name where its effective force exceeds or falls short of that declaration. That is not redundant with the current prose: the current prose says `challenge inputs ... cannot become sovereign doctrine on their own`, but does not say `SESSION-FRAMING-BRIEF is Class 2 by declaration, exerts Class 1-adjacent effective force via repeated citation, and that divergence is itself a primary audit object`. Calling this distinction `rephrasing only` would miss the specific routing move the corpus enables.

2. **Dependency maps as revisable hypotheses** (chunk 37). `WORKSPACE-AUTHORITY-AND-ORGANIZATION.md` is presented as settled authority-classing at a fixed date. The corpus insists that a routing map is a hypothesis about current ownership and force, not a stable truth. The note currently has no footer / review-trigger statement of its own revisability; a later reader opening the file at `t+2` has no cue that its classing should be reread against actual citation patterns.

Two distinctions map onto the surface but do not add operational leverage beyond what the prose already does:

- **Hidden governance regimes catalog** (chunk 13: drift, institutional disavowal, procedural convenience, infrastructural sedimentation, burden displacement, prestige transfer, governance scarcity). Useful as a diagnostic vocabulary when the declared/effective split is first exposed on a specific artifact, but on its own does not change what the authority note needs to say beyond the declared/effective reread.
- **Dependency-update threshold** (chunk 37). Reasonable guidance for future map edits but does not earn a local proposal on current evidence; `STATUS.md` already carries delta-shaped live state.

### Remaining limits

- Even with a declared/effective reread, operator-prose dependence is not eliminated. The reread still requires a careful reader.
- Without evidence that a specific Class-2 artifact is currently exerting Class-1 effective force (SESSION-FRAMING-BRIEF is the obvious candidate, but there's no concrete audit of its citation pattern), the declared/effective reread's downstream payoff is projected rather than measured.
- The chunk 37 revisability move is conservative but adds a footer; if the workspace already treats the authority note as revisable in practice, the footer is ceremony. If it does not, the footer is load-bearing. The evidence is ambiguous.

## Surface B: Audit Setup And Output Registers

### Baseline strengths (restated from survey)

- Symmetric burdens in `AUDIT-CHARTER.md` and `QUESTION-SET.md`; both non-intervention and intervention face burdens.
- Anti-tame quota (≥3 rejected interventions per lane).
- Four-axis comparison model and packeting bands.
- Six-proposal space with named switch triggers.
- Explicit execution-capacity model in the charter.

### Corpus-added or challenge-added leverage

Three distinctions change how I read this surface:

1. **Graded underreach vocabulary** (chunk 26: `explicit miss` / `traceable undercarry` / `retrospective possibility`). The baseline's language uses `underreach` as a single register across the audit charter, current-state summary, and question set. The corpus decomposes that into three epistemic strengths that warrant different critique tones, different burdens of proof, and different remedy families. This is not rephrasing: the baseline's symmetric burdens do not discriminate between `the readiness package had this in front of it and missed it` (explicit miss, licensing sharper backward critique) and `the possibility became reachable only through later-developed distinctions` (retrospective possibility, licensing lighter indictment and future-design attention). The uniform `underreach` framing risks hindsight inflation on cases where later doctrine genuinely added carriers the earlier package could not have had.

2. **Internal comparison order for anchor cases** (chunk 25). The baseline's `Generative Anti-Tame Quota` and symmetric BOP rules are unordered; they can be satisfied in any sequence. The corpus specifies a prospective sequence: earlier visibility (terrain richness) → recommendation language → burden placement → omitted adjacent surfaces → and only then later corrective intervention. This protects against hindsight inflation more concretely than the baseline's `contestable + revisable + vulnerable to rival explanation` framing. The corpus's discipline is implementable; the baseline's is an ethos.

3. **Revealing case criteria** (chunk 25: rich prior terrain + thin recommendation + visible burden asymmetry + later corrective pressure + real downstream force). The baseline's `QUESTION-SET.md` names required output registers but does not name how anchor cases should be selected. A main-wave lane could satisfy all the required registers while selecting cases that confirm the current frame. The corpus's criteria would force the selection to be adversarial.

Two distinctions map onto the surface but do not add operational leverage beyond what the prose already does on this evidence:

- **Guide-shaped response matrix vs. scoring** (chunk 26). The baseline already uses prose dispositions (`local proposal / bounded stress test / reviewer register / not earned`) without composite seriousness rollups. The corpus would validate the practice; it does not reform it.
- **Intervention menus for indirect rule** (chunk 13: exposure, elevation, reduction, replacement, redistribution, preserved indirectness). A plausible extension of `PLAN-PROPOSALS.md`, but the baseline's six proposals already carry the range (A = immediate exposure, B-extended = elevation, etc. — a rough map, not exact). Adding a categorization layer would be ceremony without evidence that the current proposals confuse intervention families.

### Remaining limits

- The graded underreach vocabulary is testable against existing cases, but the baseline's flat `underreach` has not yet produced a concrete failure; promoting the gradient without a failure-case demonstration risks pure descriptive sophistication.
- The internal comparison order is implementable but operator-heavy to enforce; unless a later main-wave lane is actually structured with it, the order remains a recommendation.
- The `Revealing Case Criteria` move is strong but requires at least one lane run against it to show it changes case selection versus confirms the operator's intuition.

## Surface C: Claim / Doctrine Surfaces

### Baseline strengths (restated from survey)

- Seven-type × three-support × three-basis claim marker system with explicit mixed forms and interpretation rules.
- Post-falsificationist review doctrine with working definitions, gap subtypes, progressive/degenerating, anti-regret scope judgment.
- Future-flexibility statusing buckets (direct doctrine / bounded-open branches / preserve-only seams / reversal-sensitive boundaries / inquiry debt).
- Artifact class taxonomy and staleness protocol.
- Lane-03 already tested the strongest corpus candidates (function/force, declared/effective authority, graded underreach) against three readiness-era cases and found `rephrasing_only` with the existing markers doing the case-discrimination work.

### Corpus-added or challenge-added leverage

The honest reading is that this surface is the place where the corpus earns the least operational leverage and where the temptation to promote is the highest. The spec specifically warns against forcing `CLAIM-TYPES.md` to be the flagship target because it is symbolically attractive.

One distinction does produce genuine but bounded pressure:

1. **Decay diagnostics for preserved tensions** (chunk 19). `CLAIM-TYPES.md` has no temporal dimension. A claim marked `[o:r:i]` or `[d:c+i]` sits on a surface with no cue that its support may have decayed since it was written. The readiness-era doctrine (`POST-FALSIFICATIONIST-REVIEW-DOCTRINE.md`) is four or more iterations from the current session's ecology; some of its `[g:c:i]` sentences point to checkpoint-5 mechanics that may no longer be current. The corpus's temporal sharpening (`the conditions that once made the tension useful may no longer be carried`) names a real gap. But the fix is not to extend `CLAIM-TYPES.md` — that would be ceremony that does not change diagnosis or remedy per the corpus's own `labels that change practice` test. The fix, if any, is a reviewer-register convention that load-bearing doctrine files carry a review-cadence footer.

Several corpus distinctions map onto the surface but explicitly do not earn inheritance:

- **Two-layer ontology proposal** (derivative translation, chunks 13 and 19). The derivative translation explicitly proposes `claim` as provisional handle alongside `framing move / operative distinction / vocabulary regime / exclusion / inheritance relation / sequencing move / closure move / orientation-setting gesture`. Lane-03 tested the strongest three of these against three readiness-era cases and found the existing markers sufficient. Carriage-and-operationalization rereading does not add new evidence against the lane-03 verdict; if anything, it sharpens the warning that adding markers without operational test is the kind of premature inheritance both this spec and the corpus's own `labels that change practice` test forbid.
- **Declared / effective authority as a new support or basis mode**. Candidate move: add an `e` (effective) support dimension or an explicit `[d/e]` axis to the marker system. Rejected: lane-03 tested this as a diagnostic vocabulary on three cases; it worked as a label but did not change warrant structure. Promoting it into the marker system would add burden on every downstream claim-writer without evidence the current markers are failing to discriminate. The move would be closure pressure masquerading as carriage gain.
- **Fertile / empty incompleteness applied to `[o:r:i]` claims**. Reasonable reviewer-register discipline, not a claim-type extension.

### Remaining limits

- The honest disposition on this surface is that non-change is genuinely justified on the evidence. This review cannot rule out that a different evidence base (doctrine-stressed cases) would flip the call, but the current lane is not that base.
- The temporal-decay pressure is real; the appropriate carrier for it is a reviewer-register convention or a footer on load-bearing doctrine files, not a claim-type extension.
- A skeptical reviewer might argue that even the reviewer-register convention is ceremony. The defense: the corpus's `dependency maps as revisable hypotheses` pressure is not specific to claim markers — it applies to any settled-looking surface — and the reviewer-register lives as a reading rule, not as new per-claim discipline.

## Surface D: Workflow / Harness Carrier Surfaces

### Baseline strengths (restated from survey)

- Seven-section CONTEXT.md template; `<future_awareness>` with 4 buckets distinct from `<deferred>`.
- `discuss-phase.md` scope-guardrail, gray-area identification, exploratory mode, advisor mode, checkpoint discipline.
- Canonical refs mandatory.
- Multiple pacing modes (auto, chain, power, batch, analyze).

### Corpus-added or challenge-added leverage

This is the surface where the corpus earns the most operational leverage on this evidence. Chunks 13 and 14 model the harness-carrier problem directly: small carriers that exercise governance force while appearing to be mere convenience. The current workflow scaffolding is exactly what chunks 13-14 describe.

Four distinctions produce genuine operational pressure:

1. **Answerable indirect carriers / minimum sufficient legibility** (chunks 13-14). The corpus specifies a minimum-legibility set for runtime scaffolds: provenance, burden notes, local rationale, override or redraw conditions, doctrine cross-reference, downstream effect traces. The current `discuss-phase.md` and `context.md` carriers lack most of these in any explicit structural way. The `<canonical_refs>` section is the closest thing but does not include override conditions, downstream effect surfaces, or local rationale for the carrier itself. This is not about adding ceremony to every prompt; it is about the carriers already asking their outputs (CONTEXT.md, canonical_refs) to be legible while being less legible themselves. The corpus's configurational legibility thresholds (chunk 14: size provenance / rationale / burden by force, risk, horizon, burden effects, reversibility, inheritance likelihood) apply: `discuss-phase.md` has high inheritance likelihood because every phase runs through it, which argues for richer legibility than a narrow-scope wrapper would need.

2. **Disposition as state change** (chunk 36: `act now` / `open inquiry` / `defer with updated trigger` / `retire`). The CONTEXT.md `<open_questions>` section currently lists questions without a required disposition. Downstream agents read them as pending but not as routed. Adding a compact disposition per entry would force state-change discipline at the point where it matters (phase-to-phase handoff) without requiring a repo-wide infrastructure rebuild. This is a concrete, bounded template edit with testable downstream effect.

3. **Real deferral requires new interrupt logic** (chunk 36). The `<deferred>` section currently captures out-of-scope ideas but does not reconfigure future interrupt logic. A deferred idea gets carried forward in prose but has no concrete wake condition, no affected decision surface, and no next-expected-wake-point. The chunk 34-36 specification (research obligation → three inquiry forms → actionable inquiry debt → disposition-as-state-change → delta-shaped trace) jointly argues that the template's current `<deferred>` shape is closer to polite burial than to reconfigured deferral. Promoting every `<deferred>` entry to the full chunk-34 debt schema would be ceremony; requiring a concrete trigger + decision surface for items that matter enough to cross phases is bounded.

4. **Fertile / empty incompleteness** (chunk 23) applied to `<open_questions>`. The template accepts open questions without requiring that each be fertile (oriented unknown + directional next move) rather than empty (vague underdevelopment). The corpus's distinction is operationally testable: can the open question state what remains open, why it matters, and what next inquiry would clarify it? If yes, the question is fertile and belongs in phase steering. If no, it should be downgraded or retired. This works as a reviewer-register rule rather than as a template field, because template fields risk turning every open question into a mini-spec.

Two distinctions map onto the surface but are overbroad for bounded operationalization:

- **Grammar of consequence as ensemble** (chunk 18). Useful diagnostic vocabulary for describing what the workflow does (it is an ensemble of doctrine + prompts + burden rules + reviews), but not an implementable local move.
- **Profile revision triggers / reproductive supports of stale profiles** (chunk 14). Describes why workflows ossify; would support a meta-audit of the workflow itself but widens beyond the surface set.

### Remaining limits

- Every move here risks adding ceremony to carriers that are already working. The operational test is whether the move changes downstream behavior, not whether it is elegant.
- Disposition-as-state-change is testable on one phase; if it turns out that adding `disposition:` per open-question entry does not change planner or executor behavior, the template edit should be rolled back rather than normalized.
- Answerable indirect carriers is the strongest corpus contribution but the hardest to scope; adding minimum-legibility signals to every workflow file simultaneously is the ceremonial trap. The right test is one carrier at a time.

## Cross-Surface Operationalization Matrix

| Candidate distinction or pressure | Target surface | Expected carriage gain | Primary disposition | Why not one step stronger or weaker |
|---|---|---|---|---|
| Declared / effective authority reread of class membership (per-artifact) | A (`WORKSPACE-AUTHORITY-AND-ORGANIZATION.md`) | Exposes where situated artifacts (esp. `SESSION-FRAMING-BRIEF.md`) exert Class-1-adjacent effective force while declared Class-2; improves later-reviewer reproducibility of the authority call | `ready_for_local_proposal` | Stronger (rewrite the 5-class system around function/force) would widen into authority-model redesign without an observed failure of the current classes. Weaker (`reviewer_register_only`) underclaims because the authority note is canonical for this workspace and a bounded supplementary section is implementable without rewrite. |
| Dependency-map-as-revisable-hypothesis footer + next-reread trigger | A (`WORKSPACE-AUTHORITY-AND-ORGANIZATION.md`) | Prevents the authority note from being silently inherited as stable floor; ties revisability to a named trigger rather than to operator vigilance | `ready_for_local_proposal` | Stronger (scheduled periodic rereads) risks ceremonial drift per chunk 35. Weaker (`reviewer_register_only`) underclaims because the note is at the top of the source-of-truth hierarchy and a one-paragraph footer is the minimum structural move that matches the corpus's `revisable hypothesis` pressure. |
| Disposition field per `<open_questions>` entry (`act-in-plan` / `research-next` / `defer-with-trigger` / `retire`) | D (`context.md` template + `discuss-phase.md` write step) | Forces state-change discipline at phase-to-phase handoff; prevents the `acknowledged/noted` ritual chunk 36 warns against | `ready_for_local_proposal` | Stronger (propagate disposition discipline to every CONTEXT.md section) widens beyond the surface set. Weaker (`reviewer_register_only`) underclaims because this is a concrete template edit whose effect on downstream planner behavior is directly testable on one phase. |
| Graded underreach vocabulary (`explicit miss` / `traceable undercarry` / `retrospective possibility`) for audit output registers | B (`QUESTION-SET.md` required output register list) | Calibrates critique strength to continuity strength; reduces hindsight inflation; couples label to tone/burden/remedy per chunk 26 | `ready_for_bounded_stress_test` | Stronger (`ready_for_local_proposal`) would add to `QUESTION-SET.md` now without evidence the flat `underreach` register has actually produced a failure; the gradient needs to be tried against 2-3 cases first. Weaker (`reviewer_register_only`) underclaims because the gradient passes the `labels that change practice` test on its face — it names different remedy families — and the minimum discriminating test is concrete. Minimum discriminating test: reclassify the `04-17` bridge audit's narrowing (traceable undercarry?) and at least one readiness-era `underreach` claim (explicit miss vs. retrospective possibility?) and see whether the remedy families diverge. |
| Revealing-case criteria + internal comparison order for main-wave lanes | B (`QUESTION-SET.md` and lane specs) | Disciplines anchor-case selection against hindsight-inflation; fixes prospective comparison sequence that symmetric burdens alone leave unordered | `ready_for_bounded_stress_test` | Stronger (require in `QUESTION-SET.md` now) presumes the gain; evidence requires one main-wave lane structured this way. Weaker (`reviewer_register_only`) underclaims because the corpus specifies the criteria concretely enough to test. Minimum discriminating test: the next main-wave lane proposes its anchor case by stating which criteria it meets, and the comparison order is audited against chunk 25's sequence. |
| Answerable indirect carriers / minimum sufficient legibility on one harness carrier | D (one of `discuss-phase.md`, `context.md`, or an agent prompt) | Makes the carrier's own governance force legible; tests whether provenance + override + downstream-effect markup changes downstream agent behavior | `ready_for_bounded_stress_test` | Stronger (add minimum legibility to every carrier now) is the ceremonial trap chunk 35 warns against. Weaker (`reviewer_register_only`) underclaims because chunks 13-14 model this exact surface type and the minimum legibility set is concretely specifiable. Minimum discriminating test: add provenance + local rationale + override conditions + downstream effect surfaces to one carrier (propose: the `<future_awareness>` section preamble in `context.md`), run one phase through it, and compare what the researcher and planner actually surface versus baseline. |
| Real-deferral discipline on `<deferred>` entries that cross phases | D (`context.md` + `discuss-phase.md`) | Prevents polite burial of cross-phase ideas; couples each durable `<deferred>` entry to trigger + decision surface + next wake point | `ready_for_bounded_stress_test` | Stronger (`ready_for_local_proposal`) presumes the full chunk-34 inquiry-debt schema is the right shape; evidence requires trying the compact delta trace first. Weaker (`reviewer_register_only`) underclaims because the chunk-36 delta-trace specification is compact and directly implementable. Minimum discriminating test: one phase's `<deferred>` entries that are judged likely to resurface get the delta trace; the next phase's discuss loop checks whether the trigger fires. |
| Fertile / empty incompleteness reviewer-register rule on `<open_questions>` | D (reviewer-register, not template) | Disciplines open-question quality per chunk 23 without adding required template fields | `reviewer_register_only` | Stronger (template field) adds ceremony without clear payoff. Weaker (`not_earned`) underclaims because the distinction genuinely sharpens existing practice and can be applied in review without code-level changes. Gain considered and rejected: requiring an `inquiry` field on every open question would flatten phases where the open question is genuine but not yet shaped into a next-inquiry. |
| Hidden governance regimes catalog (drift, institutional disavowal, procedural convenience, infrastructural sedimentation, burden displacement, prestige transfer, governance scarcity) | Cross-surface, primarily A and D | Sharper diagnostic vocabulary for audit prose; useful when a function/force divergence is already detected | `reviewer_register_only` | Stronger (embed into `QUESTION-SET.md` required registers) adds a categorization ceremony without test evidence. Weaker (`not_earned`) underclaims because the catalog does sharpen the `under-propagated` prose on this evidence. Gain considered and rejected: a standing catalog entry in `AGENTS.md` would duplicate what a reviewer can apply as-needed. Reopen trigger: a specific audit where the standard `under-propagated` language visibly fails to diagnose the regime. |
| Two-layer ontology extension of `CLAIM-TYPES.md` (claim layer + framing/exclusion/inheritance layer) | C (`CLAIM-TYPES.md`) | A second ontology layer for discourse-forming moves | `not_earned_on_this_evidence` | Stronger (`reviewer_register_only`) would let the layer live as review prose; that is already lane-03's outcome. The move is downgraded one step further because this lane tested the same pressure through the carriage-quality lens and found no new evidence that the existing markers are failing to carry the job. Gain considered and rejected: function/force / declared/effective / graded underreach as new markers — lane-03 tested all three on three cases and found rephrasing only. Reopen trigger: a case where the existing markers cannot discriminate operative moves the new layer can. |
| Decay-diagnostics surface added to `CLAIM-TYPES.md` (temporal support dimension) | C (`CLAIM-TYPES.md`) | Exposes that `[o:r:i]` or `[d:c+i]` claims may have decayed warrant | `not_earned_on_this_evidence` | Stronger (`reviewer_register_only` as a footer convention) is appropriate and is separately reflected in the dependency-map-as-hypothesis move on surface A for load-bearing docs. Weaker (full silence) underclaims because the temporal gap is real. Gain considered and rejected: add a per-claim temporal status marker. This would be pure ceremony; no evidence that any current claim has materially decayed. Reopen trigger: a specific preserved readiness-era `[g:c:i]` clause that turns out to steer current work despite its originating ecology having changed. |

## What Is Ready For Local Proposal

Three bounded local proposals are justified on this evidence.

### LP-1. Declared / effective authority supplementary pass in the workspace authority note

Move: add a new section to `WORKSPACE-AUTHORITY-AND-ORGANIZATION.md` titled `Declared vs. Effective Authority Reread` that explicitly states, for each of the five authority classes, which specific artifacts currently carry effective force exceeding their declared class and why. This does not rewrite the five-class model; it annotates it.

Closure pressure this introduces: the supplementary pass commits the workspace to acknowledging specific divergences (most obviously, `SESSION-FRAMING-BRIEF.md` as Class-2 by declaration with Class-1-adjacent effective force due to repeated citation in spec-writing). A later reviewer can hold the workspace to the reread when new artifacts are added. If the reread is wrong about a specific divergence, it is visibly wrong and contestable.

Narrower move considered: leave the issue as reviewer-register prose only. Rejected because the authority note is at the top of the source-of-truth hierarchy and a compact supplementary section has higher reproducibility than a diffuse convention. The narrower move would leave the declared/effective distinction undocumented; later reviewers would re-derive it each time.

Why the stronger move (full rewrite around function/force) is still justified as no: no evidence that the five-class model is failing to carry its current jobs; the supplementary pass gives the declared/effective distinction a bounded home without committing to model redesign.

### LP-2. Dependency-map-as-revisable-hypothesis footer in the workspace authority note

Move: add a short footer (1-2 paragraphs) stating that the authority classing is a hypothesis about current routing, name a concrete reread trigger (e.g., `reread this note when a new artifact class is added, when a Class-2 artifact is cited in three or more lane specs as steering authority, or at the start of each main-wave audit program`), and note which routing observations would indicate the map needs revision.

Closure pressure this introduces: the footer commits the workspace to actually rereading the authority note at the named trigger. If operators skip the reread, the footer becomes visibly ceremonial and can be criticized.

Narrower move considered: leave revisability implicit in the `Current Session Rule` language. Rejected because the current prose says the directory is `audit trail + program design, not canon` — that is a claim about the directory's status, not a trigger for rereading the authority note specifically.

Why the stronger move (scheduled periodic rereads) is still justified as no: chunk 35's `ceremonial drift` pressure argues against automatic recurrence. A concrete trigger is both compact and resistant to ceremonial drift.

### LP-3. Disposition field per `<open_questions>` entry in the CONTEXT.md template

Move: add a required `disposition:` field per `<open_questions>` entry in `.codex/get-shit-done/templates/context.md`, with allowed values `act-in-plan` / `research-next` / `defer-with-trigger` / `retire`. Update the `discuss-phase.md` write step to surface the disposition in the summary. Do not propagate the pattern to other CONTEXT.md sections yet.

Closure pressure this introduces: the template edit means every phase's open questions are now dispositioned rather than listed. A phase that fills all dispositions as `defer-with-trigger` without specifying a trigger violates the chunk-36 discipline and is visibly weak. A phase with zero open questions is no longer silently identical to a phase with four dispositioned ones.

Narrower move considered: make the disposition an optional field. Rejected because chunk 36's discipline is about forcing state-change; an optional field gets omitted under pressure.

Why the stronger move (propagate disposition to every CONTEXT.md section and to plan-phase / execute-phase / review-phase templates) is still justified as no: the spec forbids widening into repo-wide template rewrite, and the bounded single-template edit is directly testable on one phase before any widening.

## What Is Ready For Bounded Stress Test

Four bounded stress tests are earned on this evidence. Each names a minimum discriminating test rather than calling for another research round.

### BST-1. Answerable indirect carrier retrofit on one workflow carrier

Test: retrofit one carrier — candidate: the `<future_awareness>` block preamble in `.codex/get-shit-done/templates/context.md` — with the minimum-legibility set (provenance / local rationale / override conditions / doctrine cross-reference / downstream effect surfaces). Do not touch other carriers. Run one phase through the retrofitted template.

Minimum discriminating test: compare what the phase's researcher and planner surface as future-awareness constraints versus what the baseline template would have produced. If the retrofit changes what gets surfaced in ways that affect the plan, the minimum-legibility pattern earns local-proposal status. If not, the retrofit is reversed.

What would count as success: at least one future-awareness constraint that would have been listed generically (`protect future hosting modes`) instead appears with provenance (which artifact decided this), local rationale (why this matters here), override conditions (when this can be reopened), and a downstream-effect surface (where in the plan it should be visible).

What would count as failure: the retrofitted template produces the same content as the baseline or produces minimum-legibility blocks that are filled formulaically without changing the plan.

### BST-2. Graded underreach vocabulary applied to readiness-era `underreach` claims

Test: take the readiness package's and `04-17` bridge audit's most-cited `underreach` claims (roughly the set flagged in `CURRENT-STATE.md` and `AUDIT-CHARTER.md`) and reclassify each as `explicit miss`, `traceable undercarry`, or `retrospective possibility`. Record which remedy family each reclassification suggests per chunk 26 (`explicit miss` → strongest backward critique; `traceable undercarry` → translation/carrier repair; `retrospective possibility` → future-design attention).

Minimum discriminating test: do the reclassifications cluster such that the remedy families diverge? If yes, the gradient earns a place in `QUESTION-SET.md` as a required register for main-wave lanes. If no (all claims are `traceable undercarry`, say), the gradient collapses and remains reviewer-register only.

What would count as success: at least one reclassification where the `traceable undercarry` remedy family is clearly more appropriate than the `explicit miss` family, or vice versa, and where the baseline's flat `underreach` register would have missed the distinction.

What would count as failure: every claim reclassifies to the same grade, or the grades produce no discernibly different remedy families.

### BST-3. Revealing-case selection + internal comparison order on one main-wave lane

Test: the next main-wave lane (whichever shape is launched from `Proposal B-extended`) declares its anchor case by explicitly stating which of chunk 25's criteria it meets (rich prior terrain / thin recommendation / visible burden asymmetry / later corrective pressure / real downstream force). Its internal comparison order follows chunk 25's sequence (terrain → recommendation → burden → omitted adjacent surfaces → only then later correction).

Minimum discriminating test: does the anchor-case declaration produce a different case than the operator's gut-first choice would have? If yes, the selection discipline earns `ready_for_local_proposal` status for `QUESTION-SET.md`. If no (the operator's gut-first choice satisfies all the criteria), the discipline is validated as reviewer-register only.

What would count as success: the criteria rule out at least one candidate anchor case that would otherwise have been chosen, or rule in a case the operator was initially resistant to.

What would count as failure: the criteria confirm whatever the operator was going to do anyway.

### BST-4. Real-deferral delta-trace on `<deferred>` entries that cross phases

Test: at the end of the next phase-01 rerun CONTEXT.md, the `<deferred>` section's entries are split into two sets: (i) entries that the operator judges likely to resurface in a future phase, and (ii) entries that belong to the general backlog. Only set (i) gets the chunk-36 delta trace (prior trigger / updated trigger / short rationale / affected decision surface / next expected wake point).

Minimum discriminating test: at the start of the *next* phase's discuss loop, does the delta trace actually fire? That is: does the wake condition appear, the affected surface get checked, and the deferred item become part of the next phase's gray-area identification?

What would count as success: at least one deferred item surfaces at the right time in the next phase via the delta trace rather than via operator memory.

What would count as failure: the delta traces sit unread, or the operator surfaces items from memory regardless of trace.

## What Should Remain Reviewer-Register Only

Three candidates earn reflexive / reviewer-register status on this evidence.

### RR-1. Function / force diagnostic vocabulary

Use: apply `declared function` vs. `effective force` as a review-prose vocabulary when a specific artifact divergence needs naming. Do not embed as new claim-type markers or as required registers.

Gain considered and rejected as stronger: promote to `QUESTION-SET.md` as a required per-lane register. Rejected because lane-03 tested this at surface-level on three cases and found rephrasing only; the carriage-quality reread does not add new evidence that the prose vocabulary is insufficient without the register. Promoting without test evidence is closure pressure.

What would reopen it: a specific audit where function/force is the only vocabulary that discriminates an operative move from its descriptive neighbor, and where the outcome would be different without the distinction.

Why non-change is not just ease: the downgrade commits reviewers to actually using the vocabulary in audit prose when applicable. It is the register where the corpus's sharpest label should live. The register is non-trivial because it can be audited — a reviewer can be asked, `did you consider function/force here?` — without the ceremony of a required field.

### RR-2. Hidden governance regimes catalog

Use: reference the chunk-13 catalog (drift / institutional disavowal / procedural convenience / infrastructural sedimentation / burden displacement / prestige transfer / governance scarcity) when a function/force divergence is already detected and needs regime-diagnosis.

Gain considered and rejected as stronger: standing reference in `AGENTS.md` or `QUESTION-SET.md`. Rejected because the regime catalog is diagnostic (applied to already-surfaced divergences), not generative; a reviewer who has spotted the divergence reaches for the catalog, and a standing reference would not help them spot it.

What would reopen it: a specific audit where the default `under-propagated` language visibly fails to diagnose the regime, and where a chunk-13 regime label produces a materially different remedy.

Why non-change is not just ease: the downgrade preserves the catalog as recoverable vocabulary without embedding it in canon where it would need maintenance.

### RR-3. Fertile / empty incompleteness discipline on open-question quality

Use: when reviewing a CONTEXT.md or lane output, ask whether each open question is fertile (oriented unknown + directional next move) or empty (vague underdevelopment). Apply in review prose, not as a template field.

Gain considered and rejected as stronger: add a required `inquiry:` field per `<open_questions>` entry. Rejected because the corpus itself argues (chunk 23) that a good question-path does not always yield a concrete next-inquiry; forcing one risks anti-recapture failure — the open question gets translated into the incumbent schema's terms prematurely.

What would reopen it: a pattern across multiple phases where operators repeatedly write empty open questions that survive into planning, and where the fertile/empty distinction would have caught them prospectively.

Why non-change is not just ease: the reviewer-register application is already strong enough to catch the worst cases; a template field would risk flattening legitimate unstructured open questions.

## What Should Not Be Rewritten Yet

### NR-1. `CLAIM-TYPES.md`

`CLAIM-TYPES.md` is the tempting surface to rewrite because the corpus proposes a two-layer ontology and the derivative translation makes that proposal explicit. The spec warns against this specifically. Rewrite is premature for three reasons:

1. Lane-03 already tested the strongest corpus candidates (function/force, declared/effective authority, graded underreach) against three readiness-era cases with the existing markers and found rephrasing only. The carriage-and-operationalization reread does not add new evidence that the markers are insufficient.
2. The cases that would stress-test the markers are doctrine-stressed cases (cases where the existing doctrine genuinely under-determines disposition). Lane-03's three cases were doctrine-friendly. Rewriting `CLAIM-TYPES.md` on rephrasing-only evidence would be exactly the kind of premature inheritance the spec forbids.
3. The corpus's own `labels that change practice` test (chunk 26) argues against adding markers that do not change tone, burden, remedy, or rerun pressure. None of the candidate markers has produced a tested effect.

What would reopen it: a doctrine-stressed stress test (different from lane-03) where the existing markers cannot discriminate an operative move the new layer can.

Why non-change is not just ease masquerading as caution: there is a bounded move available (LP-2 dependency-map footer, RR-3 reviewer-register rule on open-question quality) that picks up the temporal-decay pressure without marker-system commitment. Non-change here is specifically non-rewrite; the pressure is absorbed in the nearby surfaces that can carry it.

### NR-2. `POST-FALSIFICATIONIST-REVIEW-DOCTRINE.md`

Tempting to rewrite because the graded underreach vocabulary (chunk 26) would sharpen its flat `underreach` language and because the decay diagnostic (chunk 19) applies to its own readiness-era anchoring. Rewrite is premature because the doctrine already has epistemic gap subtypes, progressive/degenerating, and anti-regret scope judgment that cover most of the corpus's sharper labels. BST-2 above tests whether the graded underreach vocabulary earns a register in `QUESTION-SET.md` first; rewriting the doctrine before that test is closure pressure.

What would reopen it: BST-2 success showing the gradient produces divergent remedy families, plus evidence that the doctrine's anti-regret clause fails to catch a specific case the gradient catches.

### NR-3. Runtime wrapper workflow files beyond `discuss-phase.md` + `context.md`

Tempting to widen the LP-3 disposition field and the BST-1 answerable indirect carrier retrofit to every workflow carrier (plan-phase.md, execute-phase.md, review-phase.md, research-phase.md). Widening is premature because the bounded stress tests have not yet produced the one-carrier evidence. The spec specifically forbids silent widening into ecosystem doctrine.

What would reopen it: BST-1 success on one carrier plus LP-3 success on the CONTEXT.md template plus a visible downstream-behavior change; at that point, a specific widening proposal could be written with test evidence.

## Rejected Stronger Moves Register

Five stronger candidate operationalizations were considered and downgraded. Each is named with its plausibility, its expected gain, the downgrade target, and the evidence that would reopen it.

### RSM-1. Full rewrite of `CLAIM-TYPES.md` around a two-layer ontology (claim layer + discourse-forming handles)

Plausibility: the derivative translation explicitly proposes this. Chunks 13 (function/force, hidden governance regimes) and 19 (provisional audit ontology) pressure directly at claim-centrism. Chunk 04's carry-forward gradient (reflective / reinforcing / reproductive / instituting) is sharper than the doctrine's progressive/degenerating.

Possible gain: a second ontology layer would operationalize `contestation of framing`, `contestation of burden structure`, and `metalevel contestation of the audit ontology itself` as first-class review moves rather than as prose work. Reproducibility by later reviewers would improve for cases where the current marker system depends on operator judgment about which level a criticism lives at.

Why downgraded to `not_earned_on_this_evidence`: lane-03 tested the same pressure on three readiness-era cases with the existing markers and found rephrasing only; carriage-and-operationalization rereading adds no new evidence that the markers are failing. The move would require either (a) doctrine-stressed cases showing the current markers under-determine disposition, or (b) operational evidence that reviewers confuse layers in practice. Neither exists in this evidence base.

Evidence that would reopen: a stress-test lane constructed from cases where existing doctrine genuinely does not discriminate (unlike lane-03's doctrine-friendly set), plus at least one case where a two-layer marker produces a different final disposition than the existing markers produce.

### RSM-2. Mandatory disposition-as-state-change pass across all harness carriers (discuss-phase, plan-phase, execute-phase, review-phase, research-phase)

Plausibility: chunk 36's disposition vocabulary is compact (`act now` / `open inquiry` / `defer with updated trigger` / `retire`) and would produce structural clarity across the runtime. The current workflow relies on prose conventions that agents may or may not follow.

Possible gain: eliminates `acknowledged / noted` rituals across the runtime; forces state-change discipline at every phase boundary; makes downstream-agent reading of prior phase output more reliable.

Why downgraded to `ready_for_bounded_stress_test` (already caught above as LP-3 + BST-1 on narrower scope): widens from the bounded surface set into runtime-wide template rewrite, which the spec forbids. Also, the evidence base (chunks 35-37) supports the pattern but has not been tested on even one carrier — widening without test evidence is the ceremonial trap chunk 35 explicitly warns against.

Evidence that would reopen: LP-3 success on `<open_questions>` + BST-1 success on the answerable-indirect-carrier retrofit, plus at least one observed failure case where a downstream agent missed an open question or deferred item because the template did not force disposition.

### RSM-3. Build a decay-review harness — scheduled rereads of preserved tensions, inquiry debt entries, authority classes, with delta-shaped traces and dependency-map updates

Plausibility: chunks 19 + 34 + 35 + 36 + 37 jointly propose this infrastructure; it would protect against the fossilization risk they all name. Would combine temporal discipline (chunk 19 decay diagnostics), routing discipline (chunks 34-35), and delta-trace discipline (chunks 36-37).

Possible gain: prevents the workspace's own canon from silently aging into stale authority; makes future-preservation machinery more legible.

Why downgraded to `not_earned_on_this_evidence`: creates schedule infrastructure whose cost is high without evidence any current preserved claim has actually decayed. Schedule infrastructure is itself the kind of ceremonial-drift-generating machinery chunk 35 warns about. The narrower move (LP-2 dependency-map footer with concrete trigger, BST-4 delta trace on `<deferred>` entries) picks up the most load-bearing pieces without the scheduled-rerun apparatus.

Evidence that would reopen: at least one concrete instance of a preserved claim from the readiness package that turns out to be steering current work despite the originating ecology having changed. Or an accumulation of cases where phase-to-phase `<deferred>` handoff fails specifically because delta traces would have helped.

### RSM-4. Require `revealing case` selection criteria and internal comparison order in `QUESTION-SET.md` as a register before main-wave launch

Plausibility: chunks 25-26 specify a principled sequencing. This would bind the anti-tame quota to a concrete discipline rather than to operator interpretation.

Possible gain: prevents hindsight-inflated critique across all main-wave lanes; makes case-selection and internal-comparison-order reproducible by later reviewers.

Why downgraded to `ready_for_bounded_stress_test` (caught above as BST-3): the discipline has not been tested against a main-wave-style lane. Promoting it now without evidence the criteria actually change the operator's case-selection would be the kind of procedural infrastructure cost the carriage criteria downgrade. The BST version lets one lane run through the discipline first.

Evidence that would reopen as `ready_for_local_proposal`: BST-3 success, i.e., the criteria rule out or rule in at least one case the operator would otherwise have chosen differently.

### RSM-5. Add function/force annotation as a standing field in `SESSION-FRAMING-BRIEF.md` and any similar situated-authority artifact, making the declared-vs-effective distinction a per-artifact convention

Plausibility: chunks 04 + 13 pressure lands directly on the situated-brief genre; `SESSION-FRAMING-BRIEF.md` is the most obvious candidate where declared Class-2 and effective Class-1-adjacent force diverge.

Possible gain: makes the divergence auditable per-artifact rather than having to be recovered from the workspace authority note.

Why downgraded to `reviewer_register_only`: a standing field in every situated-brief artifact would cascade into a repo-wide convention about what authority-class language each brief should carry. LP-1 (the workspace authority note's supplementary pass) already covers the per-artifact divergence at one level removed. Adding function/force annotation to each brief without evidence that the authority note's supplementary pass is inadequate would be the kind of repo-wide template rewrite the spec forbids.

Evidence that would reopen as `ready_for_local_proposal`: LP-1 landing shows that the supplementary pass captures the divergence correctly, and a specific case where a later reviewer cannot recover the class from the supplementary pass but could from a per-brief field.

## Missing Evidence And Failure Modes

This review could fail in the following ways; a skeptical reviewer should test each.

- **Corpus-sympathy failure**: reading the corpus too charitably and finding operational gain where there is only descriptive sharpening. Mitigation: lane-03's verdict is respected on Surface C (`CLAIM-TYPES.md` rewrite is rejected); the moves that are promoted are all (LP-1, LP-2, LP-3) concrete template/annotation edits with testable downstream effects; the bounded stress tests all specify discriminating criteria.
- **Corpus-skepticism failure**: reading the existing baseline too generously and missing where the corpus actually surfaces something the baseline cannot carry. Candidate underclaims: Surface D's answerable-indirect-carriers pressure could warrant a local proposal rather than a bounded stress test; Surface A's declared/effective authority could warrant a rewrite rather than a supplementary pass. The supplementary-pass and bounded-stress-test dispositions are conservative but defensible on the evidence.
- **Burden-symmetry failure**: the charter's symmetric burdens demand that both non-change and change carry burden of proof. This review promotes three local proposals (LP-1, LP-2, LP-3) — each states what closure pressure it introduces and what narrower move was rejected. It downgrades one stronger move (RSM-1 two-layer ontology) to `not_earned_on_this_evidence` and names what would reopen it. A skeptical reviewer could argue either (a) the local proposals are too shallow for the pressure the corpus exerts, or (b) some of them are ceremony without evidence of need. Both are live criticisms. The minimum-discriminating tests specified for the BST dispositions are the primary procedural protection against (b).
- **Surface-set bias failure**: the surface set was chosen by the spec; a different surface set (e.g., if `.planning/LONG-ARC.md` or wrapper agent `SKILL.md` files had been included) might have produced different dispositions. This is a real limitation. The dispositions are conditional on the four-surface set.
- **Frame-inheritance failure**: the spec itself specifies the four disposition labels and the nine carriage criteria. If those are too coarse (e.g., `reviewer_register_only` might collapse meaningful sub-distinctions like `registered for reviewer use` versus `registered for spec-writing economy`), dispositions could be too blunt. The mitigation is the `Why not one step stronger or weaker` column in the matrix, which forces each disposition to defend its step against both neighbors.
- **Lane-03 leakage failure**: reading this lane through lane-03's verdict biases toward conservative outcomes. The mitigation is Section NR-1's explicit acknowledgment that Surface C stays unchanged specifically because lane-03 tested the same pressure and carriage-reread adds no new evidence, while other surfaces (A, B, D) produce local proposals or bounded stress tests that lane-03 did not reach.
- **Session-framing-brief bias failure**: `SESSION-FRAMING-BRIEF.md`'s posture (anti-tame, anti-human-timeline, intervention-friendly) pushes toward finding operational gains. The mitigation is that Surface C stays unchanged against this pressure; the three local proposals are all bounded; the stronger moves (RSM-1, RSM-3) that would most satisfy the anti-tame posture are explicitly rejected.

## Recommended Next Local Sequence

The ordered sequence below names concrete next moves, not another research round. Each step either commits to a bounded edit or specifies a minimum discriminating test.

1. **Adopt the three local proposals as draft supplements / template edits**, in order LP-1 → LP-2 → LP-3:
   - LP-1: draft the `Declared vs. Effective Authority Reread` section into `WORKSPACE-AUTHORITY-AND-ORGANIZATION.md`; name at least two concrete divergences (expected candidates: `SESSION-FRAMING-BRIEF.md`; `STATUS.md`); submit for local review before committing.
   - LP-2: draft the dependency-map-as-revisable-hypothesis footer into `WORKSPACE-AUTHORITY-AND-ORGANIZATION.md`, specifying the reread trigger concretely; submit for local review before committing.
   - LP-3: edit `.codex/get-shit-done/templates/context.md` to add the `disposition:` field per `<open_questions>` entry; update `discuss-phase.md` write step to surface dispositions in the summary; do not propagate to other templates.
2. **Run BST-4 on the next CONTEXT.md** that has `<deferred>` entries likely to cross phases. The delta trace is applied only to entries the operator judges likely to resurface; at the start of the next phase's discuss loop, check whether the trace fires.
3. **Run BST-2 (graded underreach vocabulary)** as a single-session pass against the readiness package's and `04-17` bridge audit's cited `underreach` claims. This is a bounded offline pass, not a new lane. Its output is a reclassification table.
4. **Run BST-1 (answerable indirect carrier retrofit)** on the `<future_awareness>` block preamble in `context.md` after LP-3 has landed. BST-1 follows LP-3 because the template is already being edited.
5. **Run BST-3 (revealing case + internal comparison order)** as part of the next main-wave lane spec rather than as a standalone test. This means the next `Proposal B-extended` lane spec declares its anchor case against chunk 25's criteria and fixes the internal comparison order per chunk 25.
6. **Do not rewrite `CLAIM-TYPES.md`, `POST-FALSIFICATIONIST-REVIEW-DOCTRINE.md`, or any other runtime wrapper workflow file** until BST-1 and LP-3 have produced concrete evidence.
7. **Treat reviewer-register uses (RR-1 through RR-3) as immediately live** in any subsequent audit prose. They do not require a separate commit to activate; they activate when a reviewer uses them.
8. **If any BST fails its discriminating test**, the corresponding operationalization rolls back (template edit reverted, register dropped) rather than being renormalized. The rollback posture is part of the anti-ceremonial-drift discipline chunks 35 and 36 explicitly demand.

## Optional Concrete Proposal Fragments

The fragments below are illustrative, not authorized changes. They are included so a reviewer can see the specific prose the local proposals would add.

### LP-1 fragment: draft supplementary section for `WORKSPACE-AUTHORITY-AND-ORGANIZATION.md`

```markdown
## Declared Vs Effective Authority Reread

- [d:r:i] The five authority classes define declared authority. Effective authority — the force an artifact actually exerts on later reading and routing — can diverge from its declared class.
- [d:r:i] On this workspace as of {date}, the following divergences are currently visible:
  - `SESSION-FRAMING-BRIEF.md` is declared Class 2 (briefing and procedural scaffolding). Its effective force on spec-writing and lane framing is closer to Class 1 because most lane specs cite it as situated authority and most external reviewers read it as the primary framing artifact. The declared-vs-effective gap is acknowledged; the brief itself states it is not neutral transcript replacement and should be pressure-tested against other primary artifacts.
  - `STATUS.md` is declared Class 1 (governing setup spine). Its effective force is lower than other Class 1 artifacts because it is live mutable state and a later reader at `t+1` cannot recover its state at `t` without git blame. Treat it as live relay, not as stable ratified state.
- [o:r:i] Additional divergences should be recorded here when they become visible.

### How To Read A Divergence

- A Class 2 artifact with Class 1 effective force does not automatically promote; the gap is an audit object.
- A Class 1 artifact with lower effective force does not automatically demote; it may need revisability reinforcement, not declassification.
- Where declared and effective authority disagree, write prose that acknowledges both rather than choosing one.
```

### LP-2 fragment: draft footer for `WORKSPACE-AUTHORITY-AND-ORGANIZATION.md`

```markdown
## Revisability Of This Authority Note

- [d:r:i] This note is a hypothesis about current routing and authority in this workspace, not settled routing truth. Dependency maps fossilize; this one will too if it is not reread.
- [d:r:i] Concrete reread triggers:
  - a new artifact class appears in this directory
  - a Class 2 artifact is cited as steering authority in three or more lane specs
  - a main-wave audit program is launched
  - a substantive disagreement arises about which class an artifact belongs to
- [d:r:i] When rereading, classify mismatches as map error, execution drift, or changed authority architecture rather than explaining them away as noise.
```

### LP-3 fragment: draft template change for `context.md` `<open_questions>` section

```markdown
<open_questions>
## Open Questions

Each open question carries a compact disposition that forces a state change at the end of the phase rather than passing a `noted` item forward silently.

Format per entry:

- **OQ-01:** [question]
  - *disposition:* one of `act-in-plan` | `research-next` | `defer-with-trigger` | `retire`
  - *wake condition* (only for `defer-with-trigger`): [concrete trigger + affected decision surface]

Allowed dispositions:

- `act-in-plan` — the open question must be resolved inside the current phase's plan; downstream planner is responsible for routing.
- `research-next` — the question routes into the next research lane; a bounded packet or trigger-conditioned entry is appropriate.
- `defer-with-trigger` — the question is carried forward, with a concrete wake condition and affected decision surface. `When this becomes relevant` is not a trigger.
- `retire` — the question was answered, superseded, or judged empty; briefly state which.

[If none: omit this section.]

</open_questions>
```

These fragments are not commits. They are the minimum prose the local proposals would add if accepted after local review, and they are included to show the dispositions are concrete rather than aspirational.
