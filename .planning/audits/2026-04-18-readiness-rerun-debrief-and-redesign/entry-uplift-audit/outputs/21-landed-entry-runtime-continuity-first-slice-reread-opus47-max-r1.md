Date: 2026-04-22
Status: active reread output

# Landed Entry Runtime Continuity First Slice Reread — Opus 4.7 Max R1

## What The Landed Entry Slice Now Carries More Clearly

- [d:r:i] The repo-local entry/runtime continuity surface now exists as one dedicated reference at [tooling/portable-gsd/overlay/get-shit-done/references/entry-runtime-uplift-continuity.md](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/get-shit-done/references/entry-runtime-uplift-continuity.md), owned by the tracked overlay as `add` in [tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json) line 24, distinct from `mandatory-initial-read.md` rather than absorbed into it. That distinction was a live ambiguity at proposal time; it is now a lived sibling boundary that future edits must move through manifest ownership rather than through implicit grammar widening.
- [d:r:i] The five-section minimum (`Primary Compact Read`, `Supporting Narrative Read`, `Deeper Typed Read`, `Interpretation Frame`, `When To Surface`) is now load-bearing rather than aspirational, because [tooling/codex/tests/test_entry_runtime_continuity_shared_reference_contract.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/tests/test_entry_runtime_continuity_shared_reference_contract.py) line 26-30 asserts each heading. The `122/123` shape now carries forward to a third reference instance, intensifying the cross-reference grammar as a repeatable pattern rather than a one-off precedent.
- [d:r:i] The grammar-only character of `mandatory-initial-read.md` is now both lived and tested. [tooling/portable-gsd/overlay/get-shit-done/references/mandatory-initial-read.md](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/get-shit-done/references/mandatory-initial-read.md) carries no mention of the new reference, and the contract at line 38-43 enforces that absence with a literal `assertNotIn`. The grammar-versus-content boundary now needs a deliberate test edit to drift, not just a forgotten line.
- [d:r:i] The four route states named in `129` are now structurally explicit inside the reference (lines 35, 43, 51, 59) and structurally enforced by the contract test (line 33-36). What was a verification slot in `128` is now a section topology that future edits can extend within rather than re-author from scratch.
- [d:r:i] The reference handles the absent-`.planning/` case explicitly through line 11 ("When `.planning/` does not exist yet, keep the compact read at route-state plus current execution context rather than pretending durable uplift memory already exists") and line 31 (Interpretation Frame's "When `.planning/` does not exist yet, use this reference to keep generated canon and operator reasoning aligned, not to claim that durable uplift memory already exists"). The milestone-boundary precedent at [tooling/portable-gsd/overlay/get-shit-done/references/milestone-boundary-uplift-continuity.md](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/get-shit-done/references/milestone-boundary-uplift-continuity.md) did not have to handle that case, so the entry-side reference carries one disambiguation the milestone-side reference never needed.
- [d:r:i] The read-only character of the entry route is now restated in three durable surfaces rather than one. The reference itself (line 30: "Do not run `$gsd-uplift-project --write` from `new-project.md` or `ingest-docs.md`"), the workflow consumer at [new-project.md](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/get-shit-done/workflows/new-project.md) line 119 ("Do not run `$gsd-uplift-project --write` from `new-project.md`; if later repo-local uplift refresh is still needed after initialization, route from `$gsd-progress`"), and [ingest-docs.md](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/get-shit-done/workflows/ingest-docs.md) line 111 ("Do not run `$gsd-uplift-project --write` inside ingest; keep later repo-local uplift refresh as a separate route after import") each carry the same boundary in their own voice, with the contract test at line 55, 67 enforcing each consumer-side line. The read-only-versus-write-side split is no longer a single clause that one edit could erase.
- [d:r:i] The provider horizon stays bounded to `.codex` observed basis plus held `.claude` annotation at the reference's Interpretation Frame (line 27), and STATE.md's `## Project Uplift` block at [.planning/STATE.md](/home/rookslog/workspace/projects/prix-guesser/.planning/STATE.md) line 88-90 ("Compatibility posture: observed_basis_only / Observed runtime basis: 1.38.3 / Held runtime annotation: .claude 1.34.2 (held_annotation)") encodes the same posture in durable form. The reference does not invent posture that the durable carriers fail to back.
- [d:r:i] The propagation refresh now sits as the eighth consecutive compatibility-family carrier in [propagation-audit/48-entry-runtime-continuity-shared-reference-change-triggered-refresh.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/propagation-audit/48-entry-runtime-continuity-shared-reference-change-triggered-refresh.md), placed after `47` exactly as the prior reread asked. The lane-position carry from `16` through `47` extends in one slot rather than backfilling into an older lifecycle row.
- [d:r:i] The typed `v2` registry now carries a dedicated contract row (`entry_runtime_uplift_continuity_contract` at [propagation-audit/artifacts/03-propagation-registry-v2-declared-contracts.json](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/propagation-audit/artifacts/03-propagation-registry-v2-declared-contracts.json) line 286-297 covering `install_overlay`, `workflow_output`, `reference_behavior`, `state_continuity`), one node, five edges (`overlay_to_entry_runtime_uplift_reference`, `entry_runtime_uplift_reference_to_new_project_workflow`, `entry_runtime_uplift_reference_to_ingest_docs_workflow`, `entry_runtime_uplift_reference_to_state`, `entry_runtime_uplift_reference_to_uplift_report`, `entry_runtime_uplift_reference_to_uplift_manifest` in [04-propagation-registry-v2-semantic-map.json](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/propagation-audit/artifacts/04-propagation-registry-v2-semantic-map.json) line 922-2302), three new evidence ids, and an explicit `_and_entry_runtime_continuity_shared_reference` suffix in [06-propagation-registry-v2-coverage-and-refresh.json](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/propagation-audit/artifacts/06-propagation-registry-v2-coverage-and-refresh.json) line 127. The carry into typed registry surfaces is no longer paraphrase; it is concrete row, node, and edge.

## Where The Live Implementation Boundary Still Thins Or Compresses Distinct Jobs

- [d:r:i] The reference's Primary Compact Read names "active route-state signal" with a sub-bullet that includes "current execution-context runtime", but does not scope which runtime values the entry-continuity step should treat as in-scope versus out-of-scope. `new-project.md` line 86-97 still detects `RUNTIME` across `.codex`, `.gemini`, `.opencode`, and `.claude` (default). A reader following the reference's compact-read step under a `gemini` execution context would have to traverse the `Interpretation Frame` (line 27 names the `.codex`/`.claude` horizon) before realizing the entry-continuity surface is not the place to inherit `gemini`-runtime work. The compact-read step and the provider-horizon discipline now live two sections apart inside the same reference, when the natural reader path runs through the compact-read step first.
- [d:r:i] The `When To Surface` triggers carry uneven evaluability across the four route states. Brownfield (line 47, "existing code or docs show this repo is not a blank start even though `.planning/` is absent"), New Mode (line 55, "imported docs are older or vanilla GSD docs"), and Merge Mode (line 63, "existing `.planning/STATE.md` already carries `## Project Uplift`") each carry one route-evaluable signal that maps to init JSON or filesystem state. Greenfield (line 37) carries only interpretive signals — "repo-local runtime or governing doctrine is already present in the repo and should remain explicit in the first canon" needs operator judgment with no state-evaluable hook. The four route states sit under one heading shape, but the trigger evaluability lands at three out of four rather than four.
- [d:r:i] `new-project.md` Section `1.5` (line 112-121) sits between Step 1 (runtime detection) and Step 2 (Brownfield Offer). That position compresses two route-state moments — post-runtime-detection-pre-brownfield-decision and post-brownfield-decision-pre-mapping — into one continuity-review beat. The Brownfield trigger inside the reference depends on signals (existing code/docs, prior findings, repo-local doctrine) that the upstream Step 2 may surface explicitly through `needs_codebase_map` and the brownfield mapping offer. The two beats now run independently rather than sharing one decision boundary, even though the proposal contemplated four route states with potentially distinct surfacing moments.
- [d:r:i] `ingest-docs.md`'s `<step name="review_entry_runtime_continuity">` (line 104-113) fires after MODE detection but before `<step name="discover_docs">` (line 115). That places the continuity-review beat ahead of the discovery approval gate (line 178-184). In merge mode, the operator can read uplift continuity surfacing for a merge that subsequently fails to discover any importable docs or that the operator aborts. The slice does not contradict its read-only character, but it does compress the merge-vs-no-merge distinction one beat earlier than the doc-discovery gate would naturally surface it.
- [d:r:i] The contract test enforces the heading topology and key safety lines, but its claim boundary thins in two places relative to the reread's verification list:
  - The test at line 31 enforces `Compatibility posture: observed_basis_only`, but does not enforce presence of "observed `.codex` basis" or "held `.claude` annotation" lines. A future reference edit that drops the explicit `.codex`/`.claude` provider-horizon language while keeping the top-level posture line would not break the contract.
  - The test at line 33-36 enforces the four route-state headings, but does not enforce that any trigger bullets sit underneath each heading. A future edit that empties the bullet lists while keeping the headings would still pass the contract.
- [d:r:i] The conditional surfacing language now appears in three sites within `new-project.md`: the `<supporting_reading>` block (line 16, "only when the greenfield/brownfield route states below say repo-local entry continuity should surface"), the Step 1 layered-packet bullet (line 109, same phrasing), and Section `1.5` itself (line 114). The contract test at line 50-55 enforces presence of the reference path string and Section `1.5`'s heading, not synchronization across the three repetitions. A future edit that updates one site without the others could drift the entry-side surfaces apart silently.
- [d:r:i] The asymmetry between the entry-side reference's `_when_present` qualifier on the state edge ([04-propagation-registry-v2-semantic-map.json](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/propagation-audit/artifacts/04-propagation-registry-v2-semantic-map.json) line 2273, `shared_reference_points_first_to_compact_state_digest_when_present`) and the milestone-side reference's unqualified equivalent (line 1629, `shared_reference_points_first_to_compact_state_digest`) carries the real semantic distinction that entry routes can hit a repo without `.planning/` while milestone boundaries cannot. The slice handled the asymmetry in the typed registry, but the reference text itself does not flag the asymmetry; a reader entering through the reference alone has no signal that this entry-side compact read carries a conditional its milestone-side sibling does not.
- [d:r:i] The implementation note `130` (line 21) records overlay ownership for the new reference as `add`, but does not flag that `new-project.md` and `ingest-docs.md` retained their pre-existing `overwrite` ownership rather than being newly tracked. [OVERLAY-MANIFEST.json](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json) line 44 and 46 confirm both stayed `overwrite`. A later reader inheriting from `130` alone could infer the slice silently widened manifest ownership across three files, when in fact only one new ownership slot was opened.

## What The Landed Mechanics Improve In Live Use

- [d:r:i] Grammar-only mandatory read versus sibling continuity reference: the live `mandatory-initial-read.md` carries no content pointer to entry-runtime continuity, the new reference lives as a sibling under the same `references/` folder, and the consumer workflows point at both surfaces by name through their `<required_reading>` (mandatory-initial-read) and `<supporting_reading>` (entry-runtime-uplift-continuity) blocks. The split that `122/123` proved at milestone boundaries now holds at entry boundaries, intensifying the doctrine that read-packet grammar ownership and topic-specific content ownership stay in different files even when the same workflow consumes both.
- [d:r:i] Route-state compact read versus existing uplift memory fallback: the reference's Primary Compact Read carries an explicit branch — start from route-state signal, conditionally widen into `## Project Uplift` when STATE.md exists, stay at route-state plus execution context when `.planning/` does not yet exist. The compact-read grammar now distinguishes "uplift memory exists" from "uplift memory does not yet exist" at the same step, which the milestone-boundary reference never had to do because milestone boundaries always run with `.planning/` present.
- [d:r:i] Greenfield versus brownfield entry surfacing: the reference and Section `1.5` of `new-project.md` carry the distinction explicitly. Brownfield's trigger language maps to init JSON state (`is_brownfield: true` even when `planning_exists: false`); Greenfield's trigger language stays interpretive but acknowledges that some greenfield repos already carry repo-local runtime/doctrine that the first canon should preserve rather than discard.
- [d:r:i] New-mode versus merge-mode ingest surfacing: `ingest-docs.md`'s Section `review_entry_runtime_continuity` carries the merge-mode trigger gated on the literal `.planning/STATE.md` `## Project Uplift` presence, and the new-mode trigger gated on imported-doc vintage and the route's later-uplift likelihood. Merge mode's trigger evaluation can run from filesystem state alone; new-mode's trigger evaluation needs operator judgment about doc vintage. Both modes route their later repo-local uplift through `$gsd-uplift-project --write` as a separate beat (line 315 in `ingest-docs.md` carries the same route split for the route_merge_mode step).
- [d:r:i] Observed `.codex` basis plus held `.claude` annotation carry: the reference's Interpretation Frame (line 27), STATE.md's `## Project Uplift` block (line 88-90), [.planning/UPLIFT-REPORT.md](/home/rookslog/workspace/projects/prix-guesser/.planning/UPLIFT-REPORT.md) line 24-46, and [.planning/UPLIFT-MANIFEST.json](/home/rookslog/workspace/projects/prix-guesser/.planning/UPLIFT-MANIFEST.json) line 78-110 all carry the same `observed_basis_only` posture plus the held-annotation slot. The reference does not invent a posture the durable carriers fail to back. Held-annotation carry stays at annotation, not relabeling.
- [d:r:i] Propagation visibility: the typed `v2` registry now carries one new contract, one new node, six edges, three new evidence ids, and a refresh slot named `48`. A reader inheriting this slice from the typed registry alone (without re-reading `130`) can trace the full producer/consumer/state/widening graph for the entry-runtime reference. The propagation chain visibility now matches the milestone-boundary slice's chain visibility row-for-row, except that the entry-side state edge carries the `_when_present` qualifier the milestone-side state edge does not need.

## What Still Deserves Revision Before Later Entry-Adjacent Follow-Through

- [d:r:i] Tighten the contract test to lock the `.codex`/`.claude` provider-horizon language. Add `assertIn("observed `.codex` basis", reference)` (or close paraphrase tolerant to typographic backticks) and `assertIn("held `.claude` annotation", reference)` so the provider horizon cannot drift through reference edits that keep `Compatibility posture: observed_basis_only` while dropping the explicit `.codex`/`.claude` lines. The Interpretation Frame is the place the provider horizon sits live; the contract should match.
- [d:r:i] Tighten the contract test to lock at least one trigger bullet under each of the four `When To Surface` route-state headings. A bounded addition would parse each heading's section and assert at least one bullet bullet. That stops a future thinning edit from preserving the heading topology while emptying the trigger content.
- [d:r:i] Connect the Primary Compact Read's "current execution-context runtime" wording to the provider-horizon discipline at the same step rather than only at the Interpretation Frame. One added sentence at the compact-read step — scoping the runtime signal to the `.codex` observed basis or `.claude` held annotation when surfacing entry continuity — would close the section-traversal gap between "read this signal first" and "this signal's provider horizon is bounded".
- [d:r:i] Concretize the Greenfield trigger list with at least one route-evaluable signal alongside the interpretive lines. One example: presence of `.codex/` or `.claude/` runtime files in a repo where `.planning/` is absent. That gives the operator a route-evaluable hook that maps to init JSON or filesystem state, matching the evaluability the Brownfield, New Mode, and Merge Mode triggers each carry.
- [d:r:i] Surface the `_when_present` asymmetry inside the reference's Interpretation Frame or `When To Surface` prose. The semantic-map edge already records that the entry-side compact-read step has a conditional the milestone-side equivalent does not need; the reference text should mention the asymmetry once so a reader entering through the reference alone sees the distinction without having to read the registry.
- [d:r:i] Sharpen the implementation note `130` to flag that overlay ownership widened for the new reference only, while `new-project.md` and `ingest-docs.md` retained their pre-existing `overwrite` ownership. The current note risks future readers inferring three new ownership slots opened when only one did.
- [d:r:i] Consider folding these reference-side and contract-side sharpenings into the next consumer-inheritance batch (the `update` plus `gsd-update` slice) rather than carrying a standalone harden round. The `update` consumer will surface the provider-horizon ambiguity (does `update` running under a non-`.codex`/non-`.claude` runtime still surface entry continuity?) regardless of whether the reference is hardened first; co-staging the harden with the new consumer keeps one slice covering both moves and reduces the risk of recursive harden loops without new consumer proof.

## Later Families To Keep Explicit

- [d:r:i] `update` plus `gsd-update` consumer carry stays the next adjacent branch rather than a held-forever family. The shared-reference shape is now proven at two workflow consumers; the next workflow-plus-wrapper inheritance is the natural next bounded move. Holding it for a separate batch — rather than pulling it into this slice's implementation note retroactively — keeps installer-supported runtime breadth and repo-local operator continuity in distinct slices at their introduction moment.
- [d:r:i] `from-gsd2` plus `gsd-from-gsd2` skill-wrapper consumer carry stays held until the workflow-plus-wrapper precedent lands at `update`. The shared-reference shape has zero wrapper-level precedent; staging the skill-wrapper case after the workflow-plus-wrapper case keeps each level proving once before the next level inherits.
- [d:r:i] Broader installer/runtime detection narrowing inside the workflow runtime-detection blocks (`codex`/`gemini`/`opencode`/`claude` listing in `new-project.md` line 86-97 and the equivalent block in `ingest-docs.md` line 94-100) stays a separate later question. The new reference does not authorize narrowing that block; the slice intentionally added a bounded continuity carrier beside ordinary runtime detection rather than rewriting it. Future narrowing remains a separate route with its own authorization.
- [d:r:i] Live `.claude` route translation or parity push stays held. The Interpretation Frame uses "held `.claude` annotation" — annotation, not parity — and STATE.md's posture confirms the held character.
- [d:r:i] Compatibility matrix, version-window, third-runtime, and standalone compatibility-carrier widening stay held. UPLIFT-MANIFEST.json line 105-109 keeps `held_later` for "version-window claims beyond the observed runtime basis", "cross-runtime compatibility matrix", and "upstream-template drift compatibility"; the slice did not move any of these.
- [d:r:i] Silent widening of `mandatory-initial-read.md` from grammar-only to grammar-plus-content-pointer stays held. The contract test enforces absence of the entry-runtime path string from `mandatory-initial-read.md`. Any future widening would need its own authorization and would need to update the contract test in the same slice rather than slip past it.
- [d:r:i] Helper-side `project_uplift.py` widening, `RUNTIME_DIRS` versus `HELD_CLAUDE_RUNTIME_VERSION_REL_PATH` asymmetry, structural-row promotion inside STATE.md or UPLIFT-MANIFEST.json, write-recommending `compatibility_drift_reasons` widening, and standalone compatibility carrier all stay held. The slice did not touch `project_uplift.py`.
- [d:r:i] Extraction or npm/`npx` distribution work from `115` stays held until the current uplift, cross-runtime, and propagation contracts sharpen further across `update`, `from-gsd2`, and any later consumer.
- [d:r:i] Whether `complete-milestone.md` later adopts the full `required`/`supporting`/`deeper` packet grammar stays a separate later-family question; the entry-runtime slice does not change that judgment.
- [d:r:i] Family-6 wider route-asymmetry mapping stays parallelizable rather than silently sequenced behind the entry-runtime branch.

## Strongest Adjacent Strengthening Route

- [d:r:i] The strongest adjacent route is `update` plus `gsd-update` next, with the small reference-side sharpenings folded inside that same slice rather than carried as a standalone harden round. Reasons in the carry / propagation visibility / maintainability / route-ownership frame:
  - carry: `update.md` is the workflow-plus-wrapper pair carrying the widest installer-supported runtime breadth among the candidate consumers. Pulling it in next forces the entry-runtime reference to face the disambiguation between "update running under `.codex` or `.claude` should surface entry continuity" versus "update running under another runtime should not". That disambiguation is what the reference's Primary Compact Read currently leaves implicit; the `update` consumer slice is the natural moment to make it explicit, in both the reference text and the contract test.
  - propagation visibility: a single `update` slice with the matching reference-side sharpening lets one propagation refresh sibling (the next slot after `48`) carry both the new consumer and the reference harden in one row, rather than a refresh for the harden alone followed by another refresh for the consumer. The typed registry already carries an `update_follow_through_contract` row at [03-propagation-registry-v2-declared-contracts.json](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/propagation-audit/artifacts/03-propagation-registry-v2-declared-contracts.json) line 172-183 that the new consumer would augment rather than introduce; one refresh sibling preserves the eight-step compatibility-family chain from `16` through `48` without inserting a maintenance-only beat between them.
  - maintainability: the workflow-plus-wrapper precedent should land before the skill-wrapper-only case (`from-gsd2`) inherits. `update` carries both layers; staging it first lets the reference earn its first wrapper-level proof while the workflow-level proof is fresh. `from-gsd2` then inherits a reference that has both workflow and wrapper precedents rather than starting wrapper-side without a workflow-side companion.
  - route ownership: `update` owns runtime/package change and clean-install-warning; `from-gsd2` owns format migration. Staging `update` next keeps installer-supported runtime widening beside the entry-runtime continuity widening at one moment, and migration carry separate at a later moment. Both stay distinct from this slice's earliest-entry continuity carry, which has now proven at two workflow consumers.
- [d:r:i] An alternative — harden the reference standalone before the next consumer inherits — is a defensible route when the reference's gaps are load-bearing for the next consumer regardless of which consumer comes next. Here the gaps (provider-horizon at compact read, evaluable Greenfield trigger, contract-test thinning) are real but small, and the `update` consumer slice would have to revisit them anyway when its own runtime-detection block intersects the entry-continuity step. Co-staging the harden with the consumer reduces the risk of a harden-reread-harden recursion that does not prove the reference at a third consumer. The standalone harden route should remain available if the `update` consumer slice later surfaces a gap that the harden cannot fold into the same batch cleanly.
- [d:r:i] A second adjacent alternative — open `from-gsd2` first because it is a smaller, more bounded surface than `update` — would invert the workflow-then-wrapper precedent the family has been carrying. The skill-wrapper layer would inherit a reference that has zero wrapper-side proof, and the workflow-plus-wrapper case would land later with the wrapper-side precedent already partially set by the skill-wrapper case. That inversion would compress the contract layers in a way the reread's `122/123` precedent argues against.

## How This Landed Slice Should Be Inherited

### Carry Forward

- [d:r:i] The dedicated reference at `tooling/portable-gsd/overlay/get-shit-done/references/entry-runtime-uplift-continuity.md` with the five-section minimum and the four route-state sub-sections, owned by the overlay manifest as `add`.
- [d:r:i] `mandatory-initial-read.md` staying grammar-only, enforced by the contract test's `assertNotIn`.
- [d:r:i] `new-project.md`'s direct `<supporting_reading>` pointer plus Section `1.5` Review Entry Runtime Continuity, enforced by the contract test's pointer-presence and section-header assertions.
- [d:r:i] `ingest-docs.md`'s direct `<supporting_reading>` pointer plus `<step name="review_entry_runtime_continuity">`, enforced by the same contract test pattern.
- [d:r:i] The read-only character of both consumer routes, with the literal "Do not run `$gsd-uplift-project --write`" language at the reference, both consumer surfaces, and the contract test.
- [d:r:i] The repo-local provider horizon of `.codex` observed basis plus held `.claude` annotation in the reference's Interpretation Frame, backed by STATE.md, UPLIFT-REPORT.md, and UPLIFT-MANIFEST.json.
- [d:r:i] The compact-to-narrative-to-typed widening order through `STATE.md` `## Project Uplift`, `UPLIFT-REPORT.md`, and `UPLIFT-MANIFEST.json`, with the entry-side conditional that handles absent `.planning/`.
- [d:r:i] The propagation refresh `48` placed as the sibling after `47`, extending the eight-step compatibility-family chain from `16` through `48` without backfill into older lifecycle rows.
- [d:r:i] The typed `v2` registry carry: contract row, node, six edges, three evidence ids, and the explicit `_and_entry_runtime_continuity_shared_reference` suffix on the change-triggered-slice-refresh status string.
- [d:r:i] The held-later naming of `update` plus `gsd-update` and `from-gsd2` plus `gsd-from-gsd2` as the next adjacent consumer branches, in that staged order.
- [d:r:i] The not-authorized boundaries the slice preserved: no broader installer/runtime detection rewriting in this slice, no `.claude` translation, no matrix/version-window/standalone-carrier widening, no helper-side `project_uplift.py` change.

### Revise Before Widening

- [d:r:i] Tighten the contract test at [tooling/codex/tests/test_entry_runtime_continuity_shared_reference_contract.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/tests/test_entry_runtime_continuity_shared_reference_contract.py) to assert presence of the `.codex` observed basis line and the `.claude` held annotation line in the reference, and to assert at least one trigger bullet under each of the four `When To Surface` route-state headings. Land the test edits inside the next `update` consumer slice.
- [d:r:i] Add one sentence at the reference's Primary Compact Read step that scopes the "current execution-context runtime" signal to the `.codex` observed basis or `.claude` held annotation when surfacing entry continuity, so the compact-read step and the provider-horizon discipline live at the same beat rather than two sections apart.
- [d:r:i] Concretize the Greenfield trigger list with at least one route-evaluable signal (for example, presence of `.codex/` or `.claude/` runtime files in a repo where `.planning/` is absent) so the four route states carry uniform trigger evaluability.
- [d:r:i] Surface the `_when_present` asymmetry between the entry-side and milestone-side state edges in one sentence inside the reference's Interpretation Frame, so a reader entering through the reference alone sees the conditional that the typed registry already records.
- [d:r:i] Sharpen the implementation note `130` to record that overlay ownership widened only for the new reference, while `new-project.md` and `ingest-docs.md` retained pre-existing `overwrite` ownership. Add the clarification next to the line that names overlay ownership as `add`.

### Hold For Later

- [d:r:i] Pulling `update` into a same-slice retroactive expansion of this entry-runtime batch. `update` should land as a separate next consumer slice that co-stages the reference-side sharpenings, not as backfill into the landed `130`.
- [d:r:i] Pulling `from-gsd2` into the next consumer slice. The skill-wrapper case should follow the workflow-plus-wrapper case, not run beside it.
- [d:r:i] Standalone harden of the reference without a new consumer inheritance. If the `update` consumer slice surfaces a gap the harden cannot fold cleanly, a standalone harden becomes available; until then, fold harden into the consumer slice.
- [d:r:i] Narrowing the `codex`/`gemini`/`opencode`/`claude` runtime-detection prose inside the entry workflow blocks. That broader rewriting stays a separate later question.
- [d:r:i] `.claude` route translation or parity work.
- [d:r:i] Compatibility matrix, version-window, third-runtime, or standalone compatibility-carrier widening.
- [d:r:i] Helper-side `project_uplift.py` widening, `RUNTIME_DIRS` versus `HELD_CLAUDE_RUNTIME_VERSION_REL_PATH` asymmetry, structural-row promotion inside STATE.md or UPLIFT-MANIFEST.json, write-recommending `compatibility_drift_reasons` widening.
- [d:r:i] Extraction and npm/`npx` distribution work from `115`.
- [d:r:i] Silent widening of `mandatory-initial-read.md` from grammar-only to grammar-plus-content-pointer; that grammar-surface change stays a separate explicit authorization.
- [d:r:i] Whether `complete-milestone.md` later adopts the full `required`/`supporting`/`deeper` packet grammar.
- [d:r:i] Family-6 wider route-asymmetry mapping; stays parallelizable rather than silently sequenced behind this branch.
