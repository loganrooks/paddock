Date: 2026-04-22
Status: active reread output

# Update Entry Runtime Continuity Follow-Through Proposal Reread — Opus 4.7 Max R1

## What The Update Consumer Branch Now Opens More Clearly

- [d:r:i] `131` chooses the first workflow-plus-wrapper consumer after the earliest-entry pair rather than the skill-wrapper-only `from-gsd2` case. [tooling/portable-gsd/overlay/get-shit-done/workflows/update.md](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/get-shit-done/workflows/update.md) and [tooling/portable-gsd/overlay/skills/gsd-update/SKILL.md](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/skills/gsd-update/SKILL.md) together carry a layer the landed pair does not yet reach: the wrapper's `<execution_context>` at line 62-64 directly feeds the workflow a repo-local `.codex/get-shit-done/workflows/update.md` path, which intensifies the workflow-to-wrapper contract into one concrete edge before the skill-wrapper-only `from-gsd2` case is asked to inherit. Staging `update` first broadens the precedent from a workflow-only surface into a workflow-plus-wrapper surface, so the next wrapper-only case (`from-gsd2`) enters a reference that has both levels of prior proof rather than a wrapper-level slot that has never been exercised.
- [d:r:i] `131` holds installer-supported runtime breadth and repo-local operator continuity as separate carries at the introduction moment. `update.md` line 52 names seven runtime candidate directories (`claude:.claude`, `opencode:.config/opencode`, `opencode:.opencode`, `gemini:.gemini`, `kilo:.config/kilo`, `kilo:.kilo`, `codex:.codex`), while the shared reference's Interpretation Frame at [tooling/portable-gsd/overlay/get-shit-done/references/entry-runtime-uplift-continuity.md](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/get-shit-done/references/entry-runtime-uplift-continuity.md) line 30 keeps repo-local continuity at observed `.codex` basis plus held `.claude` annotation. The proposal's explicit boundary ("not silently upgrade the broader multi-provider detection frontier into a repo-local continuity claim") names the exact seam where the two carriers could otherwise blur, so `update` becomes the slice that proves the installer-supported runtime detection block and the entry-runtime continuity reference can live in the same workflow without either absorbing the other.
- [d:r:i] `131` keeps the three-way route-ownership split that `update.md` line 341-348 already carries: update owns runtime/package change and clean-install warning; structural-health issues route to `$gsd-health`; later repo-local posture refresh routes to `$gsd-uplift-project --write`. That split already appears at three durable sites in the current `update.md` (line 313-315 in `compare_versions`, line 341-348 in `keep_route_boundaries_explicit`, line 578-579 in `display_result`). The proposal's decision to preserve that split rather than collapse routing into a generic uplift hand-off means the continuity reference inherits a route-separation surface that is already three-way rather than one that has to be re-authored in this slice.
- [d:r:i] `131` preserves `update` as a runtime/package-movement route rather than upgrading it into write-side uplift dispatch. That preservation matters because `update`'s existing clean-install step wipes and replaces `get-shit-done/` (line 378-380: `get-shit-done/` will be wiped and replaced), which means the continuity reference itself is a file that `update` both reads (as continuity carrier) and replaces (via installer rematerialization from the overlay). Keeping `update`'s character as runtime/package owner avoids blurring that reader/rewriter asymmetry into one hand-off where update somehow governs continuity content rather than merely surfacing it for operator reading.
- [d:r:i] `131` names `from-gsd2` as the next-next adjacent carrier after `update`, staying with the workflow-plus-wrapper-then-skill-wrapper order the `122/123` precedent established at milestone boundaries. The active held-list at proposal line 61-66 (`from-gsd2`, broader runtime-detection narrowing, `.claude` translation, matrix/version-window widening, extraction/npm distribution from `115`) matches the held-list the disposition at [entry-uplift-audit/dispositions/21-landed-entry-runtime-continuity-first-slice-reread-inheritance.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/entry-uplift-audit/dispositions/21-landed-entry-runtime-continuity-first-slice-reread-inheritance.md) line 53-59 carried forward.

## Where The Proposed Update Follow-Through Still Blurs Distinct Jobs

- [d:r:i] The proposal's "Fold the lane-17 local harden into the same batch" list at line 28-32 describes work that has already landed in-place. The commit `6f588ab` that inherited the reread also carried the three reference-side harden items, the two contract-test items, and the implementation-note clarification inside the same diff. The live reference now carries provider-horizon wording at the Primary Compact Read step (line 10-12), the `_when_present` asymmetry sentence inside the Interpretation Frame (line 32), and a route-evaluable Greenfield trigger (line 43: "`.codex/` or `.claude/` repo-local runtime files are already present while `.planning/` is still absent"). The live contract test at [tooling/codex/tests/test_entry_runtime_continuity_shared_reference_contract.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/tests/test_entry_runtime_continuity_shared_reference_contract.py) now carries `assertIn("observed \`.codex\` basis", reference)` (line 40), `assertIn("held \`.claude\` annotation", reference)` (line 41), and a whole `test_each_route_state_keeps_at_least_one_trigger_bullet` method (line 48-61). Note [intervention-proposals/130-entry-runtime-continuity-shared-reference-first-slice-implementation.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/intervention-proposals/130-entry-runtime-continuity-shared-reference-first-slice-implementation.md) line 23-24 now flags that overlay ownership widened only for the new sibling reference while the two consumer workflows stayed pre-existing `overwrite` carriers. The proposal's framing treats these as pending items to travel with the update slice; the carrier state treats them as already absorbed. That mismatch narrows the actual new work of `131` to the workflow-side pointer edit, the wrapper-side boundary edit, the contract-coverage extension for those two new surfaces, the re-materialization, and the propagation refresh — not the five-item harden batch the proposal describes.
- [d:r:i] The proposal leaves the sequencing of the continuity-reader beat inside `update.md` ambient. The current workflow runs: `get_installed_version` → `check_latest_version` → `compare_versions` → `keep_route_boundaries_explicit` → `show_changes_and_confirm` → `backup_custom_files` → `run_update` → `display_result` → `check_local_patches`. The continuity reference reads `.planning/STATE.md` `## Project Uplift` as its compact surface, which `update` does not mutate. But `update`'s `run_update` rematerializes the overlay, which includes the continuity reference file itself. So a continuity-reader placed before `run_update` reads the pre-update reference content; a reader placed after `run_update` may read freshly-installed content that the operator has not yet inspected. The proposal says `update.md` should "point directly at `entry-runtime-uplift-continuity.md` as a bounded supporting continuity surface" but does not say where in the nine-step flow that pointer surfaces or what the operator should read before versus after the clean-install wipe-and-replace. The `new-project.md` precedent placed the review at Section `1.5`, before mapping; `ingest-docs.md` placed it as a `<step name="review_entry_runtime_continuity">` between MODE detection and doc discovery. Neither precedent answers the `update` placement question.
- [d:r:i] The proposal's "provider-horizon" boundary language at line 44-46 is stated as a negative ("should not silently upgrade the broader multi-provider detection frontier into a repo-local continuity claim") without a positive evaluability hook. The `update.md` runtime detection at line 52 already binds `PREFERRED_RUNTIME` to one of five tokens (`claude`/`opencode`/`gemini`/`kilo`/`codex`). Two of those map to the repo-local continuity provider horizon (`codex`, `claude`); three do not (`opencode`, `gemini`, `kilo`). The proposal could carry one evaluable gate — surface the continuity reference only when `PREFERRED_RUNTIME` is `codex` or `claude` and the repo carries `.codex/` or `.claude/` state — that maps cleanly to existing state already computed in step 1. Without that gate, a reader following the proposal alone is likely to add the continuity-reader beat unconditionally, widening the reference silently into `opencode`/`gemini`/`kilo` territory that the Interpretation Frame explicitly excludes.
- [d:r:i] The proposal's workflow-carrier and wrapper-carrier asks are stated as near-parallel items ("Teach `update.md`... Teach `gsd-update/SKILL.md`...") but the two carriers have distinct jobs. The workflow carries a long step-by-step process (nine steps, heavy runtime detection, bash blocks); the wrapper is a thin forwarder whose `<objective>` at gsd-update/SKILL.md line 59 carries the route-ownership claim ("This skill owns runtime/package update and installer refresh. It does not by itself own structural planning repair or broader repo-local posture uplift"). The workflow-side pointer edit likely belongs in `<supporting_reading>` and/or as a sequenced review step; the wrapper-side edit likely belongs at the `<objective>` boundary and maybe at the `<process>` routing text at line 79-81. The proposal does not distinguish these two jobs, which risks either duplicating the workflow pointer inside the wrapper or dropping the wrapper-side boundary into the workflow's step list.
- [d:r:i] The proposal's verification list at line 51-55 blends work that has already landed with work that still needs to land. Item one ("`update.md` pointing at the shared reference") is new. Item two ("`gsd-update` preserving the same route and boundary language") is new. Item three ("continued separation between update-owned movement, structural-health routing, and later write-side uplift refresh") is new (the current test does not assert update-side route separation). Item four ("the widened reference-side assertions from lane `17`") is already landed inside the live test. Leaving item four in the verification list invites a reader to re-author assertions that are already present; splitting it out as "already landed" would keep the slice scope focused on the three new assertions.
- [d:r:i] The proposal's "Hold Outside This Slice" list at line 61-66 correctly holds the big families but stays silent on the narrower pressure that `mandatory-initial-read.md` might accumulate a content pointer through the update pathway. The contract test at line 68 (`assertNotIn("entry-runtime-uplift-continuity.md", text)`) already locks that absence for `mandatory-initial-read.md`. The proposal could name explicitly that the update slice keeps that grammar-only boundary, so a reader inheriting the slice has one fewer ambient question about where the pointer does and does not go.
- [d:r:i] The proposal asks for the propagation refresh "in the next available `propagation-audit/` slot" (line 71) without naming slot `49`. [propagation-audit/48-entry-runtime-continuity-shared-reference-change-triggered-refresh.md](/home/rookslog/workspace/projects/prix-guesser/.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/propagation-audit/48-entry-runtime-continuity-shared-reference-change-triggered-refresh.md) is the current end of the compatibility-family chain; the update slice's refresh should land at `49` to extend that chain by one slot rather than backfilling into an older row. Naming the slot keeps the compatibility-family lane-position carry from `16` through `48` visible to the implementer.

## What The Workflow Plus Wrapper Pair Should Carry Explicitly

- [d:r:i] `update.md` should carry the shared reference at its `<supporting_reading>` block, beside the existing packet lines (current line 11-16). The existing packet is sized for runtime-update narrowness ("command arguments and any immediately previous update output when this is a rerun", "detected runtime/version frontier", "`.planning/UPLIFT-MANIFEST.json` or `.planning/UPLIFT-REPORT.md` only when runtime/package movement is already understood and the next route becomes later repo-local posture refresh"). Adding one conditional line — the shared reference only when `PREFERRED_RUNTIME` is `codex` or `claude` and the repo carries repo-local runtime files — keeps the packet narrow rather than pulling in a multi-provider continuity surface that the reference's Interpretation Frame excludes.
- [d:r:i] `update.md` should carry one sequenced review beat at the place where the operator has already read installed/latest versions but has not yet wiped-and-replaced the overlay. That placement — between `compare_versions` and `show_changes_and_confirm`, or inside the existing `keep_route_boundaries_explicit` step — lets the operator read continuity from the current overlay content before the clean-install step replaces the reference file. After `run_update`, the reference is freshly-installed from the overlay; the pre-update read is the one that surfaces how the current repo understood continuity just before the clean install moved runtime/package state. The proposal currently does not name that placement.
- [d:r:i] `update.md` should carry the provider-horizon gate explicitly, not ambiently. Two gate conditions evaluate from state the workflow already computes: `PREFERRED_RUNTIME` ∈ {`codex`, `claude`} (already derived in `get_installed_version`), and presence of `.codex/` or `.claude/` repo-local state (already tested by the runtime-dir loop). The review beat can read the continuity reference when both conditions hold, and can name the alternative ("when running under `opencode`, `gemini`, or `kilo`, keep continuity ambient at the installer-supported runtime level rather than surfacing the repo-local `.codex`/`.claude` reference") so a reader does not infer the reference applies everywhere.
- [d:r:i] `update.md` should restate the read-only boundary at the review beat itself. The reference already carries "Do not run `$gsd-uplift-project --write` from `new-project.md` or `ingest-docs.md`"; the update-side review beat should carry a parallel line ("Do not run `$gsd-uplift-project --write` from `update.md`; if runtime/package movement is handled but repo-local posture still deserves refresh, route separately to `$gsd-uplift-project --write`") so the read-only character propagates in the workflow's own voice, matching the existing `keep_route_boundaries_explicit` step rather than depending on the reference alone.
- [d:r:i] `gsd-update/SKILL.md` should carry the boundary at `<objective>`, where it already owns scope ("This skill owns runtime/package update and installer refresh. It does not by itself own structural planning repair or broader repo-local posture uplift"). One added sentence — "This skill reads repo-local entry-runtime continuity when the active runtime is `.codex` or `.claude`; it does not translate that continuity into parity, matrix, or version-window claims" — keeps the wrapper's voice at the scope layer without duplicating the workflow's step-level content. The `<process>` routing text at line 79-81 can stay as-is; the boundary work belongs at `<objective>`.
- [d:r:i] `gsd-update/SKILL.md` does not need a separate `<execution_context>` pointer at the continuity reference. The wrapper's current `<execution_context>` at line 62-64 already points at the workflow file, and the workflow carries the pointer. Adding a second `<execution_context>` row would duplicate the reference-path string across two files and create a drift surface where one could be updated without the other.
- [d:r:i] The contract test should add three assertions distinct from the already-landed reference-side ones:
  - `update.md` contains the `@__PROJECT_ROOT__/.codex/get-shit-done/references/entry-runtime-uplift-continuity.md` pointer string.
  - `update.md` contains the update-side read-only boundary line (something like "Do not run `$gsd-uplift-project --write` from `update.md`").
  - `gsd-update/SKILL.md` contains the wrapper-side boundary sentence at `<objective>`.
- [d:r:i] The propagation refresh at slot `49` should record: the new consumer edge from the shared reference to `update.md`, the new wrapper edge from the shared reference to `gsd-update/SKILL.md` (through the workflow), the preserved read-only character at the update side, the provider-horizon gate at `codex`/`claude`, and the continuity-reader beat's placement between `compare_versions` and `show_changes_and_confirm`. That refresh is the typed-registry carrier for the consumer widening that the proposal asks for.

## What To Fold Into This Same Slice Before Implementation

- [d:r:i] Acknowledge explicitly that the lane-17 reference-side, contract-side, and note-side harden items have already landed in-place, so the slice's scope is the workflow-side pointer, the wrapper-side boundary, the three new contract assertions, the re-materialization, and the propagation refresh slot `49`. Naming this lets the implementer avoid redundant work on reference/test/note lines that already carry the harden.
- [d:r:i] Name the continuity-reader placement inside `update.md` (recommended: between `compare_versions` and `show_changes_and_confirm`, so the operator reads continuity before the clean-install wipe-and-replace). Without a named placement, the implementer can drop the pointer into `<supporting_reading>` alone and leave the review beat ambient across nine steps.
- [d:r:i] Name the provider-horizon gate evaluably: surface the continuity-reader beat only when `PREFERRED_RUNTIME` is `codex` or `claude` and the repo carries `.codex/` or `.claude/` state. The workflow already computes both conditions in step one; the gate can read those values directly rather than asking the operator to interpret the Interpretation Frame.
- [d:r:i] Name the wrapper's boundary location at `gsd-update/SKILL.md` `<objective>`, distinct from the workflow's `<supporting_reading>` and review beat. The distinction between the wrapper's scope-layer boundary and the workflow's step-layer boundary intensifies the workflow-plus-wrapper carry that `update` proves first for this family.
- [d:r:i] Name the propagation refresh as slot `49`, extending the compatibility-family chain by one row rather than backfilling into older lifecycle rows.
- [d:r:i] Restate that `mandatory-initial-read.md` stays grammar-only through this slice and that the contract test's `assertNotIn("entry-runtime-uplift-continuity.md", text)` continues to lock that absence. Making this explicit in the proposal stops a reader from inferring the update slice is the right moment to widen grammar-only into grammar-plus-content-pointer.
- [d:r:i] Clarify that `update`'s clean-install step rewrites the continuity reference via overlay rematerialization, so `update` both reads and (via installer) writes the reference file. That asymmetry is what distinguishes `update` from `new-project.md` and `ingest-docs.md` at the continuity-reader beat; naming it in the proposal stops later readers from treating `update` as a purely passive consumer.

## What To Keep Explicitly Later

- [d:r:i] `from-gsd2` wrapper carry stays the next adjacent consumer after the `update` slice lands, preserving the workflow-plus-wrapper-then-skill-wrapper order that `122/123` established at milestone boundaries.
- [d:r:i] Broader runtime-detection narrowing inside `update.md`'s `RUNTIME_DIRS` block (line 52, seven runtime candidate directories) stays held. The update slice adds a continuity-reader beside the existing runtime detection rather than narrowing the multi-provider detection itself.
- [d:r:i] Live `.claude` route translation or parity work stays held. The reference's Interpretation Frame keeps `.claude` at "held annotation" posture; the update slice reads that annotation, it does not translate the annotation into parity.
- [d:r:i] Compatibility matrix, version-window, third-runtime, and standalone compatibility-carrier widening stays held per the matching entries in [.planning/UPLIFT-MANIFEST.json](/home/rookslog/workspace/projects/prix-guesser/.planning/UPLIFT-MANIFEST.json) line 105-109.
- [d:r:i] Extraction and npm/`npx` distribution work from `115` stays held until the current uplift/cross-runtime and propagation contracts sharpen farther across update, `from-gsd2`, and later consumers.
- [d:r:i] Standalone harden-only recursion over the already-landed reference/test/note boundary stays held. If the update consumer slice surfaces a reference-side gap the slice cannot absorb, a standalone harden becomes available; until then, co-staging is the cleaner route.
- [d:r:i] Widening `mandatory-initial-read.md` from grammar-only to grammar-plus-content-pointer stays held; the contract test continues to lock the absence.
- [d:r:i] Helper-side `project_uplift.py` widening, `RUNTIME_DIRS` versus `HELD_CLAUDE_RUNTIME_VERSION_REL_PATH` asymmetry, structural-row promotion inside `STATE.md` or `UPLIFT-MANIFEST.json`, and write-recommending `compatibility_drift_reasons` widening all stay held.
- [d:r:i] Whether `complete-milestone.md` later adopts the full `required`/`supporting`/`deeper` packet grammar stays a separate later-family question.
- [d:r:i] Family-6 wider route-asymmetry mapping stays parallelizable rather than silently sequenced behind the update slice.

## Strongest Next Route

- [d:r:i] The strongest adjacent route is the `update.md` plus `gsd-update/SKILL.md` consumer slice, with scope tightened to the workflow-side pointer, the review-beat placement, the provider-horizon gate, the wrapper-side boundary at `<objective>`, three new contract assertions distinct from the already-landed ones, re-materialization into live `.codex`, and propagation refresh at slot `49`. Reasons in the carry / propagation visibility / maintainability / route-ownership frame:
  - carry: `update.md` is the first workflow-plus-wrapper pair whose continuity surface intersects installer-supported runtime breadth. Running the slice now intensifies the precedent that broader runtime detection (the `RUNTIME_DIRS` block) and narrower entry-runtime continuity (the `.codex` basis plus `.claude` annotation horizon) can live in the same workflow without either absorbing the other. The narrower gate — surface continuity only when `PREFERRED_RUNTIME` is `codex` or `claude` — makes that asymmetry evaluable from state the workflow already computes, widening the precedent from the two-route-state carry at `new-project.md` and the two-mode carry at `ingest-docs.md` into a provider-gated carry at `update.md`.
  - propagation visibility: one propagation refresh at slot `49` captures the new consumer edge, the new wrapper edge, the read-only boundary preservation, the provider-horizon gate, and the review-beat placement in one row, extending the eight-step compatibility chain from `16` through `48` by one slot rather than inserting a maintenance-only refresh between them. The typed registry's existing `update_follow_through_contract` row grows to cover the new consumer carry rather than requiring a parallel entry.
  - maintainability: the lane-17 harden landed in-place in `6f588ab` before this reread, so the slice does not need to re-author the reference, contract, or note. That keeps the slice's new surface bounded to the three carriers that are actually moving (workflow, wrapper, contract coverage) plus the refresh, instead of mixing new authoring with a harden pass that is already done.
  - route ownership: `update` owns runtime/package change and clean-install warning; `from-gsd2` owns format migration. Staging `update` first keeps installer-supported runtime widening beside the entry-runtime continuity widening at one moment, and migration carry separate at a later moment. Both stay distinct from the earliest-entry initialization continuity carry the landed slice already proved.
- [d:r:i] An alternative — `from-gsd2` first because it is a smaller, more bounded surface than `update` — would invert the workflow-then-skill-wrapper precedent that `122/123` established and the landed pair reinforced. The skill-wrapper layer would inherit a reference that has zero wrapper-side proof, and the workflow-plus-wrapper case would land later with the wrapper-side precedent partially set by the skill-wrapper case. That inversion compresses the contract layers in a way the current family's staging argues against.
- [d:r:i] A second alternative — run a standalone harden-only pass before the update slice — is not productive here because the five harden items the proposal lists have already landed in-place. A standalone harden-only pass would have nothing new to author; its only output would be a redundant verification that the harden is already in the carriers. Folding the (already-landed) harden visibility into the update slice's contract-coverage extension is the cleaner route.

## How This Proposal Should Be Inherited

### Carry Forward

- [d:r:i] `update.md` plus `gsd-update/SKILL.md` as the next adjacent consumer after the landed earliest-entry pair, staged before `from-gsd2`.
- [d:r:i] The three-way route-ownership split the current `update.md` already carries (runtime/package movement owned; structural-health routed to `$gsd-health`; later posture refresh routed to `$gsd-uplift-project --write`).
- [d:r:i] The shared-reference pointer at `update.md`'s `<supporting_reading>` block, conditional on the provider-horizon gate.
- [d:r:i] A sequenced review beat inside `update.md` between `compare_versions` and `show_changes_and_confirm`, so the operator reads continuity before the clean-install wipe-and-replace.
- [d:r:i] The wrapper-side boundary sentence at `gsd-update/SKILL.md` `<objective>`, distinct from the workflow-side pointer.
- [d:r:i] Three new contract assertions: workflow pointer presence, workflow read-only boundary line, wrapper-side boundary sentence.
- [d:r:i] The provider-horizon gate stated evaluably (`PREFERRED_RUNTIME` ∈ {`codex`, `claude`} plus repo-local `.codex/`/`.claude/` state), not only as a negative ("should not widen").
- [d:r:i] Re-materialization of the overlay into live `.codex` after overlay edits.
- [d:r:i] Propagation refresh at slot `49`, extending the compatibility-family chain from `16` through `48` by one row.
- [d:r:i] Explicit preservation of `mandatory-initial-read.md` as grammar-only through this slice.

### Revise Before Implementation

- [d:r:i] Replace the "Fold the lane-17 local harden into the same batch" language at proposal line 28-32 with a statement that the lane-17 harden landed in-place in commit `6f588ab` and that the update slice inherits an already-hardened reference/test/note. Narrow the slice scope to the workflow-side pointer, the review-beat placement, the provider-horizon gate, the wrapper-side boundary, and the three new contract assertions.
- [d:r:i] Name the continuity-reader beat placement (recommended: between `compare_versions` and `show_changes_and_confirm`) rather than leaving the review-beat position ambient across nine steps.
- [d:r:i] State the provider-horizon gate as a concrete evaluable condition on `PREFERRED_RUNTIME` and repo-local state, not as a negative. The current workflow's step-one variables carry both conditions already.
- [d:r:i] Separate the workflow-side job and the wrapper-side job explicitly inside the proposal rather than stating them as near-parallel items.
- [d:r:i] Split the verification list at proposal line 51-55 so the three new assertions (workflow pointer, workflow read-only line, wrapper-side boundary) are distinct from the already-landed reference-side ones.
- [d:r:i] Acknowledge that `update`'s clean-install step wipes and replaces the continuity reference itself via overlay rematerialization, so `update` is both a reader and a rewriter of the reference. Name that asymmetry so later readers do not treat `update` as a purely passive consumer.
- [d:r:i] Name the propagation refresh slot as `49` rather than "the next available slot", to keep the compatibility-family lane-position carry visible to the implementer.

### Hold For Later

- [d:r:i] `from-gsd2` plus `gsd-from-gsd2/SKILL.md` as a separate next consumer slice after `update` lands.
- [d:r:i] Narrowing the seven-runtime `RUNTIME_DIRS` block at `update.md` line 52. The update slice adds a continuity-reader beside multi-provider detection, not inside it.
- [d:r:i] `.claude` route translation or parity push. The continuity reference keeps `.claude` at held annotation; the update slice reads that posture, it does not translate it.
- [d:r:i] Matrix, version-window, third-runtime, or standalone compatibility-carrier widening.
- [d:r:i] Extraction and npm/`npx` distribution work from `115`.
- [d:r:i] Standalone harden-only recursion over the already-landed reference/test/note boundary; available only if a later consumer slice surfaces a gap the consumer cannot absorb.
- [d:r:i] Widening `mandatory-initial-read.md` from grammar-only to grammar-plus-content-pointer; the contract test's `assertNotIn` continues to lock that absence.
- [d:r:i] Helper-side `project_uplift.py` widening, `RUNTIME_DIRS` versus `HELD_CLAUDE_RUNTIME_VERSION_REL_PATH` asymmetry, structural-row promotion inside `STATE.md` or `UPLIFT-MANIFEST.json`, write-recommending `compatibility_drift_reasons` widening.
- [d:r:i] Whether `complete-milestone.md` later adopts the full `required`/`supporting`/`deeper` packet grammar.
- [d:r:i] Family-6 wider route-asymmetry mapping; stays parallelizable rather than silently sequenced behind this slice.
