Date: 2026-04-22
Status: completed Opus widening lane over first cross-runtime packet round trip

# Uplift Cross-Runtime Comparison First Exercise Reread

## What The First Packet Now Makes Explicit

- [e:c+i] The family no longer sits as a held-later label alone. Where prior durable memory kept `cross-runtime uplift composition` as a single held item [.planning/UPLIFT-MANIFEST.json:33], a concrete packet -> output -> disposition round trip now carries it: [../packets/12-uplift-cross-runtime-comparison-first-exercise-packet.md], [../outputs/10-uplift-cross-runtime-comparison-first-exercise.md], [../dispositions/10-uplift-cross-runtime-comparison-first-exercise-disposition.md]. The template carrier at [../packets/11-uplift-cross-runtime-comparison-packet-template.md] is exercised rather than only landed, so later widening answers to a real artifact instead of an ambient "compare runtimes" instruction.
- [d:r:i] The packet separates the cross-runtime posture into three distinct layers that the earlier held-later line compressed together:
  - shared doctrine / wrapper basis carried through [AGENTS.md], [CLAUDE.md], [.planning/CLAUDE.md]
  - continuation-floor overlap through `progress` and `resume-project` present on both runtimes
  - repo-local route asymmetry where `.codex` carries `uplift-project`, `propagation-review`, and `seed-migration-inventory` while `.claude` currently does not
- [e:b:i] The packet anchors the divergence in observed runtime evidence rather than appetite: `.codex` at `1.38.3` versus `.claude` at `1.34.2` plus three named Codex-only workflow routes. That makes the difference concrete enough for later sub-family triage without pulling in parity framing.
- [e:c+i] The compatibility-anchor containment is now readable as part of the comparison rather than as a separate memory line: `compatibility_posture: observed_basis_only` certifies `.codex` alone [.planning/UPLIFT-REPORT.md:25], and the packet keeps `cross-runtime compatibility matrix` explicitly held [.planning/UPLIFT-MANIFEST.json:96-100] instead of letting the exercise itself tacitly widen the anchor.
- [d:r:i] The open-composition surface now carries four distinct live questions in [../outputs/10-uplift-cross-runtime-comparison-first-exercise.md:52-55] rather than one flat composition label: doctrine-only versus doctrine-plus-translation, observed-basis versus matrix, per-route counterpart existence, and per-family translation versus parity push. That articulation sharpens the held surface by listing what still needs to be decided rather than leaving it at "composition held."
- [d:r:i] The `cross_runtime_comparison_packet` pattern joins `docs_governance_classification` and `carrier_gap_identification` as the third exercised uplift-assist family. The assist reference at [../../intervention-proposals/103-uplift-agent-assist-patterns.md:75] is now backed by an actual round trip in the pattern it routed to Opus, which makes the packet-first discipline in that family more durable across the four bounded patterns rather than only across the first two.
- [e:c+i] The parent-thread write boundary held: the disposition at [../dispositions/10-uplift-cross-runtime-comparison-first-exercise-disposition.md:27-31] records no live runtime mutation, no `.claude` translation, no compatibility-matrix claim, and no composition judgment. The uplift route at [.codex/get-shit-done/workflows/uplift-project.md:104-128] remains detect-only with operator-initiated assist routing, so the round trip demonstrates that the boundary carries weight under an actually-exercised comparison rather than only under planning prose.

## What The First Packet Still Flattens Or Leaves Under-Articulated

- [d:r:i] "Shared basis" in [../outputs/10-uplift-cross-runtime-comparison-first-exercise.md:22-28] treats wrapper routing through [AGENTS.md] as symmetric, but the Claude-side wrappers carry more than routing. [CLAUDE.md] and [.planning/CLAUDE.md] encode the anti-threshold posture, the prohibitions on deficit-pseudo-positive and static-positive phrasing, the `do not mirror` boundary against Codex launch-truth, and the propagation-neighbor obligation. The packet registers them as "thin wrappers that route back" and leaves the translated doctrine they carry ambient. That compresses the shared-doctrine family into a routing statement when it is also a translation statement.
- [d:r:i] Route asymmetry is named through three routes (`uplift-project`, `propagation-review`, `seed-migration-inventory`) because those are the routes this audit's intervention work has produced. A broader reread of `.codex/get-shit-done/workflows/` shows a wider Codex-only surface that the packet does not enumerate: `spike.md`, `spike-wrap-up.md`, `sketch.md`, `sketch-wrap-up.md`, `spec-phase.md`, `ultraplan-phase.md`, `ai-integration-phase.md`, `ingest-docs.md`, `eval-review.md`, `extract_learnings.md`. For a first exercise, triage to the audit-visible three is a reasonable narrowing, but as "the route asymmetry" it leaves the wider field uncharted. The sharper read is that the packet has exercised the intervention-family slice of route asymmetry rather than mapped the full asymmetric field.
- [d:r:i] Version drift and repo-local intervention delta sit in the packet as two separate facts. Their partial correlation stays flat: the `.codex` side is where this repo's harness work has shipped, while `.claude` has stayed closer to the upstream snapshot. That is a *posture* difference (which runtime this repo has invested in) and not only a version/route delta. The packet names the facts and leaves the posture observation implicit.
- [d:r:i] The `Shared Basis` section at [../outputs/10-uplift-cross-runtime-comparison-first-exercise.md:26-28] says both runtimes carry `progress` and `resume-project`. Inspection of [.claude/get-shit-done/workflows/progress.md] and [.claude/get-shit-done/workflows/resume-project.md] shows neither carries a route into `uplift-project` or `propagation-review`, because those surfaces do not exist on the Claude side. Meanwhile, the `.codex` side has absorbed the uplift-anchor consumer chain through `$project_uplift -> progress/resume-project` and the operator-facing posture visibility recorded in `propagation-audit/33`. The continuation floor therefore overlaps at the workflow-name level and diverges at the consumer-edge level. The packet compresses both into "both runtimes carry continuation" and leaves the consumer-chain asymmetry unnamed.
- [d:r:i] The `Held Composition Questions` list sits as four flat bullets. The spec in [../specs/07-uplift-cross-runtime-comparison-first-exercise-reread-spec.md:24-28] explicitly asks later work to separate shared doctrine, compatibility widening, route translation, and propagation-review as distinct concern families. The packet anticipates that separation and does not perform it; the questions would carry more if sorted into their concern families rather than sitting as peer bullets.
- [d:r:i] The relation between a later `.claude` route translation and `$gsd-propagation-review` is absent from the packet output. `$gsd-propagation-review` in [.codex/get-shit-done/workflows/propagation-review.md] is itself a Codex-only route that reviews baseline/delta across producers, consumers, mirrors, and registries. A later cross-runtime contract-change slice would either route through that Codex-only workflow (creating a cross-runtime review dependency the packet has not disclosed), or would need a distinct cross-runtime review variant. The packet leaves both options implicit rather than explicit.
- [d:r:i] The compatibility-anchor containment is named, and its *scope* stays ambient. The packet does not distinguish between (a) matrix-as-observed-basis-extension (widening `observed_basis_only` into `observed_.codex_plus_observed_.claude`) and (b) matrix-as-compatibility-family-widening (a new typed carrier with its own protocol and consumer chain). Those are not the same later move, and treating them as one "matrix" family compresses a real sub-family decision.
- [d:r:i] The packet never addresses whether the comparison itself counts as evidence that could upgrade the compatibility posture, or whether a bounded comparison is deliberately contained *inside* observed-basis discipline. That ambiguity carries load: it governs whether subsequent comparisons tighten the anchor or only annotate it.

## Cross-Runtime Concern Families Now Visible

1. [d:r:i] **Shared doctrine / wrapper carry**. [AGENTS.md] is vendor-neutral canon. [CLAUDE.md] is the Claude-side thin wrapper carrying anti-threshold posture, propagation-neighbor obligation, and the `do not mirror` boundary. [.planning/CLAUDE.md] is the planning-scoped counterpart. `.planning/AGENTS.md` remains the planning canon. This family is exercised at the doctrine layer on both sides.
2. [d:r:i] **Continuation-floor overlap at workflow-name level**. `progress` and `resume-project` exist on both runtimes as entry carriers. At the workflow-name layer the surface is symmetric.
3. [d:r:i] **Consumer-chain asymmetry at continuation edge**. The `.codex` continuation surfaces have absorbed the uplift-anchor consumer chain (`$project_uplift -> progress/resume-project`, posture visibility at `propagation-audit/33`). The `.claude` continuation surfaces currently do not carry that chain because the producers (`uplift-project`) do not exist there. This is distinct from family 2 because the overlap is at the surface name and the divergence is at the consumer edge.
4. [d:r:i] **Version-alignment family**. `.codex` at `1.38.3` and `.claude` at `1.34.2` as currently observed. This belongs to compatibility rather than route translation.
5. [d:r:i] **Route-asymmetry family (intervention-family slice)**. The three Codex-only routes the packet named (`uplift-project`, `propagation-review`, `seed-migration-inventory`) are the audit's intervention output. Their asymmetry is a concrete translation-pressure surface.
6. [d:r:i] **Route-asymmetry family (wider uncharted field)**. The broader set of Codex-only workflow routes (`spike`, `spec-phase`, `ultraplan-phase`, `ingest-docs`, `sketch` + wrap-ups, `eval-review`, `ai-integration-phase`, `extract_learnings`) has not been field-mapped. Whether that wider asymmetry deserves its own mapping lane or stays held is its own open question.
7. [d:r:i] **Compatibility-anchor posture family**. `observed_basis_only` certifies `.codex` only, with three explicit held items (version-window beyond observed basis, cross-runtime matrix, upstream-template drift). This family owns how the anchor should grow, not whether it should.
8. [d:r:i] **Route-translation pressure family**. Per-route triage of Codex-only routes: which have real `.claude` consumers, at what translation granularity (full workflow mirror, wrapper-plus-packet-guidance, consumer-only, narrower counterpart, or explicit hold), and under what ownership.
9. [d:r:i] **Propagation-review relation family**. How `$gsd-propagation-review` should relate to a later multi-family contract change that touches both runtimes. Whether the current Codex-only review workflow should add a cross-runtime mode, whether a cross-runtime variant should be its own bounded route, or whether the question remains held until a concrete multi-runtime contract change arrives.
10. [o:r:i] **Compatibility-family widening shape family**. The sub-family question inside family 7: whether widening should grow `observed_basis_only` into a dual-basis posture, into a new typed multi-runtime carrier, or via some narrower intermediate (for example, naming `.claude` version as explicitly held alongside `.codex` as observed). The packet has not yet separated these.

## Which Families Belong To Shared Doctrine, Compatibility, Route Translation, Or Propagation Review

- [d:r:i] **Shared doctrine / wrapper carry** (family 1): owned at the canon layer ([AGENTS.md], [.planning/AGENTS.md]) with Claude-side translations in [CLAUDE.md] and [.planning/CLAUDE.md]. The durable home for future refinement is the canon/wrapper pair, not uplift memory and not the propagation registry. Continuation-floor overlap (family 2) also belongs here at the instruction-surface level.
- [d:r:i] **Compatibility-family widening** (families 4, 7, 10): owned by the uplift-memory compatibility anchor and its later carrier. Version alignment, anchor posture, and widening shape belong together and should be separated into their own bounded sub-proposal before being combined with route translation. The sub-family question of *what shape widening should take* (family 10) is its own near-term decision surface.
- [d:r:i] **Route translation** (families 5, 6, 8): per-route triage owned by the assist-pattern family's packet-first discipline. The intervention-family slice (family 5) has enough surface to carry a bounded triage now; the wider route-asymmetry field (family 6) should earn its own full-field mapping lane before later translation is scoped. Route-translation pressure (family 8) should stay family-by-family rather than collapsing into parity. Consumer-chain asymmetry (family 3) belongs here when consumer-only translation is the candidate move, because the progress/resume consumer edge is where the first translation pressure would surface.
- [d:r:i] **Propagation-review relation** (family 9): owned by `$gsd-propagation-review` as a current Codex-only route, with its cross-runtime posture held. If a later multi-family contract change touches both runtimes, that is the route the question should open from, not a new cross-runtime review surface invented inside uplift. The held-later carrier for the cross-runtime *posture* of that route is its own sub-family and should not silently fold into route translation.
- [d:r:i] Cross-family discipline: shared doctrine, compatibility widening, route translation, and propagation-review relation should carry as four distinct concern families. Two crossings deserve explicit discipline rather than ambient handling:
  - consumer-chain asymmetry sits between shared doctrine and route translation; it should not be absorbed silently into either
  - compatibility-family widening shape (family 10) sits between compatibility and route translation; matrix-as-extension and matrix-as-new-family are different decisions and should be named as different candidate moves

## What Should Remain Explicitly Held

- [d:r:i] Any live `.claude` route translation, per [../dispositions/10-uplift-cross-runtime-comparison-first-exercise-disposition.md:35].
- [d:r:i] Any multi-runtime compatibility matrix as durable memory, per [.planning/UPLIFT-MANIFEST.json:96-100] and [../dispositions/10-uplift-cross-runtime-comparison-first-exercise-disposition.md:36].
- [d:r:i] Any cross-runtime composition judgment, per [../dispositions/10-uplift-cross-runtime-comparison-first-exercise-disposition.md:37].
- [d:r:i] Any propagation-registry redesign derived from later translation, per [../dispositions/10-uplift-cross-runtime-comparison-first-exercise-disposition.md:38].
- [d:r:i] Upstream-template drift machinery, per [.planning/UPLIFT-MANIFEST.json:39-41].
- [d:r:i] Automatic agent spawn from the uplift route, per [../../intervention-proposals/103-uplift-agent-assist-patterns.md:130] and [../../intervention-proposals/102-uplift-agent-assist-first-slice-proposal.md:77].
- [d:r:i] Any move that folds route-translation pressure into compatibility-family widening, or the reverse, before each sub-family has its own bounded carrier. The articulation home for that boundary is the next bounded lane, not the uplift memory.
- [d:r:i] The cross-runtime posture of `$gsd-propagation-review`. A cross-runtime review variant should open explicitly from that route's own doctrine when a concrete multi-family cross-runtime contract change arrives, rather than be assumed inside cross-runtime uplift packet work. The articulation home for any later variant is [.codex/get-shit-done/workflows/propagation-review.md] plus a new proposal, not this packet family.
- [d:r:i] The compatibility-family widening *shape* decision (family 10). A bounded sub-proposal should open between observed-basis-only and a typed multi-runtime carrier, with dual-basis posture and held-version annotation as intermediate candidate shapes, before the anchor itself is moved.
- [d:r:i] The wider route-asymmetry field (family 6). A full-field mapping lane should precede any translation move that reaches beyond the three intervention-family routes; otherwise later translation would silently absorb the wider asymmetry into a parity appetite it has not earned.
- [d:r:i] Whether a cross-runtime comparison packet can itself upgrade the compatibility posture. The containment question should stay explicit and held until a later lane decides whether bounded comparisons tighten the anchor or annotate it.
- [d:r:i] Any whole-harness parity push across `.codex` and `.claude` as an organizing frame. The operator-facing directive in [../../CURRENT-STATE.md:21] and the family-by-family clause in [../outputs/10-uplift-cross-runtime-comparison-first-exercise.md:55] both keep this held.

## How This State Should Be Inherited

### Carry Forward

- [d:r:i] The first packet/output/disposition round trip as the base artifact triple for the `cross_runtime_comparison_packet` family.
- [d:r:i] The three-layer separation (shared doctrine / continuation-floor overlap / route asymmetry) as a first-cut frame that sharpens the held-later label, now ready for further sub-family splitting.
- [d:r:i] The compatibility-anchor containment: `observed_basis_only` certifies `.codex` only and keeps the three held-later compatibility items intact.
- [d:r:i] The family-by-family posture: if later translation is earned, it should carry per route rather than under one parity frame.
- [d:r:i] The parent-thread write-boundary record in the disposition: no live runtime mutation, no `.claude` translation, no compatibility-matrix claim, no composition judgment. That record carries more after the round trip than under planning prose alone.
- [d:r:i] The assist-pattern discipline: the third bounded pattern is exercised, keeping the four-pattern reference at [../../intervention-proposals/103-uplift-agent-assist-patterns.md] load-bearing across the patterns it names rather than only across the first two.

### New Family Openings

- [d:r:i] **Compatibility-family widening shape** as a distinct sub-family (family 10). Candidate shapes to carry forward as bounded options:
  - dual-basis posture: `observed_basis_.codex_plus_explicitly_held_.claude`
  - annotation posture: keep `observed_basis_only` and add a named held-`.claude`-version entry in the compatibility anchor
  - typed-carrier posture: new multi-runtime compatibility carrier outside the `.codex`-anchored uplift memory
  The articulation home for this sub-family is a bounded proposal in [../../intervention-proposals/] rather than an inline uplift-memory edit.
- [d:r:i] **Route-asymmetry field mapping** as a separate bounded lane (family 6). The intervention-family three are a slice. The fuller Codex-only route set (`spike`, `spec-phase`, `ultraplan-phase`, `ingest-docs`, `sketch` + wrap-ups, `eval-review`, `ai-integration-phase`, `extract_learnings`) deserves explicit field mapping before any route-translation triage widens beyond the intervention-family routes.
- [d:r:i] **Consumer-chain asymmetry scoping** as its own bounded concern (family 3). The progress/resume consumer edge is where the uplift-anchor chain exists on `.codex` and is absent on `.claude`. A bounded lane should decide whether this is carried as its own sub-family, annotated as a held row inside the compatibility anchor, or folded into a narrower consumer-only translation candidate.
- [d:r:i] **Route-translation pressure triage** as a distinct family (family 8). Bounded shape:
  - per-route candidate: `.claude`-side consumer who would benefit
  - per-route translation granularity: full-workflow mirror, wrapper-plus-packet-guidance, consumer-only, narrower counterpart, or explicit hold
  - per-route ownership: parent-thread with packet-first discipline, not auto-spawn, not helper-driven write
  - per-route disposition home: [../dispositions/] with the existing `accept` / `revise` / `park` / `reject` grammar
  The triage articulation home is a bounded proposal in [../../intervention-proposals/] that narrows the three intervention-family routes first, with the wider route-asymmetry field deferred to family-6 mapping.
- [d:r:i] **Propagation-review cross-runtime relation** as a bounded held family (family 9). The articulation home is a later proposal that opens from [.codex/get-shit-done/workflows/propagation-review.md] when a concrete multi-family cross-runtime contract change arrives, rather than a preemptive cross-runtime review surface invented here.
- [d:r:i] **Claude-side translated doctrine** as a carry-surface distinction inside family 1. The translations already in [CLAUDE.md] and [.planning/CLAUDE.md] (anti-threshold posture, deficit-pseudo-positive and static-positive prohibitions, propagation-neighbor obligation, `do not mirror` boundary) are a shared-doctrine sub-family the packet left ambient. The articulation home is the canon/wrapper pair itself when the next doctrine movement touches cross-runtime carry.

### Hold For Later

- [d:r:i] All items under `What Should Remain Explicitly Held` above.
- [d:r:i] Any combined move that crosses compatibility-family widening and route translation before each has its own bounded proposal.
- [d:r:i] Any change to `$gsd-propagation-review` that would assume cross-runtime ownership without an explicit proposal.
- [d:r:i] Any translation move that reaches outside the three intervention-family routes before family-6 mapping lands.
- [d:r:i] Any durable uplift-memory refresh triggered by this reread itself. The round trip is a parent-thread packet exercise and should not pull a write into [.planning/UPLIFT-REPORT.md] or [.planning/UPLIFT-MANIFEST.json] until a later slice earns that movement.
- [d:r:i] Any automated or helper-driven composition routing between `.codex` and `.claude`. Composition remains parent-thread-owned.

### Next Bounded Move

- [d:r:i] One bounded follow-on lane that acts on this reread by splitting the flat four-bullet held-composition surface in [../outputs/10-uplift-cross-runtime-comparison-first-exercise.md:52-55] into its concern families. The bounded shape:
  - produce a sharper concern-family map (families 1 through 10 above or the reviser's refinement of them) as a short companion artifact
  - name which families already have carriers and which need new ones
  - recommend which of the three near-term sub-families earns its own bounded proposal first: compatibility-family widening shape (family 10), wider route-asymmetry field mapping (family 6), or consumer-chain asymmetry scoping (family 3)
  - keep the lane's write boundary at parent-thread artifact only, with the assist-pattern discipline intact
- [d:r:i] That follow-on lane stays outside live `.claude` translation, outside compatibility-matrix claim, and outside cross-runtime composition judgment. Its disposition home is [../dispositions/], the same grammar the first exercise used.
- [d:r:i] If the follow-on lane later earns a narrower live implementation move, that move should open from whichever sub-family proposal lands first, family by family, rather than as one widening parity push.
