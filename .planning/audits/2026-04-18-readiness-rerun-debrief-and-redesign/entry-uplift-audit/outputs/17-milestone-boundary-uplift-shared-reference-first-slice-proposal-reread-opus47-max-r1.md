Date: 2026-04-22
Status: completed opus output

# Milestone-Boundary Uplift Shared-Reference First Slice Proposal Opus Review

## What The Proposal Chooses Well

- [e:c+r:i] `122` opens the exact second pair cleared by `119`: [new-milestone.md](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/get-shit-done/workflows/new-milestone.md) paired with [complete-milestone.md](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/get-shit-done/workflows/complete-milestone.md), classified `attach through a shared reference` with `read-only` surfacing direction. The classification-return is already the authorization surface, so `122` inherits forward rather than re-litigating whether a shared reference posture is the right carry or whether the milestone pair should open before `health.md`. Source: [119-uplift-consumer-chain-asymmetry-classification-return.md](../../intervention-proposals/119-uplift-consumer-chain-asymmetry-classification-return.md).
- [e:c+r:i] `122` opens AFTER `121` landed rather than beside it. [121-transition-state-uplift-continuity-first-slice-implementation.md](../../intervention-proposals/121-transition-state-uplift-continuity-first-slice-implementation.md) already teaches the template to declare `## Project Uplift` at top level, the helper's `--write` path to fill that slot in place, and the transition workflow to carry a bounded `review_project_uplift_continuity` step with explicit preserve and refresh paths. `122` picks up a reference shape that can now point AT a live helper-written block with stable semantics rather than inventing a surface the helper would then have to chase.
- [e:c+r:i] `122` composes with the already-landed `60` lifecycle-carry slice rather than overwriting it. The [60-milestone-boundary-lifecycle-carry-first-slice-implementation.md](../../intervention-proposals/60-milestone-boundary-lifecycle-carry-first-slice-implementation.md) note put `.planning/LONG-ARC.md` and `Future Carry Forward` rereading into [new-milestone.md](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/get-shit-done/workflows/new-milestone.md) lines 17-24 and [complete-milestone.md](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/get-shit-done/workflows/complete-milestone.md) `review_long_horizon_carry` step (lines 172-188). `122` adds uplift-continuity carry on top of that long-arc carry rather than absorbing it, which keeps the future-carry schema (preserve / keep open / posture / seeded) distinct from the uplift-scalar-digest schema (last uplift class / compatibility posture / observed runtime basis / held runtime annotation / current recommendation).
- [e:c+r:i] `122` picks the live gap that lane `15`, `119`, and the `121` landing together made visible. The live [STATE.md](/home/rookslog/workspace/projects/prix-guesser/.planning/STATE.md:81) now carries a compact `## Project Uplift` digest at top level from line 81 through line 95, and [UPLIFT-REPORT.md](/home/rookslog/workspace/projects/prix-guesser/.planning/UPLIFT-REPORT.md) plus [UPLIFT-MANIFEST.json](/home/rookslog/workspace/projects/prix-guesser/.planning/UPLIFT-MANIFEST.json) carry the supporting narrative and the typed detail. Neither milestone workflow names ANY of these three surfaces as input today. `new-milestone.md`'s supporting packet lists `PROJECT.md`, `MILESTONES.md`, `STATE.md`, `LONG-ARC.md`, `MILESTONE-CONTEXT.md`, and matched seeds; `complete-milestone.md`'s required_reading lists only `templates/milestone.md`, `templates/milestone-archive.md`, `ROADMAP.md`, `REQUIREMENTS.md`, `PROJECT.md`, `STATE.md`, `LONG-ARC.md`. The uplift digest inside `STATE.md` is read only incidentally because it sits inside the file; the uplift-posture-specific routing to report and manifest is not pinned anywhere. `122` picks the smallest shape that can close that routing gap.
- [e:c+r:i] `122` selects the three-tier primary/supporting/deeper shape that matches the read-packet grammar the repo already uses. [mandatory-initial-read.md](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/get-shit-done/references/mandatory-initial-read.md) names `required_reading`, `supporting_reading`, and `deeper_reading` tiers explicitly. `122`'s choice — primary compact read `STATE.md` `Project Uplift`, supporting narrative read `UPLIFT-REPORT.md`, deeper typed read `UPLIFT-MANIFEST.json` — carries that same tier discipline into a topic-specific reference rather than inventing a new vocabulary. That inheritance keeps later reads of the reference closer to how operators already think about read posture.
- [e:c+r:i] `122` distinguishes surfacing from dispatch. The proposal explicitly says milestone open should surface uplift posture `only when it materially affects milestone framing, carry-forward pressure, or route selection` and milestone close should surface `when it materially affects what stays explicit after close`. Both routes are read-only in character; neither becomes the place that runs `$gsd-uplift-project --write` by default. That separation keeps the write-recommending drift path from `119` explicitly held (under `write-recommending` surfacing direction for `project_uplift.py`) from silently migrating into milestone-boundary surfaces through the back door.
- [d:r:i] `122`'s exclusion list travels at proposal level rather than at implementation discovery: no write-recommending milestone-boundary dispatcher, no compatibility matrix or version-window claims, no `.claude` parity or route-translation, no structural-row promotion inside milestone workflow output, no widening of `health.md` through this slice, no cross-repo extraction or npm/`npx` work. That list composes directly with `119`'s keep-later set and `120`'s explicit-does-not-authorize set, so the keeper chain stays coherent rather than fragmenting across three proposals with different vocabularies.
- [d:r:i] `122`'s verification gates carry real design pressure: the shared reference must stay narrower than a second uplift manifest, both milestone workflows must point to the same reference rather than drifting into two separate local doctrines, the landed slice must survive repo-local rematerialization through tracked overlay carry, and the slice must leave one explicit propagation refresh and one governance-trace note. The four gates together close the common failure modes for a new reference-layer artifact — duplication drift, divergence between two consumers, ambient live-only existence, and absorption into retrospective prose.
- [d:r:i] `122`'s `Why This Route Before health.md` section gives a comparative reason rather than a threshold one. The milestone-boundary pair intensifies TWO lifecycle boundaries at once and composes with TWO already-landed slices (`60`'s long-arc carry and `121`'s transition/state continuity); `health.md` as a single carrier broadens one repair-facing surface. The proposal names `health.md` as still a real next route rather than a deferred route, which keeps the three-valued `119` priority order intact rather than collapsing it into a binary pick.
- [d:r:i] `122` keeps the scalar-versus-structural asymmetry from `120` alive. Neither milestone boundary is being asked to expose the typed `held_runtime_annotation` object at [UPLIFT-MANIFEST.json](/home/rookslog/workspace/projects/prix-guesser/.planning/UPLIFT-MANIFEST.json:88) as a structural row in `MILESTONES.md` or in a milestone retrospective. The deeper typed read is named for ambiguity cases (`real basis or annotation ambiguity`) rather than for ordinary routing, which keeps the manifest as a structural detail surface rather than a query target pulled into boundary artifacts.

## Where The Shared Reference Should Live

- [e:c+r:i] `122` proposes a new shared reference under the repo-local GSD references layer — presumably [tooling/portable-gsd/overlay/get-shit-done/references/](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/get-shit-done/references/), which already carries [agent-contracts.md](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/get-shit-done/references/agent-contracts.md), [mandatory-initial-read.md](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/get-shit-done/references/mandatory-initial-read.md), [planner-reviews.md](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/get-shit-done/references/planner-reviews.md), [planning-config.md](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/get-shit-done/references/planning-config.md), and [verification-overrides.md](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/get-shit-done/references/verification-overrides.md). The reference is not written yet, so the placement decision travels with this slice rather than being set earlier.
- [d:r:i] The dedicated new reference should live at `tooling/portable-gsd/overlay/get-shit-done/references/milestone-boundary-uplift-continuity.md` and be materialized into `.codex/get-shit-done/references/milestone-boundary-uplift-continuity.md` via an explicit `add` entry in [tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json). This placement carries three consequences directly: the reference becomes an upstream-shipped carrier if upstream later adopts it, the installer owns its presence through pristine-overwrite capture, and the repo's `setup-portable-gsd.sh` + `portable_gsd_contract.py` chain already enforces the tracked-overlay invariants that keep it from drifting ambiently.
- [d:r:i] The reference should be a DEDICATED new file rather than a topical expansion of [mandatory-initial-read.md](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/get-shit-done/references/mandatory-initial-read.md). Two reasons carry this placement choice durably:
  - `mandatory-initial-read.md` carries READ-PACKET DISCIPLINE — the grammar of `required_reading` / `supporting_reading` / `deeper_reading` tiers and the rules for widening attention. Its semantic level is orthogonal to specific topical content. Folding uplift-continuity doctrine into it would blur what that reference is FOR and make later reads ambiguous between `how should I read this packet` and `what uplift surfaces should I consult at boundaries`
  - `119` classified milestone-boundary distinctly from the read-packet/initialization/ingest/repair/migration/update cluster. Both were classified `attach through a shared reference`, but the classification return states `milestone-boundary carry already depends on shared reading doctrine and can inherit held-runtime posture through one common reference surface rather than through two separate boundary-specific expansions`, whereas the read-packet cluster's reason was `these carriers already cluster around mandatory-initial-read.md`. The two sentences describe different shared references — one is milestone-specific uplift continuity, the other is the existing read-packet discipline. Collapsing them into a single reference would blur the `119` distinction
- [d:r:i] The dedicated reference should USE the existing read-packet grammar rather than duplicate it. The new reference does not need to re-teach what `required_reading` / `supporting_reading` / `deeper_reading` mean; it names which uplift surfaces to consult in each tier and defers to [mandatory-initial-read.md](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/get-shit-done/references/mandatory-initial-read.md) for the tier-widening discipline itself. That inheritance keeps the new reference narrower than a second read-packet doctrine file.
- [d:r:i] Both milestone workflows should point to the new reference, but they carry slightly different current shapes for doing that. [new-milestone.md](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/get-shit-done/workflows/new-milestone.md) lines 7-12 already carry a `<required_reading>` block pointing at `mandatory-initial-read.md`, plus a `<supporting_reading>` block at lines 14-22 and a `<deeper_reading>` block at lines 24-26. [complete-milestone.md](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/get-shit-done/workflows/complete-milestone.md) lines 7-17 carry a `<required_reading>` block that lists specific files (templates, ROADMAP, REQUIREMENTS, PROJECT, STATE, LONG-ARC) but does NOT carry the `mandatory-initial-read.md` pointer, and has no `<supporting_reading>` / `<deeper_reading>` framing. The slice should point BOTH workflows at the new reference, and the pointer shape in `complete-milestone.md` should travel through its `<required_reading>` block rather than being grafted onto the process body. Making both workflows share the same pointer shape is what keeps the reference truly shared rather than one-workflow-strong-one-workflow-weak.
- [d:r:i] The reference does NOT need to live at `.planning/` level even though its primary compact read is `.planning/STATE.md`. The reference is a PIECE OF RUNTIME DOCTRINE — it tells the milestone workflows which uplift artifacts to consult and how to interpret them — not a piece of planning canon. Keeping it inside `tooling/portable-gsd/overlay/get-shit-done/references/` and materialized into `.codex/get-shit-done/references/` aligns with how every other operator-reading reference in the repo is carried. `.planning/` references would blur the runtime-doctrine versus project-canon boundary that the repo already maintains.
- [d:r:i] The reference should carry a stable filename rather than embedding uplift schema versioning. The helper's `UPLIFT-MANIFEST.json` already carries `"schema_version": 6`; baking a version into the reference filename would force a file rename every time the manifest schema bumps, which would propagate into `OVERLAY-MANIFEST.json`, both workflows' pointer text, and any later consumer. Keep the filename stable; let schema-version sensitivity live inside the manifest where it already does.

## What The Shared Reference Should Carry

- [e:c+r:i] Primary compact read — [STATE.md](/home/rookslog/workspace/projects/prix-guesser/.planning/STATE.md) `## Project Uplift` section at top level (lines 81-95 in the current live file). This read surfaces: last uplift class, last uplift pass timestamp, last uplift secondary signals, phase boundary signal, doctrine reference change flag, compatibility posture, observed runtime basis, held runtime annotation, seed corpus posture, pending doctrine-sensitive proposals count, current recommendation, plus pointers at the report and manifest. This is the operator-facing scalar-digest surface that the `121` landing made durable; naming it first in the reference keeps the boundary routes reading the compact helper-written shape rather than starting from the longer narrative or the typed manifest.
- [e:c+r:i] Supporting narrative read — [.planning/UPLIFT-REPORT.md](/home/rookslog/workspace/projects/prix-guesser/.planning/UPLIFT-REPORT.md). This read adds: the before-state posture section (planning surface present, current state status, runtime directories present, prior uplift memory present, doctrine reference change, phase boundary signal, phase context carrier), recommendation reasons, compatibility basis detail (observed runtime version source, manifest version alignment, overlay manifest schema version, uplift manifest schema version), the compatibility check protocol as a named five-step sequence, held runtime annotation detail with posture and note, held wider compatibility claims list, seed corpus posture breakdown, the full carrier posture table, additive install routes, doctrine-sensitive proposal routes, and held-for-later family list. The reference should name this as the next widen when the boundary route depends on operator prose context the scalar digest cannot carry.
- [e:c+r:i] Deeper typed read — [.planning/UPLIFT-MANIFEST.json](/home/rookslog/workspace/projects/prix-guesser/.planning/UPLIFT-MANIFEST.json). This read exposes: `schema_version`, `doctrine_reference_hash`, `project_fingerprint_hash`, `phase_boundary_signal` as a typed object, `absent_additive_carriers`, `pending_doctrine_sensitive_proposals`, `held_later_families` with pointer arrays, `compatibility_basis` as a typed nested object including `held_runtime_annotation` as a typed sub-object, `seed_corpus_posture` as a typed nested object, and the full typed `carriers` array with fingerprints. The reference should name this as the deeper read ONLY when the boundary route hits real basis or annotation ambiguity — not as default read.
- [d:r:i] The reference should carry the three tiers with explicit contextual framing rather than as a flat list of three paths. Three lines would collapse the surfacing logic:
  - primary compact read: `STATE.md` `## Project Uplift` — always consulted when the boundary surfaces uplift posture at all
  - supporting narrative read: `UPLIFT-REPORT.md` — consulted when the boundary needs operator-facing prose context the compact digest does not carry (for instance, understanding why the current recommendation landed where it did)
  - deeper typed read: `UPLIFT-MANIFEST.json` — consulted when a typed value disagreement emerges between the digest and the report, or when held-runtime annotation shape needs to be read as a typed object rather than as a scalar breadcrumb
  The three verbs (always / when / only when) travel inside the reference; they are not separate rules the workflows re-derive.
- [d:r:i] The reference should pin the interpretation frame alongside the three reads rather than leave it to the boundary routes to re-interpret:
  - `compatibility_posture: observed_basis_only` remains the top-level anchor visible at the digest. Boundary routes quote it as an observation rather than convert it into a policy
  - held runtime annotation appears as a scalar breadcrumb (`held runtime annotation: .claude 1.34.2 (held_annotation)` in the digest, `Held Runtime Annotation` subsection in the report, typed `held_runtime_annotation` object in the manifest). Boundary routes surface the annotation as annotation; they do NOT relabel the compatibility basis as dual-basis
  - milestone boundaries MAY surface the posture when framing depends on it. They do NOT promote the posture into parity, translation, version-window, or compatibility-matrix claims
  - if a milestone-opening route depends on `pending_doctrine_sensitive_proposals` being > 0, the reference names that path as a surfacing trigger (so the milestone framing stays honest about proposed doctrine drift) rather than a dispatch route (which would widen into write-side posture)
- [d:r:i] The reference should carry explicit surfacing triggers for each boundary. Leaving `when it materially affects milestone framing` as the full guidance would invite ambient drift toward either always-surfacing or never-surfacing. A short list per boundary travels more durably:
  - at milestone open — surface when observed runtime basis has moved since the last milestone, when `pending_doctrine_sensitive_proposals` carries count > 0, when `held_later_families` carries a `partially landed` family relevant to the milestone's target scope, or when `phase_boundary_signal.mid_phase_signal` is true and the rerun boundary posture affects what gets loaded into the milestone-opening packet
  - at milestone close — surface when the closed milestone moved observed runtime basis, when the seed corpus posture changed during the milestone, when `held_later_families` gained or closed partial-landing entries, or when carrier posture fingerprints changed in the doctrine-sensitive group during the milestone's phases
  These triggers keep the boundary routes reading the digest deliberately rather than by reflex.
- [d:r:i] The reference should NAME the relationship to `60`'s LONG-ARC / Future Carry Forward reread directly. The two rereads live together at each boundary; neither absorbs the other. A one-line clause — `this reference adds uplift continuity alongside the long-arc / future-carry reread; it does not replace it, and the future-carry reread does not replace this reference's uplift posture` — keeps the two schemas from silently flattening at the implementation slice.
- [d:r:i] The reference should NAME the relationship to `121`'s landed `review_project_uplift_continuity` step directly. At milestone open, the transition step (if any phases completed inside the previous milestone's tail) has already preserved or refreshed the `## Project Uplift` block; the milestone-opening route reads the block as-is rather than rerunning `--write`. At milestone close, the same relationship applies in reverse: the transition step handles intra-phase runtime movement; the milestone-close route surfaces posture without assuming the digest is stale. Without this naming, the implementation slice could rediscover `review_project_uplift_continuity` as a missing step inside the milestone routes, which would widen the slice into the transition surface it was designed not to touch.
- [d:r:i] The reference should keep its voice bounded to read-only surfacing. Lines that say `boundary operator runs X` or `if Y, then execute Z` travel in workflow bodies rather than in this reference. The reference voice is closer to `when X, consult surface Y first; widen into surface Z only if the primary reading does not settle the question`. That posture is what keeps the reference's character as read-only even as later consumers widen.
- [d:r:i] The reference should stay narrower than the `121` template slot. `templates/state.md` now declares `## Project Uplift` with the minimum scalar field set (last uplift class, compatibility posture, observed runtime basis, held runtime annotation, current recommendation). The reference does not duplicate that template's declaration; it points at the live `STATE.md` output and names how to read it. Duplicating the template's declaration inside a reference would force a later template rename or schema change to propagate in two places, which the gate `stay narrower than a second uplift manifest` is already guarding against.

## What Must Stay Out Of Scope

- [d:r:i] No write-recommending milestone-boundary dispatcher. Neither milestone open nor milestone close should gain a route that runs `$gsd-uplift-project --write` by default, aggregates write-side signals, or dispatches remediation actions. The write-recommending drift path in `project_uplift.py` remains the place where compatibility drift reasons collect; this reference does not widen that path into the boundary surfaces.
- [d:r:i] No parity or translation claims. The reference points at `STATE.md` `## Project Uplift`, `UPLIFT-REPORT.md`, and `UPLIFT-MANIFEST.json` as observations in an observed-basis-only compatibility register. It does not authorize the milestone routes to claim `.codex` and `.claude` are compatible at a given version pair, nor to describe the held-runtime annotation as a dual-basis relabel.
- [d:r:i] No structural-row promotion inside milestone workflow output. The `MILESTONES.md` entry written by `gsd-sdk query milestone.complete` carries version, date, phase counts, plan counts, tasks, and accomplishments; it does not acquire a `Compatibility Basis` column, a `Held Runtime Annotation` column, or a typed carrier-posture snapshot row through this slice. Same rule for archived `v[X.Y]-ROADMAP.md` and `v[X.Y]-REQUIREMENTS.md`.
- [d:r:i] No compatibility matrix or version-window claims. [UPLIFT-MANIFEST.json](/home/rookslog/workspace/projects/prix-guesser/.planning/UPLIFT-MANIFEST.json:105) already holds three entries in `held_later` (`version-window claims beyond the observed runtime basis`, `cross-runtime compatibility matrix`, `upstream-template drift compatibility`). This reference must not become the place those held-later items silently re-enter live doctrine through a milestone-boundary surface.
- [d:r:i] No broader family-6 widening. The registry-versus-translation-versus-doctrine split for `.claude` routes still belongs to family-6, which is parallelizable with this slice. If the reference were to treat the milestone-open packet as a place to declare whether `.claude` has a parallel milestone workflow, it would silently pre-answer family-6 for the milestone routes before family-6 finishes its own field mapping.
- [d:r:i] No extraction/distribution work. The extraction-field map from `115` keeps npm/`npx`-style repo split and installer repackaging thinkable but held. The reference must not hold wording that presumes a later extraction boundary — for instance, declaring the reference a `first cross-repo carrier` — because that language would make the later extraction choice harder to reopen cleanly.
- [d:r:i] No widening of `compatibility_drift_reasons` through the reference. The helper's drift list remains a drift detector, not a dispatcher aggregating write-side operator signals collected from milestone-boundary reading posture.
- [d:r:i] No third-runtime annotation surfacing. The held annotation reads `.claude 1.34.2 (held_annotation)` today. The reference does not authorize surfacing `.gemini`, `.opencode`, or `.kilo` as held annotations even though the helper's `RUNTIME_DIRS` detection list spans all five runtimes. This keeps the detection-versus-annotation asymmetry explicit inside the helper rather than letting the reference dissolve it.
- [d:r:i] No verifier-lifecycle inheritance. Verifier carriers remain `explicitly held` under `119`. The reference does not cover `verify-phase.md`, the verification report template, or any phase-verification surface.
- [d:r:i] No health/repair route widening. `health.md` remains the third candidate from `119`, classified `deepen in place`. The reference must not absorb `health.md`'s posture through the milestone-boundary route or cross-reference `health.md` as an alternative surfacing path.
- [d:r:i] No setup/materialization widening. The installer entry, the `gsd-sdk` helper, and the pristine-capture stage remain `explicitly held`; install semantics and consumer semantics must not widen together inside this reference.
- [d:r:i] No absorption of `60`'s LONG-ARC / Future Carry Forward reread into the reference. The two live side by side; this reference does not replace, fold, or re-author the long-arc reread step.
- [d:r:i] No absorption of `121`'s `review_project_uplift_continuity` step into the milestone boundary. The transition step owns phase-close preserve/refresh; this reference surfaces posture at milestone open and close without taking on transition's job.
- [d:r:i] No retrospective structural widening. The `RETROSPECTIVE.md` append in `write_retrospective` should not gain a `Compatibility Basis` or `Held Runtime Annotation` section through this slice. Retrospective carry happens in prose; the reference's deeper typed read is for ambiguity resolution, not for retrospective tabling.
- [d:r:i] No silent answering of `complete-milestone.md`'s own read-packet shape question. Today `complete-milestone.md` has a `<required_reading>` block without the `mandatory-initial-read.md` pointer that `new-milestone.md` carries. Whether `complete-milestone.md` should gain that pointer is a separate question belonging to the read-packet cluster's `119` route, not this slice. The reference should be pointed at from both workflows, but the broader question of whether `complete-milestone.md` adopts the full read-packet tier grammar is held.
- [d:r:i] No test-frontier sweep. Test coverage for this slice is bounded to the pair of carriers and the reference file — a reference-presence test, a workflow-pointer test, and (optionally) an overlay-manifest carrier test. No retrofit of test coverage to other `119` priority routes under this slice's banner.

## How This Proposal Should Be Inherited

### Carry Forward

- [d:r:i] Accept `122` as the next bounded object after `121` lands. The `119` priority order (transition/state first, milestone-boundary second, `health.md` third) carries forward; `122` takes the second slot.
- [d:r:i] Keep the slice scope bounded to milestone open and milestone close. No widening into `health.md`, verifier, setup/materialization, discuss/plan/execute, spec lifecycle, or third-runtime carriers under this slice.
- [d:r:i] Keep the `attach through a shared reference` classification intact. A dedicated new reference file, not two local expansions inside each workflow, not a fold-in to an existing reference.
- [d:r:i] Keep the `read-only` surfacing direction intact. Neither milestone workflow acquires a write-recommending dispatcher, an aggregator route, or a remediation executor through this slice.
- [d:r:i] Keep `compatibility_posture: observed_basis_only` as the top-level anchor quoted by both boundary routes through the reference. Held runtime annotation stays annotation, not dual-basis relabel.
- [d:r:i] Keep the three-tier primary/supporting/deeper shape as the reference's core structure: `STATE.md` `Project Uplift` as primary compact, `UPLIFT-REPORT.md` as supporting narrative, `UPLIFT-MANIFEST.json` as deeper typed.
- [d:r:i] Keep the explicit exclusion list from `122`'s `What This Proposal Does Not Authorize` section and compose it with the `119` keep-later set and the `120` does-not-authorize set. Matrix claims, version-window claims, structural-row promotion, `.claude` parity, extraction work, and `compatibility_drift_reasons` widening all stay out of scope at reference level, not only at implementation level.
- [d:r:i] Keep the four verification gates from `122` intact: the reference stays narrower than a second uplift manifest; both workflows point to the same reference; the landed slice survives repo-local rematerialization through tracked overlay carry; the slice leaves one explicit propagation refresh and one governance-trace note.
- [d:r:i] Keep `health.md` explicit as the next adjacent single-carrier route after `122` lands. The `119` priority order carries forward unchanged.
- [d:r:i] Keep family-6 wider route-asymmetry mapping parallelizable rather than absorbed into this slice. The reference must not silently pre-answer family-6's registry-versus-translation-versus-doctrine split for the milestone routes.
- [d:r:i] Keep extraction pressure from `115` held. `122`'s landing, sitting beside `116` (anchor-side) and `121` (consumer-side transition/state), grows the set of worked compatibility-policy examples a later extraction route can inherit from; it does not open that extraction route.
- [d:r:i] Keep the scalar-versus-structural asymmetry from `120` alive through the reference. The reference does not promote the typed `held_runtime_annotation` object into milestone-boundary output as a structural row.
- [d:r:i] Keep `60`'s landed LONG-ARC / Future Carry Forward reread as independent carry. The reference adds uplift continuity alongside it; the two schemas stay distinct.
- [d:r:i] Keep `121`'s landed `review_project_uplift_continuity` transition step as independent carry. The milestone boundary reads the block as-is rather than rerunning the transition step inside itself.

### Revise Locally

- [d:r:i] Inside `122`'s `Shared-Reference Shape` section, add a one-sentence rationale for choosing a dedicated new reference rather than a topical expansion of `mandatory-initial-read.md`. The rationale: `mandatory-initial-read.md` carries read-packet tier discipline (grammar), and this reference carries milestone-boundary uplift content (topic); mixing the two would blur what each reference is for. Making the choice explicit inside the proposal keeps later readers from reopening it as a generic appetite for consolidation.
- [d:r:i] Inside `122`'s `Shared-Reference Shape` section, pin the concrete file path the slice intends: `tooling/portable-gsd/overlay/get-shit-done/references/milestone-boundary-uplift-continuity.md`, materialized into `.codex/get-shit-done/references/milestone-boundary-uplift-continuity.md` via an explicit `add` entry in [tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json). Pinning the path travels durably; leaving it to the implementation slice risks a later rename that propagates into both workflow pointers.
- [d:r:i] Inside `122`'s `Workflow Follow-Through Intended By This Proposal` section, name the asymmetry between the two milestone workflows' current read-packet shapes:
  - [new-milestone.md](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/get-shit-done/workflows/new-milestone.md) lines 7-26 carry `<required_reading>` + `<supporting_reading>` + `<deeper_reading>` already, pointing at `mandatory-initial-read.md` in the required block
  - [complete-milestone.md](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/get-shit-done/workflows/complete-milestone.md) lines 7-17 carry a `<required_reading>` block without the `mandatory-initial-read.md` pointer and without supporting/deeper blocks
  The slice should point BOTH at the new reference through each workflow's `<required_reading>` block. Whether `complete-milestone.md` should also gain the `mandatory-initial-read.md` pointer (and the supporting/deeper grammar) is a separate question belonging to the read-packet cluster's `119` route; this slice does not answer it but also does not silently close it. Naming the asymmetry inside `122` keeps the later decision reopenable.
- [d:r:i] Inside `122`'s `Proposed First Slice` section, split the single phrase `teach both workflows to read that shared reference as part of their boundary packet` into two explicit edits — `add the reference pointer to new-milestone.md's <required_reading> block` and `add the reference pointer to complete-milestone.md's <required_reading> block`. This keeps the implementation slice from delegating the workflow-side shape to local judgment during implementation.
- [d:r:i] Inside `122`'s `Shared-Reference Shape` section, add explicit surfacing triggers for each boundary rather than leaving `when it materially affects milestone framing` as the full guidance. Proposed triggers:
  - at milestone open — observed runtime basis has moved since the last milestone; `pending_doctrine_sensitive_proposals` count > 0; `held_later_families` carries a `partially landed` family relevant to the milestone's target scope; `phase_boundary_signal.mid_phase_signal` is true and affects what enters the milestone-opening packet
  - at milestone close — the closed milestone moved observed runtime basis; seed corpus posture changed during the milestone; `held_later_families` gained or closed partial-landing entries during the milestone; carrier posture fingerprints in the doctrine-sensitive group changed during the milestone's phases
  Naming the triggers keeps the boundary reading deliberate rather than reflexive.
- [d:r:i] Inside `122`'s `Shared-Reference Shape` section, add an explicit clause stating the reference is read-only in voice, not just in consequence. A one-line style note — `this reference names which uplift surfaces to consult and how to interpret them; it does not instruct the operator to run helper commands from milestone boundaries` — keeps the reference's voice from drifting toward workflow-body tone over later edits.
- [d:r:i] Inside `122`'s `Shared-Reference Shape` section, explicitly name the relationship to `60`'s LONG-ARC / Future Carry Forward reread and to `121`'s `review_project_uplift_continuity` transition step. One line for each: `this reference adds uplift continuity alongside the long-arc / future-carry reread; the two live together, not one inside the other` and `this reference reads the live ## Project Uplift block at the boundary; phase-close preserve/refresh is already owned by the transition step and does not travel into milestone boundaries`.
- [d:r:i] Inside `122`'s `Verification Gates` section, pin the expected propagation-refresh slot as a compatibility-family consumer-chain refresh SIBLING to [propagation-audit/44](../../propagation-audit/44-transition-state-uplift-continuity-change-triggered-refresh.md) (the transition/state one). It should not backfill into [propagation-audit/22](../../propagation-audit/22-milestone-boundary-lifecycle-carry-change-triggered-refresh.md) (the lifecycle-carry milestone-boundary one). Naming the slot keeps the compatibility-family register typed rather than blurred into the lifecycle-carry register.
- [d:r:i] Inside `122`'s `Verification Gates` section, pin where the governance-trace note lives. Proposed placement: the entry-uplift-audit subtree, at `entry-uplift-audit/dispositions/` keyed by the lane that accepts the `122` reread, and a paired `intervention-proposals/` implementation note (following the pattern established by `121`'s implementation-note carrier sitting in `intervention-proposals/`). Pinning the placement durably carries the slice's governance legibility beyond proposal acceptance.
- [d:r:i] Inside `122`'s `Shared-Reference Shape` section, add an explicit minimum-content floor so the reference does not shrink into a skeletal pointer file during implementation. Proposed floor: three sections — `Primary Compact Read`, `Supporting Narrative Read`, `Deeper Typed Read`; one section — `Interpretation Frame` — carrying the observed-basis / held-annotation / boundary-surface-vs-policy distinctions; and one section — `When To Surface` — carrying the per-boundary trigger lists. Naming the floor carries the reference's character durably rather than leaving it to the first implementation pass.
- [d:r:i] Inside `122`'s `What This Proposal Does Not Authorize` section, add an explicit clause `no silent adoption of mandatory-initial-read.md into complete-milestone.md through this slice`. The broader decision about `complete-milestone.md`'s read-packet tier shape belongs to the `119` read-packet cluster route; this slice points both workflows at the new reference without forcing that broader adoption. Naming the non-adoption explicitly keeps the later decision reopenable on its own schedule.
- [d:r:i] Inside `122`'s `Held Later` section, add `third-runtime held-runtime annotation widening through the reference` as an explicit keep-later. The helper's `RUNTIME_DIRS` detection spans `.codex`, `.claude`, `.gemini`, `.opencode`, `.kilo` while the annotation reader reads only `.claude`; the reference must not dissolve that asymmetry by surfacing all five runtimes at milestone boundaries.
- [d:r:i] Inside `122`'s `Held Later` section, add `MILESTONES.md / archived ROADMAP / archived REQUIREMENTS / RETROSPECTIVE.md structural widening` as an explicit keep-later. Those output surfaces remain in their current scalar/prose shapes; the reference surfaces posture for operator judgment, not for output structural promotion.
- [d:r:i] Inside `122`'s `Verification Gates` section, add a governance-trace-existence gate: the landed slice must produce one disposition or inheritance artifact in this audit subtree, not only workflow prose plus a reference file. This gate already implicitly follows from `one governance-trace note`, but naming the placement (dispositions vs intervention-proposals vs a fresh lane packet) closes the ambiguity that `governance-trace note` currently carries.

### Keep Later

- [d:r:i] `health.md` `deepen in place` single-carrier slice remains the third candidate from `119` rather than a companion to `122`. Its opening does not wait on `122` and does not arrive with `122`.
- [d:r:i] Broader family-6 route-asymmetry mapping (registry-versus-translation-versus-doctrine split for `.claude` routes) remains parallelizable. The reference does not close or pre-answer this family's questions.
- [d:r:i] Whether `complete-milestone.md` should adopt the `mandatory-initial-read.md` pointer and the `<supporting_reading>` / `<deeper_reading>` grammar remains a separate question belonging to the read-packet cluster's `119` route. This slice points both workflows at the new uplift-continuity reference without forcing or silently closing that broader read-packet-shape question.
- [d:r:i] Structural-row promotion of the held-runtime object (or any other typed `UPLIFT-MANIFEST.json` field) into `STATE.md`, `MILESTONES.md`, archived roadmap/requirements, or `RETROSPECTIVE.md`.
- [d:r:i] Dual-basis compatibility posture relabel. The reference surfaces `observed_basis_only` at boundaries; it does not promote the held annotation into a second observed basis.
- [d:r:i] Typed multi-runtime compatibility carrier separate from the uplift anchor. Whether a standalone `.planning/COMPATIBILITY.md` or similar surface makes sense stays a later-family question.
- [d:r:i] Live `.claude` route translation, `.claude` parity push, or cross-runtime composition judgment.
- [d:r:i] Cross-runtime compatibility matrix, version-window claims, upstream-template-drift compatibility. These stay explicit inside [UPLIFT-MANIFEST.json](/home/rookslog/workspace/projects/prix-guesser/.planning/UPLIFT-MANIFEST.json:105) `held_later` array.
- [d:r:i] Third-runtime (`.gemini`, `.opencode`, `.kilo`) held-runtime annotation, including any silent widening of the annotation reader or the reference's surfacing triggers to include those runtimes.
- [d:r:i] Extraction implementation under `115`. Hold until at least the next two consumer-chain slices have landed (`122` as milestone-boundary, `health.md` as the third `119` route) so the extraction route has a richer set of worked compatibility-policy examples to inherit from.
- [d:r:i] `compatibility_drift_reasons` widening into a general write-side dispatcher outside held-runtime drift.
- [d:r:i] Verifier lifecycle held-runtime awareness remains `explicitly held` under `119`. The reference does not cover verifier carriers and does not authorize milestone-boundary surfacing to leak into verifier reading.
- [d:r:i] Discuss/plan/execute entry points remain `explicitly held` while family-6 still owns the registry-versus-translation-versus-doctrine split.
- [d:r:i] Setup/materialization bridge from [propagation-audit/20](../../propagation-audit/20-setup-portable-gsd-robustness-change-triggered-refresh.md) remains `explicitly held`; install and consumer semantics must not widen together under the reference.
- [d:r:i] Absorption of `60`'s LONG-ARC / Future Carry Forward reread into the reference, or absorption of this reference into the long-arc reread. The two schemas stay distinct.
- [d:r:i] Absorption of `121`'s `review_project_uplift_continuity` transition step into the milestone boundary, or duplication of that step inside the milestone routes. The transition step keeps phase-close continuity; the milestone boundary reads the block that step produces.
- [d:r:i] Any move that collapses the observed-basis discipline into a runtime-aware-everywhere parity push under the milestone-boundary surfacing label.
- [d:r:i] Retrospective or MILESTONES.md structural widening through the reference. The reference surfaces posture for operator judgment at the boundary; it does not shape the outputs the boundary writes.
- [d:r:i] A unified single-reference-for-all-routes widening — for instance, folding `mandatory-initial-read.md`, the new milestone-boundary uplift reference, and any later `health.md` deepen-in-place reference surface into one reference file. Each reference carries a different kind of content; folding them would blur what each is for.

### Next Bounded Move

- [d:r:i] Accept `122` as the second `119` route after `121` lands, with the revisions above treated as small local edits inside `122` rather than as grounds to reopen `119`, `121`, or `120`.
- [d:r:i] The next step after `122`'s acceptance is the implementation slice itself, scoped to four concrete edits:
  - reference-side: create the dedicated new file `tooling/portable-gsd/overlay/get-shit-done/references/milestone-boundary-uplift-continuity.md` with the three-tier read structure (primary / supporting / deeper), the interpretation frame (observed-basis-only anchor, held annotation as annotation, surface-vs-policy distinction), the per-boundary surfacing triggers, and the read-only voice note
  - overlay-manifest-side: add an `add` entry for the new reference to [tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/OVERLAY-MANIFEST.json) so the installer owns its presence
  - new-milestone-side: add a pointer to the new reference in [new-milestone.md](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/get-shit-done/workflows/new-milestone.md)'s `<required_reading>` block beside the existing `mandatory-initial-read.md` pointer, and extend the `Load Context` step (or a dedicated new step) to consult the new reference's surfacing triggers before proceeding to requirements shaping
  - complete-milestone-side: add a pointer to the new reference in [complete-milestone.md](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/get-shit-done/workflows/complete-milestone.md)'s `<required_reading>` block beside the existing file list, and extend the `review_long_horizon_carry` step (or a dedicated new step adjacent to it) to consult the new reference's surfacing triggers before proceeding to archival and project evolution
- [d:r:i] After the implementation slice lands, land one change-triggered slice refresh as a sibling to [propagation-audit/44](../../propagation-audit/44-transition-state-uplift-continuity-change-triggered-refresh.md). The new refresh extends the compatibility-family consumer-chain with the milestone-boundary uplift continuity move; it does not backfill into [propagation-audit/22](../../propagation-audit/22-milestone-boundary-lifecycle-carry-change-triggered-refresh.md)'s lifecycle-carry slot. That keeps the typed `v2` registry's compatibility-family versus lifecycle-carry distinction intact as the consumer chain widens.
- [d:r:i] Alongside the refresh, land one governance-trace note. Proposed placement: an `entry-uplift-audit/dispositions/` inheritance note keyed to whichever lane accepts the reread, plus an `intervention-proposals/` implementation-note following the `121` shape. The two together carry the slice's governance legibility into this audit subtree durably.
- [d:r:i] After `122`'s milestone-boundary slice lands, `health.md` remains the next candidate from `119` as a single-carrier `deepen in place` slice inside the repair/migration route. `health.md` opens on its own schedule, not as a sequel to `122`.
- [d:r:i] In parallel, family-6 wider route-asymmetry mapping can open as a separately authored field-disclosure lane without inheriting held-runtime awareness as a premise and without waiting on any carrier in the `119` priority order. The milestone-boundary slice's landing does not pre-answer family-6 and does not block family-6 from moving.
- [d:r:i] After `122` lands, the small adjacent question of whether `complete-milestone.md` should adopt `mandatory-initial-read.md` plus the full `<supporting_reading>` / `<deeper_reading>` grammar becomes reopenable on its own schedule. That question belongs to the read-packet cluster's `119` route rather than to this slice.
- [d:r:i] Extraction-field map timing from `115` remains held. `122`'s landing gives a later extraction route its third worked compatibility-policy example (anchor-side `116`, consumer-side transition/state `121`, consumer-side milestone-boundary `122`). The extraction route's earliest useful next step remains a bounded field map rather than an implementation slice.
- [d:r:i] The narrow helper-clarity widening from lane `14` (naming the landed implementation-slice choice inside `116` and surfacing the `RUNTIME_DIRS` detection-list versus `HELD_CLAUDE_RUNTIME_VERSION_REL_PATH` annotation-reader asymmetry inside the helper) remains available as a small local revision alongside `122` rather than a blocker for it.
