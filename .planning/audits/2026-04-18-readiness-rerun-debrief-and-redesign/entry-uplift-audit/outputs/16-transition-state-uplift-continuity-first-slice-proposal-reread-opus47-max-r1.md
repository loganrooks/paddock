Date: 2026-04-22
Status: completed opus output

# Transition-State Uplift Continuity First Slice Proposal Opus Review

## What The Proposal Chooses Well

- [e:c+r:i] `120` selects the exact first pair cleared by `119`: [transition.md](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/get-shit-done/workflows/transition.md) paired with [templates/state.md](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/get-shit-done/templates/state.md), classified `deepen in place` with `read-only` surfacing direction. That classification-return is now the authorization surface, so `120` inherits forward rather than re-litigating which carrier opens first or which of the three classification values applies. The proposal moves from `which carriers could open` into `what this specific pair must preserve` without reaching past the pair. Source: [119-uplift-consumer-chain-asymmetry-classification-return.md](../../intervention-proposals/119-uplift-consumer-chain-asymmetry-classification-return.md).
- [e:c+r:i] `120` picks the live gap that lane `14`, lane `15`, and `119` together made visible. The live [STATE.md](/home/rookslog/workspace/projects/prix-guesser/.planning/STATE.md:81) now carries a compact `## Project Uplift` digest between lines 81 and 95 that was written by the helper's `--write` path during the `116` landing — last uplift class, phase boundary signal, compatibility posture, observed runtime basis `1.38.3`, held runtime annotation `.claude 1.34.2 (held_annotation)`, current recommendation, plus pointers into the manifest and report. The template in [templates/state.md](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/get-shit-done/templates/state.md) does not carry that shape at all, and [transition.md](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/get-shit-done/workflows/transition.md) has no step that preserves or refreshes it at phase close. The gap is concrete: the live digest exists because one helper run was recent when `116` landed; state regeneration at phase boundaries has no ownership of the continuity block today. `120` picks the smallest pair that can close that gap.
- [e:c+r:i] `120` keeps the scalar-summary character of the digest rather than promoting the typed `held_runtime_annotation` object inside [UPLIFT-MANIFEST.json](/home/rookslog/workspace/projects/prix-guesser/.planning/UPLIFT-MANIFEST.json:88) into a structural row. The state-continuity surface stays parallel to `held_runtime_annotation_summary` at [UPLIFT-MANIFEST.json](/home/rookslog/workspace/projects/prix-guesser/.planning/UPLIFT-MANIFEST.json:95) and to the scalar line in [UPLIFT-REPORT.md](/home/rookslog/workspace/projects/prix-guesser/.planning/UPLIFT-REPORT.md:40) rather than opening a new structural query target. That choice preserves the scalar-versus-structural asymmetry lane `14` inherited and lane `15` explicitly asked to keep intact on the consumer side.
- [e:c+r:i] `120` distinguishes `preserve` from `refresh` rather than flattening the two. Phase close can carry two shapes: a stable runtime where the existing block survives, and intra-phase runtime movement where the check protocol calls for rerunning `$gsd-uplift-project --write` ([UPLIFT-MANIFEST.json](/home/rookslog/workspace/projects/prix-guesser/.planning/UPLIFT-MANIFEST.json:101) step 5). Keeping both verbs inside the proposal leaves room for the implementation slice to handle each case explicitly rather than collapsing into one sweep.
- [d:r:i] `120`'s exclusion list travels with the `119` classification return and the lane `15` keep-later set: no widening of `compatibility_drift_reasons`, no structural-row promotion inside `STATE.md`, no standalone compatibility carrier, no `.claude` translation or parity, no extraction/npm/`npx` work. The exclusions are named at proposal level instead of left to the implementation slice to remember.
- [d:r:i] `120`'s sizing heuristic — `small enough to sit beside Future Carry Forward rather than competing with it` — anchors the block's character to the operator-facing uplift digest rather than to the manifest. That comparison keeps the block as a read-once continuity surface rather than a query target, which is the character `deepen in place` plus `read-only` was meant to preserve.
- [d:r:i] `120` keeps the three-valued classification frame alive in content, not only in label. It does not reach for `attach through a shared reference` (which would pull `mandatory-initial-read.md` into scope) and does not slip toward `explicitly held` (which would defer the slice). The proposal stays inside `deepen in place` without blurring the neighboring classifications.

## How The Continuity Shape Should Travel

- [e:c+r:i] The reference shape already exists in the live `STATE.md`. The implementation slice would not invent a shape; it would lift the existing helper-written structure into the template as a placeholder block and teach the transition workflow not to erase it. That inheritance keeps the slice inside the `deepen in place` posture rather than drifting into a parallel authoring surface.
- [d:r:i] The template addition should sit as a separate top-level section `## Project Uplift` matching the live shape, not nested under `Accumulated Context`. Two reasons land here:
  - the helper already writes the block at top level in live [STATE.md](/home/rookslog/workspace/projects/prix-guesser/.planning/STATE.md); nesting under `Accumulated Context` would force the helper's write path to change just to match a relocated template slot
  - `Future Carry Forward` inside `Accumulated Context` carries a different schema (preserve / keep open / posture / seeded), and mixing uplift scalar fields into that bullet family would flatten two distinct carry shapes into one
- [d:r:i] The transition carry shape should treat the `Project Uplift` block the way the `review_accumulated_context` step already treats `Future Carry Forward`: a review step that checks the block against current runtime state rather than regenerating it. Two distinct paths the slice should keep legible:
  - preserve path — when observed basis and held annotation match the block's current values, the step confirms the block as still-live and leaves it
  - refresh path — when runtime movement has happened inside the phase, the step reruns `$gsd-uplift-project --write` so the block reflects the current observed/held values before transition closes
  Naming both paths inside the slice turns `120`'s `preserve or refresh` posture into an implementation commitment the later slice can select against rather than infer.
- [d:r:i] The template-side placeholder should stay thin. The helper already knows how to write all the fields; the template does not need to duplicate their semantics. A top-level header, a short prose line describing the block as helper-written, and one-line placeholders for a minimum scalar field set (last uplift class, compatibility posture, observed runtime basis, held runtime annotation, current recommendation) would carry the shape without making the template a parallel authoring surface.
- [d:r:i] The template/helper co-ownership commitment belongs inside the proposal. Before `120`, the template does not own `## Project Uplift`; the helper does. After `120`, the template declares the section exists and the helper fills it in place. The implementation slice would extend [tooling/codex/project_uplift.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/project_uplift.py) `--write` path so it writes into the declared slot rather than appending. Saying this directly keeps the slice from backing into either `template owns everything` or `helper keeps writing wherever it wants`.
- [d:r:i] The transition workflow's `update_roadmap_and_state` step currently delegates basic state updates to `gsd-tools.cjs phase complete` ([transition.md](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/get-shit-done/workflows/transition.md) lines 216-234). That CLI touches `Current Phase`, `Status`, `Current Plan`, and `Last activity`. The `Project Uplift` section is adjacent in the file but independent in semantics, so the slice should name that `phase complete` must not strip or blank the block during its edits — otherwise the preserve path silently fails. Whether the CLI is already safe by editing specific lines only, or whether a bounded change is needed, is a real sub-question the slice would sharpen by naming rather than leaving to implementation discovery.
- [d:r:i] The propagation-refresh shape should extend the compatibility chain `16 -> 17 -> 43` with one new slot for the consumer-chain transition/state slice. Naming this slot inside `120` (the way lane `15` named the expected chain extension when `118` was accepted) keeps the registry from backfilling the refresh into `21` retrospectively. The lifecycle-carry refresh in [propagation-audit/21](../../propagation-audit/21-transition-lifecycle-carry-change-triggered-refresh.md) is the ancestor; the new consumer-chain refresh is a compatibility-family sibling on top of `21`, not a rewrite of it.
- [d:r:i] The refresh shape also sits alongside `future_preservation`, which `transition.md` already loads at `load_future_preservation_carry` (lines 55-85). The two carry shapes are distinct — future-preservation moves seams / non-decisions / posture / seeded strengthening routes across phase boundaries, while the `Project Uplift` continuity preserves compatibility digest across state regeneration. The slice should travel without folding one into the other; the proposal keeps that separation implicit but it would sharpen by stating directly that the new step is orthogonal to the future-preservation step rather than a layer on top of it.

## What Must Stay Out Of Scope

- [d:r:i] No widening of the block into a structural row. The block should read `held runtime annotation: .claude 1.34.2 (held_annotation)` as a scalar breadcrumb rather than expose the typed `held_runtime_annotation` object's field schema from [UPLIFT-MANIFEST.json](/home/rookslog/workspace/projects/prix-guesser/.planning/UPLIFT-MANIFEST.json:88). Preserving the scalar-versus-structural asymmetry is what keeps lane `14`'s keeper alive after the slice lands.
- [d:r:i] No widening of `compatibility_drift_reasons` through the state-continuity surface. The helper's drift list must remain a drift detector rather than a dispatcher for write-side operator signals collected through state regeneration. If transition-time drift appears, it travels through the existing `$gsd-uplift-project --write` route, not through a new state-side aggregation point.
- [d:r:i] No standalone compatibility carrier. The slice must not graduate the `Project Uplift` section in `STATE.md` into a sibling file like `.planning/COMPATIBILITY.md`. The separation between `UPLIFT-MANIFEST.json` (structural), `UPLIFT-REPORT.md` (operator prose), and the compact state digest (read-once continuity) is the live shape; a fourth surface would dilute rather than sharpen the family.
- [d:r:i] No route translation, no `.claude` parity push. The `Project Uplift` section names `.claude 1.34.2` as held annotation, and that must not become the place a `.claude/` workflow or skill gains a parity hook. Family-6 still owns the registry-versus-translation-versus-doctrine split for Codex-only routes; this slice must not silently pre-answer that question through the state-continuity surface.
- [d:r:i] No third-runtime (`.gemini`, `.opencode`, `.kilo`) annotation. The scalar summary naming `.claude` as the single held runtime should not grow into a list when the slice lands. The helper's `RUNTIME_DIRS` detection list at [tooling/codex/project_uplift.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/project_uplift.py:73) still spans five runtimes while the annotation reader at [tooling/codex/project_uplift.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/project_uplift.py:23) still reads only `.claude`. The state-continuity surface must not become where that asymmetry silently dissolves.
- [d:r:i] No milestone-boundary follow-through. The `119` priority order lists [new-milestone.md](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/get-shit-done/workflows/new-milestone.md) + [complete-milestone.md](/home/rookslog/workspace/projects/prix-guesser/tooling/portable-gsd/overlay/get-shit-done/workflows/complete-milestone.md) as the second candidate pair through `mandatory-initial-read.md`; their carry is classified `attach through a shared reference` rather than `deepen in place`. `120` must not fold milestone-boundary carry into the transition slice even though both touch state-continuity at close boundaries.
- [d:r:i] No family-6 pre-answer. If family-6 later classifies `transition.md` as a translation-sensitive route, the consumer-chain continuity block should remain classification-independent. The `120` slice must keep its reason line narrow — the block preserves the compact uplift digest, not a posture about whether the transition route itself is runtime-specific.
- [d:r:i] No verifier-lifecycle inheritance. Verifier carriers remain `explicitly held` under `119`. A state-continuity block that is read by verifier routes under future-preservation review would widen the slice into compatibility judgment, which the family is not answering now.
- [d:r:i] No setup/materialization widening. The installer entry, the `gsd-sdk` helper, and the pristine-capture stage named in [propagation-audit/20](../../propagation-audit/20-setup-portable-gsd-robustness-change-triggered-refresh.md) sit at the install/runtime frontier and remain `explicitly held`; install semantics and consumer semantics must not widen together inside the first consumer-chain slice.
- [d:r:i] No extraction pressure absorption. The extraction-field map from `115` remains later. `120`'s landing gives the extraction route a second worked compatibility-policy example (anchor-side `116` plus consumer-side `120`), which intensifies what a later extraction route can inherit from, but the extraction route itself does not open here.
- [d:r:i] No test-frontier sweep. Test widening is per-carrier; this slice's test frontier is bounded to the pair of carriers it touches. A template-presence test and a transition preserve/refresh test are candidates, not obligations that need resolving before the slice lands.

## What The Proposal Risks

- [d:r:i] The `preserve or refresh` phrase leaves the implementation path underdetermined. Without a per-path rule, the later slice could drift into rerunning the helper unconditionally at every transition (which widens the transition surface beyond the bounded step) or into preserve-only behavior (which silently drops intra-phase runtime movement detected after the last `--write`). Naming both paths inside `120` would travel alongside the `deepen in place` classification rather than being discovered during implementation.
- [d:r:i] The proposal does not address the co-ownership shift between template and helper. Today the live `## Project Uplift` section exists because the helper appends into a file that had no such section; after the slice, the template declares the section and the helper fills it in place. That is a real contract move and leaving it unstated risks a later slice re-discovering it as a surprise rather than inheriting it as a known carry rule.
- [d:r:i] The proposal does not name the `gsd-tools.cjs phase complete` interaction. The transition workflow routes basic state updates through that CLI; if the CLI blanks or rewrites the state file in a way that does not preserve the `Project Uplift` section, the preserve path fails silently. Whether the CLI edits specific lines only (in which case the carry rule is confirmatory) or whether a narrow change is needed (in which case the carry rule is part of this slice's scope) is a real sub-question the proposal would sharpen by naming.
- [d:r:i] The proposal does not name the expected propagation-refresh slot explicitly. Lane `15`'s revise-locally list called for the chain `16 -> 17 -> 43 -> (new consumer-chain slice refresh)` when the first consumer-chain implementation slice lands. Without a named slot inside `120`, the refresh risks being absorbed into `21` retrospectively, which would blur the compatibility-family register with the lifecycle-carry register.
- [d:r:i] The `sit beside Future Carry Forward` phrasing is intuitively helpful but structurally loose. `Future Carry Forward` sits inside `Accumulated Context`; the live `Project Uplift` section sits at the top level. If the implementation slice reads the phrase literally, it could nest the block inside `Accumulated Context`, which would diverge from the live shape and force the helper's write path to change. A one-line clarification that `beside` means `adjacent top-level section, not nested` would close that risk.
- [d:r:i] The proposal names the continuity block as summary-shaped but does not pin a minimum scalar field set. The live digest carries roughly twelve lines; the template placeholder could carry three, five, or all twelve. Without a minimum, the implementation slice could over-widen under the `deepen in place` label and approach a second manifest, or under-widen and lose digest character. Proposing a minimum field set (last uplift class, compatibility posture, observed runtime basis, held runtime annotation, current recommendation) would keep the template shape tight while still letting the helper fill the full digest.
- [d:r:i] The proposal risks silent collapse with milestone-boundary carry if not explicitly separated. Both `transition.md` and `complete-milestone.md` touch `STATE.md` at close boundaries; the `119` classification distinguishes them (deepen in place versus attach through a shared reference), but a template edit that milestone-boundary wrappers naturally inherit from could retroactively blur the shared-reference classification. Naming that the `120` template slot is not itself the shared reference for milestone boundary would keep the distinction.
- [d:r:i] The proposal does not explicitly exclude `Session Continuity` or `Performance Metrics` from held-runtime awareness. They should not acquire it, and `120` does not widen there, but naming that exclusion would keep later readers from re-opening the question as ambient appetite.
- [d:r:i] The proposal does not currently pin the relationship between the new transition step and the existing `load_future_preservation_carry` and `review_accumulated_context` steps. Those steps operate on distinct carry shapes from the `Project Uplift` digest, but the implementation slice needs to know whether the new handling sits as its own step, folds into `review_accumulated_context`, or sits inside `update_roadmap_and_state`. Pinning the step's placement keeps the transition workflow's surface from silently growing.
- [d:r:i] The proposal's `What The Slice Should Carry` section preserves the three-field distinction (observed runtime basis / held runtime annotation / current recommendation) but does not carry forward the lane `15` note about keeping the scalar-summary-versus-structural-column separation durable as the consumer chain widens. Naming that separation explicitly inside `120` prevents a later consumer from reading the summary string as a structural query target and drifting toward structural-row promotion by the back door.

## How This Proposal Should Be Inherited

### Carry Forward

- [d:r:i] Keep `120` as the first ≤2-carrier implementation proposal candidate after the `119` classification return. The selection aligns with the `119` priority order and the lane `15` next-bounded-move list.
- [d:r:i] Keep the slice bounded to `transition.md` + `templates/state.md`. No widening into milestone-boundary, repair/migration, spec-lifecycle, or verifier surfaces under this slice.
- [d:r:i] Keep `compatibility_posture: observed_basis_only` as the top-level anchor through the slice's landing and through the change-triggered refresh it generates.
- [d:r:i] Keep the scalar-summary shape inside the state-continuity block. The typed `held_runtime_annotation` object remains inside [UPLIFT-MANIFEST.json](/home/rookslog/workspace/projects/prix-guesser/.planning/UPLIFT-MANIFEST.json:88) rather than migrating into `STATE.md` as a structural row.
- [d:r:i] Keep the three-valued classification frame from `119` intact. `deepen in place` governs this slice; `attach through a shared reference` still governs the later milestone-boundary pair; `explicitly held` still governs verifier, discuss/plan/execute, setup/materialization, and the write-recommending drift path.
- [d:r:i] Keep the explicit exclusion list `120` names: no `compatibility_drift_reasons` widening, no structural-row promotion in `STATE.md`, no standalone compatibility carrier, no `.claude` translation or parity, no extraction/npm/`npx` work.
- [d:r:i] Keep the `read-only` surfacing direction. The new transition step should not become a write-recommending dispatcher; any write recommendation continues to route through the existing helper path.
- [d:r:i] Keep the expected propagation-refresh shape consistent with the lane `15` chain extension. The slice's landing extends `16 -> 17 -> 43` into `16 -> 17 -> 43 -> (new transition/state consumer-chain refresh)`, not into `21`'s lifecycle-carry refresh.
- [d:r:i] Keep family-6 wider route-asymmetry mapping parallelizable. The `120` slice must not answer family-6's registry-versus-translation-versus-doctrine split for the transition route.
- [d:r:i] Keep later extraction pressure from `115` held until after `120` lands. The slice's landing is what gives the extraction route its second worked compatibility-policy example alongside `116`.
- [d:r:i] Keep the helper-side asymmetry between [RUNTIME_DIRS in tooling/codex/project_uplift.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/project_uplift.py:73) and [HELD_CLAUDE_RUNTIME_VERSION_REL_PATH in tooling/codex/project_uplift.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/project_uplift.py:23) explicit. The state-continuity surface must not accidentally widen the annotation reader to match the detection list.

### Revise Locally

- [d:r:i] Inside `120`'s `Proposed Slice` section, split the `preserve or refresh` phrase into two named paths:
  - preserve path — when observed basis and held annotation match the block's current values, the transition step confirms and leaves the block in place
  - refresh path — when intra-phase runtime movement is detected, the transition step reruns the existing `$gsd-uplift-project --write` route so the block reflects current values before transition closes
  Naming both paths keeps the implementation commitment explicit rather than implicit.
- [d:r:i] Inside `120`'s `What The Slice Should Carry` section, name the template/helper co-ownership shift directly: the template declares the `## Project Uplift` section as a placeholder; the helper's `--write` path fills it in place rather than appending. One sentence carries the shift durably rather than leaving ownership ambient.
- [d:r:i] Inside `120`'s `Proposed Slice` section, clarify that the `## Project Uplift` block sits as a top-level section in `STATE.md`, adjacent to the `Accumulated Context` section that contains `Future Carry Forward`, not nested inside it. This keeps the live shape and the template shape in register and prevents the helper's write path from having to move.
- [d:r:i] Inside `120`'s `What The Slice Should Carry` section, propose the minimum scalar field set for the template placeholder: last uplift class, compatibility posture, observed runtime basis, held runtime annotation, current recommendation. The helper continues to fill the full digest from [UPLIFT-MANIFEST.json](/home/rookslog/workspace/projects/prix-guesser/.planning/UPLIFT-MANIFEST.json); the template placeholder only needs the minimum.
- [d:r:i] Inside `120`'s `Proposed Slice` section, name the `gsd-tools.cjs phase complete` interaction: the CLI that `transition.md` invokes for basic state updates must preserve the `Project Uplift` section across its edits. If the CLI already edits only specific lines, the slice confirms that carry rule; if not, a narrow change to the CLI is part of this slice's scope.
- [d:r:i] Inside `120`'s `Proposed Slice` section, pin where the new transition step sits: as its own bounded step, distinct from `load_future_preservation_carry` and `review_accumulated_context`, so the transition workflow's step set remains legible rather than silently folding the new carry into an adjacent step.
- [d:r:i] Inside `120`'s `Verification Gates` section, add a gate that the propagation-refresh chain extends explicitly: the slice's landing produces a new consumer-chain refresh on top of `43`, not a backfill into `21`. That gate keeps the compatibility-family register typed rather than blurred into the lifecycle-carry register.
- [d:r:i] Inside `120`'s `Verification Gates` section, add a per-carrier test-frontier note: whatever template-presence or transition-preserve/refresh tests this slice adds are bounded to the pair of carriers it touches, not a sweeping retrofit across other carriers from the `119` priority list.
- [d:r:i] Inside `120`'s `What This Slice Does Not Authorize` section, name explicitly: no third-runtime annotation surfacing through the state-continuity block, even though the detection list already names `.gemini`, `.opencode`, `.kilo`. This makes the detection-versus-annotation asymmetry durable rather than only reconstructable from lane `14`'s revise-locally list.
- [d:r:i] Inside `120`'s `What This Slice Does Not Authorize` section, also name explicitly: no widening of the block into a shared-reference surface that `new-milestone.md` or `complete-milestone.md` could later inherit from. That separation keeps the `119` second candidate pair's `attach through a shared reference` classification from being retroactively blurred.
- [d:r:i] Inside `120`'s `What The Slice Should Carry` section, carry forward the lane `15` note: the scalar-summary-versus-structural-column separation stays durable through the consumer chain so the scalar string in `STATE.md` does not become the first step toward structural-row promotion by default.

### Keep Later

- [d:r:i] Structural-row promotion of the held-runtime object inside `STATE.md` or any other consumer surface.
- [d:r:i] Dual-basis posture relabel.
- [d:r:i] Typed multi-runtime compatibility carrier separate from the uplift anchor.
- [d:r:i] Live `.claude` route translation, `.claude` parity push, or cross-runtime composition judgment.
- [d:r:i] Cross-runtime compatibility matrix, version-window claims, upstream-template-drift compatibility. These remain explicit inside [UPLIFT-MANIFEST.json](/home/rookslog/workspace/projects/prix-guesser/.planning/UPLIFT-MANIFEST.json:105).
- [d:r:i] Third-runtime held-runtime annotation (`.gemini`, `.opencode`, `.kilo`), including any silent widening of the annotation reader alongside the detection list.
- [d:r:i] Extraction implementation under `115`. Hold until at least one more consumer-chain slice lands alongside `116` and `120` so the extraction route has a richer set of worked compatibility-policy examples to inherit from.
- [d:r:i] `compatibility_drift_reasons` widening into a general write-side dispatcher outside held-runtime drift.
- [d:r:i] Milestone-boundary consumer-chain work (`new-milestone.md` + `complete-milestone.md` through `mandatory-initial-read.md`) remains the second candidate pair from `119` rather than a parallel obligation of `120`.
- [d:r:i] `health.md` single-carrier `deepen in place` slice remains the third candidate from `119` rather than a companion to `120`.
- [d:r:i] Verifier lifecycle held-runtime awareness remains `explicitly held`; this slice must not silently open that field under a consumer-chain framing.
- [d:r:i] Discuss/plan/execute entry points remain `explicitly held` while family-6 still owns the registry-versus-translation-versus-doctrine split.
- [d:r:i] Setup/materialization bridge from [propagation-audit/20](../../propagation-audit/20-setup-portable-gsd-robustness-change-triggered-refresh.md) remains `explicitly held`; install and consumer semantics must not widen together inside the first consumer-chain slice.
- [d:r:i] Any move that collapses the observed-basis discipline into a runtime-aware-everywhere parity push under the state-continuity label.

### Next Bounded Move

- [d:r:i] Accept `120` as the first ≤2-carrier implementation proposal candidate, with the revisions above treated as small local edits inside `120` rather than as grounds to reopen `119`, `118`, or `116`.
- [d:r:i] The next step after `120`'s acceptance is the implementation slice itself, scoped to `transition.md` and `templates/state.md`:
  - template-side: declare `## Project Uplift` as a top-level section placeholder adjacent to `Accumulated Context`, with the minimum scalar field set named in the revise-locally list
  - helper-side: extend the `--write` path in [tooling/codex/project_uplift.py](/home/rookslog/workspace/projects/prix-guesser/tooling/codex/project_uplift.py) to fill the declared section in place rather than append into an undeclared slot
  - workflow-side: add a bounded transition step that handles preserve and refresh paths explicitly, invoking `$gsd-uplift-project --write` only when intra-phase runtime movement is detected
  - CLI-side: confirm (or narrow-change) `gsd-tools.cjs phase complete` so it preserves the `Project Uplift` section across basic state edits
- [d:r:i] After the implementation slice lands, land one change-triggered slice refresh on top of `43` — a compatibility-family consumer-chain refresh distinct from the lifecycle-carry refresh in [propagation-audit/21](../../propagation-audit/21-transition-lifecycle-carry-change-triggered-refresh.md). That refresh extends the typed `v2` registry with the transition/state consumer-chain move rather than backfilling into the lifecycle-carry slot.
- [d:r:i] After that refresh lands, the next candidate pair from the `119` priority order is `new-milestone.md` + `complete-milestone.md` through `mandatory-initial-read.md`, classified `attach through a shared reference`. That pair travels as a separately authored proposal rather than a follow-on slice, so the shared-reference surface stays distinct from the deepen-in-place pair.
- [d:r:i] After the milestone pair, the third candidate from `119` is `health.md` as a single-carrier `deepen in place` slice inside the repair/migration route. That slice opens on its own schedule, not as a sequel to `120`.
- [d:r:i] In parallel, family-6 wider route-asymmetry mapping can open as a separately authored field-disclosure lane without inheriting held-runtime awareness as a premise and without waiting on any carrier in the `119` priority order.
- [d:r:i] The narrower helper-clarity widening from lane `14` (naming the landed implementation-slice choice inside `116` and surfacing the detection-list-versus-annotation-reader asymmetry inside the helper) remains available as a small local revision alongside `120` rather than a blocker.
- [d:r:i] Extraction-field map timing from `115` remains held. A later extraction route would now have two worked compatibility-policy examples to inherit from — `116` (anchor-side annotation) and `120` (consumer-side state continuity) — once `120` lands and its refresh is recorded. The extraction route's earliest useful next step remains a bounded field map rather than an implementation.
