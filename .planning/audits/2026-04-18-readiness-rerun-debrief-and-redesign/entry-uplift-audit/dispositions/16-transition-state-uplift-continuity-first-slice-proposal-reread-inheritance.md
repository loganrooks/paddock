Date: 2026-04-22
Status: completed local inheritance

# Transition-State Uplift Continuity First Slice Proposal Reread Inheritance

## Local Disposition

- [d:r:i] `accept with local revision`
- [d:r:i] Lane `12` keeps `120` as the first implementation slice opened by `119`.
- [d:r:i] The next move is no longer another proposal loop.
- [d:r:i] The next move is the bounded implementation slice on:
  - `transition.md`
  - `templates/state.md`
  with the local revisions below carried directly into `120`.

## Carry Forward

- [d:r:i] Keep `120` as the first ≤2-carrier implementation slice.
- [d:r:i] Keep the pair bounded to:
  - `transition.md`
  - `templates/state.md`
- [d:r:i] Keep `compatibility_posture: observed_basis_only` as the top-level anchor.
- [d:r:i] Keep the `Project Uplift` block summary-shaped rather than structural.
- [d:r:i] Keep the scalar-versus-structural distinction durable:
  - scalar summary in `STATE.md`
  - typed object remains in `UPLIFT-MANIFEST.json`
- [d:r:i] Keep the three-valued classification frame from `119` intact:
  - `deepen in place`
  - `attach through a shared reference`
  - `explicitly held`
- [d:r:i] Keep the read-only surfacing direction.
- [d:r:i] Keep the out-of-scope set explicit:
  - no widening of `compatibility_drift_reasons`
  - no structural-row promotion
  - no standalone compatibility carrier
  - no `.claude` translation or parity
  - no third-runtime annotation
  - no extraction/npm/`npx`
  - no milestone-boundary widening under this slice
  - no family-6 pre-answer
  - no verifier-lifecycle widening
  - no setup/materialization widening

## Revise Locally

- [d:r:i] In `120`, split `preserve or refresh` into two explicit paths:
  - preserve path
  - refresh path
- [d:r:i] In `120`, name the template/helper co-ownership shift directly:
  - template declares the section
  - helper fills it in place
- [d:r:i] In `120`, pin the block as a top-level `## Project Uplift` section adjacent to `Accumulated Context`, not nested inside it.
- [d:r:i] In `120`, name the minimum scalar field set for the template placeholder:
  - last uplift class
  - compatibility posture
  - observed runtime basis
  - held runtime annotation
  - current recommendation
- [d:r:i] In `120`, name the `gsd-tools.cjs phase complete` interaction explicitly:
  - it must preserve the `Project Uplift` section across basic state edits
- [d:r:i] In `120`, pin the new transition handling as its own bounded step, distinct from:
  - `load_future_preservation_carry`
  - `review_accumulated_context`
- [d:r:i] In `120`, add a propagation gate:
  - the landing must extend the compatibility-family chain on top of `43`
  - it must not backfill that move into lifecycle-carry `21`
- [d:r:i] In `120`, add the per-carrier test-frontier note explicitly:
  - bounded tests for this pair only
  - no sweeping retrofit
- [d:r:i] In `120`, name explicitly:
  - no third-runtime annotation surfacing through the state block
  - no widening of this block into a milestone shared-reference carrier

## Keep Later

- [d:r:i] Structural-row promotion of held-runtime detail in `STATE.md`.
- [d:r:i] Dual-basis posture relabel.
- [d:r:i] Typed multi-runtime compatibility carrier.
- [d:r:i] Live `.claude` route translation or parity.
- [d:r:i] Cross-runtime compatibility matrix and version-window claims.
- [d:r:i] Third-runtime held-runtime annotation.
- [d:r:i] Milestone-boundary consumer-chain widening under `120`.
- [d:r:i] `health.md` widening under `120`.
- [d:r:i] Extraction implementation under `115`.

## Next Bounded Move

- [d:r:i] Revise `120` with the local changes above.
- [d:r:i] Then implement the first consumer-chain continuity slice:
  - template-side section declaration
  - helper-side in-place fill
  - transition-side preserve/refresh step
  - CLI-side preservation check for `phase complete`
- [d:r:i] After the implementation lands, add one compatibility-family consumer-chain refresh on top of `43`.
- [d:r:i] After that refresh, return to the next priority candidate from `119`:
  - milestone-boundary pair through a shared reference
