Date: 2026-04-22
Status: completed local inheritance

# Uplift Consumer-Chain Asymmetry Proposal Reread Inheritance

## Local Disposition

- [d:r:i] `accept with local revision`
- [d:r:i] Lane `11` keeps `118` as the right next proposal.
- [d:r:i] It also clears the next internal step after `118`:
  - per-carrier classification return first
  - then a ≤2-carrier implementation slice

## Carry Forward

- [d:r:i] Keep `118` as the next bounded compatibility-family proposal.
- [d:r:i] Keep the already-landed read-only four out of scope.
- [d:r:i] Keep the three-valued classification frame:
  - `deepen in place`
  - `attach through a shared reference`
  - `explicitly held`
- [d:r:i] Keep `compatibility_posture: observed_basis_only`.
- [d:r:i] Keep family-6 wider route-asymmetry mapping parallelizable.
- [d:r:i] Keep extraction/distribution later, with timing anchored to a second worked compatibility-policy example after a consumer-chain slice lands.

## Revise Locally

- [d:r:i] In `118`, name the setup/materialization bridge from propagation `20` directly inside the inventory frontier.
- [d:r:i] In `118`, add the surfacing-direction axis:
  - read-only surfacing
  - write-recommending surfacing
- [d:r:i] In `118`, bring the helper-side detection frontier versus annotation-reader asymmetry directly into `What Must Remain Explicit`.
- [d:r:i] In `118`, add explicit keepers against:
  - silent widening of `compatibility_drift_reasons`
  - silent promotion of scalar summary fields into downstream structural columns
- [d:r:i] In `118`, state that test-frontier widening is per-carrier, not a sweeping retrofit.
- [d:r:i] In the surrounding governance surfaces, replace the old “proposal leads directly to implementation” implication with the now-cleared intermediate step:
  - classification return first

## Keep Later

- [d:r:i] Structural-row promotion.
- [d:r:i] Dual-basis relabel.
- [d:r:i] Typed standalone carrier.
- [d:r:i] Live `.claude` translation or parity push.
- [d:r:i] Compatibility matrix / version-window / upstream-template-drift compatibility.
- [d:r:i] Third-runtime held-runtime annotation.
- [d:r:i] Extraction implementation under `115`.
- [d:r:i] Any widening of `compatibility_drift_reasons` into a broader dispatcher for write-side signals.

## Next Bounded Move

- [d:r:i] Accept revised `118`.
- [d:r:i] Then land one per-carrier classification return artifact inside the same family.
- [d:r:i] That classification return should:
  - assign one of the three classification values per named silent carrier or carrier-group
  - attach one short reason line
  - carry the read-only versus write-recommending distinction where it matters
- [d:r:i] Only after that classification return lands should the first ≤2-carrier implementation slice open.
- [d:r:i] Family-6 wider route mapping and the narrower helper-clarity widening can keep traveling in parallel.
