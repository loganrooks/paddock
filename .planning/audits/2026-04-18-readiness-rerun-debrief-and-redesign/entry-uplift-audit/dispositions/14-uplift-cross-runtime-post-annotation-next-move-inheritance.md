Date: 2026-04-22
Status: completed local inheritance

# Uplift Cross-Runtime Post-Annotation Next-Move Inheritance

## Local Disposition

- [d:r:i] `accept as widening input`
- [d:r:i] Lane `10` does not reopen the shape choice already landed in `116`.
- [d:r:i] It sharpens what that landed slice now makes reachable, and it clears one bounded next move:
  - `consumer-chain asymmetry` proposal first
  - family-6 wider route mapping still parallelizable
  - extraction/distribution still later

## Carry Forward

- [d:r:i] Keep `compatibility_posture: observed_basis_only` as the continuing top-level anchor label.
- [d:r:i] Keep the landed held-runtime annotation shape:
  - typed object in `UPLIFT-MANIFEST.json`
  - scalar summaries in `UPLIFT-REPORT.md` and `STATE.md`
  - held-runtime comparison step in the compatibility check protocol
- [d:r:i] Keep the already-landed read-only four as the covered side of the consumer chain:
  - `progress`
  - `resume-project`
  - `uplift-project`
  - `gsd-uplift-project`
- [d:r:i] Keep the helper-side narrow `.claude`-specific implementation choice explicit instead of letting it remain only reconstructable from proposal prose plus code.
- [d:r:i] Keep the typed refresh chain explicit:
  - `16 -> 17 -> 43`
- [d:r:i] Keep the adjacent family split explicit:
  - consumer-chain asymmetry
  - structural-row annotation
  - family-6 wider route-asymmetry mapping
  - route-translation pressure
  - standalone-carrier pressure
  - extraction/distribution pressure

## Revise Locally

- [d:r:i] In `116`, name the landed helper-side choice directly as the narrow `.claude`-specific constant rather than leaving it ambient.
- [d:r:i] In `116` and `43`, surface the asymmetry between:
  - the wider runtime detection list
  - the narrower held-runtime annotation reader
- [d:r:i] In `117`, mark the field-map question as answered and route the next move toward one bounded proposal instead of leaving the note framed as if the field-disclosure task were still the open step.
- [d:r:i] In `CURRENT-STATE.md`, replace the older live question about which annotation sub-shape should land first with the narrower next question opened by lane `10`.

## Keep Later

- [d:r:i] Structural-row promotion of the held-runtime object.
- [d:r:i] Dual-basis posture relabel.
- [d:r:i] Typed multi-runtime carrier outside the uplift anchor.
- [d:r:i] Live `.claude` route translation or parity push.
- [d:r:i] Compatibility-matrix or version-window claims.
- [d:r:i] Third-runtime held-runtime annotation (`.gemini`, `.opencode`, `.kilo`).
- [d:r:i] Extraction implementation under `115`.

## Next Bounded Move

- [d:r:i] Open one bounded `consumer-chain asymmetry` proposal inside the uplift compatibility family.
- [d:r:i] That proposal should:
  - explicitly exclude the already-landed read-only four
  - inventory silent write-side and operator-routing carriers from the propagation family
  - classify each carrier as:
    - `deepen in place`
    - `attach through a shared reference`
    - `explicitly held`
  - preserve `compatibility_posture: observed_basis_only`
  - stay out of route translation, matrix claims, and typed standalone carrier work
- [d:r:i] Keep family-6 wider route-asymmetry mapping openable in parallel rather than silently queued behind the consumer-chain proposal.
