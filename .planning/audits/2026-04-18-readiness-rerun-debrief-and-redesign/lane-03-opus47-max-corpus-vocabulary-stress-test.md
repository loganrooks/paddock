# Lane 03 — Opus 4.7 Max Corpus Vocabulary Stress-Test

Reviewer: `Opus 4.7 Max` via Claude Code CLI (`opus[1m]`, `max`).
Spec: `.planning/audits/2026-04-18-readiness-rerun-debrief-and-redesign/OPUS-CORPUS-VOCABULARY-STRESS-TEST-SPEC.md`.
Output mode: single markdown artifact, this file.

## Frame Note

- [g:r:i] This lane is a bounded stress-test, not a rewrite, not an illustration exercise, and not a generic repo audit. Its only job is to test whether the commentary corpus's new or sharpened vocabulary materially changes what can be said about three specific readiness-era cases compared with what existing repo doctrine, `CLAIM-TYPES.md`, and disciplined prose already carry.
- [g:r:i] The corpus is treated as challenge input, not as sovereign doctrine. The existing claim-marker system is treated as load-bearing, not as the final ontology. Both sides of that comparison are testable.
- [g:r:i] The three cases are: `05` sensitivity narrowing and self-correction (Case A); the bridge-audit `revise + guarded hybrid reseed` synthesis (Case B); and the `R5.17d3` governance-exclusion adjudication (Case C). They are cases the lane-02 framing flagged as the most likely to either reward or expose corpus distinctions.
- [g:r:i] Every classification is allowed exactly one primary outcome from `changed_recommendation`, `changed_warrant_same_recommendation`, `changed_salience_only`, or `rephrasing_only`. No case may be classified as "all of these" or as "more than one applies in different respects." The classification is forced because the question itself is forced.

## Contamination Control Record

- [s:r:i] Staged read order followed. Stage 1 was completed before any Stage 2 file was opened. Specifically: `CLAIM-TYPES.md`, `POST-FALSIFICATIONIST-REVIEW-DOCTRINE.md`, the four Case A/B/C source pairs, then a written commit of this artifact's `Frame Note`, `Contamination Control Record`, and three `Without Corpus` sections to disk. Only after that disk write was the Stage 2 corpus packet opened.
- [s:r:i] Baseline reviews were not silently rewritten after corpus exposure. If any factual mistake required a baseline correction, it would appear in this record under `Baseline Correction Note`. As of the final pass: no baseline correction was needed.
- [s:r:i] Residual leakage risk that cannot be procedurally eliminated:
  - Reviewer prior knowledge of the lane-02 candidate distinctions (declared/effective, function/force, productive tension, conservative openness, prelicensed outcomes, fertile/empty incompleteness, graded underreach) was already in this reviewer's working context from spec-reading, since the spec itself names them as the candidate set. That cannot be unread. The mitigation is to require each `With Corpus` section to name the corpus distinctions actually used and to forbid use of distinctions whose actual textual substance was first encountered in Stage 2.
  - Reviewer prior knowledge that the broader workspace is already debating whether the corpus is philosophical sprawl. That biases toward skeptical reading of the corpus, not toward sympathetic reading. The mitigation is to write `What The Corpus Added` before `What The Corpus Did Not Earn`, so that any earned leverage is named first rather than buried under skepticism.
- [s:r:i] No case substitution. The three cases were used as named in the spec.

## Case A Without Corpus

### Operative move

The case has two artifacts, and its operative move is the contrast between them. The first artifact (`05-sensitivity-converged-synthesis-output.md`) issues a `proceed to a narrow canon-patch response pass` gate verdict with `0` blocking findings and `3` patch-first findings, framed as `safe enough to proceed / narrow enough to patch lightly / still open enough that the patch must not pretend to close rankings`. The second artifact (`05-post-sensitivity-response-plan.md`) is a self-correction. It accepts the gate as a gate but rejects it as a headline outcome, and replaces the narrow `passes / patch lightly` framing with a `canon uplift + Milestone 2 carry-forward + long-arc steering` framing, recommending a two-step `proposal then execution` shape and a `REQUIREMENTS.md → PROJECT.md → LONG-ARC.md → ROADMAP.md` execution order.

The operative move, in one line, is: **a post-verificationist refusal to let a passing gate stand as the headline conclusion of a substantively richer round.**

### What the baseline vocabulary can already say

The existing doctrine and claim-marker system carry this case almost in full.

- The `POST-FALSIFICATIONIST-REVIEW-DOCTRINE.md` definition of `post-verificationist review` is exactly the move the self-correction makes. The doctrine reads `post-verificationist review` as `refusing to stop at "it passes"` and as asking `what remains materially weak, under-justified, under-propagated, or misleadingly complete even when the artifact can technically clear a pass/fail gate`. The response plan's diagnosis — "the converged output translated a large amount of substantive deliberation into a narrow `passes / patch lightly` posture" — is a textbook instance of that posture being invoked against an upstream artifact.
- The doctrine's `Anti-Regret Scope Judgment` clause is even more directly load-bearing. It explicitly requires a two-sided locality judgment: not only `promotion is not required`, but also `non-promotion is defensible without avoidable regret`. The original synthesis's narrow patch was effectively a one-sided non-promotion. The correction explicitly invokes the second side: a narrow patch would underuse earned doctrine and would export Milestone 2 friction. That is anti-regret reasoning in the doctrine's own language.
- The doctrine's `progressive` vs `degenerating` distinction also discriminates the two artifacts. A response that protects the gate cosmetically while creating downstream re-litigation pressure satisfies the definition of `degenerating response`. A response that solves a real problem while improving operational coherence across the surrounding canon satisfies `progressive response`.
- The doctrine's `Deviation, Deferral, And Opportunity Handling` clause explicitly demands that real opportunities be routed visibly into `OPPORTUNITIES.md` or `TASKS.md` rather than silently absorbed. The correction's "make preserve-only and reversal-sensitive branches more explicit where silence is doing too much work" is the same pattern.
- The `CLAIM-TYPES.md` markers also discriminate the two artifacts cleanly. The original synthesis's headline verdict reads as `[s:b:i]` ("safe enough to proceed", a stipulated local rule with weak support) plus `[g:c:i]` patch directives. The correction reframes the verdict as a gate, not an endpoint, and adds new `[g:r:i]` doctrine ("the canon and roadmap now reflect the best mature-product doctrine we have actually earned"). The marker shift from `s:b:i` to `g:r:i` already names the change in epistemic status.

### Recommendation that follows from the baseline

- Adopt the corrected next step. Pursue the broader `canon uplift + Milestone 2 carry-forward + long-arc steering` response in the two-step `proposal then execution` shape recommended by the response plan, and in the recommended `REQUIREMENTS.md → PROJECT.md → LONG-ARC.md → ROADMAP.md → optional 01-CONTEXT.md → consistency review` order. Treat `STATE.md` as relay-only.
- Hold the gate verdict and the broader response as two different things. The gate verdict still licenses a response pass; it does not license the cosmetic patch the gate verdict's own headline initially implied.
- Keep the still-open rankings open. Do not let the broader uplift become a winner-selecting pass on wrappers, money family, hosting identity, contribution doctrine, or event-memory shape.

### What remains open, deferred, or contested

- Exactly which earned doctrine merits durable promotion into `LONG-ARC.md` versus continued residence in audit memory.
- Which preserve-only seams need explicit status language and which can survive on negative space alone for one more round.
- Whether the broader response itself risks creating a different overreach failure mode by carrying too much earned doctrine into canon at once.
- Sequencing pressure relative to the readiness rerun and to `01-CONTEXT.md`.

## Case A With Corpus

### Corpus distinctions actually used

- `declared / effective authority` (chunk 04)
- `reflective / reinforcing / reproductive / instituting` carry-forward gradient (chunk 04)
- `function / force` split (chunk 13)
- `prelicensed outcomes` and `genuine outcome openness` (chunk 22)
- `fertile / empty incompleteness` (chunk 23)

### Reread

The two artifacts read more crisply through the corpus than through the baseline alone, but in every case the corpus is providing an elegant label for a move the baseline doctrine already authorizes and the correction artifact already executes.

- The most direct hit is the `declared / effective authority` split. The correction's diagnosis — "useful as a gate artifact, but it is too weak as the headline outcome of this whole round" — is exactly the declared-vs-effective gap. The synthesis's *declared* function is `gate verdict`; its *effective* force in the round-narrative would have been `closure of the round under a 'patch lightly' headline`. Naming this gap with the corpus label makes the diagnosis crisper than the doctrine's `premature closure` language alone. But the corrective recommendation does not change. The `Anti-Regret Scope Judgment` clause already authorized rejecting the narrow patch on the same grounds.
- The `reflective / reinforcing / reproductive / instituting` gradient does similar work. The original synthesis is *reflective* of the gate state. The narrow-patch directive risks being *reinforcing* of a default closure pattern, and the proposed `REQUIREMENTS.md` clarification it would have triggered would have been *reproductive* (codifying that closure pattern into canon). The corpus's gradient discriminates these levels more finely than the doctrine's two-step `progressive vs degenerating` distinction. But again: the recommended response is the same. Both vocabularies say `do not let the patch directive harden into an instituting closure.`
- The `function / force` split is essentially the same observation re-described: the synthesis's *function* is `permit a response pass`; its *force* in the absence of the correction would have been `settle the round headline`. No new burden, no new failure mode, no new disposition.
- The `prelicensed outcomes` diagnostic does register here. The synthesis's framing prelicensed `narrow patch` by setting it up as the default response shape, with `do not patch as if sensitivity just chose final winners` written as a constraint *on a patch* rather than as `consider whether this round actually demands more than a patch.` The correction de-prelicenses this. The doctrine's `Anti-Regret Scope Judgment` already asks "is non-promotion defensible without avoidable regret?" which performs the same de-prelicensing function. The corpus gives the diagnostic a sharper name.
- The `fertile vs empty incompleteness` distinction lands on the four preserve-only seams the synthesis flags as still alive (collective/mirror governance; partner-brand/creator-profile; reviewed submissions; segregated unsupported listings). The correction's worry — "preserved mostly by negative space. That is too fragile" — is the corpus's `empty underdevelopment` diagnosis applied to preserve-only seams. The corpus has a name for this; the correction has the substance. The recommendation (`make status more explicit where useful`) is the same.

### What changed

- No recommendation changed. The disposition (broader uplift, two-step shape, four-doc execution order, `STATE.md` relay-only, `01-CONTEXT.md` careful) is identical with and without the corpus.
- No warrant structure changed. The doctrine's anti-regret two-sided judgment, post-verificationist refusal, progressive-vs-degenerating distinction, and routing-into-OPPORTUNITIES discipline jointly authorize the correction. The corpus offers sharper labels for the moves the doctrine already authorizes.
- No residual-open handling changed. The open items the without-corpus review identified (which doctrine merits durable promotion; which seams need explicit status; sequencing relative to rerun; whether the broader response itself risks overreach) are unchanged.
- What did change: the *descriptive sharpness* of the failure-mode narrative. Saying "the synthesis's declared function is a gate, its effective force would have been a closure headline, and the patch directive was at risk of moving from reinforcing to reproductive carry-forward" is more compact than the doctrine's "premature closure plus non-promotion that exports avoidable regret." The compression is real. But it is description, not warrant.

## Case A Classification And Defense

**Primary classification:** `rephrasing_only`.

**Defense.** The candidate corpus distinctions all map onto moves the baseline doctrine and claim-marker system already license and the correction artifact already executes. The recommendation is identical. The warrant structure (anti-regret two-sided judgment + post-verificationist refusal + progressive/degenerating discrimination + visible routing into OPPORTUNITIES) is identical. The residual-open list is identical. The corpus's contributions are descriptive — sharper labels for `gate-vs-headline`, for `narrow-patch-as-reinforcing-closure`, for `seams-preserved-by-negative-space`. Those labels are useful for spec-writing and reviewer training, but they do not change what the correction must say or do. Calling this `changed_warrant_same_recommendation` would overclaim: the corpus is not changing the burden structure; it is supplying a more elegant vocabulary for the burden the doctrine already places. Calling it `changed_salience_only` would also overclaim: the correction artifact does not need the corpus to surface its diagnosis; the diagnosis was reachable and was reached. The honest classification is that the corpus rephrases, with discipline, what the doctrine already carries.

## Case B Without Corpus

### Operative move

The case's operative move is a convergent program-level synthesis. After two upstream lanes (`lane-01` upstream-docs freshness audit and `lane-02` docs-vs-readiness crosswalk) and a `lane-03` synthesis judgment, the bridge-audit settles on `revise + guarded hybrid reseed` as the smallest honest next program. That verdict explicitly rejects both `docs-only restart` and `preserve-unchanged`. It requires that the active Checkpoint 5 closure path be left intact, that future upstream grounding be treated as a `ranked hybrid seed` (corroborated docs + changelog + code/tests/templates/install surfaces where claims are contested), that readiness-package seam categories and anti-regret doctrine be preserved, and that a program-revision branch be opened before any readiness-package mutation.

The operative move, in one line, is: **a program-level synthesis that uses anti-regret two-sided reasoning to reject both restart and stasis, and routes the consequence into a separate proposal branch rather than into a silent widening of the active checkpoint.**

### What the baseline vocabulary can already say

The existing doctrine and claim-marker system carry this case in full.

- The `POST-FALSIFICATIONIST-REVIEW-DOCTRINE.md` `Anti-Regret Scope Judgment` clause is the exact frame `lane-03` deploys. The judgment is structured as the doctrine prescribes: "regret if we restart too aggressively" and "regret if we do not revise at all" are both written out, both are sourced, and both are weighed before the final disposition. The doctrine's two-sidedness is not just acknowledged; it is the load-bearing mechanism.
- The `AUDIT-COMPARISON-POLICY` (cited via the doctrine) is also explicit. Four options (`preserve-mostly`, `full docs-only restart`, `guarded hybrid reseed`, `explicit branch / subphase`) are compared by upside, failure mode, and standing, with citations into upstream lane outputs. There is no flattening into consensus and no single-sovereign attack on the prior mapping.
- The doctrine's `Propagation Analysis` clause is satisfied by the explicit separation of `next readiness action` (Checkpoint 5 closure) from `next program-design action` (revision branch). The lane explicitly identifies the timing risk that a casual reader would treat the reseed as a reason to widen Checkpoint 5 ownership immediately, and explicitly refuses that move. That is post-falsificationist propagation analysis in operation.
- The doctrine's `Audit Production` clause is satisfied by the fact that the synthesis explicitly classifies what the obligations did not capture (`What The Obligations Didn't Capture` section) — naming, in particular, the unstated separation between `next readiness action` and `next program-design action`, the unstated dependency of `lane-05 ontology reconciliation` on whether the reseed is executed, and the under-described checkpoint-timing risk. That is the doctrine's "what remains under-owned, under-propagated, treated as settled too early" question being asked of the lane's own spec.
- The `CLAIM-TYPES.md` markers carry the verdict structure tightly: `[d:c+i]` for the convergent program judgment, `[d:r:i]` for the four option dispositions and the recommended program consequence, `[g:c+i]` for the framing rules, `[o:c+i]` for the "what must stay open" set, `[p:r:i]` for the conditional later consequences. The markers separate decided dispositions, governing rules, open questions, and projected consequences without smudging.

### Recommendation that follows from the baseline

- Adopt the verdict as written. Route the consequence to the proposal artifact (`lane-06-program-revision`) before any readiness-package mutation. Hold the current Checkpoint 5 review/closure path on its own track.
- Treat the bounded reconciliation subphase between Checkpoint 5 and Checkpoint 6 as conditional but probable. Its naming, scope, and ownership are open and should be designed by the proposal artifact, not pre-committed in this synthesis.
- Promote `lane-05-ontology-reconciliation` from optional to mandatory only if and when the hybrid reseed is actually executed.
- Treat the future mapping rule (`seed from corroborated config/feature/command/changelog/installer/template/test surfaces; require corroboration when docs speak about inventory/completeness/counts/defaults/version provenance; exclude doc-only completeness claims unless code/test crosswalk revalidates them`) as the operative reseed contract, not as boilerplate.

### What remains open, deferred, or contested

- Direct docs-vs-runtime drift remains unresolved (the lane explicitly defers this).
- Whether upstream `main` materially changes the trust ranking relative to pinned `v1.36.0`.
- Exact placement, naming, and scope of any `Checkpoint 5.x: Hybrid Upstream Baseline Reconciliation` subphase.
- Whether the proposal branch itself should require external review before any readiness-package mutation.

## Case B With Corpus

### Corpus distinctions actually used

- `declared / effective authority` (chunk 04)
- `function / force` split (chunk 13)
- `productive tension vs accidental contradiction` (chunk 18)
- `decay diagnostics for preserved tensions` (chunk 19)
- `genuine outcome openness` and `de-prelicensing without equalization` (chunk 22)
- `graded underreach` (`explicit miss` / `traceable undercarry` / `retrospective possibility`) (chunks 25, 26)

### Reread

The bridge-audit is the case where the corpus is most likely to look like it earns its keep, because the bridge-audit's diagnoses align so cleanly with the corpus vocabulary. But on close reading, the alignment is parallel rather than causal: the corpus would label the same moves the lane already executes, not move the disposition.

- The `declared / effective authority` split lands precisely on the lane-01 finding. `docs/README.md`, `docs/AGENTS.md`, and `docs/ARCHITECTURE.md` carry inventory/navigation surfaces whose *declared* role is authoritative summary but whose *effective* force on the readiness package is weak (stale counts, incomplete claims). The bridge-audit's response — distinguishing seed-eligible families from corroboration-required families — is operationally a declared/effective gap repair. The corpus name is sharper than the lane's prose. But the lane's choice to rank docs by corroboration would have been identical without the corpus label, because it was already grounded in lane-01's direct file evidence.
- The `function / force` split runs in parallel through the readiness-package side of the comparison. The lane's preservation rule — "keep the readiness package's seam categories... as separate governing layers rather than flattening them into upstream package prose" — is a function/force preservation: the readiness doctrine carries effective governance force that exceeds upstream-doc declared sovereignty, and flattening would erase that force. The corpus has a name; the lane has the substance.
- The `graded underreach` vocabulary maps with unusual exactness onto the lane's diagnosis. Lane-02's "the readiness mapping was not directionally wrong... partial under-grounding because the package had to reason from local runtime and readiness concerns without a clean first-party doc seed" is `traceable undercarry` in the corpus's words. The corpus's mapping from `traceable undercarry` to remedy family `translation/carrier repair` would prescribe exactly what the bridge-audit chose: `guarded hybrid reseed` (translating upstream into readiness via ranked corroboration, not restarting). The match is exact. But it is parallel: the lane reached `guarded hybrid reseed` through anti-regret two-sided reasoning before the corpus could have cued the response family.
- The `productive tension vs accidental contradiction` distinction names the lane's two main preserved tensions as well-formed: (i) preserve current Checkpoint 5 closure path while opening a program-revision branch; (ii) reseed from corroborated families while excluding doc-only completeness claims. Both preserve real plurality (closure work and revision work serve different jobs at different horizons; reseeding and exclusion serve different epistemic burdens). The corpus would mark them productive. The doctrine's `Audit Comparison` clause already prescribes the same discipline. The corpus offers the label; the lane's discipline is intact regardless.
- The `decay diagnostics for preserved tensions` distinction (chunk 19) does almost surface a residual-open question the lane's existing handling does not explicitly answer: are the readiness package's preserved seam categories themselves still warranted-as-preserved, or have any of them decayed since their original installation? The lane treats them as load-bearing without decay-testing. The doctrine's `Audit Production` clause does ask "what has been treated as settled too early?" — that is the same question with weaker temporal framing. So the corpus's pressure here is real but absorbable into the existing doctrine without changing the bridge-audit's recommendation.
- The `genuine outcome openness` and `de-prelicensing without equalization` (chunk 22) reads back into the lane's four-option comparison and confirms it is well-formed. `preserve-mostly` is given a real defense (lowest disruption, compatible with current path) before being qualified; `full restart` is given a real failure-mode analysis before being rejected. There is no fake symmetry and no hidden prelicensing of `guarded hybrid reseed`. The corpus would confirm the practice. The doctrine already required it.

### What changed

- No recommendation changed. `revise + guarded hybrid reseed`, preserve Checkpoint 5 closure path, route to lane-06, conditional Checkpoint 5.x, conditional lane-05 — all identical with and without the corpus.
- No warrant structure changed. The lane's anti-regret two-sided judgment, audit-comparison-policy four-option weighing, and propagation analysis (separating `next readiness action` from `next program-design action`) are intact and load-bearing in both readings.
- No residual-open handling materially changed. The corpus does add one residual-open question — whether preserved readiness seams have decayed — but this is absorbable into the doctrine's existing "treated as settled too early" question. It is a sharpening, not a structural addition.
- What did change: the *narrative compactness*. Saying "the lane diagnoses a `traceable undercarry` and selects the matching `translation/carrier repair` remedy family while preserving `function/force` distinctions across the readiness package and the upstream baseline" is more compact than the doctrine's longer prose path through anti-regret reasoning, comparison policy, and propagation analysis. The compression is real. But the substance is the same.

## Case B Classification And Defense

**Primary classification:** `rephrasing_only`.

**Defense.** The bridge-audit is the case where the corpus comes closest to looking like it earns operational inheritance, because its vocabulary maps so cleanly onto the lane's diagnoses. But the mapping is parallel, not causal. The lane reaches `revise + guarded hybrid reseed` through anti-regret two-sided judgment plus four-option comparison plus propagation analysis — all of which are baseline doctrine clauses, not corpus distinctions. The corpus would have labeled the same moves with sharper words; it would not have changed the disposition, the warrant structure, or any failure-mode diagnosis. The decay-diagnostic pressure on preserved seams is the closest the corpus comes to surfacing a residual-open question the lane does not already carry, but the doctrine's "treated as settled too early" question covers the same ground with weaker temporal framing. Calling this `changed_warrant_same_recommendation` would smuggle in an inheritance claim that the case does not earn: the burden structure for selecting `guarded hybrid reseed` over `full restart` was anti-regret two-sided judgment, not the corpus's `traceable undercarry → translation/carrier repair` mapping. Calling it `changed_salience_only` would slightly overclaim too: the lane's "What Must Stay Open" section already carries the residual questions the corpus would have surfaced. The honest classification is that the corpus rephrases the bridge-audit's diagnoses, and would have made the lane's spec-writing more compact, but did not change what the lane needed to see, weigh, or recommend.

## Case C Without Corpus

### Operative move

The case's operative move is a comparative adjudication of governance-scope exclusions. After internal and cross-vendor `R5.17c` lanes audited the heuristics that earlier lanes used to exclude governance/doctrine surfaces from Checkpoint 5 follow-through scope (heuristics like `meta-governance`, `just prompt language`, `just reference text`, `just routing prose`, `not runtime-authoritative`, `supporting evidence only`), the `R5.17d3` adjudication settles on a three-way classification — `Shared Governance Exclusion Failures`, `Contested Governance Exclusions`, `Surviving Defensible Governance Exclusions` — and then writes four `Operational Consequences For Governance Scope` (`direct doctrine`, `bounded-open branches`, `mandatory explicit disposition`, `preserve-only seams` plus `inquiry debt`).

The operative move, in one line, is: **a comparison-policy adjudication that refuses to flatten two sibling lanes into either consensus or single-sovereign-attack, and that converts surviving exclusions into explicit propagation-target categories rather than silent defaults.**

### What the baseline vocabulary can already say

The existing doctrine and claim-marker system carry this case very nearly in full, but the case puts pressure on one place where the baseline's edges are visible.

- The `POST-FALSIFICATIONIST-REVIEW-DOCTRINE.md` definition of `gap exposure` and `completeness challenge` is exactly the posture the bundle and the adjudication operate under. The bundle spec's `Standing Rules` ("Do not treat exclusion as a neutral default... if a lane concludes that a surface should remain excluded, it must justify... why exclusion will not knowingly export avoidable audit pain, doctrine drift, or quality loss") is anti-regret two-sided judgment applied to exclusion language. The adjudication's failure findings (`PROTOCOL.md` and `AUDIT-SPEC-TEMPLATE.md` cannot be excluded as `meta-governance`; `review.md` / `planner-reviews.md` / `plan-phase.md` cannot be excluded as `just prompt language`) are exactly the doctrine's "claimed gap should be able to answer where the gap lives, what kind of gap it is, why it matters downstream, what the right disposition is" applied to exclusion claims.
- The doctrine's `Audit Comparison` clause is the operative idiom for the adjudication's three-way classification. The two `R5.17c` lanes are not flattened into consensus; their convergent core (governance is operative; the exclusion heuristics fail) is named, and their genuine remaining disagreement (literal doctrine-name propagation, full ladder propagation, runtime paraphrase, B5 severity) is preserved as `Contested`. That is the doctrine's "do not flatten into consensus; compare by spec quality, source coverage, self-justification quality, claim survivability" in operation.
- The doctrine's `Deviation, Deferral, And Opportunity Handling` clause is satisfied by the adjudication's `mandatory explicit disposition` operational consequence, which forces include-now / defer-with-reopen-trigger / accepted bounded risk with rationale rather than silent omission. That is the doctrine's exact disposition language applied locally.
- The four operational categories the adjudication uses (`direct doctrine`, `bounded-open branches`, `mandatory explicit disposition`, `preserve-only seams` + `inquiry debt`) come from the existing repo planning vocabulary (`.planning/AGENTS.md` is cited at lines 44-58 and 84-94). They are not invented for this adjudication; they are inherited from existing repo doctrine.
- The `CLAIM-TYPES.md` markers carry the verdict structure tightly: `[d:c+r:i]` for the failure findings and operational consequences, `[o:c+r:i]` for the contested findings. The combination of `c+r` ("not just quoted from a source; the current artifact is also doing non-trivial synthesis or inference") is materially right for an adjudication of two sibling lanes against direct file evidence.

The place where the baseline's edges are visible is narrower. The adjudication has to discriminate between (i) "exclude this surface because it is decorative" (which fails) and (ii) "exclude word-for-word doctrine-label propagation across all runtime surfaces because it would be a different kind of overreach" (which survives), and the existing doctrine carries both judgments by appeal to the same `gap exposure` posture without giving them distinct names. The baseline covers it by leaning on the bundle spec's `Standing Rules` plus the doctrine's `What Counts As A Real Gap` clause, but the discrimination is carried in prose rather than in a named distinction.

### Recommendation that follows from the baseline

- Adopt the adjudication as written. The shared core survives. The contested branches stay open as bounded-open branches rather than being flattened into either side's preferred resolution. The surviving defensible exclusions are correctly limited to literal label-spray and immediate baseline rewrites.
- Treat the four operational consequences as binding for downstream `R5.18` follow-through scoping: `PROTOCOL.md` and `AUDIT-SPEC-TEMPLATE.md` are presumptively in-scope governing authorities for any future exclusion/adjudication spec; `review.md`, `planner-reviews.md`, and `plan-phase.md` are presumptive doctrine-propagation consumers; the contested branches must remain explicit; B5 reviewer-prompt tightening must receive explicit disposition.
- Carry the inquiry debt forward: varied-term doctrine-vocabulary verification across `.codex/` and B5 ordering severity remain live questions and should be visibly tracked rather than treated as resolved.

### What remains open, deferred, or contested

- Whether literal philosophy-label propagation across all runtime surfaces is ever required, or whether semantic carriage with narrower naming discipline is sufficient.
- Whether the full eight-verdict `PROTOCOL.md` disposition ladder must propagate into runtime review/planner consumers, or whether a tightened `Addressed`/`Deferred`/`Rejected`/`Bounded-Risk` quartet would discharge the doctrine.
- Whether `REVIEW-TEMPLATE.md` and `REVIEW-POLICY.yaml` semantics must be paraphrased into runtime consumers immediately, or whether they can remain planning-side baselines while nearer consumer fixes land first.
- Whether B5 reviewer-prompt tightening should be promoted alongside the main carrier/consumer chain or sequenced after it.

## Case C With Corpus

### Corpus distinctions actually used

- `function / force` split (chunk 13) — central
- `regimes of hidden governance` (drift, institutional disavowal, procedural convenience, infrastructural sedimentation, burden displacement, prestige transfer, governance scarcity) (chunk 13)
- `declared / effective authority` (chunk 04)
- `reflective / reinforcing / reproductive / instituting` carry-forward gradient (chunk 04)
- `answerable indirect carriers` (provenance, burden notes, local rationale, override conditions, doctrine cross-reference, downstream effect traces) (chunk 18)
- `intervention menus for indirect rule` (exposure, elevation, reduction, replacement, redistribution, preserved indirectness) (chunk 18)
- `productive tension vs accidental contradiction` (chunk 18)
- `genuine outcome openness` and `judgeability before adoptability` (chunks 22, 23)

### Reread

The R5.17 governance-exclusion adjudication is the case where the corpus's central distinction — the `function / force` split — is operationally most visible, because the case is *already* an audit of exclusion heuristics that conflate declared function with operational force. The adjudication carries the substance throughout. The corpus's value is that it would let the adjudication be *named* more compactly, not that it would let the adjudication be *decided* differently.

- The `function / force` split is the spine of the entire R5.17 bundle. The exclusion heuristics under audit (`meta-governance`, `just prompt language`, `just reference text`, `just routing prose`, `not runtime-authoritative`, `supporting evidence only`) are exactly the corpus's "treating declared function as the only relevant axis." The adjudication's failure findings — `PROTOCOL.md` and `AUDIT-SPEC-TEMPLATE.md` cannot be excluded as `meta-governance`; `review.md` / `planner-reviews.md` / `plan-phase.md` cannot be excluded as `just prompt language` — are operationally function/force corrections. The surfaces' declared role is smaller than their effective force on read-set adequacy obligations, on what reviewers are asked, and on the must-address/lone-high-signal contract. The corpus has the name; the adjudication has the substance.
- The `regimes of hidden governance` catalog applies almost line-by-line to the runtime consumer chain. `review.md` exemplifies *institutional disavowal* (declared as prompt language while exercising governance-grade force on reviewer framing). `planner-reviews.md` exemplifies *procedural convenience* (carries the must-address contract via convenient reference rather than through visible canon). `plan-phase.md` exemplifies *infrastructural sedimentation* (the workflow position determines whether the planner and checker can see the contract at all). The doctrine's `Audit Production` clause asks "what has been under-propagated?" — the corpus's catalog gives names to the *kinds* of under-propagation. This is genuinely a sharper diagnostic vocabulary than the doctrine's prose. But the adjudication's response is unchanged: treat them as presumptive doctrine-propagation consumers.
- The `reflective / reinforcing / reproductive / instituting` gradient sorts the surfaces in question. `POST-FALSIFICATIONIST-REVIEW-DOCTRINE.md`, `REVIEW-TEMPLATE.md`, `REVIEW-POLICY.yaml`, `PROTOCOL.md` are *instituting* surfaces (they codify governance into canon). The runtime consumers in their current state are *reinforcing* weaker patterns. The adjudication's surviving exclusion #2 ("immediate rewrites of the already-strong governance baselines remain a defensible exclusion") and operational consequence ("the nearer repair site is the runtime/reference consumer chain") essentially say: do not edit the *instituting* layer; raise the discipline of the *reinforcing/reproductive* layer. The corpus has the gradient; the adjudication carries the consequence.
- The `answerable indirect carriers` model from chunk 18 (provenance, burden notes, local rationale, override conditions, doctrine cross-reference, downstream effect traces) is the closest the corpus comes to specifying *what* propagation discipline should look like for the runtime consumers. The adjudication's `direct doctrine` consequence and `mandatory explicit disposition` consequence collectively say "include in scope and force explicit disposition," but they do not specify the implementation discipline that would make the indirect carriers answerable. The corpus would specify it. But this specification belongs to a downstream implementation spec, not to the adjudication itself; the adjudication's job was scope/exclusion adjudication, and on that scope it is complete without the corpus.
- The `intervention menus for indirect rule` (exposure, elevation, reduction, replacement, redistribution, preserved indirectness) is broader than what the adjudication needs. The adjudication's three-option vocabulary (include-now / defer-with-reopen-trigger / accepted bounded risk with rationale) is locally appropriate and adequately textured for the contested branches it has to manage.
- `productive tension vs accidental contradiction` reads back into the four contested governance branches. Each is well-formed productive tension: literal-label propagation vs operational economy; centralized doctrine vs distributed paraphrase; full ladder propagation vs minimum-equivalent quartet; B5 ordering vs main carrier/consumer chain ordering. The corpus would say: do not flatten these. The doctrine's `Audit Comparison` clause already prescribes the same. Same observation as Cases A and B.
- `genuine outcome openness` and `judgeability before adoptability` (chunks 22, 23) read back into the contested branches as well. Each contested branch has explicit failure modes for both poles, explicit triggers for promotion, and stated next clarifying moves. They are judgeable as bounded-open branches. The doctrine's `What Counts As A Real Gap` clause already requires this judgeability. Same observation.

### What changed

- No recommendation changed. Failure findings, contested findings, surviving findings, and the four operational consequences are identical with and without the corpus.
- No warrant structure changed. The doctrine's `gap exposure`, `audit comparison`, `propagation analysis`, and `mandatory explicit disposition` clauses jointly authorize the adjudication. The corpus offers sharper labels for the moves the doctrine already authorizes.
- No residual-open handling changed. The four contested branches stay contested; the inquiry debt stays as inquiry debt.
- What did change: a *more textured failure-mode diagnosis*. Saying "these runtime consumers exemplify institutional disavowal — they declare themselves as prompt language while exercising governance-grade force" is a sharper failure-mode narrative than the doctrine's "under-propagated." The corpus's `regimes of hidden governance` catalog is genuinely a sharper diagnostic vocabulary than the doctrine carries. But sharper failure-mode *narrative* is not changed failure-mode *diagnosis* in the spec's sense — the response is identical regardless of which catalog one uses to name the regime.

### One genuine pressure

There is one place where the corpus surfaces something the adjudication does not quite carry: the `answerable indirect carriers` minimum-legibility specification (provenance, burden notes, local rationale, override conditions, doctrine cross-reference, downstream effect traces). The adjudication says runtime consumers are "presumptive doctrine-propagation consumers" but does not specify what propagation should look like beyond `mandatory explicit disposition`. The corpus would supply a specific implementation pattern. This is a forward-design contribution, not an adjudication-changing one — the adjudication's scope was exclusion judgment, not implementation design — but it is the corpus's strongest non-rephrasing offering across the three cases. It would belong to a downstream `R5.18` implementation spec, not to the `R5.17d3` adjudication itself.

## Case C Classification And Defense

**Primary classification:** `rephrasing_only`.

**Defense.** The R5.17d3 adjudication is operationally a function/force adjudication, and the corpus's vocabulary maps cleanly onto its central moves. But every load-bearing piece of the adjudication is licensed by the existing doctrine: `gap exposure` and `completeness challenge` license the auditing of exclusion heuristics; `Audit Comparison` licenses the three-way classification (failures / contested / survivors) without flattening; `Propagation Analysis` licenses the rejection of `under-propagation` as silent default; `Mandatory Explicit Disposition` licenses the include-now / defer / bounded-risk discipline; the `.planning/AGENTS.md` vocabulary licenses the four operational categories. The corpus offers more elegant labels for these moves and a sharper diagnostic catalog for the regimes producing function/force divergence. But the recommendation is unchanged, and the warrant structure is unchanged. The `answerable indirect carriers` specification is the strongest non-rephrasing offering, but it is forward-design for `R5.18` implementation, not adjudication-changing for `R5.17d3`. Calling this `changed_warrant_same_recommendation` would smuggle in an inheritance claim the case does not earn: the adjudication's burden structure for failing the exclusion heuristics is `gap exposure` plus `What Counts As A Real Gap`, not the corpus's function/force split. Calling it `changed_salience_only` would also overclaim: the adjudication already names every contested branch, every operational consequence, and every line of inquiry debt the corpus would have surfaced. The honest classification is that the corpus rephrases an already-strong adjudication with a sharper diagnostic vocabulary, and that the strongest forward-looking corpus contribution belongs to a different artifact than the one being tested here.

## Cross-Case Inheritance Judgment

The three cases force a sober answer. The cases are themselves strong examples of the existing doctrine working at high discipline. The corpus's distinctions map onto the cases as more compact labels for moves the doctrine already authorizes and the artifacts already execute. No distinction earned operational inheritance on this evidence; several earned reflexive-only inheritance as reviewer-register vocabulary; one or two earned nothing meaningful here.

### Earned partial inheritance

- **None on this evidence.** No corpus distinction tipped a recommendation, changed a warrant structure, or surfaced a residual-open question that the existing doctrine could not surface as well. The cases are all rephrasing-only, and partial inheritance requires at least one case where the corpus's contribution was operational rather than descriptive.

### Earned reflexive-only inheritance

These are corpus distinctions that worked as accurate, sharper labels for moves the cases already executed. They could be useful as standardized reviewer-register vocabulary in future audit specs, glossaries, or reviewer training, without being promoted into governing doctrine.

- **`function / force` split** — accurate description of Case C's central operation and of Case A and Case B's secondary moves. The strongest reflexive-inheritance candidate. Earns its keep as a reviewer label even though it never moved a decision.
- **`declared / effective authority`** — accurate description of Case A's gate-vs-headline diagnosis, Case B's docs-corpus diagnosis, and Case C's runtime-consumer diagnosis. Mostly redundant with `function / force` in this evidence base; the two distinctions overlap heavily and probably do not both deserve standardization.
- **`graded underreach` (`explicit miss` / `traceable undercarry` / `retrospective possibility`)** — accurate description of Case B's calibrated remedy choice. Useful as a response-family selector. Did not appear with operational force in Case A or Case C, but its appearance in Case B is sharp enough to qualify.
- **`reflective / reinforcing / reproductive / instituting` carry-forward gradient** — registered in Case A (response-pass risk) and Case C (governance baselines vs runtime consumers). Sharper than the doctrine's two-step `progressive vs degenerating` for some cases, but not so much sharper that it must displace the existing distinction.
- **`fertile vs empty incompleteness`** — registered in Case A (preserve-only seams via negative space) and indirectly in Case B (residual-open list). Useful as a reviewer label for fragility diagnosis on preserve-only branches.
- **`prelicensed outcomes` and `genuine outcome openness`** — registered in Case A (synthesis framing prelicensed narrow patch) and confirmed Case B's four-option comparison as genuinely open. Useful as a reviewer label, redundant with the doctrine's `Anti-Regret Scope Judgment` two-sided clause.
- **`answerable indirect carriers`** — registered in Case C as forward-design discipline. The closest the corpus comes to a non-rephrasing contribution, but the contribution is forward-design rather than adjudication-changing. Useful as design vocabulary for downstream implementation specs.

### Earned nothing on this evidence

- **`productive tension vs accidental contradiction`** — registered marginally in Cases B and C as a label for already-well-formed plurality-preservation moves, but never did discriminating work the doctrine's `Audit Comparison` clause could not do. Did not earn even reflexive inheritance.
- **`decay diagnostics for preserved tensions`** — surfaced a residual-open question for Case B (whether preserved seams have decayed) but the question was already covered by the doctrine's "treated as settled too early" clause with weaker temporal framing. The temporal sharpening did not earn its own slot.
- **`intervention menus for indirect rule` (exposure, elevation, reduction, replacement, redistribution, preserved indirectness)** — appeared in Case C but the adjudication's three-option vocabulary (include-now / defer-with-reopen-trigger / accepted bounded risk) was locally adequate. The broader menu was not load-bearing.
- **`hidden governance regimes` catalog (drift, institutional disavowal, procedural convenience, infrastructural sedimentation, burden displacement, prestige transfer, governance scarcity)** — sharper diagnostic vocabulary than the doctrine carries, but never moved a Case C disposition. Reflexive at best; on this evidence, did not earn even that.
- **`conservative openness`** — named in the spec as a candidate distinction, but its actual textual substance was not encountered in the eight-chunk corpus packet read for Stage 2. Cannot be judged from this evidence and should not be presumed earned.

### Distinctions that appeared with different results across cases

- The `function / force` split is the only distinction with genuinely different per-case force: it is central to Case C's operation, secondary to Case B, and tertiary to Case A. Even at its strongest (Case C), it remains rephrasing rather than operational gain. This unevenness is itself a reason to be cautious about promoting it: it is the corpus's strongest distinction in the case where the doctrine's `gap exposure` posture was already most active.

## What The Existing Baseline Already Carried

This section credits the existing repo doctrine where it was already sufficient. On the evidence of these three cases, the baseline's coverage is broad and sharp.

- **`Anti-Regret Scope Judgment`** (`POST-FALSIFICATIONIST-REVIEW-DOCTRINE.md`) carried Case A almost entirely (the correction's two-sided refusal of narrow non-promotion) and Case B almost entirely (the lane's two-sided regret-if-restart and regret-if-no-revise structure). It is the doctrine clause doing the most work across the three cases. The corpus offers no replacement and no clear operational gain on top of it.
- **`Audit Comparison Policy`** (cited via the doctrine) carried Case B's four-option comparison (preserve-mostly / restart / hybrid reseed / branch) and Case C's three-way classification (failures / contested / survivors). The discipline of "do not flatten into consensus; compare by spec quality, source coverage, self-justification quality, claim survivability" is the load-bearing mechanism in both cases.
- **`Propagation Analysis`** carried Case B's separation of `next readiness action` from `next program-design action`, and Case C's identification of runtime consumers as governance carriers. The corpus's function/force vocabulary maps onto the same diagnoses without changing them.
- **`Gap Exposure` and `Completeness Challenge`** carried Case C's auditing of exclusion heuristics as burden-bearing claims rather than neutral defaults.
- **`Mandatory Explicit Disposition`** (the doctrine's `Deviation, Deferral, And Opportunity Handling` clause) carried Case A's recommendation that preserve-only seams need explicit status language, Case B's recommendation that the proposal artifact route through lane-06 visibly, and Case C's `mandatory explicit disposition` operational consequence.
- **`Progressive vs Degenerating`** carried Case A's discrimination between narrow patch (degenerating) and canon uplift (progressive).
- **`Practical Questions for Future Audits`** clause (the doctrine's "what is being treated as settled too early?", "what assumptions are downstream consumers being asked to absorb without owning?", "what later audit pain or re-litigation pressure would we be knowingly exporting?") carried much of the residual-open handling across all three cases.
- **`CLAIM-TYPES.md` markers** (`e/d/g/o/a/p/s` × `c/r/b` × `i/d/t`) carried the disposition discipline of all three cases. The artifacts deploy markers with sufficient precision to support the doctrine's reading rules without requiring the corpus's two-layer ontology proposal.
- **`.planning/AGENTS.md` vocabulary** (`direct doctrine`, `bounded-open branches`, `preserve-only seams`, `inquiry debt`) supplied the four operational categories of Case C and supported the disposition language of Cases A and B.
- **The bundle spec's standing rules** (Case C's `R5.17` bundle: "Do not treat exclusion as a neutral default... burden of exclusion is higher... must justify why exclusion will not knowingly export avoidable audit pain") carried the case-local burden assignment that the corpus would only have re-described.

## What The Corpus Added

This section names what the corpus contributed across the three cases, without overclaiming beyond them.

- **A sharper, more compact diagnostic vocabulary** for failure modes the doctrine already names. Specifically: `function / force` for declared-vs-operational divergence; `declared / effective authority` for the same gap framed by authority rather than force; `graded underreach` (explicit miss / traceable undercarry / retrospective possibility) for hindsight-calibrated critique; `reflective / reinforcing / reproductive / instituting` for carry-forward strength gradation; `fertile / empty incompleteness` for the maturity of preserve-only seams.
- **A diagnostic catalog of regimes producing function/force divergence**: drift, institutional disavowal, procedural convenience, infrastructural sedimentation, burden displacement, prestige transfer, governance scarcity. Genuinely sharper than the doctrine's "under-propagated" prose, although Case C's adjudication landed identically without it.
- **A forward-design pattern for indirect carriers**: provenance, burden notes, local rationale, override conditions, doctrine cross-reference, downstream effect traces as the minimum-legibility set for runtime consumers asked to carry governance load. This is the corpus's strongest non-rephrasing contribution, but it is forward-design for downstream implementation rather than adjudication-changing for the cases tested.
- **A stricter temporal framing of the doctrine's "treated as settled too early" question**, via the `decay diagnostics for preserved tensions` distinction. Surfaced a residual-open question for Case B that the doctrine already covered with weaker framing.
- **Reviewer-register reusability**: the corpus's vocabulary is plausibly reusable across future audit specs as compact lexicon, even though it did not change any of the three cases tested. That reusability is a real contribution to spec-writing economy, distinct from operational doctrine inheritance.

What the corpus did *not* add, on this evidence: any new substantive judgment-axis that the existing doctrine does not already authorize. Every operational move the cases make is licensed by the doctrine. The corpus's contribution is descriptive, not directive.

## What The Corpus Did Not Earn

- **Operational inheritance into governing doctrine.** No case showed the corpus tipping a decision the doctrine would not have tipped, or surfacing a residual-open question the doctrine could not have surfaced. Promoting any corpus distinction into load-bearing doctrine on this evidence would be unjustified.
- **A sibling analytic layer.** The derivative translation's two-layer ontology proposal (lower layer for explicit operational units; upper layer for discourse-forming handles) is not justified by these three cases. The existing claim-marker system handled all three cases adequately. A second layer would be paying for capacity the cases did not exercise.
- **Replacement of disciplined prose.** The doctrine's prose carries the substance the corpus would label. The corpus's labels add elegance, not novel capacity. Replacing prose with corpus tags would lose discriminating context that the prose currently supplies.
- **Repo-wide vocabulary uplift on the strength of three cases.** Three cases is not a sufficient base for repo-wide propagation of corpus distinctions, even if the distinctions are genuinely sharper as labels. The spec's anti-misread rules forbid inferring from one or even three successful labelings that a distinction deserves doctrine promotion.
- **Validation of the corpus's broader theoretical claims.** The corpus's metaphysical and Derridean framing apparatus (trace, deferral, presence, supplementation, displacement) is *not* tested by these three cases. The cases test only whether named distinctions like `function / force` change anything operationally. The broader theoretical apparatus could be right or wrong; this lane does not adjudicate it.
- **Inheritance of `productive tension vs accidental contradiction` even reflexively.** This distinction did not register strongly enough in any of the three cases to be worth standardizing. The doctrine's `Audit Comparison` clause carries the same discipline.
- **Inheritance of `decay diagnostics for preserved tensions` even reflexively.** Surfaces a question the doctrine already covers with weaker temporal framing. Did not earn its own slot on this evidence.
- **Demonstration that the existing claim-marker system is insufficient.** The cases used the existing markers tightly and to good effect. The corpus does not show a marker the existing system cannot produce; it shows alternative labels for things the existing system already classifies.

## Honest Failure-Mode Registry

The lane could fail in any of the following ways, and a skeptical reviewer should test each:

- **Over-credit failure**: reading the corpus too sympathetically and finding warrant change where there is only descriptive sharpening. Mitigation: every claimed corpus contribution was tested against the spec's anti-misread rules and the question "does the doctrine already license the same move?" In each case the answer was yes.
- **Under-credit failure**: reading the doctrine too generously and missing where the corpus actually surfaced a question the doctrine does not carry. The candidate underclaims are: the `decay diagnostics for preserved tensions` pressure on Case B; the `answerable indirect carriers` specification missing from Case C's adjudication. Both are genuine corpus contributions; both are absorbable into existing doctrine without changing the case dispositions. The judgment that they are absorbable is itself contestable.
- **Over-generalization failure**: extrapolating from three cases to corpus-wide claims in either direction. Mitigation: the inheritance judgment is explicitly bounded to "on this evidence." Promotion or rejection of corpus distinctions on a different evidence base is not adjudicated by this lane.
- **Elegant-labeling failure**: treating "more elegant naming" as automatic earning. This is the spec's primary anti-misread rule. The lane's classification of all three cases as `rephrasing_only` is the strongest mitigation: elegant labels are credited as reflexive-only inheritance, not as operational gain.
- **Selection-bias failure**: ignoring corpus distinctions that don't fit the three cases but might fit other cases. The corpus's `intervention menus for indirect rule`, `hidden governance regimes` catalog, and broader theoretical framing apparatus may be load-bearing in other cases not tested here. The lane's negative inheritance judgment for these is conditional on this evidence base.
- **Frame-inheritance failure**: accepting the spec's candidate-distinction list as the correct stress-test set. The spec selected these distinctions as "the most likely stress-test candidates"; it is possible the corpus's actually-strongest distinctions are not on this list. The mitigation is to also note distinctions encountered in Stage 2 that the spec did not flag (e.g., `regimes of hidden governance`, `decay diagnostics`, `answerable indirect carriers`), and to test those as well. They were tested above and reached the same conclusion.
- **Skeptical-prior failure**: the reviewer's prior knowledge that the workspace debates corpus-as-sprawl biases toward conservative reading. The procedural mitigation (`What The Corpus Added` written before `What The Corpus Did Not Earn`) is partial. A sympathetic-prior reviewer running the same comparison might find the same `rephrasing_only` classifications but read the inheritance judgment as harsher than necessary.
- **Spec-frame failure**: the spec itself specifies the four classification options and the three inheritance categories. If those options are too coarse (e.g., if `rephrasing_only` collapses meaningful sub-distinctions like "rephrasing that aids spec-writing economy" vs "rephrasing that adds nothing"), the lane's classifications could be too blunt. The mitigation is to record this concern in the failure-mode registry rather than smuggle the sub-distinction into the classification language.
- **Case-scope failure**: the three cases were chosen because they were likely to either reward or expose corpus distinctions. A sample biased toward "doctrine-friendly" cases (cases where the doctrine was already operating at high discipline) would predictably show rephrasing-only outcomes. All three cases here are doctrine-friendly in exactly that sense. A different sample (cases where doctrine struggled) might show different results.

## Disposition Recommendation

The spec asks three questions. They are answered in order.

### Should a proposal lane for bounded doctrine supplementation be opened?

**No, not on this evidence.** Three cases of `rephrasing_only` are not a sufficient base for promoting any corpus distinction into governing doctrine. A bounded doctrine-supplementation lane could be justified by a different evidence base — specifically, by cases where the existing doctrine genuinely under-determines disposition and where the corpus's distinctions do operational work the doctrine cannot do. None of the three cases tested here meet that bar.

A narrower variant of the proposal lane is plausible: a *vocabulary-curation* lane (not a doctrine-supplementation lane) that names a small set of corpus distinctions — `function / force`, `declared / effective authority`, `graded underreach`, `fertile / empty incompleteness`, `answerable indirect carriers` — as standardized reviewer-register lexicon. That would let future audit specs use the labels compactly without giving them governing force. Whether even that vocabulary lane is worth the workspace's attention is a separate judgment that depends on whether the existing prose-with-doctrine-citations idiom is felt to be too verbose for routine spec-writing. This lane does not adjudicate that question.

### Should the corpus remain reviewer-register only?

**Yes, on this evidence.** The corpus distinctions earned reflexive-only inheritance at most. They are useful as compact labels that an experienced reviewer can deploy in spec-writing or in audit prose, but they did not change any of the three cases tested. The corpus should remain a reviewer-register resource — *named*, *citable*, *reusable* — without being promoted into load-bearing canon, governance doctrine, or workflow scaffolding.

### Or should the workspace narrow back toward the lane-01 revisions without corpus uplift?

**Yes, on this evidence.** The lane-01 revisions can proceed on the strength of the doctrine that already carried the bridge-audit, the sensitivity correction, and the R5.17d3 adjudication. They do not require corpus uplift to land cleanly. Adding corpus distinctions to the revisions on the strength of three rephrasing-only cases would be the kind of premature inheritance the spec's anti-misread rules forbid.

### Synthesis

The honest disposition is the conservative one: keep the corpus as reviewer-register vocabulary; let lane-01 revisions proceed without corpus uplift; if the workspace later runs a different stress test against doctrine-stressed cases (cases where the doctrine genuinely under-determines disposition), reopen the question of operational inheritance at that point. A bounded vocabulary-curation lane is plausible but not required.

This recommendation is intended to be readable in two registers. A skeptical reviewer who thinks the corpus is philosophical sprawl should be able to read this artifact and see that the corpus did not earn operational inheritance on the three cases tested. A corpus-sympathetic reviewer should also be able to see that the corpus's distinctions are credited as accurate reviewer-register labels, that the strongest forward-design contribution (`answerable indirect carriers`) is named explicitly, and that the negative inheritance judgment is conditional on this evidence base rather than absolute. Both readings should find the inheritance judgment harder to fake than a less-disciplined comparison would have made it.
