Date: 2026-04-22
Status: completed local inheritance

# Parallelization Internal Cross-Audit Inheritance

## Local Disposition

- [d:r:i] `accept as protocol-pressure companion`
- [d:r:i] Keep the external Opus field map as the diagnostic baseline.
- [d:r:i] Treat this internal cross-audit as the sharper local critique of what was still under-carried after inheritance.

## Carry Forward

- [d:r:i] Preserve the core judgment:
  - the three-domain split is worth keeping
  - the three-class taxonomy is worth keeping
  - the weaker point is not the mapping itself, but the thinness of the local protocol carry afterward
- [d:r:i] Carry forward the four strongest local implications:
  - lane-local read-set / companion-work / must-wait / recheck declaration is still missing
  - the current `single-writer governance surfaces` notion is too narrow and should grow toward lane-exclusive read-set and authority surfaces
  - already-landed workflow changes should receive paired internal propagation review when they are parallelization-adjacent
  - external-lane measurement/provenance belongs inside responsible closure, not in an ownerless afterthought bucket

## Land Now

- [d:r:i] Use [../../intervention-proposals/166-harness-modifier-development-program-plan.md](../../intervention-proposals/166-harness-modifier-development-program-plan.md) as the immediate carrier for these implications rather than opening another broad map.
- [d:r:i] Keep the next protocol tranche on harness-modifier development, not harness-in-action workflow rewrites.
- [d:r:i] Preserve [../launch-truth/02-parallelization-internal-cross-audit-launch-truth.md](../launch-truth/02-parallelization-internal-cross-audit-launch-truth.md) as durable requested-versus-effective launch truth for the internal paired review.

## Keep Explicitly Later

- [d:r:i] Do not yet widen into workflow-local harness-in-action parallelization changes.
- [d:r:i] Do not yet promote a final single-writer or fan-out doctrine into root/planning agent doctrine.
- [d:r:i] Do not yet reopen a full-field parallelization mapping lane.

## Exact Next Moves

1. [d:r:i] Fold this internal cross-audit into the integrated development-program plan.
2. [d:r:i] Audit that plan cross-vendor.
3. [d:r:i] After the plan audit returns, cut a bounded protocol tranche for:
   - frozen-basis definition
   - lane-local companion-work / must-wait / recheck rules
   - composition-keeping delegation
   - intervention lifecycle review / monitor / iterate discipline
4. [d:r:i] Pair that tranche with a targeted propagation audit on already-landed parallelization-adjacent workflow changes before any harness-in-action rewrite family opens.
